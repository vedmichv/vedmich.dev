---
phase: 11-logo-favicon-refresh
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/components/Header.astro
  - src/layouts/BaseLayout.astro
autonomous: true
requirements: [REQ-007]
tags: [component, layout, favicon, header, a11y, pwa, theme-color]

must_haves:
  truths:
    - "Header logo renders from /vv-logo-hero.png with alt='Viktor Vedmich'"
    - "Header logo is above-the-fold (loading='eager' decoding='sync')"
    - "BaseLayout head contains full icon block: favicon.svg + favicon.ico + apple-touch-icon + manifest + theme-color"
    - "Browser tab favicon resolves to multi-size .ico on legacy Safari / Edge"
    - "theme-color meta is #14B8A6 (Deep Signal brand primary)"
    - "No i18n keys touched — alt='Viktor Vedmich' is a proper noun identical in EN + RU"
    - "npm run build exits 0 and emits 7 pages"
    - "No deprecated cyan hex literals (#06B6D4 / #22D3EE) introduced"
  artifacts:
    - path: "src/components/Header.astro"
      provides: "Header with Deep Signal hero logo (D-02)"
      contains: 'src="/vv-logo-hero.png"'
    - path: "src/layouts/BaseLayout.astro"
      provides: "Full icon + PWA manifest + theme-color head block (D-06)"
      contains: 'rel="manifest"'
  key_links:
    - from: "src/components/Header.astro"
      to: "public/vv-logo-hero.png"
      via: "root-relative <img src> attribute"
      pattern: "/vv-logo-hero\\.png"
    - from: "src/layouts/BaseLayout.astro"
      to: "public/favicon.svg"
      via: '<link rel="icon" type="image/svg+xml">'
      pattern: "favicon\\.svg"
    - from: "src/layouts/BaseLayout.astro"
      to: "public/favicon.ico"
      via: '<link rel="icon" type="image/x-icon">'
      pattern: "favicon\\.ico"
    - from: "src/layouts/BaseLayout.astro"
      to: "public/apple-touch-icon.png"
      via: '<link rel="apple-touch-icon">'
      pattern: "apple-touch-icon"
    - from: "src/layouts/BaseLayout.astro"
      to: "public/site.webmanifest"
      via: '<link rel="manifest">'
      pattern: "site\\.webmanifest"
    - from: "src/layouts/BaseLayout.astro"
      to: "browser chrome"
      via: '<meta name="theme-color" content="#14B8A6">'
      pattern: "theme-color"
---

<objective>
Wire the Plan 01 assets into the site chrome: swap the Header `<img>` to the new
64x64 hero PNG with accessible alt text, and expand the BaseLayout `<head>`
icon line into the full 5-item block (SVG + multi-size ICO + apple-touch-icon +
PWA manifest + theme-color). Close the final component wiring of Phase 11 and
verify the whole phase via `npm run build`, i18n invariants, and hex-literal
hygiene.

Purpose: Close D-02, D-06, D-11, D-12 of Phase 11 and satisfy the REQ-007
acceptance criteria that cannot be verified without the components pointing at
the new assets.

Output: 2 file edits (both surgical — no unrelated code touched), build-green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-logo-favicon-refresh/11-CONTEXT.md
@.planning/phases/11-logo-favicon-refresh/11-PATTERNS.md
@.planning/phases/11-logo-favicon-refresh/11-01-PLAN.md
@src/components/Header.astro
@src/layouts/BaseLayout.astro

<interfaces>
<!-- Current Header.astro lines 37–43 (verbatim, do NOT change outer <a> or <span>) -->
    <!-- Logo -->
    <a href={getLocalizedPath('/', locale)} class="flex items-center gap-2.5 group">
      <img src="/favicon.svg" alt="VV" width="32" height="32" class="w-8 h-8 rounded-[7px] shrink-0" />
      <span class="font-display font-bold text-base text-text-primary group-hover:text-brand-primary transition-colors tracking-tight">
        vedmich.dev
      </span>
    </a>

<!-- Current BaseLayout.astro lines 31–41 (verbatim) -->
    <link rel="canonical" href={canonicalURL} />
    <link rel="alternate" hreflang={altLocale} href={altURL} />
    <link rel="alternate" hreflang={locale} href={canonicalURL} />

    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="sitemap" href="/sitemap-index.xml" />

    <!-- Fonts: self-hosted from /public/fonts/ via design-tokens.css (@font-face) -->
    <link rel="preload" href="/fonts/inter-latin-400-normal.woff2" as="font" type="font/woff2" crossorigin />
    <link rel="preload" href="/fonts/space-grotesk-latin-600-normal.woff2" as="font" type="font/woff2" crossorigin />
    <link rel="preload" href="/fonts/jetbrains-mono-latin-400-normal.woff2" as="font" type="font/woff2" crossorigin />

<!-- Indentation fact: all <head> children at 4 spaces from column 0. -->
<!-- Sibling pattern (lines 38-41): comment-above + one tag per line + contiguous block. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Swap Header logo to /vv-logo-hero.png with a11y alt + eager loading</name>
  <files>src/components/Header.astro</files>
  <read_first>
    - /Users/viktor/Documents/GitHub/vedmich/vedmich.dev/src/components/Header.astro (see current line 39)
    - /Users/viktor/Documents/GitHub/vedmich/vedmich.dev/.planning/phases/11-logo-favicon-refresh/11-CONTEXT.md (D-02, D-11)
    - /Users/viktor/Documents/GitHub/vedmich/vedmich.dev/.planning/phases/11-logo-favicon-refresh/11-PATTERNS.md (section "Pattern Assignments — src/components/Header.astro")
  </read_first>
  <action>
Replace line 39 of `src/components/Header.astro` (the single `<img>` tag inside the `<a href={getLocalizedPath('/', locale)}>` block) with this exact tag:

```astro
      <img src="/vv-logo-hero.png" alt="Viktor Vedmich" width="32" height="32" class="w-8 h-8 rounded-[7px] shrink-0" loading="eager" decoding="sync" />
```

Four changes from the current line:
1. `src="/favicon.svg"` → `src="/vv-logo-hero.png"` (point to Plan 01's new optimised 64x64 PNG per D-01 + D-02; new filename also closes D-12 — cache-invalidation via filename bypass, no query string needed)
2. `alt="VV"` → `alt="Viktor Vedmich"` (user decision D-02: a11y-first over reference's `alt="VV"`)
3. Add `loading="eager"` (D-02: above-the-fold, do not lazy-load)
4. Add `decoding="sync"` (D-02: pair with `loading="eager"` so paint isn't blocked on async decode)

Keep identical: opening `<img `, `width="32"`, `height="32"`, `class="w-8 h-8 rounded-[7px] shrink-0"`, the closing ` />`. Keep 6-space indent (inside `<a>` inside `<nav>` inside `<header>`). Do NOT touch lines 37, 38, 40, 41, 42, 43 (comment, `<a>`, `<span>`, `vedmich.dev` text, `</span>`, `</a>`).

Do NOT edit any other part of Header.astro — no navigation, no mobile menu, no search pill changes.
  </action>
  <verify>
    <automated>cd /Users/viktor/Documents/GitHub/vedmich/vedmich.dev && grep -c 'src="/vv-logo-hero.png"' src/components/Header.astro | grep -q '^1$' && grep -c 'alt="Viktor Vedmich"' src/components/Header.astro | grep -q '^1$' && grep -c 'loading="eager"' src/components/Header.astro | grep -q '^1$' && grep -c 'decoding="sync"' src/components/Header.astro | grep -q '^1$' && ! grep -q 'src="/favicon.svg"' src/components/Header.astro && ! grep -q 'alt="VV"' src/components/Header.astro && grep -q 'rounded-\[7px\]' src/components/Header.astro</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'src="/vv-logo-hero.png"' src/components/Header.astro` returns `1`
    - `grep -c 'alt="Viktor Vedmich"' src/components/Header.astro` returns `1`
    - `grep -c 'loading="eager"' src/components/Header.astro` returns `1`
    - `grep -c 'decoding="sync"' src/components/Header.astro` returns `1`
    - `grep -c 'src="/favicon.svg"' src/components/Header.astro` returns `0` (old value removed)
    - `grep -c 'alt="VV"' src/components/Header.astro` returns `0` (old value removed)
    - `grep -c 'rounded-\[7px\]' src/components/Header.astro` returns at least `1` (reference borderRadius:7 preserved)
    - `grep -c 'w-8 h-8' src/components/Header.astro` returns at least `1` (32px CSS size preserved)
    - `grep -c '<img ' src/components/Header.astro` returns exactly `1` (no duplicate tags from botched edit)
    - `wc -l < src/components/Header.astro` returns the same value as before the edit (single-line swap, no line count change)
  </acceptance_criteria>
  <done>Line 39 replaced per spec, all surrounding markup untouched, grep gates pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Expand BaseLayout icon block + phase-level build / i18n / hex gate</name>
  <files>src/layouts/BaseLayout.astro</files>
  <read_first>
    - /Users/viktor/Documents/GitHub/vedmich/vedmich.dev/src/layouts/BaseLayout.astro (see current line 35 + sibling pattern at lines 38–41)
    - /Users/viktor/Documents/GitHub/vedmich/vedmich.dev/.planning/phases/11-logo-favicon-refresh/11-CONTEXT.md (D-06, D-11, D-12)
    - /Users/viktor/Documents/GitHub/vedmich/vedmich.dev/.planning/phases/11-logo-favicon-refresh/11-PATTERNS.md (section "Pattern Assignments — src/layouts/BaseLayout.astro")
    - /Users/viktor/Documents/GitHub/vedmich/vedmich.dev/public/site.webmanifest (exists after Plan 01 Task 2)
    - /Users/viktor/Documents/GitHub/vedmich/vedmich.dev/public/vv-logo-hero.png (exists after Plan 01 Task 2)
  </read_first>
  <action>
Replace line 35 of `src/layouts/BaseLayout.astro` (the single existing `<link rel="icon" type="image/svg+xml" href="/favicon.svg" />`) with EXACTLY this 6-line block (5 tags + 1 comment header), preserving 4-space indent:

```astro
    <!-- Icons + PWA manifest -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" sizes="16x16 32x32 48x48" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <meta name="theme-color" content="#14B8A6" />
```

Rules:
- Keep the block at 4-space indent from column 0 (matches lines 31–41 sibling style).
- The `<!-- Icons + PWA manifest -->` comment mirrors the `<!-- Fonts: ... -->` comment style on line 38 per 11-PATTERNS.md recommendation.
- `theme-color="#14B8A6"` is the only hex literal introduced; it is EXPLICITLY ALLOWED per D-06 (meta tags cannot reference CSS custom properties; value equals `--brand-primary`; REQ-007 tracks this exception in CLAUDE.md).
- Leave line 36 (`<link rel="sitemap" href="/sitemap-index.xml" />`) untouched — it now follows the icon block.
- Leave lines 38–41 (fonts preload block) untouched.

Do NOT introduce `#06B6D4` or `#22D3EE` (deprecated cyan — see CLAUDE.md "Anti-Patterns" table). The only valid hex in this file is `#14B8A6`.

After saving the file, run these verification gates from the repo root:

1. **Build gate:** `npm run build` must exit 0 with 7 pages emitted. If Astro warns about unknown `<meta name="theme-color">` or missing manifest, re-read Plan 01 Task 2 — the assets must exist in `public/` before the build.
2. **i18n invariant (D-11):** `grep -n "favicon\|vv-logo" src/i18n/en.json src/i18n/ru.json` must return 0 matches (no i18n string mentions the assets).
3. **Hex hygiene:** `grep -E '#06B6D4|#22D3EE' src/components/Header.astro src/layouts/BaseLayout.astro` must return 0 matches.
4. **Theme-color is the ONLY hex in BaseLayout.astro for this phase:** `grep -cE '#[0-9A-Fa-f]{3,6}' src/layouts/BaseLayout.astro` should match the pre-existing count + 1 (the new `#14B8A6`). If any unrelated hex appears, revert it — only the theme-color line may add a hex.
  </action>
  <verify>
    <automated>cd /Users/viktor/Documents/GitHub/vedmich/vedmich.dev && grep -c '<link rel="icon" type="image/svg+xml" href="/favicon.svg" />' src/layouts/BaseLayout.astro | grep -q '^1$' && grep -c '<link rel="icon" type="image/x-icon" href="/favicon.ico" sizes="16x16 32x32 48x48" />' src/layouts/BaseLayout.astro | grep -q '^1$' && grep -c '<link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />' src/layouts/BaseLayout.astro | grep -q '^1$' && grep -c '<link rel="manifest" href="/site.webmanifest" />' src/layouts/BaseLayout.astro | grep -q '^1$' && grep -c '<meta name="theme-color" content="#14B8A6" />' src/layouts/BaseLayout.astro | grep -q '^1$' && grep -c '<link rel="sitemap" href="/sitemap-index.xml" />' src/layouts/BaseLayout.astro | grep -q '^1$' && ! grep -qE '#06B6D4|#22D3EE' src/components/Header.astro src/layouts/BaseLayout.astro && [ "$(grep -c 'favicon\|vv-logo' src/i18n/en.json src/i18n/ru.json 2>/dev/null | awk -F: '{sum+=$2} END {print sum}')" = "0" ] && npm run build 2>&1 | tee /tmp/ph11-build.log && grep -q '7 page(s) built' /tmp/ph11-build.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<link rel="icon" type="image/svg+xml" href="/favicon.svg" />' src/layouts/BaseLayout.astro` returns `1`
    - `grep -c '<link rel="icon" type="image/x-icon" href="/favicon.ico" sizes="16x16 32x32 48x48" />' src/layouts/BaseLayout.astro` returns `1`
    - `grep -c '<link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />' src/layouts/BaseLayout.astro` returns `1`
    - `grep -c '<link rel="manifest" href="/site.webmanifest" />' src/layouts/BaseLayout.astro` returns `1`
    - `grep -c '<meta name="theme-color" content="#14B8A6" />' src/layouts/BaseLayout.astro` returns `1`
    - `grep -c '<link rel="sitemap" href="/sitemap-index.xml" />' src/layouts/BaseLayout.astro` returns `1` (preserved)
    - `grep -cE '#06B6D4|#22D3EE' src/components/Header.astro src/layouts/BaseLayout.astro` returns `0` (no deprecated cyan)
    - `grep -c "favicon\|vv-logo" src/i18n/en.json` returns `0` (D-11 invariant)
    - `grep -c "favicon\|vv-logo" src/i18n/ru.json` returns `0` (D-11 invariant)
    - `npm run build` exits 0
    - Build log contains `7 page(s) built` (Astro output marker)
    - `test -f dist/en/index.html` AND `test -f dist/ru/index.html` after build
    - `grep -q 'vv-logo-hero.png' dist/en/index.html` (Header markup rendered with new src)
    - `grep -q 'theme-color' dist/en/index.html` (BaseLayout meta rendered)
    - `grep -q 'apple-touch-icon' dist/en/index.html` (BaseLayout link rendered)
    - `grep -q 'site.webmanifest' dist/en/index.html` (BaseLayout link rendered)
    - EN and RU dist pages are symmetric: `grep -c 'vv-logo-hero.png' dist/en/index.html` == `grep -c 'vv-logo-hero.png' dist/ru/index.html`
  </acceptance_criteria>
  <done>
    6-line icon block placed at line 35 of BaseLayout.astro, build green with 7 pages,
    EN + RU markup symmetric, zero deprecated cyan, zero i18n asset-name leakage.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User browser ← dist HTML | Link/meta tag values are rendered into every page and served to every visitor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Spoofing + Information Disclosure | `<link rel="manifest">` exposes `site.webmanifest` publicly | accept | Manifest is designed to be public (browsers fetch it without credentials). Contents are brand metadata (theme color, name, icon paths) — no PII, no secrets. |
| T-11-07 | Information Disclosure | Apple-touch-icon + Android icons reveal brand colors to passive observers | accept | Icons are public brand artifacts, already shipped via `favicon.svg`. Adding high-resolution variants does not change threat model. |
| T-11-08 | Tampering | Third party could MITM-replace raster icons over HTTP | accept | GitHub Pages serves vedmich.dev over HTTPS with HSTS. Out of scope for this phase (server-side setting). |
</threat_model>

<verification>
Run end-to-end verification from repo root after Task 2 completes:

```
# 1. All Plan 01 assets still present
test -f public/vv-logo-hero.png && test -f public/favicon.ico && test -f public/apple-touch-icon.png && test -f public/android-chrome-192x192.png && test -f public/android-chrome-512x512.png && test -f public/site.webmanifest

# 2. Components wired correctly
grep -q 'src="/vv-logo-hero.png"' src/components/Header.astro
grep -q 'alt="Viktor Vedmich"' src/components/Header.astro
grep -q '<link rel="manifest"' src/layouts/BaseLayout.astro
grep -q 'theme-color' src/layouts/BaseLayout.astro

# 3. i18n untouched (D-11)
[ "$(grep -c 'favicon\|vv-logo' src/i18n/en.json src/i18n/ru.json 2>/dev/null | awk -F: '{sum+=$2} END {print sum}')" = "0" ]

# 4. No deprecated cyan introduced (CLAUDE.md hygiene)
! grep -rE '#06B6D4|#22D3EE' src/components/Header.astro src/layouts/BaseLayout.astro

# 5. Build passes with 7 pages
npm run build
```

All 5 gates must pass.
</verification>

<success_criteria>
- [ ] Header.astro line 39 serves `/vv-logo-hero.png` with `alt="Viktor Vedmich"`, `loading="eager"`, `decoding="sync"`.
- [ ] BaseLayout.astro line 35 area contains the 6-line comment+5-tag icon/manifest/theme-color block per D-06.
- [ ] `npm run build` exits 0 and builds exactly 7 pages.
- [ ] EN and RU dist pages render the new logo + icon tags symmetrically.
- [ ] Zero new hex literals except the D-06-approved `#14B8A6` theme-color.
- [ ] Zero i18n string changes (no key references `favicon` or `vv-logo`).
- [ ] REQ-007 acceptance items 1–4 (see REQUIREMENTS.md:156–161) all met:
  - 3 canonical SVGs in public/ + .design-handoff/ (Plan 01 Task 2).
  - Browser tab favicon refreshed to Deep Signal teal V (visual; verified live after push).
  - Header logo renders from new primary/hero PNG (this plan Task 1).
  - `npm run build` passes (this plan Task 2).
</success_criteria>

<output>
After completion, create `.planning/phases/11-logo-favicon-refresh/11-02-SUMMARY.md`.
Record: Header diff summary, BaseLayout diff summary, `npm run build` page count +
duration, computed hex-literal delta (should be exactly +1 in BaseLayout.astro),
and any deviations from D-02 / D-06 / D-11 / D-12. Note any follow-ups deferred
per CONTEXT.md "Deferred Ideas" (Lighthouse audit, OG image refresh, etc.).
</output>
</content>
</invoke>