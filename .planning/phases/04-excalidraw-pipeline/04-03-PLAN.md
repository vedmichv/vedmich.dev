---
phase: 04-excalidraw-pipeline
plan: 03
type: execute
wave: 2
depends_on:
  - 04-02
files_modified:
  - diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.excalidraw.json
  - diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.meta.json
  - public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg
  - src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md
  - src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md
autonomous: false
requirements:
  - DIAG-04
tags:
  - excalidraw
  - mcp-post
  - bilingual
  - wave-2

must_haves:
  truths:
    - "`diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.excalidraw.json` exists and is a valid v2 Excalidraw JSON representing the client ↔ MCP server ↔ external resource flow"
    - "`diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.meta.json` has `{ title, descEn, descRu }` keys with MCP-specific content"
    - "`public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg` exists, is ≤ 10 KB, contains `<title>` + `<desc>`, was emitted by `scripts/excalidraw-to-svg.mjs`"
    - "`src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md` no longer contains the ASCII `←→` diagram; contains a `<img src=\"/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg\"` block with `loading=\"eager\"` + numeric `width`/`height` + EN alt text"
    - "`src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md` no longer contains the ASCII `←→` diagram; contains the SAME `<img src=\"...\"` block with IDENTICAL `src`/`width`/`height`/`loading`, RU-localized alt text"
    - "`npm run build` succeeds; `dist/en/blog/2026-03-02-mcp-servers-plainly-explained/index.html` contains the `<img>` tag with correct src + width + height + `<script` count = 0 (T-04-02 verification)"
  artifacts:
    - path: "diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.excalidraw.json"
      provides: "MCP client-server diagram source-of-truth (authored in Excalidraw)"
    - path: "diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.meta.json"
      provides: "a11y metadata for MCP client-server diagram"
      contains: '"title"'
    - path: "public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg"
      provides: "optimized SVG diagram served from /blog-assets/.../diagrams/"
      max_bytes: 10240
    - path: "src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md"
      provides: "EN post with ASCII diagram replaced by Excalidraw SVG embed"
      contains: "client-server.svg"
    - path: "src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md"
      provides: "RU post with ASCII diagram replaced by Excalidraw SVG embed"
      contains: "client-server.svg"
  key_links:
    - from: "src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md"
      to: "public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg"
      via: "<img src=\"/blog-assets/...\""
      pattern: "diagrams/client-server\\.svg"
    - from: "src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md"
      to: "public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg"
      via: "same SVG, RU-localized alt"
      pattern: "diagrams/client-server\\.svg"
    - from: "scripts/excalidraw-to-svg.mjs"
      to: "public/blog-assets/.../diagrams/client-server.svg"
      via: "node invocation"
      pattern: "client-server\\.svg"
---

<objective>
Author the MCP client-server diagram in Excalidraw, run `scripts/excalidraw-to-svg.mjs` to produce the SVG, and swap the ASCII line-21 `←→` diagram in both EN and RU locales of the MCP post for a bilingual `<img>` embed. Ships DIAG-04.

Purpose:
- Deliver DIAG-04 — the phase's primary content-delivery requirement.
- Prove pipeline works end-to-end on a real blog post (the first "real use" of the script outside fixtures).
- Establish the `diagrams-source/<date-prefixed-slug>/` layout convention (per planner clarification — see `<slug_convention>` block).

Output: 5 files (one `.excalidraw.json`, one `.meta.json`, one `.svg`, two bilingual `.md` edits). One atomic `fix(04-03): ...` commit.

**Autonomy:** `autonomous: false`. Task 1 (diagram authoring) is marked as `checkpoint:human-verify` — Claude CAN emit a minimal programmatic JSON as a fallback, but the MCP post ships publicly, so a human-authored Excalidraw diagram is strongly preferred for visual quality. The human-verify checkpoint captures the review.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-excalidraw-pipeline/04-CONTEXT.md
@.planning/phases/04-excalidraw-pipeline/04-RESEARCH.md
@.planning/phases/04-excalidraw-pipeline/04-PATTERNS.md
@.planning/phases/04-excalidraw-pipeline/04-02-SUMMARY.md

# Files to edit:
@src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md
@src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md

# Analog (existing per-post asset pattern):
@public/blog-assets/2026-03-20-karpenter-right-sizing/

<slug_convention>
<!-- PLANNER CLARIFICATION — important. Source and asset paths use the FULL date-prefixed slug. -->

Existing convention at `public/blog-assets/2026-03-20-karpenter-right-sizing/` uses the date-prefixed blog-slug. The MCP blog post file is `2026-03-02-mcp-servers-plainly-explained.md`. Therefore the MCP diagram assets live at:

- Source:  `diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.excalidraw.json`
- Meta:    `diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.meta.json`
- Output:  `public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg`
- `<img src>` absolute URL: `/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg`

CONTEXT.md and RESEARCH.md sometimes drop the `2026-03-02-` prefix — the planner overrides that to match the existing karpenter convention. This is consistent with the existing asset directory pattern; do NOT drop the date prefix.
</slug_convention>

<interfaces>
**Script invocation (CLI from PLAN 02):**
```
node scripts/excalidraw-to-svg.mjs \
  diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.excalidraw.json \
  public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg
```
Exits 0 on success, prints `✓ public/blog-assets/.../client-server.svg (N B)` + `  intrinsic: WxH` + title/desc echo.

**The `<img>` block to insert in BOTH locale files (verbatim shape, alt differs):**
```html
<img
  src="/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg"
  alt="..."
  loading="eager"
  width="W"
  height="H"
  style="max-width: 100%; height: auto;"
/>
```
Where `W` and `H` are the numeric intrinsic values printed by the script (from viewBox).

**Existing ASCII diagram to DELETE (both locales — 3-line fenced code block at line 20-22):**

EN (verbatim, lines 20-22):
````
```
[ Client: Claude Code ]  ←→  [ MCP Server ]  ←→  [ External resource: docs / DB / API ]
```
````

RU (verbatim, lines 20-22):
````
```
[ Клиент: Claude Code ]  ←→  [ MCP-сервер ]  ←→  [ Внешний ресурс: docs / DB / API ]
```
````

**`loading="eager"` rationale:** line 21 of a 75-line post is above-the-fold on 1440×900 and near-fold on 375px. Per RESEARCH §DIAG-04 and CONTEXT code_context, `eager` is the correct override of the D-03 default `lazy`.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author-authored `.excalidraw.json` text content → SVG | Text elements may contain HTML-ish payload; sanitized by PLAN 02's SVGO `removeScripts` + `escapeXml` |
| MDX HTML passthrough → Astro build → dist HTML | `<img>` tag flows straight through; verified `<script>` count = 0 in built HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering / XSS | MCP diagram text elements | mitigate | Owned by PLAN 02 (SVGO `removeScripts` + `escapeXml`). **PLAN 03 contribution:** post-build grep asserts `! grep -q '<script' dist/en/blog/2026-03-02-mcp-servers-plainly-explained/index.html` and `... dist/ru/... index.html`. |
| T-04-03 | DoS | Authored diagram `files` blob | mitigate | Owned by PLAN 02 validator. **PLAN 03 contribution:** author MCP diagram with Virgil-only text, NO embedded raster images (keeps `files` empty; sidesteps 100 KB guard entirely). |
</threat_model>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: Author the MCP client-server Excalidraw diagram</name>
  <files>diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.excalidraw.json, diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.meta.json</files>
  <read_first>
    - @src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md (lines 14-27 — "The shape of it" section for context on what the diagram illustrates)
    - @.planning/phases/04-excalidraw-pipeline/04-PATTERNS.md (§Metadata Sibling File lines 374-388 — exact meta shape)
    - @.planning/phases/04-excalidraw-pipeline/04-RESEARCH.md (§Code Examples — MDX Embed lines 510-534 — target shape of what we're going to swap to)
  </read_first>
  <action>
    Claude (or the user via Excalidraw desktop/web) authors the source `.excalidraw.json` and the `.meta.json` sidecar.

    **Preferred path (human authoring via Excalidraw):**
    1. Open Excalidraw (https://excalidraw.com or desktop app).
    2. Draw 3 labeled rectangles connected by 2 bidirectional arrows, left-to-right:
       - Rectangle 1: "Client: Claude Code"
       - Rectangle 2: "MCP Server"
       - Rectangle 3: "External resource: docs / DB / API"
    3. **Use Virgil font only** (Excalidraw default) — keeps `@font-face` subset small for the 10 KB budget (per RESEARCH A3).
    4. Press `Shift+1` (zoom-to-fit selection) to normalize viewBox (Pitfall 3).
    5. File → Save as → download `client-server.excalidraw.json`.
    6. Move downloaded file to `diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.excalidraw.json`.

    **Fallback (if human authoring unavailable):** Claude can write a minimal programmatic JSON matching the expected shape (3 rectangles + 2 arrows + 3 text labels), using the fixture shape from PLAN 01 as a template. This is acceptable as a PR-quality stopgap; human can replace the JSON later without re-editing MDX.

    **Regardless of authoring path:** Create `diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.meta.json` with EXACTLY:

    ```json
    {
      "title": "MCP client-server architecture",
      "descEn": "Claude Code (client) connects via JSON-RPC to an MCP server, which fans out to external resources: docs, databases, APIs.",
      "descRu": "Claude Code (клиент) подключается по JSON-RPC к MCP-серверу, который обращается к внешним ресурсам: docs, DB, API."
    }
    ```
  </action>
  <verify>
    1. Open `diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.excalidraw.json` in a text editor — should start with `{"type":"excalidraw","version":2,` or similar schema-valid JSON.
    2. `jq -r '.type' diagrams-source/.../client-server.excalidraw.json` returns `excalidraw`.
    3. `jq -r '.elements | length' diagrams-source/.../client-server.excalidraw.json` returns a value `>= 5` (3 rectangles + 2 arrows at minimum; more if text elements are separate).
    4. `test -f diagrams-source/.../client-server.meta.json` passes.
    5. `jq -r '.title' diagrams-source/.../client-server.meta.json` returns `MCP client-server architecture` (exact string).
    6. `jq -r '.descEn' diagrams-source/.../client-server.meta.json` contains `Claude Code`.
    7. `jq -r '.descRu' diagrams-source/.../client-server.meta.json` contains `MCP` and Cyrillic characters.
    8. **Visual review (user):** open the `.excalidraw.json` in https://excalidraw.com → verify the diagram reads left-to-right, labels are legible, arrows go BOTH directions (bidirectional per post prose), no stray strokes.
  </verify>
  <resume-signal>Type `approved` to proceed to SVG export, or describe changes needed.</resume-signal>
  <acceptance_criteria>
- `test -f diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.excalidraw.json` passes.
- `test -f diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.meta.json` passes.
- `jq -r '.type' diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.excalidraw.json` returns `excalidraw`.
- `jq -r '.title' diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.meta.json` returns `MCP client-server architecture`.
- User approves the diagram visually (checkpoint resume signal).
  </acceptance_criteria>
  <done>
Diagram source JSON + meta sidecar committed. User approved the visual.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the pipeline script + commit the output SVG</name>
  <files>public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg</files>
  <read_first>
    - @scripts/excalidraw-to-svg.mjs (full file from PLAN 02)
    - Previous task's `diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.excalidraw.json`
  </read_first>
  <action>
Run the pipeline from repo root. Use EXACTLY this command (paths are date-prefixed per the slug convention clarification above):

```bash
node scripts/excalidraw-to-svg.mjs \
  diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.excalidraw.json \
  public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg
```

**Expected stdout:**
```
✓ public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg (N B)
  intrinsic: WxH
  title: MCP client-server architecture
  desc: Claude Code (client) connects via JSON-RPC...
```

**Record `N`, `W`, `H` values** — these are needed for Task 3 (MDX edits need numeric `width` / `height`).

**If the script exits 1 with "exceeds 10 KB budget":**
- Per Pitfall 3: the source diagram likely has a wide viewBox or too many unique glyphs.
- Return to Excalidraw, press `Shift+1` to zoom-to-fit, delete any stray decorative marks, reduce text to shorter labels.
- Re-export the `.excalidraw.json`, re-run the script. Iterate until under 10 KB.

**If stdout reports `intrinsic:` without width/height** (viewBox parse failed):
- The wrapper output lacked a conforming viewBox attribute — unusual for modern Excalidraw exports. Inspect the raw SVG before SVGO by running the script with a quick `console.log(svgString.slice(0, 200))` added. Report finding in SUMMARY; for Task 3, use `width="1800"` and `height="400"` as Excalidraw-typical defaults.

**Post-export verification:**
```bash
# File exists + under budget
test -f public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg
test $(wc -c < public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg) -le 10240

# a11y elements present
grep -q "<title>MCP client-server architecture</title>" public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg
grep -q "<desc>Claude Code.*MCP server" public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg

# No <script> (T-04-02 post-SVGO — defense in depth)
! grep -q '<script' public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg
```
  </action>
  <verify>
    <automated>test -f public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg &amp;&amp; test $(wc -c &lt; public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg) -le 10240 &amp;&amp; grep -q "&lt;title&gt;MCP client-server architecture&lt;/title&gt;" public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg &amp;&amp; grep -q "&lt;desc&gt;" public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg &amp;&amp; ! grep -q '&lt;script' public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg</automated>
  </verify>
  <acceptance_criteria>
- `test -f public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg` passes.
- `wc -c < public/blog-assets/.../client-server.svg` returns a value `<= 10240`.
- `grep -q '<title>MCP client-server architecture</title>' public/blog-assets/.../client-server.svg` passes.
- `grep -q '<desc>' public/blog-assets/.../client-server.svg` passes (DIAG-03 a11y).
- `! grep -q '<script' public/blog-assets/.../client-server.svg` passes (T-04-02 post-SVGO defense-in-depth).
- `grep -oE 'viewBox="[^"]+"' public/blog-assets/.../client-server.svg` returns exactly one match (viewBox preserved — SVGO v4 default).
- Script stdout recorded the intrinsic width/height values (numeric) for Task 3 to consume.
  </acceptance_criteria>
  <done>
Pipeline executed successfully. SVG exists under 10 KB with `<title>` + `<desc>` + viewBox. Intrinsic W/H recorded for the next task.
  </done>
</task>

<task type="auto">
  <name>Task 3: Swap ASCII diagram for bilingual &lt;img&gt; in EN + RU MCP posts + build gate</name>
  <files>src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md, src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md</files>
  <read_first>
    - @src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md (lines 18-24 — the 3-line fenced code block to replace and surrounding context)
    - @src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md (lines 18-24 — mirror)
    - Previous task's stdout intrinsic dimensions (W × H)
  </read_first>
  <action>
**Edit EN file — replace lines 20-22 (the 3-line fenced code block containing the ASCII `←→` diagram).**

**DELETE EXACTLY these 3 lines (verbatim, lines 20-22 of current EN file):**
```
```
[ Client: Claude Code ]  ←→  [ MCP Server ]  ←→  [ External resource: docs / DB / API ]
```
```

**INSERT IN PLACE EXACTLY this 7-line block** (replace `W` and `H` with the intrinsic values recorded by Task 2 — if viewBox parse failed, use `1800` and `400`):

```html
<img
  src="/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg"
  alt="MCP client-server architecture: Claude Code connects to an MCP Server, which fans out to external resources (docs, databases, APIs)"
  loading="eager"
  width="W"
  height="H"
  style="max-width: 100%; height: auto;"
/>
```

**Edit RU file — same shape, RU-localized alt.**

**DELETE EXACTLY these 3 lines (verbatim, lines 20-22 of current RU file):**
```
```
[ Клиент: Claude Code ]  ←→  [ MCP-сервер ]  ←→  [ Внешний ресурс: docs / DB / API ]
```
```

**INSERT IN PLACE EXACTLY this 7-line block** (SAME `src`, `width`, `height`, `loading`, `style` — ONLY `alt` changes):

```html
<img
  src="/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg"
  alt="Архитектура MCP клиент-сервер: Claude Code подключается к MCP-серверу, который обращается к внешним ресурсам (docs, DB, API)"
  loading="eager"
  width="W"
  height="H"
  style="max-width: 100%; height: auto;"
/>
```

**Constraints:**
- Keep everything above line 20 and below line 22 byte-identical. ONLY the 3 fenced ASCII lines get replaced.
- Parity check: EN and RU `<img>` blocks must have IDENTICAL `src`, `width`, `height`, `loading`, `style`. ONLY `alt` differs.
- Do NOT add a closing `</img>` — self-closing `/>` is correct for HTML-in-markdown passthrough (valid XHTML + CommonMark-tolerated).
- Do NOT wrap the `<img>` in a `<figure>` or `<div>` — keep it inline in prose.

**After edits, run the Astro build gate:**

```bash
npm run build 2>&1 | tail -30
```

**Verify built HTML (both locales):**

```bash
# EN build artifact — img src present, width/height numeric, no <script> (T-04-02)
grep -q 'src="/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg"' dist/en/blog/2026-03-02-mcp-servers-plainly-explained/index.html
grep -qE 'width="[0-9]+" height="[0-9]+"' dist/en/blog/2026-03-02-mcp-servers-plainly-explained/index.html
! grep -q '<script' dist/en/blog/2026-03-02-mcp-servers-plainly-explained/index.html

# RU build artifact — same
grep -q 'src="/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg"' dist/ru/blog/2026-03-02-mcp-servers-plainly-explained/index.html
grep -qE 'width="[0-9]+" height="[0-9]+"' dist/ru/blog/2026-03-02-mcp-servers-plainly-explained/index.html
```

**If grep fails on `dist/.../index.html`:** the Astro route pattern may differ. Check the actual build output:
```bash
find dist/en/blog -type d | head -20
find dist/ru/blog -type d | head -20
```

Adjust grep to the actual path. Record the real path in the SUMMARY.

**Commit this task as:** `fix(04-03): mcp-servers — swap ASCII diagram for client-server SVG (EN + RU)` — single atomic commit with the 5 files (source JSON + meta + output SVG + EN edit + RU edit).
  </action>
  <verify>
    <automated>! grep -q "←→" src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md &amp;&amp; ! grep -q "←→" src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md &amp;&amp; grep -q 'diagrams/client-server.svg' src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md &amp;&amp; grep -q 'diagrams/client-server.svg' src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md &amp;&amp; grep -q 'loading="eager"' src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md &amp;&amp; grep -q 'loading="eager"' src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md &amp;&amp; grep -qE 'width="[0-9]+" height="[0-9]+"' src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md &amp;&amp; npm run build 2>&amp;1 | grep -qE "pages built|generating static routes"</automated>
  </verify>
  <acceptance_criteria>
- `! grep -q "←→" src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md` passes (ASCII gone, EN).
- `! grep -q "←→" src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md` passes (ASCII gone, RU).
- `grep -q 'diagrams/client-server.svg' src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md` passes.
- `grep -q 'diagrams/client-server.svg' src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md` passes.
- `grep -q 'loading="eager"' src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md` passes.
- `grep -q 'loading="eager"' src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md` passes.
- `grep -qE 'width="[0-9]+" height="[0-9]+"' src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md` passes (numeric dims, CLS prevention — D-03d).
- `grep -qE 'width="[0-9]+" height="[0-9]+"' src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md` passes.
- `grep -q 'alt="MCP client-server architecture' src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md` passes (EN alt).
- `grep -q 'alt="Архитектура MCP' src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md` passes (RU alt, Cyrillic start).
- `npm run build` exits 0 (build green).
- Post-build: `grep -q 'diagrams/client-server.svg' dist/en/blog/2026-03-02-mcp-servers-plainly-explained/index.html` passes (or the discovered dist path).
- Post-build: `grep -q 'diagrams/client-server.svg' dist/ru/blog/2026-03-02-mcp-servers-plainly-explained/index.html` passes.
- Post-build: `! grep -q '<script' dist/en/blog/2026-03-02-mcp-servers-plainly-explained/index.html` passes (T-04-02 XSS-as-SVG defense).
- Post-build: boundary invariant still clean: `! grep -rE "@aldinokemal2104|@excalidraw" dist/` passes.
- Post-build: `grep -cE "diagrams/.*\\.svg" dist/en/blog/2026-03-02-mcp-servers-plainly-explained/index.html` returns `>= 1` (the embed is present).
- `find public/blog-assets -path '*/diagrams/*.svg' | wc -l` returns `>= 1` (first diagram shipped — PLAN 04 will push this to ≥ 2).
  </acceptance_criteria>
  <done>
Both MCP blog posts updated: ASCII gone, Excalidraw SVG embed in both locales with correct src + intrinsic width/height + loading=eager + localized alt. Build green. T-04-02 verification clean. Atomic `fix(04-03): ...` commit lands with 5 files.
  </done>
</task>

</tasks>

<verification>
- [ ] Source JSON + meta + output SVG committed under date-prefixed slug directories.
- [ ] SVG is ≤ 10 KB and contains `<title>` + `<desc>` from meta.
- [ ] EN + RU `<img>` blocks share IDENTICAL `src`, `width`, `height`, `loading`, `style`; only `alt` differs.
- [ ] No `←→` ASCII left in either locale's MCP post.
- [ ] `npm run build` green in < 10 s.
- [ ] Post-build HTML in both locales contains the `<img>` tag.
- [ ] No `<script>` in built HTML (T-04-02).
- [ ] Boundary invariant clean in `src/` and `dist/`.
</verification>

<success_criteria>
PLAN 03 is complete when:
1. 5 files committed in one atomic commit: source JSON, meta JSON, output SVG, EN edit, RU edit.
2. Commit message: `fix(04-03): mcp-servers — swap ASCII diagram for client-server SVG (EN + RU)`.
3. Build green, post-build grep gates pass, boundary invariants hold.
4. User approved the diagram visually (checkpoint Task 1).
5. DIAG-04 requirement satisfied in full.
</success_criteria>

<output>
After completion, create `.planning/phases/04-excalidraw-pipeline/04-03-SUMMARY.md` documenting:
- Final SVG byte count (post-SVGO)
- Intrinsic width × height extracted from viewBox
- Any deviations from the diagram shape described in the task
- Real dist path where the `<img>` was verified (if differs from the default pattern)
- Whether the diagram was human-authored or programmatically synthesized (for later replacement if needed)
</output>
