---
phase: 04-excalidraw-pipeline
plan: 04
type: execute
wave: 2
depends_on:
  - 04-02
files_modified:
  - diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.excalidraw.json
  - diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.meta.json
  - public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg
  - src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx
  - src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx
autonomous: false
requirements:
  - DIAG-05
tags:
  - excalidraw
  - karpenter-post
  - bilingual
  - wave-2
  - stress-test

must_haves:
  truths:
    - "`diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.excalidraw.json` exists with a 2-pool diagram (CA vs Karpenter ownership)"
    - "`diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.meta.json` has `{title, descEn, descRu}` describing the split ownership pattern"
    - "`public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg` exists, ≤ 10 KB, emitted by the pipeline"
    - "`src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx` contains an `<img src=\"/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg\"` block inserted after the prose-only split-ownership section (around line 55)"
    - "`src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx` contains the SAME `<img>` block (same src/width/height/loading) with RU-localized alt, inserted after the mirror section (around line 53)"
    - "`loading=\"lazy\"` on both embeds (below-the-fold — line 55+ in a 132-line post)"
    - "`find public/blog-assets -path '*/diagrams/*.svg' | wc -l` returns `>= 2` (MCP + karpenter minimum ship, satisfying DIAG-05 bilingual criteria: per D-04b MCP + karpenter + stretch)"
  artifacts:
    - path: "diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.excalidraw.json"
      provides: "karpenter split-ownership diagram source"
    - path: "diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.meta.json"
      provides: "a11y metadata for split-ownership diagram"
      contains: '"title"'
    - path: "public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg"
      provides: "optimized SVG served at /blog-assets/2026-03-20-karpenter-right-sizing/diagrams/"
      max_bytes: 10240
    - path: "src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx"
      provides: "EN MDX with split-ownership diagram embed"
      contains: "split-ownership.svg"
    - path: "src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx"
      provides: "RU MDX with split-ownership diagram embed"
      contains: "split-ownership.svg"
  key_links:
    - from: "src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx"
      to: "public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg"
      via: "<img src=\"/blog-assets/...\""
      pattern: "diagrams/split-ownership\\.svg"
    - from: "src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx"
      to: "public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg"
      via: "same SVG, RU-localized alt"
      pattern: "diagrams/split-ownership\\.svg"
---

<objective>
Stress-test the pipeline on a second real blog post — the Karpenter right-sizing MDX — by authoring a "split ownership" diagram (Cluster Autoscaler owns system node groups, Karpenter owns everything else) and inserting it after the prose-only section at line 55 (EN) / line 53 (RU). Ships DIAG-05 minimum.

Purpose:
- Deliver DIAG-05 (2+ total diagrams shipped: MCP from PLAN 03 + karpenter from this plan).
- Validate the pipeline on a second distinct content domain (infra/K8s visual metaphor vs AI protocol flow).
- Fill the existing prose-only content gap at `src/content/blog/{en,ru}/2026-03-20-karpenter-right-sizing.mdx` around "The fix: split ownership." section.

Output: 5 files (one `.excalidraw.json`, one `.meta.json`, one `.svg`, two bilingual `.mdx` edits). One atomic `fix(04-04): ...` commit.

**Parallel with PLAN 03:** This plan shares NO `files_modified` with PLAN 03. Both depend on PLAN 02. They can run concurrently.

**Stretch target (OPTIONAL, folded into this plan per CONTEXT Stretch rule):** If Tasks 1-3 all ship cleanly AND wall-clock author effort is < 30 min, consider a third diagram (e.g. Karpenter NodePool requirements funnel, OR an MCP second diagram for the "Three concrete servers I use" section). Stretch is not a must-have — skip if implementation cost estimate exceeds 30 min or the content fit is unclear.

**Autonomy:** `autonomous: false` for the same reason as PLAN 03 — Task 1 diagram authoring has a `checkpoint:human-verify` gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-excalidraw-pipeline/04-CONTEXT.md
@.planning/phases/04-excalidraw-pipeline/04-RESEARCH.md
@.planning/phases/04-excalidraw-pipeline/04-PATTERNS.md
@.planning/phases/04-excalidraw-pipeline/04-02-SUMMARY.md

# Files to edit:
@src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx
@src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx

# Analog (existing per-post asset pattern):
@public/blog-assets/2026-03-20-karpenter-right-sizing/

<interfaces>
**Script invocation:**
```
node scripts/excalidraw-to-svg.mjs \
  diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.excalidraw.json \
  public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg
```

**EN insertion target** (verbatim — `src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx` lines 55-59 are the prose-only "split ownership" paragraph; the diagram goes immediately AFTER line 55):

```markdown
The fix: split ownership. Cluster Autoscaler owns system node groups (kube-system, logging, monitoring — anything that should stay on predictable, long-lived on-demand nodes). Karpenter owns everything else.
```

Insert after this paragraph, before the "Technically, you do this with taints and labels." paragraph that follows.

**RU insertion target** (`src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx` line 53):

```markdown
Исправление: разделить ownership. Cluster Autoscaler владеет system node groups (kube-system, logging, monitoring — всё, что должно оставаться на предсказуемых, долгоживущих on-demand нодах). Karpenter владеет всем остальным.
```

Insert after this paragraph.

**The `<img>` block (both locales share identical src/width/height/loading/style; only alt differs — use `loading="lazy"` per below-the-fold rationale):**

```html
<img
  src="/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg"
  alt="..."
  loading="lazy"
  width="W"
  height="H"
  style="max-width: 100%; height: auto;"
/>
```

**`loading="lazy"` rationale:** Line 55 of a 132-line MDX post is below-the-fold on all viewports. `eager` would waste bandwidth.

**MDX reminder:** The karpenter file is `.mdx` (not `.md`), which already imports `PodLifecycleAnimation` on line 8. HTML `<img>` tags are natively supported — no extra escaping needed.

**Existing carousel PNG pattern (stays as-is — this plan does NOT retrofit existing `![]()` embeds):**
Lines 31, 47, 63, 107 of the EN MDX use `![alt](/blog-assets/2026-03-20-karpenter-right-sizing/file.png)`. Leave those untouched.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

Same as PLAN 03 — inherit T-04-02 (XSS-as-SVG) + T-04-03 (files blob DoS) mitigations from PLAN 02.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering / XSS | Karpenter diagram text content | mitigate | Owned by PLAN 02 (SVGO + escapeXml). **PLAN 04 contribution:** post-build grep asserts `<script` count = 0 in dist HTML for BOTH locales of the karpenter MDX. |
| T-04-03 | DoS | Authored `files` blob | mitigate | Author diagram with Virgil-only text + NO embedded raster images. Keeps `files` key empty. |
</threat_model>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: Author the karpenter split-ownership Excalidraw diagram</name>
  <files>diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.excalidraw.json, diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.meta.json</files>
  <read_first>
    - @src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx (lines 45-59 — "Trap 2 — Running Cluster Autoscaler and Karpenter at the same time" for diagram content context)
    - @.planning/phases/04-excalidraw-pipeline/04-PATTERNS.md (§Metadata Sibling File lines 374-388 — exact meta shape)
    - @.planning/phases/04-excalidraw-pipeline/04-RESEARCH.md (§Open Questions 3 — split-ownership vs NodePool-requirements choice)
  </read_first>
  <action>
    **Preferred path (human authoring via Excalidraw):**
    1. Open Excalidraw.
    2. Draw 2 large labeled containers side-by-side:
       - LEFT container: "Cluster Autoscaler owns" — contains 2-3 pod bubbles labeled `kube-system`, `logging`, `monitoring` on "on-demand nodes" (sub-label).
       - RIGHT container: "Karpenter owns" — contains 3-4 pod bubbles labeled `app-A`, `app-B`, `stateless fleet` on "dynamically-provisioned nodes" (sub-label).
    3. Optional: dashed vertical separator between containers; add a small label "taints + tolerations" near the boundary.
    4. Use Virgil font only (Pitfall 3 + A3 — keeps font embed small).
    5. `Shift+1` → zoom to fit.
    6. Save as `diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.excalidraw.json`.

    **Fallback (programmatic JSON):** Claude can synthesize a minimal 2-pool rectangle diagram with labeled bubbles using the fixture template from PLAN 01 as a base. Acceptable for a first pass; human can replace later.

    **Meta sidecar — create `diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.meta.json` with EXACTLY:**

    ```json
    {
      "title": "Split ownership between Cluster Autoscaler and Karpenter",
      "descEn": "Cluster Autoscaler owns system node groups (kube-system, logging, monitoring) on predictable on-demand nodes. Karpenter owns the stateless fleet on dynamically-provisioned nodes. Taints and tolerations enforce the boundary — no overlap, no oscillation.",
      "descRu": "Cluster Autoscaler владеет system node groups (kube-system, logging, monitoring) на on-demand нодах. Karpenter владеет stateless fleet на динамически provision нодах. Taints и tolerations обеспечивают границу — без overlap, без oscillation."
    }
    ```
  </action>
  <verify>
    1. `test -f diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.excalidraw.json` passes.
    2. `jq -r '.type' diagrams-source/.../split-ownership.excalidraw.json` returns `excalidraw`.
    3. `jq -r '.elements | length' diagrams-source/.../split-ownership.excalidraw.json` returns a value `>= 5`.
    4. `test -f diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.meta.json` passes.
    5. `jq -r '.title' diagrams-source/.../split-ownership.meta.json` returns `Split ownership between Cluster Autoscaler and Karpenter`.
    6. `jq -r '.descEn' diagrams-source/.../split-ownership.meta.json` contains `Cluster Autoscaler` AND `Karpenter`.
    7. `jq -r '.descRu' diagrams-source/.../split-ownership.meta.json` contains `Karpenter` AND Cyrillic characters (`владеет`).
    8. **Visual review (user):** open the `.excalidraw.json` in https://excalidraw.com — confirm 2 clearly separated ownership regions with readable labels.
  </verify>
  <resume-signal>Type `approved` to proceed to SVG export, or describe changes needed.</resume-signal>
  <acceptance_criteria>
- `test -f diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.excalidraw.json` passes.
- `test -f diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.meta.json` passes.
- `jq -r '.title' diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.meta.json` returns `Split ownership between Cluster Autoscaler and Karpenter`.
- User approves the diagram visually (checkpoint resume signal).
  </acceptance_criteria>
  <done>
Diagram source JSON + meta sidecar committed. User approved visual.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the pipeline script for split-ownership.svg</name>
  <files>public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg</files>
  <read_first>
    - @scripts/excalidraw-to-svg.mjs (script CLI contract)
    - Previous task's `diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.excalidraw.json`
  </read_first>
  <action>
Run the pipeline:

```bash
node scripts/excalidraw-to-svg.mjs \
  diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.excalidraw.json \
  public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg
```

**Expected stdout:**
```
✓ public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg (N B)
  intrinsic: WxH
  title: Split ownership between Cluster Autoscaler and Karpenter
  desc: Cluster Autoscaler owns system node groups...
```

**Record `N`, `W`, `H`** — Task 3 needs the intrinsic dimensions.

**If over 10 KB:** same remediation as PLAN 03 Task 2 — simplify the diagram, zoom-to-fit, iterate.

**Post-export verification (same pattern as PLAN 03 Task 2):**
```bash
test -f public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg
test $(wc -c < public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg) -le 10240
grep -q "<title>Split ownership between Cluster Autoscaler and Karpenter</title>" public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg
grep -q "<desc>" public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg
! grep -q '<script' public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg
```
  </action>
  <verify>
    <automated>test -f public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg &amp;&amp; test $(wc -c &lt; public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg) -le 10240 &amp;&amp; grep -q "&lt;title&gt;Split ownership" public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg &amp;&amp; grep -q "&lt;desc&gt;" public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg &amp;&amp; ! grep -q '&lt;script' public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg</automated>
  </verify>
  <acceptance_criteria>
- `test -f public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg` passes.
- `wc -c < public/blog-assets/.../split-ownership.svg` returns a value `<= 10240`.
- `grep -q "<title>Split ownership between Cluster Autoscaler and Karpenter</title>" public/blog-assets/.../split-ownership.svg` passes.
- `grep -q "<desc>" public/blog-assets/.../split-ownership.svg` passes (DIAG-03 a11y).
- `! grep -q '<script' public/blog-assets/.../split-ownership.svg` passes (T-04-02).
- Script stdout recorded intrinsic W × H for Task 3.
  </acceptance_criteria>
  <done>
Split-ownership SVG emitted, under budget, a11y elements present, no script tags.
  </done>
</task>

<task type="auto">
  <name>Task 3: Insert &lt;img&gt; after split-ownership paragraph in EN + RU karpenter MDX + build gate</name>
  <files>src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx, src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx</files>
  <read_first>
    - @src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx (lines 53-59 — "split ownership" section + surrounding context)
    - @src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx (lines 51-57 — RU mirror)
    - Previous task's stdout intrinsic dimensions (W × H)
  </read_first>
  <action>
**EN edit — insert `<img>` block AFTER line 55 (the "split ownership" paragraph ending in "Karpenter owns everything else.") and BEFORE the blank line preceding "Technically, you do this with taints and labels."**

Current EN lines 53-57 (verbatim context — DO NOT modify these):
```
I've debugged this at a customer running 200 EKS clusters. Their monitoring showed 30-50 node replacements per hour. Pods were healthy, but churn was high enough to trigger false positives in their anomaly detection.

The fix: split ownership. Cluster Autoscaler owns system node groups (kube-system, logging, monitoring — anything that should stay on predictable, long-lived on-demand nodes). Karpenter owns everything else.

Technically, you do this with taints and labels. Your system node group gets `node-role.kubernetes.io/system=true:NoSchedule`. kube-system pods get a matching toleration. Everything else lands on Karpenter-managed nodes. CA and Karpenter now operate on disjoint sets — no more race.
```

**INSERT between the split-ownership paragraph (line 55) and the "Technically" paragraph (line 57)** — preserve the single blank line already separating the two paragraphs, and place the `<img>` block as its own block with its own blank-line separators:

```
The fix: split ownership. Cluster Autoscaler owns system node groups (kube-system, logging, monitoring — anything that should stay on predictable, long-lived on-demand nodes). Karpenter owns everything else.

<img
  src="/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg"
  alt="Split ownership: Cluster Autoscaler owns system node groups (kube-system, logging, monitoring) on predictable on-demand nodes; Karpenter owns the stateless fleet on dynamically-provisioned nodes with matching taints and tolerations"
  loading="lazy"
  width="W"
  height="H"
  style="max-width: 100%; height: auto;"
/>

Technically, you do this with taints and labels. Your system node group gets `node-role.kubernetes.io/system=true:NoSchedule`. kube-system pods get a matching toleration. Everything else lands on Karpenter-managed nodes. CA and Karpenter now operate on disjoint sets — no more race.
```

Replace `W` and `H` with the intrinsic values from Task 2's stdout. If viewBox parse failed, use `1800` and `600` (2-pool vertical space is taller than MCP's horizontal flow — default to `600` for height, not `400`).

**RU edit — same shape, inserted after line 53 (the RU mirror of "split ownership").**

Current RU lines 51-55 (verbatim context — DO NOT modify):
```
Я дебажил это у клиента с 200 EKS-кластерами. Их мониторинг показывал 30-50 замен нод в час. Поды были здоровы, но churn был достаточно высоким, чтобы вызывать false positive в их anomaly detection.

Исправление: разделить ownership. Cluster Autoscaler владеет system node groups (kube-system, logging, monitoring — всё, что должно оставаться на предсказуемых, долгоживущих on-demand нодах). Karpenter владеет всем остальным.

Salesforce использует этот паттерн на всех 1000+ кластерах. CA масштабирует их stateful workloads и system-компоненты. Karpenter обрабатывает stateless fleet. Чистое разделение, нулевой oscillation.
```

**INSERT between the "Исправление:" paragraph (line 53) and the "Salesforce" paragraph (line 55):**

```
Исправление: разделить ownership. Cluster Autoscaler владеет system node groups (kube-system, logging, monitoring — всё, что должно оставаться на предсказуемых, долгоживущих on-demand нодах). Karpenter владеет всем остальным.

<img
  src="/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg"
  alt="Разделение ownership: Cluster Autoscaler владеет system node groups (kube-system, logging, monitoring) на предсказуемых on-demand нодах; Karpenter владеет stateless fleet на динамически provision нодах с matching taints и tolerations"
  loading="lazy"
  width="W"
  height="H"
  style="max-width: 100%; height: auto;"
/>

Salesforce использует этот паттерн на всех 1000+ кластерах. CA масштабирует их stateful workloads и system-компоненты. Karpenter обрабатывает stateless fleet. Чистое разделение, нулевой oscillation.
```

**Constraints:**
- EN + RU `<img>` blocks share IDENTICAL `src`, `width`, `height`, `loading`, `style`. ONLY `alt` differs.
- `loading="lazy"` (below-the-fold — contrast with MCP's `eager`).
- Do NOT retrofit the existing 4 `![]()` carousel PNGs to `<img>` tags — keep them as-is.
- MDX accepts HTML natively; no escaping needed.

**Build gate:**

```bash
npm run build 2>&1 | tail -30

# EN built — img src present, numeric width/height, no <script>
grep -q 'src="/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg"' dist/en/blog/2026-03-20-karpenter-right-sizing/index.html
grep -qE 'width="[0-9]+" height="[0-9]+"' dist/en/blog/2026-03-20-karpenter-right-sizing/index.html
! grep -q '<script' dist/en/blog/2026-03-20-karpenter-right-sizing/index.html

# RU built — same
grep -q 'src="/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg"' dist/ru/blog/2026-03-20-karpenter-right-sizing/index.html

# Phase-end invariant: >= 2 total diagrams shipped
test $(find public/blog-assets -path '*/diagrams/*.svg' | wc -l) -ge 2
```

**Commit this task as:** `fix(04-04): karpenter — add split-ownership diagram (EN + RU)` — single atomic commit with the 5 files.
  </action>
  <verify>
    <automated>grep -q 'diagrams/split-ownership.svg' src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx &amp;&amp; grep -q 'diagrams/split-ownership.svg' src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx &amp;&amp; grep -q 'loading="lazy"' src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx &amp;&amp; grep -q 'loading="lazy"' src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx &amp;&amp; grep -qE 'width="[0-9]+" height="[0-9]+"' src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx &amp;&amp; npm run build 2>&amp;1 | grep -qE "pages built|generating static routes" &amp;&amp; test $(find public/blog-assets -path '*/diagrams/*.svg' 2>/dev/null | wc -l) -ge 2</automated>
  </verify>
  <acceptance_criteria>
- `grep -q 'diagrams/split-ownership.svg' src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx` passes.
- `grep -q 'diagrams/split-ownership.svg' src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx` passes.
- `grep -q 'loading="lazy"' src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx` passes.
- `grep -q 'loading="lazy"' src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx` passes.
- `grep -qE 'width="[0-9]+" height="[0-9]+"' src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx` passes.
- `grep -qE 'width="[0-9]+" height="[0-9]+"' src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx` passes.
- `grep -q 'alt="Split ownership' src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx` passes (EN alt).
- `grep -q 'alt="Разделение ownership' src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx` passes (RU alt, Cyrillic).
- Pre-existing 4 PNG embeds untouched: `grep -c "!\[.*\](/blog-assets/2026-03-20-karpenter-right-sizing/trap" src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx` returns `3` (trap-1, trap-2, trap-3 — the 4th at line 107 is 4-step-rollout).
- `grep -c "!\[.*\](/blog-assets/2026-03-20-karpenter-right-sizing/" src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx` returns `4` (all 4 original PNG embeds preserved).
- `npm run build` exits 0.
- `grep -q 'diagrams/split-ownership.svg' dist/en/blog/2026-03-20-karpenter-right-sizing/index.html` passes.
- `grep -q 'diagrams/split-ownership.svg' dist/ru/blog/2026-03-20-karpenter-right-sizing/index.html` passes.
- `! grep -q '<script' dist/en/blog/2026-03-20-karpenter-right-sizing/index.html` passes.
- `! grep -q '<script' dist/ru/blog/2026-03-20-karpenter-right-sizing/index.html` passes.
- `find public/blog-assets -path '*/diagrams/*.svg' | wc -l` returns a value `>= 2` (DIAG-05 minimum: MCP + karpenter).
- `! grep -rE "@aldinokemal2104|@excalidraw" dist/` passes (boundary invariant holds post-build).
  </acceptance_criteria>
  <done>
Split-ownership `<img>` added to EN + RU karpenter MDX after line 55 / 53 paragraphs. Build green. `>= 2` total diagrams shipped (DIAG-05 minimum). Atomic `fix(04-04)` commit with 5 files.
  </done>
</task>

<task type="auto">
  <name>Task 4 (OPTIONAL stretch): Evaluate + ship 3rd diagram if wall-clock &lt; 30 min</name>
  <files>(conditional — if skipped, no file changes)</files>
  <read_first>
    - Previous task results — gauge whether Task 1-3 landed in ≤ 1h of human+Claude time
    - @.planning/phases/04-excalidraw-pipeline/04-RESEARCH.md (§Open Questions 2 — MCP second diagram candidate: server ↔ resource fan-out)
  </read_first>
  <action>
**Skip this task entirely** unless ALL of the following are true:
1. Tasks 1-3 shipped without iteration loops (budget issues, build failures, checkpoint disapproval).
2. User explicitly opts in to a 3rd diagram — this task is not silent. Ask: "Tasks 1-3 shipped cleanly. Optional stretch: ship a 3rd diagram (MCP fan-out OR Karpenter NodePool requirements)? Skip if < 30 min authoring effort isn't cheap right now."
3. Content fit is clear. Preferred candidate per RESEARCH: **MCP second diagram for "Three concrete servers I use" section (server → docs/DB/API fan-out)**. Alternative: Karpenter NodePool requirements funnel.

**If proceeding:** Repeat the Task-1/2/3 pattern for the chosen candidate:
- Author `diagrams-source/<slug>/<name>.excalidraw.json` + `.meta.json`.
- Run `node scripts/excalidraw-to-svg.mjs ... diagrams/<name>.svg`.
- Insert `<img>` in both EN + RU locales of the target post (MCP = `.md`, Karpenter = `.mdx`).
- Commit as `fix(04-04): <slug> — add <name> stretch diagram (EN + RU)`.

**Final state after stretch:** `find public/blog-assets -path '*/diagrams/*.svg' | wc -l` returns `>= 3`.

**If skipped:** Document in SUMMARY as "stretch skipped — minimum-ship (2 diagrams) satisfies DIAG-05; third diagram deferred to Phase 6 companion posts or v1.1."
  </action>
  <verify>
    <automated>test 1 -eq 1</automated>
  </verify>
  <acceptance_criteria>
- If skipped: SUMMARY.md records rationale.
- If shipped: `find public/blog-assets -path '*/diagrams/*.svg' | wc -l` returns `>= 3`; additional atomic `fix(04-04): ...` commit landed; all post-build grep gates still pass.
- No regression: `npm run test:unit` still green, `npm run build` still green.
  </acceptance_criteria>
  <done>
Either stretch diagram shipped OR explicit skip documented in SUMMARY.
  </done>
</task>

</tasks>

<verification>
- [ ] `diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.{excalidraw,meta}.json` committed.
- [ ] `public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg` ≤ 10 KB with `<title>` + `<desc>`.
- [ ] EN + RU MDX both contain `<img src="...split-ownership.svg"` with `loading="lazy"` + numeric width/height.
- [ ] Pre-existing 4 PNG embeds in karpenter MDX untouched.
- [ ] `npm run build` green.
- [ ] `find public/blog-assets -path '*/diagrams/*.svg' | wc -l >= 2` (MCP + karpenter).
- [ ] No `<script>` in built HTML (T-04-02).
- [ ] Boundary invariant clean (`src/` + `dist/`).
</verification>

<success_criteria>
PLAN 04 is complete when:
1. Split-ownership diagram shipped: 5 files (source JSON + meta + output SVG + EN MDX edit + RU MDX edit).
2. Atomic commit: `fix(04-04): karpenter — add split-ownership diagram (EN + RU)`.
3. Build green, post-build grep gates pass.
4. DIAG-05 minimum: ≥ 2 total diagrams under `public/blog-assets/*/diagrams/*.svg`.
5. Stretch (Task 4) either shipped as a separate atomic commit OR explicitly skipped.
</success_criteria>

<output>
After completion, create `.planning/phases/04-excalidraw-pipeline/04-04-SUMMARY.md` documenting:
- Final SVG byte count for split-ownership
- Intrinsic width × height
- Whether the diagram was human-authored or programmatically synthesized
- Stretch outcome: shipped/skipped with rationale
- `find public/blog-assets -path '*/diagrams/*.svg' | wc -l` final count
</output>
