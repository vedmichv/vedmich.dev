---
phase: 03-ui-polish
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - .planning/phases/03-ui-polish/AUDIT.md
  - .planning/phases/03-ui-polish/baselines/
  - .planning/phases/03-ui-polish/after/
autonomous: false
requirements: [POLISH-06]
tags: [audit, visual-regression, playwright, design-tokens, homepage]

must_haves:
  truths:
    - "D-04: AUDIT.md contains at least 14 rows covering all 7 homepage sections (Hero, About, Podcasts, Speaking, Book, Presentations, Blog) at both 1440×900 and 375px viewports — light pass against Deep Signal reference (`app.jsx`), fixing token drift + alignment errors only"
    - "`.planning/phases/03-ui-polish/baselines/` contains 14 PNG files — one per (section × viewport) combination, captured AFTER Plans 01 + 02 land"
    - "Every row with `FIX <sha>` status has both a before PNG (in baselines/) and an after PNG (in after/)"
    - "No homepage component retains a hardcoded hex literal outside of `design-tokens.css` (audit checkpoint proves grep-zero for `#[0-9A-Fa-f]{6}` in src/components/About|Podcasts|Hero|Book|Contact|Speaking|BlogPreview|Presentations.astro)"
    - "All audit findings are resolved to `OK` (reference match), `FIX <sha>` (fixed in atomic commit), or `DEFER` (scoped out with rationale linking to a future phase)"
  artifacts:
    - path: ".planning/phases/03-ui-polish/AUDIT.md"
      provides: "5-column audit table (Section | Viewport | Finding | Fix | Status) with ≥14 rows + H2 section per homepage section"
      contains: "| Section | Viewport | Finding | Fix | Status |"
    - path: ".planning/phases/03-ui-polish/baselines/"
      provides: "14 PNG baseline screenshots at `{section}-{viewport}.png`"
      pattern: "{hero,about,podcasts,speaking,book,presentations,blog}-{1440,375}.png"
    - path: ".planning/phases/03-ui-polish/after/"
      provides: "N PNG after screenshots, one per FIX row"
      pattern: "{section}-{viewport}.png for each FIX row"
  key_links:
    - from: "AUDIT.md row (FIX status)"
      to: "git commit SHA"
      via: "commit message `fix(03): <description>`"
      pattern: "fix\\(03\\):"
    - from: "AUDIT.md row"
      to: "baselines/ + after/ PNG files"
      via: "inline markdown image links"
      pattern: "baselines/[a-z]+-(1440|375)\\.png"
---

<objective>
Run the Phase 3 spacing/typography audit against the Deep Signal reference (`~/.claude/skills/viktor-vedmich-design/ui_kits/vedmich-dev/app.jsx`) at 1440×900 and 375px (D-04b/D-04c). Capture baseline screenshots AFTER Plans 01 + 02 land — so findings are purely about drift from the reference, not about missing bottom CTAs or wrong curve that earlier plans already addressed. Log every finding in `AUDIT.md` as a 5-column row. Ship each fix as an atomic commit with a matching after-screenshot.

Scope is intentionally "light audit" per D-04: hardcoded hex, off-grid padding, misaligned baselines, stray margin conflicts. NOT a full pixel-perfect match — the v0.4 reference audit (shipped 2026-05-01) already closed ~95% of the drift. Phase 3 brings it to ~98% and produces the audit artifact that future phases reference as the homepage baseline record.

Purpose: Deliver POLISH-06 with a durable artifact (AUDIT.md + before/after PNGs) that outlasts the phase. Each fix is co-committed with its AUDIT.md row update, matching Phase 2's atomic-commit hygiene (D-04e).
Output: One new markdown file (AUDIT.md, ~80-150 lines with tables), 14 baseline PNGs, N after PNGs (one per FIX row), atomic `fix(03): ...` commits for each finding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-polish/03-CONTEXT.md
@.planning/phases/03-ui-polish/03-PATTERNS.md
@.planning/phases/03-ui-polish/03-01-SUMMARY.md
@.planning/phases/03-ui-polish/03-02-SUMMARY.md

Reference artifact (read-only):
@$HOME/.claude/skills/viktor-vedmich-design/ui_kits/vedmich-dev/app.jsx

<interfaces>
<!-- Audit targets: 7 homepage section components. Read these to know what to capture + look at. -->

Homepage section components (in render order, per src/pages/en/index.astro and ru/index.astro):
1. Hero              — src/components/Hero.astro      (inline `<style>` for cursor, noise-overlay, grad-hero-flat)
2. About             — src/components/About.astro     (bio + skills grid + certs, `max-w-[1120px]`)
3. Podcasts          — src/components/Podcasts.astro  (DKT + AWS RU cards, `max-w-6xl` — note inconsistency vs About)
4. Speaking          — src/components/Speaking.astro  (timeline, now uses canonical CTA after Plan 02)
5. Book              — src/components/Book.astro      (cover + rating + CTA, bg-brand-accent-soft full-bleed)
6. Presentations     — src/components/Presentations.astro (grid, now with stagger + bottom CTA after Plan 02)
7. Blog              — src/components/BlogPreview.astro (grid, now with stagger + bottom CTA after Plan 02)
(Contact excluded — per D-04b, Phase 10 closed it; audit it only if a screenshot reveals drift)

Known-good visual states + drift candidates per section (from PATTERNS.md §AUDIT-ONLY table, pinned here):

| Section | Known-good anchors | Drift candidates to inspect |
|---------|--------------------|----------------------------|
| Hero | `grad-hero-flat` bg, `.noise-overlay`, authority pills `bg-surface border-border`, inline `.cursor-blink` + reduced-motion guard | Verify pills render at token values; check noise overlay opacity on 375px; no hex in inline style |
| About | `py-20 px-6`, `max-w-[1120px]`, bio `text-lg leading-[1.7]`, `text-brand-primary` accent, EXPERTISE `text-[11px]` label, pill `rounded-full px-3.5 py-1.5 text-[13px]` | `text-[11px]` and `text-[13px]` + `px-3.5 py-1.5` are off-grid (not 4px step). Verify against reference — may be reference-matched intentional |
| Podcasts | `max-w-6xl` (=1152px, DIFFERENT from About's 1120px), `card-glow animate-on-scroll` on both cards | 32px width delta vs About/Hero/BlogPreview — known historical drift, log it |
| Speaking | `border-l-2 border-border pl-5` timeline, year labels in `text-4xl font-bold text-brand-primary tracking-[-0.02em]`, Plan 02 unified the bottom CTA | Verify Plan 02 change landed cleanly — no `font-body` leftovers; baseline alignment of year labels to first event row |
| Book | Inline `.book-cover` `400ms cubic-bezier(0.16, 1, 0.3, 1)` transition (already reference-curve before Plan 01), `translateY(-6px)` on IMG hover (intentional, not affected by D-02 "no translate-Y on CARDS") | Full-bleed `bg-brand-accent-soft border-y border-brand-accent/30` — verify border opacity matches reference; Amazon rating row `★★★★★ 4.8` positioning |
| Presentations | `max-w-[1120px]` ✓, Plan 02 added stagger + bottom CTA | Verify subtitle `text-text-muted mb-12` vertical rhythm still correct with new bottom CTA below; confirm 6-card grid cascade timing feels right |
| Blog | `max-w-[1120px]` ✓, Plan 02 added stagger + bottom CTA | 3-card grid cascade: verify 0/60/120ms feels correct; empty-state guard still hides bottom CTA when posts.length === 0 |

Hardcoded-hex scan pattern (anti-pattern per CLAUDE.md):
```bash
grep -nE "#[0-9A-Fa-f]{6}" src/components/{Hero,About,Podcasts,Speaking,Book,Presentations,BlogPreview}.astro
# expected: zero matches (all colors should be tokens)
```
Exceptions: `#14B8A6` in BaseLayout.astro's `<meta name="theme-color">` is OUT OF SCOPE (not a homepage component; it's meta config).

Playwright capture pattern (D-04c, per project memory `feedback_playwright.md`):
- Use `playwright-cli attach --extension` with `PLAYWRIGHT_MCP_EXTENSION_TOKEN` (NOT fresh browser).
- Screenshot target: `https://vedmich.dev/en/#<section-id>` — live site, NOT `localhost:4321`.
- Viewports: 1440×900 (desktop) and 375px (mobile).
- File naming convention (pinned per D-04 Claude's Discretion): `baselines/{section}-{viewport}.png` flat — e.g., `baselines/hero-1440.png`, `baselines/about-375.png`.

AUDIT.md 5-column table shape (pinned per D-04d):
```markdown
| Section | Viewport | Finding | Fix | Status |
|---------|----------|---------|-----|--------|
| About | 1440 | Reference match — bio + skills grid 1.4fr/1fr aligned | — | OK |
| About | 375 | Skills pills wrap tightly; no baseline shift | — | OK |
| Podcasts | 1440 | `max-w-6xl` vs neighboring `max-w-[1120px]` — 32px wider | DEFER | DEFER — section-width harmonization is v1.1 scope, not Phase 3 polish |
```

Atomic commit pattern per fix (D-04e):
```
fix(03): tighten about.bio leading
fix(03): swap Podcasts max-w-6xl to max-w-[1120px]   (if user/planner triggers the harmonization)
fix(03): reduce grad-hero overlay opacity at 375px
```
Each commit touches the fixed component file + AUDIT.md row update in the SAME commit.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Capture 14 baseline screenshots of live homepage (D-04b, D-04c)</name>
  <files>.planning/phases/03-ui-polish/baselines/hero-1440.png, .planning/phases/03-ui-polish/baselines/hero-375.png, .planning/phases/03-ui-polish/baselines/about-1440.png, .planning/phases/03-ui-polish/baselines/about-375.png, .planning/phases/03-ui-polish/baselines/podcasts-1440.png, .planning/phases/03-ui-polish/baselines/podcasts-375.png, .planning/phases/03-ui-polish/baselines/speaking-1440.png, .planning/phases/03-ui-polish/baselines/speaking-375.png, .planning/phases/03-ui-polish/baselines/book-1440.png, .planning/phases/03-ui-polish/baselines/book-375.png, .planning/phases/03-ui-polish/baselines/presentations-1440.png, .planning/phases/03-ui-polish/baselines/presentations-375.png, .planning/phases/03-ui-polish/baselines/blog-1440.png, .planning/phases/03-ui-polish/baselines/blog-375.png</files>
  <read_first>
    - .planning/phases/03-ui-polish/03-01-SUMMARY.md (confirm Plan 01 token + stagger CSS landed on main)
    - .planning/phases/03-ui-polish/03-02-SUMMARY.md (confirm Plan 02 bottom CTAs + stagger wrapping landed on main)
    - .planning/phases/03-ui-polish/03-CONTEXT.md §D-04b, D-04c, D-04d
    - ~/.claude/skills/playwright-cli/SKILL.md (attach-to-real-Chrome mode; `PLAYWRIGHT_MCP_EXTENSION_TOKEN`)
    - ~/.claude/projects/.../memory/feedback_playwright.md — reason for `attach --extension` (fresh browser misses site auth/cookies/extensions)
  </read_first>
  <action>
**PRE-CHECK:** Plans 01 and 02 must have shipped to `main` and been deployed to `https://vedmich.dev` before this task runs. Wave 3 execution is blocked until Wave 2 (Plan 02) completes + auto-deploys (~2 min per CLAUDE.md Publishing workflow). Verify deploy with:
```bash
curl -sI https://vedmich.dev/en/ | grep -i "last-modified"
```

**CAPTURE STEP:** Launch `playwright-cli` via MCP in attach-to-real-Chrome mode (NOT fresh browser — per memory feedback_playwright.md):

```bash
# Example invocation shape (exact CLI varies by skill version — executor reads SKILL.md):
# playwright-cli attach --extension --token $PLAYWRIGHT_MCP_EXTENSION_TOKEN
```

For each of the 7 homepage sections at each of the 2 viewports:

1. **Navigate:** `https://vedmich.dev/en/#<section-id>` where section-id is `hero`, `about`, `podcasts`, `speaking`, `book`, `presentations`, or `blog`.
2. **Set viewport:**
   - Desktop: 1440×900
   - Mobile: 375×667 (iPhone SE dimensions — standard "small mobile" reference)
3. **Wait for scroll-into-view animations to settle** — scroll stops, `.is-visible` class added to all `.animate-on-scroll` children, 600ms fadeInUp + up-to-540ms stagger delay completed. ~2 seconds after scroll settles is safe.
4. **Screenshot the section** — either a full-page screenshot cropped to the section's bounding rect, or an element screenshot targeting `section#<id>`. Element-screenshot is preferred when possible (tighter frame = cleaner before/after diff).
5. **Save as:** `.planning/phases/03-ui-polish/baselines/{section}-{viewport}.png` where viewport is `1440` or `375` (no multiplier suffix, no width×height filename — just the width number).

**Expected filenames (exactly 14):**

```
.planning/phases/03-ui-polish/baselines/hero-1440.png
.planning/phases/03-ui-polish/baselines/hero-375.png
.planning/phases/03-ui-polish/baselines/about-1440.png
.planning/phases/03-ui-polish/baselines/about-375.png
.planning/phases/03-ui-polish/baselines/podcasts-1440.png
.planning/phases/03-ui-polish/baselines/podcasts-375.png
.planning/phases/03-ui-polish/baselines/speaking-1440.png
.planning/phases/03-ui-polish/baselines/speaking-375.png
.planning/phases/03-ui-polish/baselines/book-1440.png
.planning/phases/03-ui-polish/baselines/book-375.png
.planning/phases/03-ui-polish/baselines/presentations-1440.png
.planning/phases/03-ui-polish/baselines/presentations-375.png
.planning/phases/03-ui-polish/baselines/blog-1440.png
.planning/phases/03-ui-polish/baselines/blog-375.png
```

If `.planning/phases/03-ui-polish/baselines/` doesn't exist yet, create the directory first (`mkdir -p .planning/phases/03-ui-polish/baselines`).

**Pause for human verification** — present the 14 screenshots to the user (URLs to the files in the repo after commit, OR via `open <file>` on macOS). User confirms:
- All 14 captures are sharp (not blurry, not loading spinners, not cookie banners covering content).
- Animations completed (no mid-fade state mid-screenshot).
- Authority pills / code blocks / etc. show expected content (not placeholder / empty states).
  </action>
  <what-built>14 PNG baselines saved to `.planning/phases/03-ui-polish/baselines/` via playwright-cli attach-to-real-Chrome mode at 1440×900 + 375px on the live `vedmich.dev` site — capturing state AFTER Plans 01 (curve + stagger CSS) and 02 (bottom CTAs + stagger wiring) have landed and deployed.</what-built>
  <how-to-verify>
Please verify:
1. All 14 files exist and are valid PNGs: `ls -la .planning/phases/03-ui-polish/baselines/*.png | wc -l` returns `14`.
2. Each PNG is > 10 KB and < 2 MB (no blank captures, no multi-MB full-page captures with wasted pixels): `find .planning/phases/03-ui-polish/baselines -name '*.png' -size +10k -size -2048k | wc -l` returns `14`.
3. Open each `-1440.png` and confirm:
   - Content matches the live `https://vedmich.dev/en/#<section>` view at desktop width.
   - Animations settled (no mid-fade grey cards).
   - Bottom CTAs visible in `blog-1440.png` and `presentations-1440.png` (Plan 02 output).
   - Speaking bottom CTA uses the quiet `text-text-muted` tone in `speaking-1440.png` (Plan 02 Task 3).
4. Open each `-375.png` and confirm:
   - Content reflows for mobile (cards single-column).
   - Header doesn't overlap content.
   - Bottom CTAs still visible below their grids.
5. If anything looks wrong (blurry, loading state, cookie banner, wrong viewport, missing section), respond with the offending file(s) and re-capture that subset before proceeding.
  </how-to-verify>
  <resume-signal>Type "approved" once all 14 baselines look correct, OR list which files need re-capture and I'll retry those specific ones.</resume-signal>
  <acceptance_criteria>
    - `ls .planning/phases/03-ui-polish/baselines/*.png | wc -l` returns `14`
    - All 14 files named per the convention `{section}-{viewport}.png` (lowercase section, `1440` or `375`)
    - Each PNG file size > 10 KB (sanity check — no blank/aborted captures)
    - Each PNG is a valid PNG (run `file .planning/phases/03-ui-polish/baselines/*.png` — all lines end in `PNG image data`)
    - User explicitly approved with "approved" reply
  </acceptance_criteria>
  <done>14 PNG baselines committed to `baselines/`, user-approved as accurate captures of the post-Plan-02 live homepage.</done>
</task>

<task type="auto">
  <name>Task 2: Create AUDIT.md scaffold + log all findings; fix each as atomic commit (POLISH-06, D-04, D-04d, D-04e)</name>
  <files>.planning/phases/03-ui-polish/AUDIT.md, plus homepage component files as findings require (e.g., src/components/About.astro, src/components/Podcasts.astro, etc.), plus .planning/phases/03-ui-polish/after/*.png</files>
  <read_first>
    - .planning/phases/03-ui-polish/baselines/*.png (all 14 captured in Task 1)
    - ~/.claude/skills/viktor-vedmich-design/ui_kits/vedmich-dev/app.jsx (the reference — compare each section's look against this)
    - src/components/{Hero,About,Podcasts,Speaking,Book,Presentations,BlogPreview}.astro (source for inspecting current state)
    - src/styles/design-tokens.css (the ONLY place hardcoded hex should live)
    - .planning/phases/03-ui-polish/03-CONTEXT.md §Spacing/Typography Audit (D-04, D-04b, D-04d, D-04e)
    - .planning/phases/03-ui-polish/03-PATTERNS.md §AUDIT.md shape
    - .planning/phases/02-code-block-upgrades/02-SUMMARY.md (structural analog for an audit-heavy phase artifact)
  </read_first>
  <action>
**Step A — Create AUDIT.md scaffold.**

Create `.planning/phases/03-ui-polish/AUDIT.md` with this exact skeleton:

```markdown
# Phase 3: UI Polish — AUDIT

**Audited:** YYYY-MM-DD (fill in at run time)
**Viewports:** 1440×900 (desktop) + 375×667 (mobile)
**Reference:** `~/.claude/skills/viktor-vedmich-design/ui_kits/vedmich-dev/app.jsx`
**Scope:** Light pass — hardcoded hex, off-grid padding, misaligned baselines, stray margin conflicts. Not a full pixel-match.
**Capture tool:** playwright-cli, attach-to-real-Chrome mode against live `vedmich.dev`.

---

## Hardcoded-hex scan (grep gate)

Run:
```bash
grep -nE "#[0-9A-Fa-f]{6}" src/components/{Hero,About,Podcasts,Speaking,Book,Presentations,BlogPreview}.astro
```
Expected: zero matches. Any match = a FIX row below.

**Result at audit time:** <fill in — either "zero matches, OK" or list the offenders and create FIX rows>

---

## Hero

**Baselines:** [baselines/hero-1440.png](baselines/hero-1440.png) · [baselines/hero-375.png](baselines/hero-375.png)

| Section | Viewport | Finding | Fix | Status |
|---------|----------|---------|-----|--------|
| Hero | 1440 | <finding or "Reference match"> | <— or short description> | <OK / FIX {sha} / DEFER — rationale> |
| Hero | 375 | <finding> | <fix> | <status> |

---

## About

**Baselines:** [baselines/about-1440.png](baselines/about-1440.png) · [baselines/about-375.png](baselines/about-375.png)

| Section | Viewport | Finding | Fix | Status |
|---------|----------|---------|-----|--------|
| About | 1440 | <finding> | <fix> | <status> |
| About | 375 | <finding> | <fix> | <status> |

---

## Podcasts

**Baselines:** [baselines/podcasts-1440.png](baselines/podcasts-1440.png) · [baselines/podcasts-375.png](baselines/podcasts-375.png)

| Section | Viewport | Finding | Fix | Status |
|---------|----------|---------|-----|--------|
| Podcasts | 1440 | <finding> | <fix> | <status> |
| Podcasts | 375 | <finding> | <fix> | <status> |

---

## Speaking

**Baselines:** [baselines/speaking-1440.png](baselines/speaking-1440.png) · [baselines/speaking-375.png](baselines/speaking-375.png)

| Section | Viewport | Finding | Fix | Status |
|---------|----------|---------|-----|--------|
| Speaking | 1440 | <finding> | <fix> | <status> |
| Speaking | 375 | <finding> | <fix> | <status> |

---

## Book

**Baselines:** [baselines/book-1440.png](baselines/book-1440.png) · [baselines/book-375.png](baselines/book-375.png)

| Section | Viewport | Finding | Fix | Status |
|---------|----------|---------|-----|--------|
| Book | 1440 | <finding> | <fix> | <status> |
| Book | 375 | <finding> | <fix> | <status> |

---

## Presentations

**Baselines:** [baselines/presentations-1440.png](baselines/presentations-1440.png) · [baselines/presentations-375.png](baselines/presentations-375.png)

| Section | Viewport | Finding | Fix | Status |
|---------|----------|---------|-----|--------|
| Presentations | 1440 | <finding — verify Plan 02 stagger landed correctly, bottom CTA present, subtitle rhythm intact> | <fix> | <status> |
| Presentations | 375 | <finding — verify mobile stagger + bottom CTA alignment> | <fix> | <status> |

---

## Blog

**Baselines:** [baselines/blog-1440.png](baselines/blog-1440.png) · [baselines/blog-375.png](baselines/blog-375.png)

| Section | Viewport | Finding | Fix | Status |
|---------|----------|---------|-----|--------|
| Blog | 1440 | <finding — verify 3-card 0/60/120ms stagger + bottom CTA> | <fix> | <status> |
| Blog | 375 | <finding — verify single-column stagger on mobile> | <fix> | <status> |

---

## Summary

- **Total rows:** <N>
- **OK:** <count>
- **FIX:** <count>
- **DEFER:** <count>
- **Reference-match baseline (post-Plan-02):** ~95-98% — log here whatever ratio falls out of the scan.
```

**Step B — Run the hardcoded-hex grep gate.**

Run the grep command at the top of AUDIT.md. Fill in the result block. If any hex appears in a homepage component, add a FIX row ("`<file>:<line>` hardcoded `#XXXXXX` — replace with token `<name>`") and resolve it in Step C.

**Step C — For each section, compare baseline against reference.**

Open each baseline PNG alongside the matching section of `~/.claude/skills/viktor-vedmich-design/ui_kits/vedmich-dev/app.jsx` (Read the file; it's a React reference implementation — focus on component structure, Tailwind classes used, spacing/typography). For each (section × viewport) row:

- If baseline matches reference within the "light audit" scope (D-04): status `OK`, fill finding with one-line description of what matches.
- If drift found AND it's in-scope per D-04 (hardcoded hex, off-grid padding, misaligned baselines, stray margins): write the finding, then implement the fix in the matching `src/components/<Section>.astro` file. Fix each one as a separate atomic commit.
- If drift found BUT out of scope per D-04 (e.g., major structural change, section-width harmonization `max-w-6xl` vs `max-w-[1120px]` — which is >32px delta and would touch multiple sections): status `DEFER` with rationale linking to a future phase.

**Atomic commit pattern per fix (D-04e):**

```
git add src/components/<Section>.astro .planning/phases/03-ui-polish/AUDIT.md
git commit -m "fix(03): <one-line finding description>"
```

Then capture the "after" screenshot at the same viewport:
```
playwright-cli ... > .planning/phases/03-ui-polish/after/{section}-{viewport}.png
```
Update the AUDIT.md row's Status to `FIX <short-sha>` where `<short-sha>` is the 7-char git SHA of the fix commit (`git rev-parse --short HEAD`).

**Step D — Fill in AUDIT.md's "Summary" tally at the bottom** once all 14+ rows are resolved.

**Important constraints:**
- DO NOT touch any CSS/component file for a reason NOT covered in an AUDIT.md row. Every edit must trace to a row.
- DO NOT widen scope beyond the 7 homepage sections. Contact/Header/Footer audits are deferred (see D-04b: Contact optional, not this phase).
- DO NOT change component logic (data fetching, conditionals) — audit scope is presentation only.
- DO NOT modify `design-tokens.css` — token changes are Plan 01's domain; if audit reveals a token drift, log as `DEFER` pointing to a future token-review phase.
- DO NOT update `ROADMAP.md` / `REQUIREMENTS.md` during the audit — those doc-drift fixes are the D-01e post-phase task that the orchestrator handles after phase ships.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-ui-polish/AUDIT.md && [ $(grep -cE "^\| (Hero|About|Podcasts|Speaking|Book|Presentations|Blog)" .planning/phases/03-ui-polish/AUDIT.md) -ge 14 ]</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/03-ui-polish/AUDIT.md` exists
    - AUDIT.md contains `## Hero`, `## About`, `## Podcasts`, `## Speaking`, `## Book`, `## Presentations`, `## Blog` section headers (7 total): `grep -cE "^## (Hero|About|Podcasts|Speaking|Book|Presentations|Blog)" .planning/phases/03-ui-polish/AUDIT.md` returns `7`
    - AUDIT.md contains at least 14 data rows (7 sections × 2 viewports): `grep -cE "^\| (Hero|About|Podcasts|Speaking|Book|Presentations|Blog)" .planning/phases/03-ui-polish/AUDIT.md` returns `>= 14`
    - Each data row has one of three statuses: `OK`, `FIX <sha>`, or `DEFER`: `grep -cE '\| (OK|FIX [a-f0-9]{7}|DEFER)' .planning/phases/03-ui-polish/AUDIT.md` returns `>= 14`
    - Hardcoded-hex grep gate result documented in AUDIT.md: `grep -c "zero matches\|\[0-9A-Fa-f\]{6}" .planning/phases/03-ui-polish/AUDIT.md` returns `>= 1`
    - For every `FIX <sha>` row, a matching after PNG exists in `.planning/phases/03-ui-polish/after/`: `for sha_row in $(grep -oE "FIX [a-f0-9]{7}" .planning/phases/03-ui-polish/AUDIT.md); do ...; done` (manual verification OK — formal check: `ls .planning/phases/03-ui-polish/after/*.png | wc -l` should equal the number of FIX rows)
    - For every `FIX <sha>` row, `git log --oneline | grep -c <sha>` returns `1`
    - For every `FIX <sha>` commit, `git show <sha> --stat` shows the commit touches only (a) the homepage component file being fixed AND (b) AUDIT.md (no other files)
    - All `fix(03):` commits follow the message convention: `git log --oneline | grep -cE "^[a-f0-9]{7} fix\\(03\\):" ` returns `>= 0` (0 if no fixes needed, ≥1 if fixes shipped)
    - Summary tally at bottom of AUDIT.md populated (Total / OK / FIX / DEFER counts non-empty)
    - `npm run build` still exits 0 after all audit fixes
  </acceptance_criteria>
  <done>AUDIT.md shipped with ≥14 rows covering all (section × viewport) cells; every FIX row has a matching atomic commit + after PNG; every DEFER row has a rationale; every OK row confirms reference match with a one-line finding; build stays green throughout.</done>
</task>

</tasks>

<verification>
```bash
# AUDIT.md structure
test -f .planning/phases/03-ui-polish/AUDIT.md && echo "AUDIT.md exists"

# 7 section headers
grep -cE "^## (Hero|About|Podcasts|Speaking|Book|Presentations|Blog)" .planning/phases/03-ui-polish/AUDIT.md
# expected: 7

# ≥14 data rows
grep -cE "^\| (Hero|About|Podcasts|Speaking|Book|Presentations|Blog)" .planning/phases/03-ui-polish/AUDIT.md
# expected: >= 14

# 14 baseline PNGs
ls .planning/phases/03-ui-polish/baselines/*.png | wc -l
# expected: 14

# Hardcoded-hex grep gate passes
grep -nE "#[0-9A-Fa-f]{6}" src/components/{Hero,About,Podcasts,Speaking,Book,Presentations,BlogPreview}.astro
# expected: zero matches (or if matches found, they're logged as FIX rows in AUDIT.md)

# Every FIX row has an after PNG
grep -c "FIX [a-f0-9]\{7\}" .planning/phases/03-ui-polish/AUDIT.md  # N fixes
ls .planning/phases/03-ui-polish/after/*.png 2>/dev/null | wc -l     # N after PNGs (0 if zero fixes)

# Atomic fix commits follow convention
git log --oneline main -- .planning/phases/03-ui-polish/AUDIT.md src/components/*.astro 2>/dev/null | grep "fix(03):"
# expected: one commit per FIX row

# Build still green
npm run build
# expected: exit 0
```
</verification>

<success_criteria>
Plan 04 done when:
- [ ] 14 baseline PNGs captured AFTER Plans 01 + 02 deployed (Task 1)
- [ ] User approved all 14 baselines as accurate captures
- [ ] AUDIT.md exists with all 7 section H2s, ≥14 data rows, and a filled-in Summary tally
- [ ] Hardcoded-hex grep gate result documented (zero matches OR each match resolved to a FIX row)
- [ ] Every FIX row has an after PNG in `after/` + a matching atomic `fix(03): ...` commit
- [ ] Every DEFER row has a one-line rationale linking to a future phase or v1.1+ milestone
- [ ] Every OK row has a one-line "matches reference" finding (not just a blank or "—")
- [ ] `npm run build` exits 0
- [ ] `ROADMAP.md` Phase 3 success criterion #4 ("zero nits remain") is demonstrably true: either all rows `OK` / `FIX`, or `DEFER` rows are genuinely out-of-scope for the "light audit" per D-04
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-polish/03-04-SUMMARY.md` following the GSD summary template. Record:
- Number of FIX rows and the one-line commit message for each.
- Number of DEFER rows and what future phase/milestone each defers to.
- Count of baseline PNGs (14), count of after PNGs (= FIX count).
- Any reference drift that surprised the auditor (e.g., `max-w-6xl` vs `max-w-[1120px]` section-width inconsistency from PATTERNS.md).
- Whether Contact section was spot-checked despite D-04b marking it optional (record as a bonus row if yes; skip if no).
- Hand-off note for the Phase 3 SUMMARY: REQUIREMENTS.md has doc-drift on POLISH-01/02/03 key naming (D-01e) — to be resolved in a post-phase doc-only commit by the orchestrator.
</output>
