---
phase: 01-rich-media-primitives
plan: 06
type: execute
wave: 4
depends_on: [01-01, 01-02, 01-03, 01-04, 01-05]
files_modified:
  - src/components/visuals/README.md
  - .planning/phases/01-rich-media-primitives/time-to-port-report.md
autonomous: false
requirements:
  - PRIMS-06
user_setup: []
tags:
  - documentation
  - metric
  - phase-7-trigger
must_haves:
  truths:
    - "D-25: src/components/visuals/README.md documents every primitive's API (props tables), usage examples, Iconify vocabulary + Slidev find+replace transform, SMIL-vs-CSS-offset-path gotcha with link to Phase 9 v0.4 hotfix rationale, reduced-motion rules, tone palette, and gotchas list."
    - "D-21: Elapsed time to port one additional slide from /Users/viktor/Documents/GitHub/vedmichv/slidev-theme-vv/presentations/vv-demo/pages/ onto primitives is measured and recorded in .planning/phases/01-rich-media-primitives/time-to-port-report.md."
    - "Phase 7 checkpoint rule: <10 min = Phase 7 skipped; 10-15 min = user decides; >15 min = Phase 7 (codegen) triggered. The user reviews the time-to-port report and chooses after this plan completes."
    - "The README is EN-only (developer docs, not user-facing content) per CLAUDE.md bilingual policy — primitives themselves have no text strings, so i18n parity is trivially satisfied."
    - "README content is copy-pasteable: every example uses concrete, runnable code that an executor or post-author can drop into an MDX file without modification."
  artifacts:
    - path: "src/components/visuals/README.md"
      provides: "Primitive API reference + usage guide + gotchas for future slide ports (Phase 6 skill updates link here)"
      contains: "Iconify vocabulary"
      min_lines: 200
    - path: ".planning/phases/01-rich-media-primitives/time-to-port-report.md"
      provides: "Time-to-port metric for D-21 Phase 7 checkpoint decision"
      contains: "Elapsed"
      min_lines: 15
  key_links:
    - from: "src/components/visuals/README.md"
      to: "src/components/visuals/VvStage.astro and sibling primitives"
      via: "Props tables document each .astro component's interface"
      pattern: "VvStage"
---

<objective>
Complete Phase 1 by shipping the component-folder README and measuring the time-to-port metric that gates the optional Phase 7 codegen decision. The README is the single source of truth for how authors compose diagrams with Vv* primitives — it is linked from the `vv-blog-from-vault` skill's `references/visuals-routing.md` in Phase 6 (CONTENT-05) and will be companion-post authors' primary onboarding resource. The time-to-port measurement tests the "primitives alone hit the <10 min slide-lift target" hypothesis: after porting one additional slide from slidev-theme-vv/presentations/vv-demo/pages/, the user reviews the report and decides whether Phase 7 (codegen) is needed.

Purpose: PRIMS-06 + ROADMAP Phase 1 success criterion #5 + ROADMAP Phase 7 gate condition.
Output: `src/components/visuals/README.md` (200+ lines), `time-to-port-report.md` (15+ lines) containing the measured elapsed time + Phase 7 recommendation, final user checkpoint on Phase 7 go/skip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-rich-media-primitives/01-CONTEXT.md
@.planning/phases/01-rich-media-primitives/01-RESEARCH.md
@.planning/phases/01-rich-media-primitives/01-PATTERNS.md
@src/components/visuals/VvStage.astro
@src/components/visuals/VvNode.astro
@src/components/visuals/VvWire.astro
@src/components/visuals/VvPacket.astro
@src/components/visuals/_vv-registry.ts
@src/components/PodLifecycleAnimation.astro
@CLAUDE.md

<interfaces>
<!-- Required README sections (from D-25 + PATTERNS.md lines 544-556) -->

1. `# Visuals primitives` (purpose)
2. `## Import block` (5 paste-in lines for any MDX author)
3. `## Primitives API` with one `###` subsection per primitive + props table
4. `## Tone palette` table (tone name → CSS var → reference hex)
5. `## Examples` (hello world, PodLifecycle excerpt, bezier + dashed + via waypoints)
6. `## Iconify vocabulary + Slidev find+replace`
7. `## SMIL vs CSS offset-path` (the gotcha — link to the Phase 9 v0.4 hotfix rationale)
8. `## Reduced-motion rules`
9. `## Gotchas`

<!-- Time-to-port protocol (from D-21 + RESEARCH.md lines 958-988) -->

1. Select a slide from /Users/viktor/Documents/GitHub/vedmichv/slidev-theme-vv/presentations/vv-demo/pages/ — NOT slide 84 (Pod lifecycle, already used). The file `10-complex-schemas.md` contains multiple candidates (AWS three-tier at lines 13-131 is one). Pick one with 5-10 nodes + 1-2 packets to match PodLifecycle complexity.
2. Timer starts: first edit to a new scratch file (e.g., src/components/ScratchDiagram.astro, or a scratch page at src/pages/en/scratch/ported-slide.astro).
3. Timer stops: Playwright or manual browser screenshot of the new diagram + `npm run build` passes + all primitives resolve correctly.
4. Record elapsed time in minutes + notes on what was fast / slow / confusing.
5. Apply the Phase 7 decision rule:
   - <10 min → recommend SKIP Phase 7
   - 10-15 min → recommend USER DECIDES
   - >15 min → recommend TRIGGER Phase 7
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author src/components/visuals/README.md</name>
  <files>src/components/visuals/README.md</files>
  <read_first>
    - src/components/visuals/VvStage.astro (final Props interface)
    - src/components/visuals/VvNode.astro (final Props interface)
    - src/components/visuals/VvWire.astro (final Props interface INCLUDING the fromPoint/toPoint escape hatch from Plan 05)
    - src/components/visuals/VvPacket.astro (final Props interface)
    - src/components/visuals/_vv-registry.ts (VvTone + VvNodeAnimation unions)
    - src/components/PodLifecycleAnimation.astro (refactored — use as the reference example in §Examples)
    - CLAUDE.md §Deep Signal — color tokens
    - src/styles/design-tokens.css lines 76-118 — tone hex values for the README's reference-hex column (never use these in component code, only in the README table)
    - .planning/phases/01-rich-media-primitives/01-RESEARCH.md §astro-icon Integration + §SMIL animateMotion Patterns + §Reduced-motion Pattern + §Pitfall F / G
    - .planning/phases/01-rich-media-primitives/01-PATTERNS.md §README.md (lines 534-556)
  </read_first>
  <action>
    Create `/Users/viktor/Documents/GitHub/vedmich/vedmich.dev/src/components/visuals/README.md`. The file must contain all 9 top-level sections listed in the `<interfaces>` block above, plus 4 `###` subsections under `## Primitives API` (one per primitive). Target ~200-300 lines.

    Required content per section:

    **Section 1 `# Visuals primitives`** (single paragraph + status line):
    - One-sentence purpose: "Reusable Astro primitives for inline animated SVG diagrams in MDX blog posts."
    - Note: ported from `slidev-theme-vv` Vue components; coordinate copy-paste parity; zero runtime JS; build-time SSR + SMIL.
    - "Ship status: Phase 1 (milestone v1.0). Consumers: src/components/PodLifecycleAnimation.astro. Future consumers: companion posts (Phase 6)."

    **Section 2 `## Import block`** — exactly this code block, copy-pasteable:
    ```astro
    import VvStage from '../../../components/visuals/VvStage.astro';
    import VvNode from '../../../components/visuals/VvNode.astro';
    import VvWire from '../../../components/visuals/VvWire.astro';
    import VvPacket from '../../../components/visuals/VvPacket.astro';
    import { Icon } from 'astro-icon/components';
    ```
    - Plus a paragraph explaining that `astro-icon` is wired in `astro.config.mjs` and resolves names from 8 collections (list them: carbon, logos, lucide, ph, simple-icons, solar, streamline-flex, twemoji).
    - Warning: icon `name` must be a static string literal; dynamic `name={var}` breaks tree-shaking.

    **Section 3 `## Primitives API`** — four `###` subsections:

    `### VvStage` — table columns: Prop | Type | Default | Description. Rows: `title`, `caption`, `viewBox` (default `"0 0 1800 820"`). Plus a prose note: "VvStage cannot nest; all Vv* children must be inside a VvStage or they throw."

    `### VvNode` — props table with rows for: id (required), x (required, user-space number), y (required), w (default 220), h (default 160), label (required), tone (default 'teal'), animation (`'slide-x' | 'pop' | 'drop'`, default 'pop'), delay (ms, default 0). Prose note: default slot accepts Icon, inline SVG, or any content; tone controls border-left only (D-09b).

    `### VvWire` — table rows: from (required), to (required), id (default `${from}-${to}`), curve (bezier 0..1), via (waypoints), dashed (bool default false), tone (default 'teal'), delay (ms), fromPoint (escape hatch), toPoint (escape hatch). Prose: default geometry is ray-from-center-to-rect-border intersection. `via` dominates `curve`.

    `### VvPacket` — table rows: wire XOR path (required), duration (required SMIL time), delay (required SMIL time), r (default 10), glow (default true), tone (default 'teal'), loop (default true), gap (default '4s'). Prose: loop formula is `begin="{delay};{pktId}.end+{gap}"`. Reduced-motion hides packets.

    **Section 4 `## Tone palette`** — table with 6 rows. Columns: Tone | CSS variable | Reference hex | Typical use. Rows mirror D-10:
    | `teal` (default) | `--brand-primary` | `#14B8A6` | Brand primary, most content |
    | `amber` | `--brand-accent` | `#F59E0B` | Highlighted / final-step |
    | `k8s` | `--topic-k8s` | `#3B82F6` | Kubernetes diagrams |
    | `architecture` | `--topic-architecture` | `#818CF8` | System-design |
    | `opinion` | `--topic-opinion` | `#F43F5E` | Editorial / opinion |
    | `tutorial` | `--topic-tutorial` | `#22C55E` | Tutorial posts |
    Plus a note: Status colors (`--success`/`--warning`/`--error`/`--info`) are excluded from the tone palette; reserve them for status UI only.

    **Section 5 `## Examples`** — three code blocks:

    (a) "Hello world" — 2 nodes, 1 wire, 1 packet, using `<Icon name="carbon:laptop"/>` and `<Icon name="logos:aws-api-gateway"/>`.

    (b) "Bezier + dashed" — 3 short wire examples demonstrating `curve={0.3}` (arc one way), `curve={-0.3}` (arc the other way), and `dashed` flag.

    (c) "Polyline waypoints" — a single wire with `via={[[500, 300], [700, 400]]}`.

    (d) Short reference: "See `src/components/PodLifecycleAnimation.astro` for a 7-node / 7-wire / 7-packet canonical example."

    **Section 6 `## Iconify vocabulary + Slidev find+replace`**:
    - Short paragraph explaining Slidev uses `<logos-aws-lambda/>` (unplugin-icons) and vedmich.dev uses `<Icon name="logos:aws-lambda"/>` (astro-icon). Both resolve to identical SVG from `@iconify-json/logos`.
    - Node regex block:
    ```js
    str.replace(
      /<(logos|carbon|lucide|ph|solar|simple-icons|streamline-flex|twemoji)-([a-z0-9-]+)\b[^>]*\/>/g,
      '<Icon name="$1:$2"/>',
    );
    ```
    - sed equivalent for macOS BSD `sed -E`.
    - Note on icon sizing via `clamp(22px, 3.2vw, 40px)` from VvStage CSS; override via wrapping in a `<span style="font-size: 48px">`.

    **Section 7 `## SMIL vs CSS offset-path`**:
    - Bold rule: "Always use SMIL `<animateMotion>`. Never use CSS `offset-path`."
    - Explanation: packets must share coordinate space with wires; SVG `<animateMotion>` + `<mpath>` keeps them aligned when responsive containers resize. CSS `offset-path` uses CSS box coordinates, which drift from SVG viewBox coordinates at non-1:1 aspect ratios. This was the Phase 9 (v0.4) hotfix cause — see `PodLifecycleAnimation.astro` header comment.
    - Note: SMIL `<animateMotion>` + `<mpath>` + chained `begin="a;b.end+c"` are all W3C-standard, baseline widely available across browsers since Jan 2020.

    **Section 8 `## Reduced-motion rules`**:
    - List the 3 behaviors (nodes opacity 1 + no animation; wires opacity 0.55 + no animation; packets display:none).
    - Note: WCAG-compliant, not configurable (D-18).
    - Testing: macOS System Settings → Accessibility → Display → Reduce motion. Or Playwright's `page.emulateMedia({ reducedMotion: 'reduce' })`.

    **Section 9 `## Gotchas`** — numbered list covering at minimum:
    1. No VvStage nesting (throws).
    2. Icon names must be string literals.
    3. fromPoint/toPoint is an escape hatch for pixel-parity cases; prefer auto edge-points.
    4. Only ONE of VvPacket `wire` or `path` (throws both-set or neither-set).
    5. Node id uniqueness (duplicates log warn + overwrite).
    6. Tailwind 4 `@theme` does not reach SVG attrs — use `stroke="var(--brand-primary)"` literally inside inline SVG.
    7. `.not-prose` escape prevents `@tailwindcss/typography` overrides inside `.prose` blog containers.

    Append a short `## Further reading` section with links to: PodLifecycleAnimation.astro, _vv-registry.ts, _vv-geom.ts, _vv-path.ts, 01-RESEARCH.md, CLAUDE.md §Deep Signal.

    No build impact expected — README.md is not consumed at build time.
  </action>
  <verify>
    <automated>test -f src/components/visuals/README.md && grep -c '^# Visuals primitives' src/components/visuals/README.md && grep -c '^## Import block' src/components/visuals/README.md && grep -c '^## Primitives API' src/components/visuals/README.md && grep -c '^## Tone palette' src/components/visuals/README.md && grep -c '^## Examples' src/components/visuals/README.md && grep -c '^## Iconify vocabulary' src/components/visuals/README.md && grep -c '^## SMIL vs CSS offset-path' src/components/visuals/README.md && grep -c '^## Reduced-motion rules' src/components/visuals/README.md && grep -c '^## Gotchas' src/components/visuals/README.md && grep -c '### .VvStage' src/components/visuals/README.md && grep -c '### .VvNode' src/components/visuals/README.md && grep -c '### .VvWire' src/components/visuals/README.md && grep -c '### .VvPacket' src/components/visuals/README.md && grep -c 'logos:aws-lambda' src/components/visuals/README.md && grep -c 'fromPoint' src/components/visuals/README.md && grep -c 'prefers-reduced-motion: reduce' src/components/visuals/README.md && grep -c '\-\-brand-primary' src/components/visuals/README.md && grep -c '\-\-topic-k8s' src/components/visuals/README.md && grep -c '\-\-topic-architecture' src/components/visuals/README.md && grep -c '\-\-topic-opinion' src/components/visuals/README.md && grep -c '\-\-topic-tutorial' src/components/visuals/README.md && awk 'END { print NR }' src/components/visuals/README.md | awk '$1 >= 200 { print "loc-ok" }' | grep -q loc-ok</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/visuals/README.md` exists with ≥ 200 lines
    - 9 top-level `##` sections: Import block, Primitives API, Tone palette, Examples, Iconify vocabulary, SMIL vs CSS offset-path, Reduced-motion rules, Gotchas, Further reading (+ the `# Visuals primitives` title)
    - 4 `###` subsections under Primitives API (VvStage, VvNode, VvWire, VvPacket) — each with a props table
    - Contains the exact Iconify regex for find+replace: `<(logos|carbon|lucide|ph|solar|simple-icons|streamline-flex|twemoji)-([a-z0-9-]+)`
    - Documents the `fromPoint`/`toPoint` escape hatch with clear guidance that default edge-points are preferred
    - References all 6 CSS variable names: `--brand-primary`, `--brand-accent`, `--topic-k8s`, `--topic-architecture`, `--topic-opinion`, `--topic-tutorial`
    - Contains the phrase `prefers-reduced-motion: reduce` in the reduced-motion section
    - Contains a SMIL-vs-offset-path warning with the bold rule "Always use SMIL `<animateMotion>`. Never use CSS `offset-path`."
    - `npm run build` still passes (README isn't consumed at build time but verify no regression)
  </acceptance_criteria>
  <done>README.md is the authoritative developer reference for the Vv* primitives. A future slide porter (or Phase 6 companion-post author) can open this one file and learn everything they need — API tables, examples, Iconify vocabulary transform, SMIL constraints, reduced-motion behavior, and common pitfalls.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Port one additional slide + record time-to-port metric</name>
  <files>.planning/phases/01-rich-media-primitives/time-to-port-report.md</files>
  <read_first>
    - /Users/viktor/Documents/GitHub/vedmichv/slidev-theme-vv/presentations/vv-demo/pages/10-complex-schemas.md (candidate source slides — pick one with 5-10 nodes)
    - src/components/visuals/README.md (authored in Task 1 — the porter's primary reference)
    - src/components/PodLifecycleAnimation.astro (refactored — second reference)
  </read_first>
  <action>
    Select one slide from `/Users/viktor/Documents/GitHub/vedmichv/slidev-theme-vv/presentations/vv-demo/pages/` that has NOT been ported yet (slide 84 / PodLifecycle is already covered; skip it). Browse `10-complex-schemas.md` line 13-131 (AWS three-tier arch, ~10 nodes + 14 wires) or scan for simpler single-schema slides if the AWS three-tier is too ambitious for the metric.

    **START THE TIMER now (record a wall-clock timestamp in ISO 8601):**
    ```bash
    date -u +"%Y-%m-%dT%H:%M:%SZ" > /tmp/ttp-start.txt
    echo "Started time-to-port timer: $(cat /tmp/ttp-start.txt)"
    ```

    Create a scratch Astro component at `src/components/ScratchPortedSlide.astro` that uses VvStage + children to reproduce the chosen slide. Follow the README's Import block. Use the Iconify find+replace regex to translate any `<logos-X-Y/>` tokens in the source slide to `<Icon name="X:Y"/>`. Copy coordinates as-is from the slide (they are already in user-space per D-03).

    Temporarily wire the component into a scratch route for verification. Create `src/pages/en/scratch/ported-slide.astro`:
    ```astro
    ---
    import BaseLayout from '../../../layouts/BaseLayout.astro';
    import ScratchPortedSlide from '../../../components/ScratchPortedSlide.astro';
    ---
    <BaseLayout title="Scratch: ported slide" locale="en">
      <main class="mx-auto max-w-[1120px] px-4 py-12">
        <ScratchPortedSlide />
      </main>
    </BaseLayout>
    ```

    (If BaseLayout's prop shape requires other props, inspect `src/layouts/BaseLayout.astro` before wiring — don't spend timer budget on layout archaeology; use a minimal bare Astro page if needed: `<html><body><ScratchPortedSlide/></body></html>`.)

    Run `npm run build` — must pass. Run `npm run dev` and visually verify the scratch route shows the ported slide. Iterate as needed.

    **STOP THE TIMER when**:
    - `npm run build` passes
    - The new component renders correctly in the browser (visual parity with the Slidev source slide, modulo Deep Signal tones replacing Slidev tones)

    Record end timestamp + compute elapsed minutes:
    ```bash
    date -u +"%Y-%m-%dT%H:%M:%SZ" > /tmp/ttp-end.txt
    start=$(date -jf "%Y-%m-%dT%H:%M:%SZ" "$(cat /tmp/ttp-start.txt)" +%s 2>/dev/null || date -d "$(cat /tmp/ttp-start.txt)" +%s)
    end=$(date -jf "%Y-%m-%dT%H:%M:%SZ" "$(cat /tmp/ttp-end.txt)" +%s 2>/dev/null || date -d "$(cat /tmp/ttp-end.txt)" +%s)
    elapsed_sec=$((end - start))
    elapsed_min=$((elapsed_sec / 60))
    echo "Elapsed: ${elapsed_min} minutes (${elapsed_sec} seconds)"
    ```

    Write the report to `.planning/phases/01-rich-media-primitives/time-to-port-report.md`:
    ```markdown
    # Phase 1 — Time-to-port report (D-21)

    **Date:** {ISO date}
    **Ported by:** Claude (autonomous task in Phase 1 Plan 06)
    **Source slide:** /Users/viktor/Documents/GitHub/vedmichv/slidev-theme-vv/presentations/vv-demo/pages/{filename}.md — lines {range} — "{slide title}"
    **Node count:** {N}
    **Wire count:** {N}
    **Packet count:** {N}
    **Destination:** src/components/ScratchPortedSlide.astro + src/pages/en/scratch/ported-slide.astro

    ## Timing

    | Phase | Start (UTC) | End (UTC) | Elapsed |
    |-------|-------------|-----------|---------|
    | Total port | {ISO start} | {ISO end} | **{N} minutes** |

    ## What went fast

    - {observation 1 — e.g. "Icon find+replace regex worked first try on all 12 logo tokens"}
    - {observation 2}
    - {observation 3}

    ## What was slow

    - {observation 1 — e.g. "Edge-point math gave incorrect wire endpoints for diagonal wires; had to add fromPoint/toPoint overrides on 4 wires"}
    - {observation 2}

    ## Phase 7 recommendation

    Elapsed: {N} minutes

    | Threshold | Decision |
    |-----------|----------|
    | < 10 min | SKIP Phase 7 (codegen ROI insufficient) |
    | 10-15 min | USER DECIDES (borderline) |
    | > 15 min | TRIGGER Phase 7 (codegen) |

    **Recommendation:** {SKIP / USER DECIDES / TRIGGER}

    ## Cleanup

    After this report is filed and the user reviews the recommendation, the scratch files can be removed:
    ```bash
    rm src/components/ScratchPortedSlide.astro src/pages/en/scratch/ported-slide.astro
    rmdir src/pages/en/scratch 2>/dev/null || true
    ```

    These are experimental artifacts only — not part of the Phase 1 deliverable. The report itself (this file) IS part of the deliverable.
    ```

    After writing the report, CLEAN UP the scratch files:
    ```bash
    rm -f src/components/ScratchPortedSlide.astro src/pages/en/scratch/ported-slide.astro
    rmdir src/pages/en/scratch 2>/dev/null || true
    npm run build  # confirm build still passes after cleanup
    ```

    The scratch component + route were experimental infrastructure for the metric; leaving them in tree would pollute the homepage nav / sitemap. The timing report persists in `.planning/` as the authoritative record.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-rich-media-primitives/time-to-port-report.md && grep -c "^# Phase 1 — Time-to-port report" .planning/phases/01-rich-media-primitives/time-to-port-report.md && grep -c "Elapsed" .planning/phases/01-rich-media-primitives/time-to-port-report.md && grep -c "Recommendation:" .planning/phases/01-rich-media-primitives/time-to-port-report.md && grep -cE "Recommendation:[[:space:]]+(SKIP|USER DECIDES|TRIGGER)" .planning/phases/01-rich-media-primitives/time-to-port-report.md && ! test -f src/components/ScratchPortedSlide.astro && ! test -f src/pages/en/scratch/ported-slide.astro && LOC=$(wc -l < .planning/phases/01-rich-media-primitives/time-to-port-report.md) && test $LOC -ge 15 && npm run build 2>&1 | grep -E "Completed in"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-rich-media-primitives/time-to-port-report.md` exists with ≥ 15 lines
    - Contains an elapsed-time value in minutes
    - Contains a Phase 7 recommendation line in the exact form: `**Recommendation:** SKIP` / `USER DECIDES` / `TRIGGER`
    - Contains the source slide file path under `slidev-theme-vv/presentations/vv-demo/pages/`
    - Contains sections "What went fast" and "What was slow" with at least 1 bullet each
    - Scratch files have been cleaned up: `src/components/ScratchPortedSlide.astro` and `src/pages/en/scratch/ported-slide.astro` do NOT exist
    - `npm run build` still passes after cleanup (31+ pages)
  </acceptance_criteria>
  <done>Time-to-port metric captured and recorded. User has a data-driven input for the Phase 7 go/skip decision in Task 3 below.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: User reviews time-to-port + decides on Phase 7</name>
  <decision>
    Based on the measured time-to-port in `.planning/phases/01-rich-media-primitives/time-to-port-report.md`, decide whether to proceed with Phase 7 (Slidev → Astro codegen) later in the milestone, or skip it entirely.
  </decision>
  <context>
    Phase 7 is checkpoint-gated per ROADMAP and D-21. It is an OPTIONAL phase (CODEGEN-01 marked as such in REQUIREMENTS.md) that ships `scripts/slidev-to-astro.mjs` to automate slide porting. Estimated effort: 10-15h IF triggered. Only worth doing if the manual slide lift is still >15 min/slide after Phase 1 primitives land.

    The Task 2 measurement ports one real slide using the primitives + the newly written README. That measurement is the ground truth for the decision.

    Decision thresholds (from D-21):
    - <10 min elapsed → Phase 7 codegen ROI is insufficient; SKIP.
    - 10-15 min elapsed → Borderline; user weighs against other milestone priorities.
    - >15 min elapsed → Primitives alone do not hit the <10 min target; TRIGGER Phase 7.

    Additional factors the user should consider:
    - Number of remaining Slidev slides planned for future blog posts. If only 2-3 more slides will be ported over v1.0, codegen ROI is marginal even at 15-20 min/slide. If 10+ slides are queued (companion posts + Slidev integration Phase 5), codegen ROI grows.
    - Quality of the scratch port: did Claude hit visual parity, or did it need manual correction of edge-point coordinates via fromPoint/toPoint overrides? Codegen would need to emit those overrides automatically.
    - Appetite for the 10-15h additional scope within v1.0.
  </context>
  <options>
    <option id="skip-phase-7">
      <name>SKIP Phase 7 — primitives alone hit the target</name>
      <pros>Saves 10-15h of milestone scope. v1.0 ships sooner. Primitives + README are sufficient for all planned slide ports.</pros>
      <cons>If the actual slide-port cost creeps back up over time (authors forget shortcuts, new slide patterns emerge), the friction returns with no automation backup. Mitigate by revisiting Phase 7 in a future milestone if slide volume spikes.</cons>
    </option>
    <option id="defer-phase-7">
      <name>USER DECIDES — borderline; defer decision to Phase 5 or 6 kick-off</name>
      <pros>Keeps the option open without committing scope now. Decision reviewed when actual slide-port count in Phases 5-6 is clearer.</pros>
      <cons>Creates a lingering TODO. May not be revisited until future milestone. Keeps mental overhead on the roadmap.</cons>
    </option>
    <option id="trigger-phase-7">
      <name>TRIGGER Phase 7 — primitives alone are not enough</name>
      <pros>Automates slide porting end-to-end. Scales to 10+ slides without per-slide toil. Matches the original "<10 min/slide" target.</pros>
      <cons>Adds 10-15h to v1.0 scope. Delays shipping. Codegen complexity (HTML + scoped CSS parsing) is unknown — per RESEARCH.md PITFALLS, timebox to 20h and abort if exceeded.</cons>
    </option>
  </options>
  <resume-signal>
    Select: `skip-phase-7`, `defer-phase-7`, or `trigger-phase-7`.

    Upon selection:
    - `skip-phase-7`: update ROADMAP Phase 7 status to "SKIPPED (primitives hit target)"; Phase 1 ships. Run `/gsd-verify-work 1` next.
    - `defer-phase-7`: mark Phase 7 as "DEFERRED — revisit after Phase 5"; Phase 1 ships. Run `/gsd-verify-work 1` next.
    - `trigger-phase-7`: mark Phase 7 as "CONFIRMED"; Phase 1 ships; Phase 7 will be planned after Phase 6 completes (codegen depends on primitives + Slidev integration being stable). Run `/gsd-verify-work 1` next.

    ROADMAP update is a separate small edit — can be batched with the phase verification step or done as a one-liner edit after the checkpoint clears.
  </resume-signal>
  <files>.planning/ROADMAP.md (Phase 7 status update after decision)</files>
  <action>Human-only decision — user reviews time-to-port-report.md and selects one of the three Phase 7 options above. No Claude automation.</action>
  <verify>Manual — user selects skip-phase-7, defer-phase-7, or trigger-phase-7 and ROADMAP.md is updated accordingly.</verify>
  <done>User selects a Phase 7 option; ROADMAP.md Phase 7 status updated; Phase 1 deliverable complete.</done>
</task>

</tasks>

<verification>
Phase-level checks this plan contributes to:
- PRIMS-06 (README documenting primitives API + gotchas + Iconify vocabulary): shipped
- D-21 (time-to-port metric): measured, recorded, decision captured
- D-25 (README contents): all 9 required sections present
- ROADMAP Phase 7 gate: decision made (skip / defer / trigger) based on measured data
</verification>

<success_criteria>
- [ ] `src/components/visuals/README.md` exists with ≥ 200 lines, 9 top-level sections, 4 primitive subsections
- [ ] `.planning/phases/01-rich-media-primitives/time-to-port-report.md` exists with elapsed time + Phase 7 recommendation
- [ ] Scratch files (`ScratchPortedSlide.astro`, `/en/scratch/ported-slide.astro`) deleted after measurement
- [ ] `npm run build` passes post-cleanup
- [ ] User selects one of the three Phase 7 options; decision recorded
</success_criteria>

<output>
After completion, create `.planning/phases/01-rich-media-primitives/01-06-SUMMARY.md` recording:
- README line count + section inventory
- Time-to-port elapsed minutes + source slide reference
- User's Phase 7 decision (SKIP / USER DECIDES / TRIGGER)
- Confirmation that scratch files are removed + build is still green
- Any Phase 1 exit-criterion checks against ROADMAP §Phase 1 Success Criteria (all 5 should now be satisfied; this SUMMARY is the final verification log)
</output>
