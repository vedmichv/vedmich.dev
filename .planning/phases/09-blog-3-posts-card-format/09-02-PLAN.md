---
phase: 09-blog-3-posts-card-format
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - .claude/skills/vv-blog-from-vault/SKILL.md
  - .claude/skills/vv-blog-from-vault/scripts/vault-search.py
  - .claude/skills/vv-blog-from-vault/scripts/session-recall.py
  - .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh
  - .claude/skills/vv-blog-from-vault/references/voice-guide.md
  - .claude/skills/vv-blog-from-vault/references/translation-rules.md
  - .claude/skills/vv-blog-from-vault/references/frontmatter-schema.md
  - .claude/skills/vv-blog-from-vault/references/visuals-routing.md
  - .claude/skills/vv-blog-from-vault/references/companion-sources.md
  - .claude/skills/vv-blog-from-vault/workflows/new-post.md
  - .claude/skills/vv-blog-from-vault/workflows/update-post.md
autonomous: true
requirements: [REQ-002]
tags: [blog, skill, vault, qmd, recall, deploy, python, bash]

must_haves:
  truths:
    - "Project-local skill directory .claude/skills/vv-blog-from-vault/ exists and is discoverable by Claude Code"
    - "SKILL.md declares name + description with both English and Russian trigger phrases per D-35"
    - "SKILL.md description explicitly delegates to mermaid-pro, excalidraw, art, viktor-vedmich-design, recall, episodic-memory MCP, sync-claude-sessions (does not reimplement their logic)"
    - "SKILL.md does NOT mention playwright screenshot — screenshot verification is a manual step the user performs after the skill finishes (aligns with CLAUDE.md publishing workflow)"
    - "vault-search.py hard-excludes the three confidential vault prefixes (10-AWS/11-Active-Clients/, 10-AWS/14-Tips-AWS-Internal/, 10-AWS/16-Amazon-Employer/) before returning any result"
    - "deploy-post.sh uses the CLAUDE.md content commit template `Post: <title>` with NO Co-Authored-By trailer"
    - "deploy-post.sh rejects markdown files that use relative blog-assets/ paths (must be root-absolute /blog-assets/...)"
    - "deploy-post.sh does NOT call playwright (keeps the script MCP-extension-token-free and aligned with CLAUDE.md manual screenshot verification)"
    - "All 5 references/ markdown files and both workflows/ markdown files exist with non-empty content"
    - "frontmatter-schema.md documents that RU posts do NOT need an `author:` frontmatter override — the RU slug page's locale-aware render translates Latin 'Viktor Vedmich' to Cyrillic 'Виктор Ведмич' automatically (BLOCKER-1 fix from Wave 1 Task 3)"
    - "session-recall.py gracefully falls back to diary-file grep when MCP tools unavailable"
    - "Wave 3 content track can invoke the skill end-to-end — workflow steps are explicit enough that a Claude instance can execute them"
  artifacts:
    - path: ".claude/skills/vv-blog-from-vault/SKILL.md"
      provides: "Skill entry point with name, description (EN+RU triggers), context, vault map, 9-step workflow reference, hard-rules table, delegation references (no playwright mention per CLAUDE.md publishing manual-screenshot pattern)"
      contains: "name: vv-blog-from-vault"
      min_lines: 60
    - path: ".claude/skills/vv-blog-from-vault/scripts/vault-search.py"
      provides: "QMD wrapper with confidential-path post-filter"
      contains: "CONFIDENTIAL_PREFIXES"
      min_lines: 40
    - path: ".claude/skills/vv-blog-from-vault/scripts/session-recall.py"
      provides: "recall delegation + episodic-memory MCP + diary-grep fallback"
      contains: "25.70-Claude-Summaries"
      min_lines: 30
    - path: ".claude/skills/vv-blog-from-vault/scripts/deploy-post.sh"
      provides: "Build + grep gates + commit + push with CLAUDE.md Post: template (no playwright dependency — screenshot verification is manual per site-wide convention)"
      contains: "Post: ${TITLE}"
      min_lines: 20
    - path: ".claude/skills/vv-blog-from-vault/references/voice-guide.md"
      provides: "Locked voice rules (D-31) + anti-AI vocabulary ban list"
      contains: "tech-expert"
    - path: ".claude/skills/vv-blog-from-vault/references/translation-rules.md"
      provides: "Locked EN→RU translation rule table (D-30)"
      contains: "Karpenter"
    - path: ".claude/skills/vv-blog-from-vault/references/frontmatter-schema.md"
      provides: "Blog frontmatter schema mirror with examples + explicit RU author-field guidance (no override needed; RU slug page render handles Cyrillic translation automatically)"
      contains: "reading_time"
    - path: ".claude/skills/vv-blog-from-vault/references/visuals-routing.md"
      provides: "Priority 0 (reuse) / Priority 1 (delegate) decision tree per D-37 + D-39"
      contains: "karpenter-1000-clusters"
    - path: ".claude/skills/vv-blog-from-vault/references/companion-sources.md"
      provides: "Vault map of carousels/talks/podcast episodes per D-10"
      contains: "DOP202-Warsaw-Summit"
    - path: ".claude/skills/vv-blog-from-vault/workflows/new-post.md"
      provides: "9-step orchestration (topic → vault → recall → draft → translate → visuals → companion → verify → push) — verify step uses deploy-post.sh (no playwright call)"
      contains: "Step 9"
    - path: ".claude/skills/vv-blog-from-vault/workflows/update-post.md"
      provides: "5-step edit workflow for existing posts"
      contains: "Post: Update"
  key_links:
    - from: ".claude/skills/vv-blog-from-vault/scripts/vault-search.py"
      to: "confidential-path post-filter"
      via: "CONFIDENTIAL_PREFIXES tuple"
      pattern: "11-Active-Clients"
    - from: ".claude/skills/vv-blog-from-vault/scripts/deploy-post.sh"
      to: "CLAUDE.md publishing workflow"
      via: "Post: <title> commit template"
      pattern: "Post: "
    - from: ".claude/skills/vv-blog-from-vault/workflows/new-post.md"
      to: "mermaid-pro / excalidraw / art / recall"
      via: "natural-language skill delegation"
      pattern: "mermaid-pro"
    - from: ".claude/skills/vv-blog-from-vault/workflows/new-post.md"
      to: "mcp__qmd__query"
      via: "MCP tool call"
      pattern: "mcp__qmd"
    - from: ".claude/skills/vv-blog-from-vault/references/frontmatter-schema.md"
      to: "Wave 1 Task 3 locale-aware author render"
      via: "explicit RU frontmatter guidance — no author override needed"
      pattern: "Виктор Ведмич"
---

<objective>
Build the project-local skill `.claude/skills/vv-blog-from-vault/` — a reusable pipeline that grounds blog drafts in Viktor's Obsidian vault (QMD) and Claude Code session history (recall + episodic-memory), enforces the bilingual voice and translation rules, routes visual needs to the right generation skill (priority: reuse vault carousel PNGs first), and drives the CLAUDE.md publish flow (`Post: <title>` plain commit, push to main, auto-deploy). Screenshot verification is a manual step the user performs after the skill finishes — same as site-wide convention, so deploy-post.sh stays simple and playwright-dependency-free.

Purpose: Wave 3 (content) authors its 3 posts against this skill. Without the skill, Wave 3 would either (a) re-implement QMD+recall+translation+deploy ad-hoc per post, or (b) write posts from general knowledge instead of vault-grounded sources — both violate D-03 (ground in vault, not general knowledge) and D-05 (confidential exclusion). The skill is also reusable for every future vedmich.dev post beyond Phase 9.

Output:
- `.claude/skills/vv-blog-from-vault/SKILL.md` — entry point with triggers, context, vault map, hard-rules table
- `.claude/skills/vv-blog-from-vault/scripts/` — 3 files: `vault-search.py`, `session-recall.py`, `deploy-post.sh`
- `.claude/skills/vv-blog-from-vault/references/` — 5 files: `voice-guide.md`, `translation-rules.md`, `frontmatter-schema.md`, `visuals-routing.md`, `companion-sources.md`
- `.claude/skills/vv-blog-from-vault/workflows/` — 2 files: `new-post.md` (9-step orchestration), `update-post.md` (5-step edit flow)

Dependency: Wave 2 depends on Wave 1 (`depends_on: [09-01]`) because `references/frontmatter-schema.md` documents the exact Zod shape established in Wave 1 Task 1, including `reading_time`, `author`, `cover_image` — AND the RU locale-aware render pattern established in Wave 1 Task 3 (which dictates that RU posts do NOT need an `author:` override). Running Wave 2 before Wave 1 would ship a reference that doesn't match production schema or render behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-blog-3-posts-card-format/09-CONTEXT.md
@.planning/phases/09-blog-3-posts-card-format/09-RESEARCH.md
@.planning/phases/09-blog-3-posts-card-format/09-PATTERNS.md
@.planning/phases/09-blog-3-posts-card-format/09-01-SUMMARY.md
@CLAUDE.md
@src/content.config.ts

External analog files (NOT in this repo — read from absolute paths in <read_first> per task):
- ~/.claude/skills/vv-carousel/SKILL.md — brand-skill + QMD 2-query template
- ~/.claude/skills/sync-claude-sessions/SKILL.md — Python script + workflow skill template
- ~/.claude/skills/recall/SKILL.md — session recall wrapper pattern
- ~/.claude/skills/recall/workflows/topic.md — topic-mode orchestration
- ~/.claude/skills/recall/workflows/temporal.md — date-mode orchestration
- ~/.claude/skills/recall/scripts/recall-day.py — Python CLI with argparse pattern

<interfaces>
<!-- Schema contract from Wave 1 (produced by 09-01 Task 1) -->

Blog collection schema — reference exactly in `references/frontmatter-schema.md`:
```typescript
const blog = defineCollection({
  loader: glob({ pattern: '**/*.md', base: './src/content/blog' }),
  schema: z.object({
    title: z.string(),
    description: z.string(),
    date: z.coerce.date(),
    tags: z.array(z.string()),                          // required, non-empty array acceptable
    draft: z.boolean().default(false),
    author: z.string().default('Viktor Vedmich'),
    reading_time: z.number().optional(),                 // computed at build if absent
    cover_image: z.string().optional(),                  // not rendered in Phase 9
  }),
});
```

RU locale-aware author render — Wave 1 Task 3 established in `src/pages/ru/blog/[...slug].astro`:
```typescript
const authorDisplay = post.data.author === 'Viktor Vedmich'
  ? 'Виктор Ведмич'
  : post.data.author;
```
This means RU posts inherit the schema default (Latin "Viktor Vedmich") and the RU slug template translates it to Cyrillic at render. RU frontmatter does NOT need to override `author:`. `references/frontmatter-schema.md` MUST document this explicitly.

i18n keys available (from Wave 1 Task 1):
- `i.blog.title` — "Blog" / "Блог"
- `i.blog.all_posts` — "All posts" / "Все посты"
- `i.blog.read_more` — "Read more" / "Читать дальше"
- `i.blog.min_read` — "min read" / "мин чтения"
- `i.back_to_home` — "Back to Home" / "Назад на главную"

MCP tools available at skill runtime (per RESEARCH.md §"Environment Availability"):
- `mcp__qmd__query` — hybrid (BM25 + vector) search
- `mcp__qmd__search` — pure BM25
- `mcp__qmd__vsearch` — pure vector
- `mcp__qmd__get` — fetch document by path
- `mcp__qmd__multi-get` — fetch multiple documents
- `mcp__plugin_episodic-memory_episodic-memory__search` — semantic search across Claude sessions

CLI fallbacks (per RESEARCH.md):
- `qmd query <q>`, `qmd search <q>`, `qmd vsearch <q>`, `qmd get <path>` (qmd 2.1.0 at `/Users/viktor/.npm-global/bin/qmd`)
- `grep -r` over `~/Documents/ViktorVedmich/20-Calendar/25-diary/25.70-Claude-Summaries/*.md` (diary grep fallback)

Delegation targets (call by name in workflows — description-trigger matches):
- `mermaid-pro` (flowcharts, sequence, architecture diagrams)
- `excalidraw` (hand-sketched style)
- `art` (AI image generation — optimized prompts for NanoBanana / GPTImage)
- `viktor-vedmich-design` (brand-consistent visuals using the Deep Signal UI kit)
- `recall` (topic + temporal + graph modes)
- `sync-claude-sessions` (exports Claude sessions to vault — skill references as session source)

**Playwright NOT delegated** — screenshot verification is a manual step the user performs on live URLs after the skill finishes and GH Actions deploys (~60-90s). This matches CLAUDE.md "Deployment monitoring" pattern and keeps deploy-post.sh dependency-free (no MCP extension token requirement).

Commit convention (from CLAUDE.md + D-43 + RESEARCH.md Pitfall 6):
- Wave 1/Wave 2 infrastructure commits: `docs|feat|fix($PHASE-$PLAN): ...` WITH `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` trailer
- Wave 3 content commits: `Post: <title>` plain, NO Co-Authored-By trailer

Confidential vault exclusions (CLAUDE.md + D-05):
```python
CONFIDENTIAL_PREFIXES = (
    "10-AWS/11-Active-Clients/",
    "10-AWS/14-Tips-AWS-Internal/",
    "10-AWS/16-Amazon-Employer/",
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold skill directory + SKILL.md entry point with triggers, vault map, hard-rules, delegation references (D-32, D-33, D-35, D-36)</name>
  <files>.claude/skills/vv-blog-from-vault/SKILL.md</files>

  <read_first>
    - `/Users/viktor/.claude/skills/vv-carousel/SKILL.md` (brand-skill template — adapt frontmatter, vault map, 10-step workflow table, hard-rules table, references list)
    - `/Users/viktor/.claude/skills/sync-claude-sessions/SKILL.md` (project-local skill structure with Python scripts + workflows)
    - `/Users/viktor/.claude/skills/recall/SKILL.md` (workflow routing pattern)
    - `./CLAUDE.md` §"Publishing Workflow" (commit template + manual screenshot verification pattern) + §"Obsidian Vault Cross-References" (confidential paths) + §"User Context" (GSD language rules) + §"Deployment monitoring" (manual-verify-after-deploy pattern)
    - `.planning/phases/09-blog-3-posts-card-format/09-CONTEXT.md` D-32…D-44
    - `.planning/phases/09-blog-3-posts-card-format/09-PATTERNS.md` §11 (SKILL.md pattern assignment with frontmatter template)
  </read_first>

  <action>
  **1a. Create directory structure**:
  ```bash
  mkdir -p .claude/skills/vv-blog-from-vault/scripts
  mkdir -p .claude/skills/vv-blog-from-vault/references
  mkdir -p .claude/skills/vv-blog-from-vault/workflows
  ```

  **1b. Create `.claude/skills/vv-blog-from-vault/SKILL.md`** with the following sections and content. Treat this as the authoritative structure — do NOT omit sections. Do NOT mention playwright anywhere in this file — screenshot verification is a manual step the user performs on live URLs after GH Actions deploys (CLAUDE.md "Deployment monitoring" pattern).

  Frontmatter (at the very top of the file, `---` fenced):

  ```yaml
  ---
  name: vv-blog-from-vault
  description: |
    Generate and publish Viktor Vedmich's Deep Signal brand blog posts at vedmich.dev — grounded in the Obsidian vault (QMD) and Claude Code session history (recall + episodic-memory). Produces EN + RU markdown files in src/content/blog/{en,ru}/ with Zod-valid frontmatter (title, description, date, tags, optional author/reading_time/cover_image), optional inline visuals (reuses vault carousel PNGs first per D-39; delegates to mermaid-pro / excalidraw / art / viktor-vedmich-design), companion-link suggestions (talks in 44-Speaking, carousels in 45.20-Brand-Kit, podcast episodes in 32-DKT + 15.10-AWS-RU-Podcast), and a verify+push pipeline (npm run build + git commit "Post: <title>" plain with NO Co-Authored-By per CLAUDE.md + git push origin main + gh run list). After the skill finishes, the user verifies the live post manually (browser visit or playwright screenshot) on vedmich.dev — same as site-wide convention. Hard-excludes 10-AWS/11-Active-Clients/, 10-AWS/14-Tips-AWS-Internal/, 10-AWS/16-Amazon-Employer/ from every vault scan. Use when user says: "write a blog post", "new post about X", "новый пост", "новая статья", "статья из vault", "пост из доклада", "publish post", "deploy post", "blog from vault". NOT for: LinkedIn carousels (use vv-carousel), Slidev presentations (use separate slidev repos), confidential AWS client content.
  ---
  ```

  Body sections (in order):

  **## Context**
  ```markdown
  ## Context

  - **Viktor**: Senior AWS Solutions Architect in Germany. Audience for vedmich.dev blog: other senior engineers working on Kubernetes, AWS, AI-for-DevOps.
  - **Voice**: tech-expert from first person. "I've seen this fail in production" over "Some teams find…". Never neutral tutorial, never punchy-opinionated-for-its-own-sake. See `references/voice-guide.md`.
  - **Publishing**: git push to main. GH Actions auto-deploys in ~2 min. CLAUDE.md §"Adding a new blog post" is authoritative. Commit template is `Post: <title>` with NO Co-Authored-By trailer (content commits differ from code commits).
  - **Post-deploy verification**: manual — user visits the live URL on vedmich.dev (or runs `playwright-cli` separately) after the skill finishes. The skill itself does NOT call playwright. This matches CLAUDE.md "Deployment monitoring" pattern (Actions log + manual live check).
  - **Bilingual**: every post ships EN + RU in the same commit. RU is a full body translation per D-29, not frontmatter-only. Tech terms stay English per D-30. See `references/translation-rules.md`.
  - **Confidential vault exclusion**: vault search MUST hard-exclude `10-AWS/11-Active-Clients/`, `10-AWS/14-Tips-AWS-Internal/`, `10-AWS/16-Amazon-Employer/`. Enforced in `scripts/vault-search.py`.
  - **Zero JS**: blog posts must not import client-side JS libs. CSS/SVG animations only, and only if a section genuinely needs one. See `references/visuals-routing.md`.
  ```

  **## Where everything lives**
  ```markdown
  ## Where everything lives

  Blog markdown + assets live in the SITE repo (`~/Documents/GitHub/vedmich/vedmich.dev/`):

  ```
  src/content/blog/{en,ru}/YYYY-MM-DD-slug.md     ← posts (both locales, same commit)
  public/blog-assets/YYYY-MM-DD-slug/*.png        ← inline images (root-absolute paths in markdown)
  src/content.config.ts                           ← Zod schema (authoritative)
  src/i18n/{en,ru}.json                           ← UI strings (blog.title, blog.min_read, etc.)
  ```

  Source material lives in the VAULT (`~/Documents/ViktorVedmich/`):

  ```
  40-Content/45-Personal-Brand/45.20-Brand-Kit/carousel-templates/*/out/*.png  ← reusable 1080×1350 PNG slides
  40-Content/44-Speaking/44.20-Talk-Materials/*.md                             ← talk notes (stats, technical claims)
  40-Content/44-Speaking/44.10-CFPs/15.31-Talks-Materials/*.md                 ← CFP + chalk-talk notes
  20-Calendar/25-diary/25.70-Claude-Summaries/YYYY-MM-DD.md                    ← session summaries (opinion posts)
  30-Projects/33-Book-Kubernetes/                                              ← book chapters (K8s topics)
  30-Projects/37-AI-DevOps-course/                                             ← AI/MCP/Claude Code material
  70-PKM/73-KB-Tech/                                                           ← curated tech KB
  70-PKM/77-Connections/YYYY/                                                  ← topical notes
  ```

  **Never read** (confidential — enforced in `scripts/vault-search.py` post-filter):
  - `10-AWS/11-Active-Clients/`
  - `10-AWS/14-Tips-AWS-Internal/`
  - `10-AWS/16-Amazon-Employer/`
  ```

  **## Workflow**
  ```markdown
  ## Workflow

  See `workflows/new-post.md` for the 9-step pipeline: clarify topic → vault search → session recall → companion check → draft EN → translate RU → visuals audit → related section → verify+push.

  See `workflows/update-post.md` for the 5-step edit flow: identify post → load EN+RU → apply edits → verify build → push.
  ```

  **## Hard rules**

  Include the table verbatim from the PATTERNS.md §11 "Hard rules" block — 11 rows covering:

  ```markdown
  ## Hard rules

  | Rule | Why |
  |------|-----|
  | Never fabricate stats/claims | Must come from vault (QMD) or session-history (recall + episodic-memory). Blog = Viktor's credibility. |
  | Exclude confidential vault paths | `10-AWS/11-Active-Clients/*`, `10-AWS/14-Tips-AWS-Internal/*`, `10-AWS/16-Amazon-Employer/*` — CLAUDE.md + D-05. Enforced in `scripts/vault-search.py`. |
  | Voice: tech-expert, first person | "I've seen this fail" / "На клиентах я видел" — D-31. See `references/voice-guide.md`. |
  | Tech terms stay English in RU | `Karpenter`, `NodePool`, `MCP server`, `Cluster Autoscaler`, `YAML`, `kubectl`, flag names — D-30. See `references/translation-rules.md`. |
  | RU body is a full translation, not frontmatter-only | Blog prose is meant to be read — D-29. |
  | Reuse vault visuals FIRST | Check `45.20-Brand-Kit/carousel-templates/*/out/*.png` before generating new — D-39. See `references/visuals-routing.md`. |
  | Zero JS in posts | Only CSS/SVG animations if any (deferred per D-40) — CLAUDE.md. |
  | Commit: `Post: <title>`, NO Co-Authored-By | Content commits differ from code — CLAUDE.md + D-43. Enforced in `scripts/deploy-post.sh`. |
  | Image paths root-absolute `/blog-assets/...` | Relative paths break at `/ru/blog/...` — Pitfall 5. Grep gate in `deploy-post.sh`. |
  | Both locales in same commit | Bilingual constraint — CLAUDE.md. |
  | Frontmatter tags required | Schema tightened in Wave 1 — at least 1 tag per post. See `references/frontmatter-schema.md`. |
  | RU posts do NOT need `author:` override | Wave 1 Task 3 RU slug page translates schema-default "Viktor Vedmich" to "Виктор Ведмич" at render time — leave `author:` out of RU frontmatter. See `references/frontmatter-schema.md`. |
  ```

  **## Delegation targets**

  ```markdown
  ## Delegation targets

  This skill delegates rather than duplicating. Reference targets by name in workflow steps — description-trigger match activates them:

  - **`mermaid-pro`** — flowcharts, sequence diagrams, architecture (use for Karpenter consolidation loop, MCP client↔server diagram)
  - **`excalidraw`** — hand-sketched whiteboard style
  - **`art`** — AI image generation with optimized prompt (NanoBanana / GPTImage)
  - **`viktor-vedmich-design`** — brand-consistent visuals using the Deep Signal UI kit
  - **`recall`** — session temporal/topic/graph modes (most-used: topic mode for manifests-by-hand post)
  - **`sync-claude-sessions`** — not invoked live; read as the source of `20-Calendar/25-diary/25.70-Claude-Summaries/*.md` session exports

  MCP tools invoked directly (not via another skill):
  - `mcp__qmd__query`, `mcp__qmd__search`, `mcp__qmd__vsearch`, `mcp__qmd__get`, `mcp__qmd__multi-get`
  - `mcp__plugin_episodic-memory_episodic-memory__search`

  **Not delegated:** playwright. Screenshot verification of the deployed post is a manual step the user performs on vedmich.dev after GH Actions finishes (~60-90s after push). This matches the CLAUDE.md "Deployment monitoring" convention and keeps deploy-post.sh MCP-extension-token-free.

  Do NOT use `Skill()` syntax — that is agent orchestration, not user workflow.
  ```

  **## References**

  ```markdown
  ## References

  Read these as needed — do not load all five by default:

  - **`references/voice-guide.md`** — voice rules (D-31) + ban list (anti-AI vocabulary)
  - **`references/translation-rules.md`** — EN→RU rules (D-30) with examples
  - **`references/frontmatter-schema.md`** — full Zod schema from `src/content.config.ts` (post-Wave 1) + RU author-field guidance (no override needed) + examples per D-06 post type
  - **`references/visuals-routing.md`** — Priority 0 (reuse carousel PNGs) / Priority 1 (route to mermaid-pro / excalidraw / art / viktor-vedmich-design) decision tree
  - **`references/companion-sources.md`** — vault map: carousels at 45.20, talks at 44.20, podcast at 32-DKT + 15.10

  Scripts at `scripts/`:
  - **`scripts/vault-search.py`** — QMD wrapper with confidential-path post-filter
  - **`scripts/session-recall.py`** — recall delegation + episodic-memory MCP + diary-grep fallback
  - **`scripts/deploy-post.sh`** — verify + commit + push with CLAUDE.md Post: template (no playwright call — screenshot verification is manual post-deploy per site-wide convention)

  Workflows at `workflows/`:
  - **`workflows/new-post.md`** — 9-step new-post orchestration
  - **`workflows/update-post.md`** — 5-step edit flow
  ```

  Commit this task as `feat(09-02): scaffold vv-blog-from-vault skill entry point`. Co-Authored-By trailer is MANDATORY (infrastructure commit).
  </action>

  <verify>
    <automated>test -d .claude/skills/vv-blog-from-vault/scripts &amp;&amp; test -d .claude/skills/vv-blog-from-vault/references &amp;&amp; test -d .claude/skills/vv-blog-from-vault/workflows &amp;&amp; test -f .claude/skills/vv-blog-from-vault/SKILL.md &amp;&amp; grep -q "^name: vv-blog-from-vault" .claude/skills/vv-blog-from-vault/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - `test -d .claude/skills/vv-blog-from-vault/scripts` — scripts/ subdir exists
    - `test -d .claude/skills/vv-blog-from-vault/references` — references/ subdir exists
    - `test -d .claude/skills/vv-blog-from-vault/workflows` — workflows/ subdir exists
    - `test -f .claude/skills/vv-blog-from-vault/SKILL.md` — entry point file exists
    - `head -n 1 .claude/skills/vv-blog-from-vault/SKILL.md` prints `---` (YAML frontmatter fence)
    - `grep -q "^name: vv-blog-from-vault$" .claude/skills/vv-blog-from-vault/SKILL.md` — skill name declared
    - `grep -q "description:" .claude/skills/vv-blog-from-vault/SKILL.md` — description field present
    - `grep -q "новый пост" .claude/skills/vv-blog-from-vault/SKILL.md` — RU trigger phrase (D-35)
    - `grep -q "новая статья" .claude/skills/vv-blog-from-vault/SKILL.md` — RU trigger phrase (D-35)
    - `grep -q "write a blog post" .claude/skills/vv-blog-from-vault/SKILL.md` — EN trigger phrase (D-35)
    - `grep -q "publish post" .claude/skills/vv-blog-from-vault/SKILL.md` — deploy trigger (D-35)
    - `grep -q "11-Active-Clients" .claude/skills/vv-blog-from-vault/SKILL.md` — confidential exclusion cited in description (D-05)
    - `grep -q "mermaid-pro" .claude/skills/vv-blog-from-vault/SKILL.md` — delegation target mentioned (D-36)
    - `grep -q "excalidraw" .claude/skills/vv-blog-from-vault/SKILL.md` — delegation target mentioned (D-36)
    - `grep -q "art" .claude/skills/vv-blog-from-vault/SKILL.md` — delegation target mentioned (D-36) (note: this grep is case-sensitive; skill name `art` will match)
    - `grep -q "viktor-vedmich-design" .claude/skills/vv-blog-from-vault/SKILL.md` — delegation target mentioned (D-36)
    - `grep -q "recall" .claude/skills/vv-blog-from-vault/SKILL.md` — delegation target mentioned (D-36)
    - `grep -q "mcp__qmd" .claude/skills/vv-blog-from-vault/SKILL.md` — direct MCP tool call cited
    - `grep -q "Post: <title>" .claude/skills/vv-blog-from-vault/SKILL.md` — content commit template (D-43)
    - `grep -q "NO Co-Authored-By" .claude/skills/vv-blog-from-vault/SKILL.md` — content commit rule distinguishes from infrastructure commits
    - `! grep -i "playwright" .claude/skills/vv-blog-from-vault/SKILL.md` — WARNING-3 fix: SKILL.md does NOT mention playwright (screenshot verification is manual per CLAUDE.md publishing flow, not part of the skill)
    - `grep -Eq "^## Context" .claude/skills/vv-blog-from-vault/SKILL.md` — Context section present
    - `grep -Eq "^## Where everything lives" .claude/skills/vv-blog-from-vault/SKILL.md` — Vault map section present
    - `grep -Eq "^## Workflow" .claude/skills/vv-blog-from-vault/SKILL.md` — Workflow section present
    - `grep -Eq "^## Hard rules" .claude/skills/vv-blog-from-vault/SKILL.md` — Hard-rules section present
    - `grep -Eq "^## Delegation targets" .claude/skills/vv-blog-from-vault/SKILL.md` — Delegation section present
    - `grep -Eq "^## References" .claude/skills/vv-blog-from-vault/SKILL.md` — References section present
    - `grep -q "RU posts do NOT need .author: override\|author: override" .claude/skills/vv-blog-from-vault/SKILL.md` — BLOCKER-1 Wave 1 tie-in documented in Hard rules
    - `wc -l .claude/skills/vv-blog-from-vault/SKILL.md | awk '{print $1}'` returns ≥ 60 (non-trivial content)
    - Non-comment lines check: `grep -v '^[[:space:]]*#\|^---\|^[[:space:]]*$' .claude/skills/vv-blog-from-vault/SKILL.md | wc -l` returns ≥ 30 (substantive content)
  </acceptance_criteria>

  <done>
    The skill scaffold exists with a valid SKILL.md that declares triggers (EN+RU), delegation targets, the confidential exclusion policy, the CLAUDE.md commit template, the Wave 1 RU-author-no-override guidance, and pointers to references/ and workflows/. SKILL.md does NOT mention playwright — screenshot verification is a manual step the user performs after deploy. Claude Code's skill discovery can activate it on user phrases like "новый пост" or "write a blog post about X".
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create 3 scripts — vault-search.py (QMD + confidential filter), session-recall.py (recall + diary fallback), deploy-post.sh (build + grep gate + commit + push, no playwright) (D-05, D-34, D-43)</name>
  <files>.claude/skills/vv-blog-from-vault/scripts/vault-search.py, .claude/skills/vv-blog-from-vault/scripts/session-recall.py, .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh</files>

  <read_first>
    - `/Users/viktor/.claude/skills/recall/scripts/recall-day.py` (Python CLI structure with argparse — copy the shell)
    - `/Users/viktor/.claude/skills/recall/workflows/topic.md` (shows the `mcp__qmd__search` + `mcp__plugin_episodic-memory_...` usage pattern)
    - `./CLAUDE.md` §"Publishing Workflow → Adding a new blog post" (canonical 5-step flow — no playwright call in the deploy script itself; user verifies manually after deploy)
    - `.planning/phases/09-blog-3-posts-card-format/09-PATTERNS.md` §12 (vault-search.py), §13 (session-recall.py), §14 (deploy-post.sh)
    - `.planning/phases/09-blog-3-posts-card-format/09-RESEARCH.md` Code Example 4 (carousel copy), Pitfall 4 (confidential filter), Pitfall 5 (relative image paths), Pitfall 6 (commit convention)
  </read_first>

  <action>
  **2a. Create `.claude/skills/vv-blog-from-vault/scripts/vault-search.py`** — Python 3 (stdlib + optional `subprocess` to `qmd` CLI fallback). File content in full:

  ```python
  #!/usr/bin/env python3
  """Vault search wrapper for vv-blog-from-vault skill.

  Uses QMD MCP (via CLI fallback) for hybrid lex+vec search, then post-filters
  results to exclude confidential paths per CLAUDE.md and Phase 9 D-05.

  Usage:
      vault-search.py <topic> [--limit N] [--collection vault|sessions]
      vault-search.py --check PATH         # policy check: is PATH allowed?
      vault-search.py --list-excluded       # print the confidential prefix list

  Examples:
      vault-search.py "karpenter right sizing"
      vault-search.py "mcp protocol chalk talk" --collection vault --limit 10
      vault-search.py --check "10-AWS/11-Active-Clients/acme/foo.md"  # prints "DENIED"
      vault-search.py --check "40-Content/44-Speaking/foo.md"           # prints "ALLOWED"

  Confidential exclusion policy (CLAUDE.md + Phase 9 D-05):
      - 10-AWS/11-Active-Clients/
      - 10-AWS/14-Tips-AWS-Internal/
      - 10-AWS/16-Amazon-Employer/
  """

  import argparse
  import json
  import subprocess
  import sys
  from pathlib import Path

  CONFIDENTIAL_PREFIXES = (
      "10-AWS/11-Active-Clients/",
      "10-AWS/14-Tips-AWS-Internal/",
      "10-AWS/16-Amazon-Employer/",
  )

  VAULT_ROOT = Path.home() / "Documents" / "ViktorVedmich"


  def allowed(path: str) -> bool:
      """Return True if path is not inside any confidential prefix.

      Checks both bare prefix and '/<prefix>' substring to catch absolute paths.
      """
      return not any(path.startswith(p) or f"/{p}" in path for p in CONFIDENTIAL_PREFIXES)


  def qmd_query(topic: str, limit: int = 10, collection: str = "vault") -> list[dict]:
      """Invoke qmd CLI with hybrid lex+vec query. Returns list of result dicts.

      Prefers the CLI because MCP tool invocation from a Python subprocess requires
      specific MCP host wiring. Workflow-layer (new-post.md) can also call
      `mcp__qmd__query` directly from the Claude agent.
      """
      try:
          result = subprocess.run(
              ["qmd", "query", topic, "--limit", str(limit), "--json"],
              capture_output=True,
              text=True,
              check=False,
              timeout=30,
          )
          if result.returncode != 0:
              print(f"qmd CLI failed: {result.stderr}", file=sys.stderr)
              return []
          return json.loads(result.stdout or "[]")
      except FileNotFoundError:
          print("qmd CLI not found on PATH. Install via `npm i -g qmd` or invoke mcp__qmd__query directly.", file=sys.stderr)
          return []
      except json.JSONDecodeError:
          print("qmd CLI returned non-JSON output.", file=sys.stderr)
          return []
      except subprocess.TimeoutExpired:
          print("qmd CLI timed out after 30s.", file=sys.stderr)
          return []


  def filter_confidential(results: list[dict]) -> list[dict]:
      """Drop any result whose `path` field is inside a confidential prefix."""
      return [r for r in results if allowed(r.get("path", ""))]


  def main() -> int:
      ap = argparse.ArgumentParser(description="Vault search with confidential-path post-filter")
      ap.add_argument("topic", nargs="?", help="search topic")
      ap.add_argument("--limit", type=int, default=10, help="max results before filtering")
      ap.add_argument("--collection", default="vault", choices=("vault", "sessions"), help="QMD collection")
      ap.add_argument("--check", metavar="PATH", help="policy check: is PATH allowed?")
      ap.add_argument("--list-excluded", action="store_true", help="print confidential prefixes")
      args = ap.parse_args()

      if args.list_excluded:
          for p in CONFIDENTIAL_PREFIXES:
              print(p)
          return 0

      if args.check is not None:
          verdict = "ALLOWED" if allowed(args.check) else "DENIED"
          print(verdict)
          return 0 if verdict == "ALLOWED" else 1

      if not args.topic:
          ap.error("topic is required unless --check or --list-excluded is used")

      raw = qmd_query(args.topic, limit=args.limit, collection=args.collection)
      filtered = filter_confidential(raw)
      # Emit as JSON lines so workflow can pipe to jq / other tools
      for r in filtered:
          print(json.dumps(r, ensure_ascii=False))
      return 0


  if __name__ == "__main__":
      raise SystemExit(main())
  ```

  Make executable: `chmod +x .claude/skills/vv-blog-from-vault/scripts/vault-search.py`.

  **2b. Create `.claude/skills/vv-blog-from-vault/scripts/session-recall.py`** — wrapper that prefers `recall` skill delegation at the workflow layer, but ships a Python fallback that greps diary files when MCP is unavailable. File content in full:

  ```python
  #!/usr/bin/env python3
  """Session recall wrapper for vv-blog-from-vault skill.

  For a topic, aggregates experience grounding from three sources (best to worst):
    1. `recall` skill (topic mode) — delegated at workflow layer (not called here)
    2. `mcp__plugin_episodic-memory_episodic-memory__search` — called at workflow layer
    3. Diary grep over ~/Documents/ViktorVedmich/20-Calendar/25-diary/25.70-Claude-Summaries/*.md

  This script is the Tier-3 fallback. Workflows should prefer `recall` and the MCP
  episodic-memory tool first; this script guarantees we always have SOMETHING.

  Usage:
      session-recall.py <topic> [--since YYYY-MM-DD] [--limit N]

  Examples:
      session-recall.py "kubernetes manifests"
      session-recall.py "mcp server" --since 2026-04-01 --limit 20
  """

  import argparse
  import re
  import sys
  from pathlib import Path

  DIARY_DIR = Path.home() / "Documents" / "ViktorVedmich" / "20-Calendar" / "25-diary" / "25.70-Claude-Summaries"


  def grep_diary(topic: str, since: str | None = None, limit: int = 10) -> list[tuple[str, str]]:
      """Return a list of (diary_path, matching_line) tuples for topic matches.

      Case-insensitive substring match. Honors `--since` YYYY-MM-DD floor based on filename.
      """
      if not DIARY_DIR.is_dir():
          print(f"Diary dir not found: {DIARY_DIR}", file=sys.stderr)
          return []

      needle = topic.lower()
      hits: list[tuple[str, str]] = []
      since_str = since or ""

      for md in sorted(DIARY_DIR.glob("*.md")):
          if since_str and md.stem < since_str:
              continue
          try:
              for line in md.read_text(encoding="utf-8", errors="ignore").splitlines():
                  if needle in line.lower():
                      hits.append((str(md), line.strip()))
                      if len(hits) >= limit:
                          return hits
          except OSError:
              continue
      return hits


  def main() -> int:
      ap = argparse.ArgumentParser(description="Session recall fallback — diary grep")
      ap.add_argument("topic", help="search topic")
      ap.add_argument("--since", help="YYYY-MM-DD floor (inclusive)")
      ap.add_argument("--limit", type=int, default=10)
      args = ap.parse_args()

      hits = grep_diary(args.topic, since=args.since, limit=args.limit)
      if not hits:
          print(f"No diary matches for: {args.topic}", file=sys.stderr)
          return 1

      for path, line in hits:
          print(f"{path}: {line}")
      return 0


  if __name__ == "__main__":
      raise SystemExit(main())
  ```

  Make executable: `chmod +x .claude/skills/vv-blog-from-vault/scripts/session-recall.py`.

  **2c. Create `.claude/skills/vv-blog-from-vault/scripts/deploy-post.sh`** — bash script that runs build + grep gates + stages files + commits with CLAUDE.md template + pushes. Does NOT call playwright (WARNING-3 fix: screenshot verification is a manual step the user performs on the live URL after GH Actions deploys, matching CLAUDE.md "Deployment monitoring" convention). File content in full:

  ```bash
  #!/usr/bin/env bash
  # deploy-post.sh — verify + commit + push a new blog post.
  # CLAUDE.md publishing workflow (§ "Adding a new blog post") is authoritative.
  # Commit message template is "Post: <title>" — NO Co-Authored-By trailer for content.
  #
  # This script does NOT run playwright. Screenshot verification of the deployed
  # post is a manual step the user performs on the live vedmich.dev URL after
  # GH Actions finishes (~60-90s after push). This matches the CLAUDE.md
  # "Deployment monitoring" convention (Actions log + manual live check) and
  # keeps this script dependency-free (no MCP extension token required).
  #
  # Usage:
  #   deploy-post.sh <slug> "<title>"
  #
  # Example:
  #   deploy-post.sh 2026-03-20-karpenter-right-sizing "Karpenter in production: right-sizing at scale"
  #
  # Preconditions:
  #   - src/content/blog/en/<slug>.md exists
  #   - src/content/blog/ru/<slug>.md exists
  #   - Optional: public/blog-assets/<slug>/ exists if the post has inline images
  #   - Working tree is otherwise clean (no unrelated staged changes)

  set -euo pipefail

  if [ "$#" -ne 2 ]; then
    echo "Usage: $0 <slug> \"<title>\"" >&2
    exit 2
  fi

  SLUG="$1"
  TITLE="$2"

  EN_FILE="src/content/blog/en/${SLUG}.md"
  RU_FILE="src/content/blog/ru/${SLUG}.md"
  ASSETS_DIR="public/blog-assets/${SLUG}"

  # 1. Both locale files must exist (bilingual constraint — CLAUDE.md)
  if [ ! -f "$EN_FILE" ]; then
    echo "ERROR: EN file missing: $EN_FILE" >&2
    exit 1
  fi
  if [ ! -f "$RU_FILE" ]; then
    echo "ERROR: RU file missing: $RU_FILE" >&2
    exit 1
  fi

  # 2. Grep gate: no relative blog-assets/ paths in any blog markdown
  #    (Pitfall 5 — relative paths break at /ru/blog/<slug>)
  if grep -rnE '\]\(blog-assets/' src/content/blog/ 2>/dev/null; then
    echo "ERROR: relative blog-assets path(s) found. Use /blog-assets/... (root-absolute)." >&2
    exit 1
  fi

  # 3. Astro build must succeed (Zod schema + route compile + markdown pipeline)
  echo "Running npm run build..."
  npm run build

  # 4. Stage content files (+ assets if present)
  git add "$EN_FILE" "$RU_FILE"
  if [ -d "$ASSETS_DIR" ]; then
    git add "$ASSETS_DIR"
  fi

  # 5. Commit with CLAUDE.md template — NO Co-Authored-By trailer for content
  git commit -m "Post: ${TITLE}"

  # 6. Push to main (auto-deploys via GH Actions in ~60-90s)
  git push origin main

  # 7. Report deploy status
  echo ""
  echo "Commit pushed. GH Actions deploy status:"
  gh run list --branch main --limit 3 || echo "(gh CLI unavailable — check https://github.com/vedmichv/vedmich.dev/actions manually)"

  echo ""
  echo "Live URLs (verify manually in browser after deploy finishes ~60-90s):"
  echo "  EN: https://vedmich.dev/en/blog/${SLUG}"
  echo "  RU: https://vedmich.dev/ru/blog/${SLUG}"
  echo ""
  echo "(Screenshot verification is manual — use playwright-cli separately if desired.)"
  ```

  Make executable: `chmod +x .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh`.

  **2d. Verify all scripts**:
  ```bash
  # Syntax check bash script
  bash -n .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh

  # Syntax check + import check Python scripts
  python3 -c "import ast; ast.parse(open('.claude/skills/vv-blog-from-vault/scripts/vault-search.py').read())"
  python3 -c "import ast; ast.parse(open('.claude/skills/vv-blog-from-vault/scripts/session-recall.py').read())"

  # Policy check test
  python3 .claude/skills/vv-blog-from-vault/scripts/vault-search.py --check "10-AWS/11-Active-Clients/acme/foo.md" | grep -q "DENIED"
  python3 .claude/skills/vv-blog-from-vault/scripts/vault-search.py --check "40-Content/44-Speaking/foo.md" | grep -q "ALLOWED"

  # List excluded prefixes
  python3 .claude/skills/vv-blog-from-vault/scripts/vault-search.py --list-excluded | wc -l | grep -q "^[[:space:]]*3$"

  # No playwright reference in deploy-post.sh (WARNING-3 fix)
  ! grep -iE 'playwright|screenshot[^ ]*--' .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh
  ```

  All six verifications must pass. If diary dir is not present on this machine, session-recall.py will still pass syntax check — its runtime behavior is tested in Wave 3 when manifests post is drafted.

  Commit message: `feat(09-02): skill scripts — vault-search, session-recall, deploy-post`. Co-Authored-By trailer required.
  </action>

  <verify>
    <automated>python3 -c "import ast; ast.parse(open('.claude/skills/vv-blog-from-vault/scripts/vault-search.py').read())" &amp;&amp; python3 -c "import ast; ast.parse(open('.claude/skills/vv-blog-from-vault/scripts/session-recall.py').read())" &amp;&amp; bash -n .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh &amp;&amp; python3 .claude/skills/vv-blog-from-vault/scripts/vault-search.py --check "10-AWS/11-Active-Clients/foo.md" | grep -q "DENIED" &amp;&amp; python3 .claude/skills/vv-blog-from-vault/scripts/vault-search.py --check "40-Content/44-Speaking/foo.md" | grep -q "ALLOWED" &amp;&amp; ! grep -qi "playwright" .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh</automated>
  </verify>

  <acceptance_criteria>
    - `test -x .claude/skills/vv-blog-from-vault/scripts/vault-search.py` — executable bit set
    - `test -x .claude/skills/vv-blog-from-vault/scripts/session-recall.py` — executable bit set
    - `test -x .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh` — executable bit set
    - `head -n 1 .claude/skills/vv-blog-from-vault/scripts/vault-search.py` prints `#!/usr/bin/env python3`
    - `head -n 1 .claude/skills/vv-blog-from-vault/scripts/session-recall.py` prints `#!/usr/bin/env python3`
    - `head -n 1 .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh` prints `#!/usr/bin/env bash`
    - `grep -q "CONFIDENTIAL_PREFIXES" .claude/skills/vv-blog-from-vault/scripts/vault-search.py` — confidential filter tuple declared
    - `grep -q "10-AWS/11-Active-Clients/" .claude/skills/vv-blog-from-vault/scripts/vault-search.py` — prefix 1
    - `grep -q "10-AWS/14-Tips-AWS-Internal/" .claude/skills/vv-blog-from-vault/scripts/vault-search.py` — prefix 2
    - `grep -q "10-AWS/16-Amazon-Employer/" .claude/skills/vv-blog-from-vault/scripts/vault-search.py` — prefix 3
    - `grep -q "def allowed" .claude/skills/vv-blog-from-vault/scripts/vault-search.py` — policy function declared
    - `grep -q "25.70-Claude-Summaries" .claude/skills/vv-blog-from-vault/scripts/session-recall.py` — diary fallback path
    - `grep -q "Post: \${TITLE}" .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh` — CLAUDE.md commit template (D-43)
    - `! grep "Co-Authored-By" .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh` — content commit has NO trailer (D-43)
    - `! grep -i "playwright" .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh` — WARNING-3 fix: no playwright invocation in deploy script (manual verification instead)
    - `grep -q "manual" .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh` — deploy-post.sh comment explains manual verification convention
    - `grep -q "grep -rnE" .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh` — relative-path grep gate present (Pitfall 5)
    - `grep -q "'\\](blog-assets/'" .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh || grep -q '](blog-assets/' .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh` — grep matches the relative-path pattern
    - `grep -q "npm run build" .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh` — build gate
    - `grep -q "git push origin main" .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh` — push step
    - `grep -q "gh run list" .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh` — deploy status report
    - `bash -n .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh` — bash syntax valid
    - `python3 -c "import ast; ast.parse(open('.claude/skills/vv-blog-from-vault/scripts/vault-search.py').read())"` — Python syntax valid
    - `python3 -c "import ast; ast.parse(open('.claude/skills/vv-blog-from-vault/scripts/session-recall.py').read())"` — Python syntax valid
    - `python3 .claude/skills/vv-blog-from-vault/scripts/vault-search.py --check "10-AWS/11-Active-Clients/acme/foo.md"` prints `DENIED`
    - `python3 .claude/skills/vv-blog-from-vault/scripts/vault-search.py --check "40-Content/44-Speaking/foo.md"` prints `ALLOWED`
    - `python3 .claude/skills/vv-blog-from-vault/scripts/vault-search.py --list-excluded | wc -l` returns `3`
  </acceptance_criteria>

  <done>
    All 3 scripts are syntactically valid, executable, and enforce the CLAUDE.md/D-05/D-43 rules. Policy-check unit test passes (DENIED for confidential, ALLOWED for safe). Deploy script uses the content commit template with NO Co-Authored-By trailer, includes the relative-path grep gate, and does NOT call playwright (screenshot verification is manual per site-wide convention).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3a: Create 5 reference docs — voice-guide, translation-rules, frontmatter-schema (with RU author-field guidance), visuals-routing, companion-sources (D-06, D-10, D-25, D-30, D-31, D-37, D-39)</name>
  <files>.claude/skills/vv-blog-from-vault/references/voice-guide.md, .claude/skills/vv-blog-from-vault/references/translation-rules.md, .claude/skills/vv-blog-from-vault/references/frontmatter-schema.md, .claude/skills/vv-blog-from-vault/references/visuals-routing.md, .claude/skills/vv-blog-from-vault/references/companion-sources.md</files>

  <read_first>
    - `/Users/viktor/.claude/skills/vv-carousel/references/typography.md` (locked-spec reference format template)
    - `/Users/viktor/.claude/skills/vv-carousel/references/primitives.md` (table-locked reference format template)
    - `./src/content.config.ts` (post-Wave 1 blog schema — exactly mirror this in frontmatter-schema.md)
    - `./src/content/blog/en/hello-world.md` (real frontmatter example)
    - `./src/content/blog/ru/hello-world.md` (real RU frontmatter example — proves NO `author:` field needed)
    - `./src/pages/ru/blog/[...slug].astro` (Wave 1 Task 3 output — contains the `authorDisplay` locale-aware constant that frontmatter-schema.md documents)
    - `.planning/phases/09-blog-3-posts-card-format/09-CONTEXT.md` D-06 (word counts), D-30 (translation rules), D-31 (voice), D-37 (visuals signals), D-39 (reuse-first), D-10 (companion sources)
    - `.planning/phases/09-blog-3-posts-card-format/09-RESEARCH.md` Pattern 5 + Code Example 6 (visuals routing)
    - `.planning/phases/09-blog-3-posts-card-format/09-PATTERNS.md` §15 (voice-guide), §16 (translation-rules), §17 (frontmatter-schema), §18 (visuals-routing), §19 (companion-sources)
  </read_first>

  <action>
  Create 5 reference files with the content below. Each file must be non-trivial — do not emit stubs. The file `frontmatter-schema.md` must explicitly document the Wave 1 Task 3 RU locale-aware author-render pattern so Wave 3 authors do not accidentally add `author:` overrides to RU posts (BLOCKER-1 Wave 2 tie-in).

  **3a-1. `references/voice-guide.md`** (locked-spec format per vv-carousel/references/typography.md):

  ```markdown
  # Voice Guide — locked

  **Do not deviate.** Voice is **tech-expert from first person** (CONTEXT.md D-31). Audience is senior engineers (Kubernetes, AWS, AI-for-DevOps). Never neutral tutorial; never punchy-opinionated-for-its-own-sake.

  ## Stance

  - "I've seen this fail in production" over "Some teams find…"
  - Ground every claim in either the vault (QMD source) or session history (`recall` / episodic-memory). Never write from general knowledge.
  - Acknowledge when you'd use the opposite approach (e.g. manifests post: "I'd reach for Helm on a 100-service platform anyway").
  - When a carousel, talk, or podcast episode exists on the topic, link to it in a "Related" section at the end — never in the intro (D-09).

  ## Rules

  1. No Unicode bold/italic. Markdown `**bold**` and `*italic*` only.
  2. Em dashes (`—`) are permitted but sparingly — plain periods, commas, and colons are the default.
  3. Ban AI vocabulary: **delve, landscape, tapestry, harness, leverage, utilize, pivotal, seamless, groundbreaking, realm, navigate the complexities, at the forefront**. Plain English.
  4. Open with a concrete detail only Viktor would know: a number, a client reaction, a date, a line of CLI output.
  5. Use contractions: `I've`, `don't`, `it's`, `here's`, `we'd`.
  6. Vary sentence length. Short punches. Then a longer, winding sentence that earns the reader's attention by setting up the next short punch.
  7. End the lede with either a question or a promise of what comes next ("Here's what Salesforce actually did.").
  8. Code blocks use fenced markdown with explicit language (```yaml, ```bash, ```typescript). No indented 4-space code blocks — they break GitHub and Shiki highlighting.
  9. Headings: sentence case, not title case. `## Why this trips teams up` not `## Why This Trips Teams Up`.
  10. Links: descriptive anchor text, never "click here" or bare URLs. `[the Karpenter consolidation docs](...)` not `[here](...)`.

  ## Per-post register notes (D-06)

  - **Karpenter (~1500-2500 words)**: deep-dive. Stats lede → 3 trap sections → 4-step rollout → close with a heuristic. Numbers everywhere.
  - **MCP (~800-1200 words)**: "plainly explained" register. One-sentence definitions. Simple diagram. 3-4 concrete examples. No "behold the glory of stdio transport."
  - **Manifests-by-hand (~700-1000 words)**: opinion. Punchy lede. 3-4 reasons with nuance. One concession to the opposite view. One practical heuristic.
  ```

  **3a-2. `references/translation-rules.md`** (table-locked format per vv-carousel/references/primitives.md):

  ```markdown
  # Translation Rules (EN → RU) — locked

  **Source:** CONTEXT.md D-30. Follow `src/content/blog/{en,ru}/hello-world.md` precedent — tech terms stay English, prose translates to natural Russian.

  ## Rules

  | # | Rule | Example |
  |---|------|---------|
  | 1 | Tech terms stay in English | `Karpenter`, `NodePool`, `MCP server`, `YAML`, `Cluster Autoscaler`, `kubectl`, flag names, product names |
  | 2 | Concepts translate if natural | "cluster" → "кластер" in natural RU phrasing; compound "Karpenter cluster" stays as-is |
  | 3 | Code blocks identical | Never translate variable names, CLI commands, or fenced code contents (byte-for-byte equal between EN and RU file) |
  | 4 | Upstream quotes preserved | If the source (carousel, talk) is English, keep the quote in English. Mark with `*(original English)*` note when the RU reader needs orientation. |
  | 5 | Both locales in same commit | CLAUDE.md bilingual constraint. EN + RU markdown + any `public/blog-assets/{slug}/` files = single git commit. |
  | 6 | Frontmatter fields translate selectively | `title`, `description` translate. `date`, `tags`, `slug` do NOT. `reading_time` and `cover_image` stay identical if present. `author` — see note below. |
  | 7 | Common idiomatic mappings | "in production" → "в продакшне"; "at scale" → "в масштабе" (when emphasizing state — e.g. Karpenter post title "в масштабе 1000 кластеров") OR "на масштабе" (when emphasizing action — e.g. "отрабатывает на масштабе"); "right sizing" stays EN if term of art; "drift" → "дрейф"; "churn" stays EN |
  | 8 | Pronoun voice preserved | "I've seen" → "На своей практике я видел" or "Я видел". First person stays first person. |
  | 9 | Anglicisms for engineering slang | "workload" → "workload" (anglicism in RU engineering prose); "pipeline" → "pipeline". Don't force translations that sound awkward. |
  | 10 | Headings translate | Section headings render in Russian: `## Why this trips teams up` → `## Почему тут путаются команды` |

  ## Author frontmatter note (important — BLOCKER-1 Wave 1 tie-in)

  Do NOT add `author: "Виктор Ведмич"` to RU post frontmatter. The RU slug page (`src/pages/ru/blog/[...slug].astro`, Wave 1 Task 3) translates the schema-default Latin `"Viktor Vedmich"` to Cyrillic `"Виктор Ведмич"` at render time via a locale-aware `authorDisplay` constant. Leaving `author:` out of RU frontmatter (just like EN) is the correct pattern — the byline renders `Виктор Ведмич · N мин чтения` automatically.

  This means:
  - EN frontmatter: omit `author:` (inherits Latin default)
  - RU frontmatter: omit `author:` (inherits Latin default, render-layer translates to Cyrillic)
  - Guest-author posts: set `author: "Full Name"` in BOTH EN and RU frontmatter — the locale-aware render only translates when `author === 'Viktor Vedmich'`, everything else passes through unchanged

  See `references/frontmatter-schema.md` for the authoritative schema.

  ## Anti-patterns

  - Never machine-translate `Karpenter` → `Карпентер` (the brand name stays Latin).
  - Never leave an English paragraph in the RU file with a note like "TODO: translate" — D-29 requires full translation before commit.
  - Never translate YAML keys, CLI flags, or environment variable names.
  - Never insert literal Cyrillic transliterations of acronyms: `MCP` stays `MCP`, not `МСП`.
  - Never add `author: "Виктор Ведмич"` to RU frontmatter — it's handled at the render layer (Wave 1 Task 3).

  ## Validation before commit

  - RU file contains zero `<!-- TODO: translate -->` markers.
  - Code blocks between EN and RU files produce identical output from `diff` (ignoring line numbers).
  - RU reader (self-read or spot-check) finds the prose natural, not machine-translated.
  - RU frontmatter does NOT contain `author:` (unless guest author — same value as EN in that case).
  ```

  **3a-3. `references/frontmatter-schema.md`** (mirror of Wave 1 Task 1 schema + D-06 per-post examples + explicit RU author-field note per BLOCKER-1):

  ```markdown
  # Frontmatter Schema — blog post

  **Source:** `src/content.config.ts` (blog collection, post-Wave 1). Zod-enforced at build time; any violation fails `npm run build`.

  ## Required

  | Field | Type | Notes |
  |-------|------|-------|
  | `title` | string | Sentence case, descriptive. Becomes `<h1>` on slug page + card title. |
  | `description` | string | 15-30 words. Becomes card excerpt + `<p class="text-lg text-text-muted">` on slug page + OG description. |
  | `date` | ISO YYYY-MM-DD | Drives sort order on homepage BlogPreview + /blog/ index. |
  | `tags` | array of strings | At least 1 tag. Lowercase in source; rendered UPPERCASE via CSS. 2-4 tags typical. |

  ## Optional (with defaults)

  | Field | Type | Default | Notes |
  |-------|------|---------|-------|
  | `draft` | boolean | `false` | If `true`, post excluded from all collection queries. |
  | `author` | string | `"Viktor Vedmich"` | Rendered on slug page byline. **OMIT FROM EN AND RU FRONTMATTER** unless it's a guest post — see RU author-field note below. |
  | `reading_time` | number | (absent) | If absent, the `remark-reading-time` plugin computes `Math.ceil(wordCount/200)` at build. Override only when measured reading deviates significantly. |
  | `cover_image` | string | (absent) | Root-absolute path like `/blog-assets/{slug}/cover.png`. Not rendered on cards in Phase 9 — schema-ready for future UI. |

  ## RU author-field note — CRITICAL

  The schema default for `author` is Latin `"Viktor Vedmich"`. The RU slug page (`src/pages/ru/blog/[...slug].astro`, produced by Wave 1 Task 3) contains a locale-aware `authorDisplay` constant that translates `"Viktor Vedmich"` to Cyrillic `"Виктор Ведмич"` at render time. This means:

  - **EN RU frontmatter: do NOT set `author:`**. Both locales inherit the Latin default; the RU template translates it to Cyrillic automatically. This is the reason `src/content/blog/ru/hello-world.md` has no `author:` field and still renders `Виктор Ведмич · 1 мин чтения` on `/ru/blog/hello-world/`.
  - **Guest-author posts**: set `author: "Guest Name"` in BOTH EN and RU frontmatter (same value). The locale-aware render only translates when `post.data.author === 'Viktor Vedmich'` — every other value passes through unchanged.
  - **Why not a separate `author_ru` field?** Because it's extra schema surface every post would need to set correctly, and the common case (Viktor wrote it) becomes boilerplate. Render-layer translation is the cleanest pattern.

  See `references/translation-rules.md` § "Author frontmatter note" for the same guidance framed from the translation perspective.

  ## Examples by post type (D-06)

  ### Karpenter deep-dive (EN)

  ```yaml
  ---
  title: "Karpenter in production: right-sizing at scale"
  description: "How Salesforce cut 70% of cluster cost with Karpenter — the 3 traps and the 4-step rollout."
  date: 2026-03-20
  tags: ["kubernetes", "aws", "karpenter"]
  ---
  ```

  ### MCP explainer (EN)

  ```yaml
  ---
  title: "MCP servers, plainly explained"
  description: "What a Model Context Protocol server actually does — and the difference between AWS Knowledge MCP and AWS Documentation MCP."
  date: 2026-03-02
  tags: ["ai", "mcp", "agents"]
  ---
  ```

  ### Manifests opinion (EN)

  ```yaml
  ---
  title: "Why I still write Kubernetes manifests by hand"
  description: "Helm and Kustomize are great. Here's why I don't reach for them first."
  date: 2026-02-10
  tags: ["kubernetes", "opinion"]
  ---
  ```

  ### RU equivalents

  Only `title` and `description` translate. `date`, `tags`, `slug` stay identical. `author` is OMITTED (inherits Latin default; RU slug page translates to Cyrillic at render). Example:

  ```yaml
  ---
  title: "Karpenter в продакшне: right-sizing в масштабе"
  description: "Как Salesforce сократили 70% стоимости кластеров с Karpenter — 3 ловушки и 4-шаговый rollout."
  date: 2026-03-20
  tags: ["kubernetes", "aws", "karpenter"]
  ---
  ```

  Note the absence of `author:` — the RU byline will render `Виктор Ведмич · N мин чтения` automatically via the Wave 1 Task 3 locale-aware render.

  ## Rules

  - File path: `src/content/blog/{en,ru}/YYYY-MM-DD-slug.md`.
  - Slug MUST match between EN and RU — a post is one slug with two locales.
  - Image references inside the body MUST be root-absolute: `![alt](/blog-assets/{slug}/file.png)`. Relative paths break at `/ru/blog/{slug}` (Pitfall 5 in RESEARCH.md).
  - `draft: true` is the escape valve while drafting — flip to `false` (or omit) on commit.
  - Do NOT set `author:` in either locale unless it's a guest post (then set same value in both).

  ## Validation

  `npm run build` enforces the Zod schema. Any field violation = build fails with a readable error naming the field and file.
  ```

  **3a-4. `references/visuals-routing.md`** (decision tree per D-37 + D-39 + RESEARCH.md Code Example 6):

  ```markdown
  # Visuals Routing — decision tree

  **Source:** CONTEXT.md D-37 (visual signals), D-38 (asset storage), D-39 (reuse FIRST), D-40 (animations deferred).

  ## Priority 0 — Reuse FIRST (D-39)

  Before routing to a generation skill, check the vault for existing assets that fit the section.

  | Topic | Vault path to check | Notes |
  |-------|---------------------|-------|
  | Karpenter / cluster autoscaling | `~/Documents/ViktorVedmich/40-Content/45-Personal-Brand/45.20-Brand-Kit/carousel-templates/karpenter-1000-clusters/out/*.png` | 7 rendered slides (c01-cover through c07-cta), 1080×1350 @ 2× retina |
  | Kubernetes interview | `~/Documents/ViktorVedmich/40-Content/45-Personal-Brand/45.20-Brand-Kit/carousel-templates/k8s-interview/out/*.png` | Available if topic overlaps (CKA/CKS prep) |
  | S3 security | `~/Documents/ViktorVedmich/40-Content/45-Personal-Brand/45.20-Brand-Kit/carousel-templates/s3-security-8point/out/*.png` | Available if topic overlaps |
  | AWS Summit talk diagrams | `~/Documents/ViktorVedmich/40-Content/44-Speaking/44.20-Talk-Materials/*/diagrams/` | Check per talk |
  | DKT podcast visuals | `~/Documents/ViktorVedmich/30-Projects/32-DKT/` | Episode thumbnails + diagrams |

  If a slide/diagram fits the section: copy to `public/blog-assets/{slug}/<descriptive-kebab-name>.png`, skip generation.

  Rename on copy — turn raw filenames like `c03-mistake1.png` into descriptive kebab-case like `trap-1-speed.png` for maintainability.

  ## Priority 1 — Route to a skill (only if Priority 0 yielded nothing)

  | Section content signal | Delegate to | Natural-language invocation |
  |------------------------|-------------|------------------------------|
  | "flow", "sequence", "pipeline", 3+ enumerated steps | `mermaid-pro` | "Use mermaid-pro to generate a flowchart showing X (palette: midnight, platform: astro)." |
  | "architecture", component diagram, subgraphs | `mermaid-pro` | "Use mermaid-pro for an architecture diagram with subgraphs (palette: cloud if AWS content)." |
  | Hand-sketched / whiteboard style | `excalidraw` | "Sketch X in excalidraw style." |
  | Physical metaphor, hero illustration | `art` | "Use art to generate a prompt for NanoBanana showing X." |
  | Brand-consistent Deep Signal palette | `viktor-vedmich-design` | "Generate visual using viktor-vedmich-design UI kit." |
  | Opinion / reflection / pure prose section | (no visual) | — |

  ## Invocation pattern

  Per RESEARCH.md Pattern 5 — natural-language references activate the target skill's description trigger. Do NOT use `Skill()` call syntax (agent-only pattern).

  ## Astro rendering

  - PNG/JPG: markdown `![alt](/blog-assets/{slug}/file.png)`. Served from `public/` as-is, no `astro:assets` optimization (carousel PNGs are already web-optimized).
  - SVG: inline in markdown or via `<img>`. Prefer inline for zero request overhead.
  - Mermaid: not required for Phase 9 (D-39 reuse carousel PNGs). If a future post genuinely needs mermaid rendering, add `markdown.syntaxHighlight.excludeLangs: ['mermaid']` to `astro.config.mjs` and pre-render via `mermaid-pro` skill or inline the SVG directly.

  ## Asset storage

  Every image committed atomically with the post markdown (D-44):

  ```
  public/blog-assets/{YYYY-MM-DD-slug}/
  ├── cover.png              ← optional, for future cover_image field
  ├── <descriptive-name-1>.png
  └── <descriptive-name-2>.png
  ```

  ## Image path rule

  All inline images use root-absolute paths: `/blog-assets/{slug}/file.png`. Relative paths break at `/ru/blog/...` (RESEARCH.md Pitfall 5).

  The `scripts/deploy-post.sh` grep gate enforces this — any `](blog-assets/` (relative) in `src/content/blog/**/*.md` blocks the deploy.

  ## Animations (D-40, deferred)

  CSS/SVG animations are allowed in posts but not required in Phase 9. Zero JS. If a future post genuinely needs a motion element:
  - Prefer `<svg>` with `<animate>` or CSS `@keyframes` — never a JS library.
  - Respect `prefers-reduced-motion` (consistent with `.animate-on-scroll` utility in `src/styles/global.css`).
  ```

  **3a-5. `references/companion-sources.md`** (vault map per D-10):

  ```markdown
  # Companion Sources — vault map

  **Source:** CONTEXT.md D-08 / D-09 / D-10. When a draft is ready, scan the vault for related carousels, talks, and podcast episodes — suggest an end-of-post "Related" section (NOT in the intro per D-09).

  ## Carousel companions (LinkedIn)

  Source dir: `~/Documents/ViktorVedmich/40-Content/45-Personal-Brand/45.20-Brand-Kit/carousel-templates/`

  | Slug | Topic match signal |
  |------|--------------------|
  | `karpenter-1000-clusters/` | karpenter, cluster autoscaling, right-sizing, cost cut |
  | `k8s-interview/` | kubernetes interview, CKA / CKS prep |
  | `s3-security-8point/` | S3, AWS security, MFA Delete |
  | `kubectl-apply-path/` | kubectl, declarative config, API server |

  Scan method: `ls ~/Documents/ViktorVedmich/40-Content/45-Personal-Brand/45.20-Brand-Kit/carousel-templates/` plus `qmd search "<topic>" --collection vault`.

  ## Talk companions (conferences)

  Source dirs:
  - `~/Documents/ViktorVedmich/40-Content/44-Speaking/44.20-Talk-Materials/` — past talk notes
  - `~/Documents/ViktorVedmich/40-Content/44-Speaking/44.10-CFPs/15.31-Talks-Materials/` — CFP + chalk-talk notes

  | Note / Talk ID | Topic match signal |
  |----------------|--------------------|
  | `DOP202-Warsaw-Summit-Speaker-Notes.md` | karpenter, cost optimization, AWS Summit |
  | `2026-05-06-Warsaw-Summit-MCP-Chalk-Talk.md` | MCP, Claude Code, AI DevOps, chalk talk |

  Scan method: `qmd search "<topic>" --collection vault` filtered by the two dirs above.

  ## Podcast companions

  Source dirs:
  - `~/Documents/ViktorVedmich/30-Projects/32-DKT/` — DevOps Kitchen Talks episodes (91+)
  - `~/Documents/ViktorVedmich/10-AWS/15-Content-YT-Podcat-Talks/15.10-AWS-RU-Podcast/` — AWS на русском (65+, safe — not confidential)

  Scan method: `qmd search "<topic> episode" --collection vault`.

  ## Lesson / course companions

  Source dir: `~/Documents/ViktorVedmich/30-Projects/37-AI-DevOps-course/`

  | Area | Topic match signal |
  |------|--------------------|
  | `37.14-Claude-Code/Lesson-*` | Claude Code, MCP, agents |
  | `37.1X-*` | Prompt engineering, n8n, agentic patterns |

  ## Placement rule (D-09)

  "Related" section goes at end-of-post, never in intro or lede. Example closing:

  ```markdown
  ## Related

  - LinkedIn carousel: *Karpenter at 1000 clusters — 3 traps you hit before you cut cost* (https://linkedin.com/in/vedmich/...)
  - AWS Summit Warsaw 2026 chalk talk: *MCP Servers, Plainly Explained* — May 6, session DOP202
  - DKT episode: *... (if applicable)*
  ```

  ## Anti-patterns

  - Never include confidential client anecdotes in companion links — only cite public-facing artifacts (LinkedIn posts, published talks, open podcast episodes).
  - Never cite a talk that hasn't happened yet without flagging it: "*upcoming at AWS Summit Warsaw 2026-05-06*".
  - Never cite an internal AWS talk from `10-AWS/14-Tips-AWS-Internal/` (confidential exclusion).
  ```

  **3a-6. Verify all 5 files**:
  ```bash
  for f in references/voice-guide.md references/translation-rules.md references/frontmatter-schema.md references/visuals-routing.md references/companion-sources.md; do
    test -f ".claude/skills/vv-blog-from-vault/$f" || { echo "MISSING: $f"; exit 1; }
    lines=$(wc -l < ".claude/skills/vv-blog-from-vault/$f")
    test "$lines" -ge 30 || { echo "TOO SHORT: $f ($lines lines)"; exit 1; }
  done
  grep -q "Виктор Ведмич" .claude/skills/vv-blog-from-vault/references/frontmatter-schema.md
  grep -q "Виктор Ведмич" .claude/skills/vv-blog-from-vault/references/translation-rules.md
  ```

  All 5 files must exist and contain at least 30 lines. Both `frontmatter-schema.md` and `translation-rules.md` MUST mention the Cyrillic name "Виктор Ведмич" to document the locale-aware render guidance (BLOCKER-1 Wave 2 tie-in).

  Commit message: `feat(09-02): skill references — voice, translation, schema, visuals, companions`. Co-Authored-By trailer required.
  </action>

  <verify>
    <automated>for f in references/voice-guide.md references/translation-rules.md references/frontmatter-schema.md references/visuals-routing.md references/companion-sources.md; do test -f ".claude/skills/vv-blog-from-vault/$f" || exit 1; lines=$(wc -l &lt; ".claude/skills/vv-blog-from-vault/$f"); test "$lines" -ge 30 || exit 1; done &amp;&amp; grep -q "Виктор Ведмич" .claude/skills/vv-blog-from-vault/references/frontmatter-schema.md &amp;&amp; grep -q "Виктор Ведмич" .claude/skills/vv-blog-from-vault/references/translation-rules.md &amp;&amp; echo OK</automated>
  </verify>

  <acceptance_criteria>
    - `test -f .claude/skills/vv-blog-from-vault/references/voice-guide.md` — file exists
    - `test -f .claude/skills/vv-blog-from-vault/references/translation-rules.md` — file exists
    - `test -f .claude/skills/vv-blog-from-vault/references/frontmatter-schema.md` — file exists
    - `test -f .claude/skills/vv-blog-from-vault/references/visuals-routing.md` — file exists
    - `test -f .claude/skills/vv-blog-from-vault/references/companion-sources.md` — file exists
    - Each of the 5 files has at least 30 lines (`wc -l`)
    - `grep -q "tech-expert" .claude/skills/vv-blog-from-vault/references/voice-guide.md` — D-31 voice captured
    - `grep -q "delve\|leverage\|tapestry" .claude/skills/vv-blog-from-vault/references/voice-guide.md` — anti-AI ban list cited
    - `grep -q "Karpenter" .claude/skills/vv-blog-from-vault/references/translation-rules.md` — tech-term preservation example (D-30 rule 1)
    - `grep -q "кластер" .claude/skills/vv-blog-from-vault/references/translation-rules.md` — concept translation example (D-30 rule 2)
    - `grep -q "Виктор Ведмич" .claude/skills/vv-blog-from-vault/references/translation-rules.md` — BLOCKER-1 tie-in: Cyrillic author documented
    - `grep -q "Do NOT add .author: \"Виктор Ведмич\"\|do NOT set .author:" .claude/skills/vv-blog-from-vault/references/translation-rules.md` — explicit RU-no-override guidance
    - `grep -q "z.array(z.string())" .claude/skills/vv-blog-from-vault/references/frontmatter-schema.md || grep -q "tags.*required" .claude/skills/vv-blog-from-vault/references/frontmatter-schema.md` — tags required documented
    - `grep -q "reading_time" .claude/skills/vv-blog-from-vault/references/frontmatter-schema.md` — new field documented
    - `grep -q "author" .claude/skills/vv-blog-from-vault/references/frontmatter-schema.md` — author field documented
    - `grep -q "cover_image" .claude/skills/vv-blog-from-vault/references/frontmatter-schema.md` — cover_image field documented
    - `grep -q "Виктор Ведмич" .claude/skills/vv-blog-from-vault/references/frontmatter-schema.md` — BLOCKER-1 tie-in: Cyrillic rendering documented
    - `grep -q "authorDisplay\|locale-aware" .claude/skills/vv-blog-from-vault/references/frontmatter-schema.md` — Wave 1 Task 3 render pattern documented
    - `grep -q "karpenter-1000-clusters" .claude/skills/vv-blog-from-vault/references/visuals-routing.md` — D-39 reuse-first catalog entry
    - `grep -q "Priority 0" .claude/skills/vv-blog-from-vault/references/visuals-routing.md` — reuse-first priority declared
    - `grep -q "Priority 1" .claude/skills/vv-blog-from-vault/references/visuals-routing.md` — delegate-fallback priority declared
    - `grep -q "mermaid-pro" .claude/skills/vv-blog-from-vault/references/visuals-routing.md` — delegation target cited
    - `grep -q "DOP202-Warsaw-Summit" .claude/skills/vv-blog-from-vault/references/companion-sources.md` — talk companion cataloged (D-10)
    - `grep -q "45.20-Brand-Kit/carousel-templates/" .claude/skills/vv-blog-from-vault/references/companion-sources.md` — carousel source dir cited
    - `grep -q "end-of-post" .claude/skills/vv-blog-from-vault/references/companion-sources.md` — D-09 placement rule cited
    - All 5 files have at least one EXAMPLE block (```yaml or ```markdown or ```bash fenced) to serve as concrete guidance
  </acceptance_criteria>

  <done>
    All 5 references exist with substantive content. Voice rules (D-31), translation rules (D-30) with explicit RU author-field guidance, frontmatter schema (Wave 1 mirror) with documented `authorDisplay` locale-aware render pattern, visuals routing (D-37 + D-39 reuse-first), companion map (D-10). BLOCKER-1 Wave 2 tie-in is documented in both `translation-rules.md` and `frontmatter-schema.md` — Wave 3 authors know NOT to add `author:` overrides to RU frontmatter.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3b: Create 2 workflow docs — new-post (9-step orchestration) + update-post (5-step edit flow) (D-34, depends_on Task 3a)</name>
  <files>.claude/skills/vv-blog-from-vault/workflows/new-post.md, .claude/skills/vv-blog-from-vault/workflows/update-post.md</files>

  <read_first>
    - `/Users/viktor/.claude/skills/recall/workflows/topic.md` (step-by-step workflow format template)
    - `/Users/viktor/.claude/skills/recall/workflows/temporal.md` (simpler workflow template)
    - `.claude/skills/vv-blog-from-vault/SKILL.md` (the entry point that references these workflows — created by Task 1)
    - `.claude/skills/vv-blog-from-vault/references/voice-guide.md` (created by Task 3a — referenced in new-post.md Step 5)
    - `.claude/skills/vv-blog-from-vault/references/translation-rules.md` (created by Task 3a — referenced in new-post.md Step 6)
    - `.claude/skills/vv-blog-from-vault/references/frontmatter-schema.md` (created by Task 3a — referenced in new-post.md Step 5)
    - `.claude/skills/vv-blog-from-vault/references/visuals-routing.md` (created by Task 3a — referenced in new-post.md Step 7)
    - `.claude/skills/vv-blog-from-vault/references/companion-sources.md` (created by Task 3a — referenced in new-post.md Step 4 and Step 8)
    - `.claude/skills/vv-blog-from-vault/scripts/deploy-post.sh` (created by Task 2 — invoked in new-post.md Step 9 and update-post.md Step 5; does NOT call playwright)
    - `.planning/phases/09-blog-3-posts-card-format/09-CONTEXT.md` D-06, D-09, D-10, D-43
    - `.planning/phases/09-blog-3-posts-card-format/09-RESEARCH.md` Code Example 5 (workflow step 2 — vault search pattern)
    - `.planning/phases/09-blog-3-posts-card-format/09-PATTERNS.md` §20 (new-post workflow), §21 (update-post workflow)
  </read_first>

  <action>
  This task depends on Task 3a (all 5 references exist) and Task 2 (deploy-post.sh exists) because the workflows reference those files by path. Commit Task 3a first, then this task. Separate commits keep the file count per commit manageable and make the history easier to audit (WARNING-2 fix).

  Create both workflow files. Neither workflow invokes playwright — screenshot verification is a manual step the user performs on live URLs after GH Actions finishes (CLAUDE.md "Deployment monitoring" pattern).

  **3b-1. `workflows/new-post.md`** (9-step orchestration — the core workflow Wave 3 invokes):

  ```markdown
  # Workflow — new post

  9 sequential steps. Pause between steps to confirm with the user unless they've said "just ship it."

  ## Step 1 — Clarify topic + slug + companions

  Ask the user (in Russian if they started in Russian):

  1. Topic (e.g. "karpenter right-sizing")
  2. Slug — must be kebab-case and date-prefixed: `YYYY-MM-DD-topic`. Example: `2026-03-20-karpenter-right-sizing`.
  3. Word-count target — propose from `references/voice-guide.md` per-post register notes (D-06), let the user tweak.
  4. Any specific sources to prioritize? (vault paths, talk IDs, diary date ranges)

  ## Step 2 — Vault search (QMD + confidential filter)

  Use `mcp__qmd__query` with a 2-query pattern (lex + vec). Prioritize these source dirs in order:

  1. `40-Content/44-Speaking/44.20-Talk-Materials/` — talk notes, stats, technical claims
  2. `40-Content/45-Personal-Brand/45.20-Brand-Kit/carousel-templates/<slug>/` — carousels (companion sources)
  3. `30-Projects/33-Book-Kubernetes/` — book chapters (if K8s topic)
  4. `70-PKM/73-KB-Tech/` — curated tech KB
  5. `20-Calendar/25-diary/25.70-Claude-Summaries/*.md` — session summaries (opinion posts)
  6. `30-Projects/37-AI-DevOps-course/` — AI/MCP/Claude Code material

  Wrap with `scripts/vault-search.py "<topic>"` which applies the confidential post-filter automatically. Alternatively call `mcp__qmd__query` directly and then filter results via `scripts/vault-search.py --check PATH` per result path.

  Present top 5 survivors. Operator selects 2-3 to read fully via `mcp__qmd__get` or `qmd get <path>`.

  If nothing credible surfaces, STOP and either ask the user or suggest reframing. Do NOT fabricate.

  ## Step 3 — Session history (recall)

  Delegate to the `recall` skill in topic mode. Natural-language invocation: "recall <topic>".

  For the manifests-by-hand post specifically — primary grounding is `recall` (no vault note exists per D-04). If `recall` yields fewer than 3 distinct grounded experiences, fall back to `scripts/session-recall.py "<topic>"` (diary grep) OR halt with AskUserQuestion for manual input.

  Extract: quotes, decisions, concrete client reactions (strip identifying detail before quoting).

  ## Step 4 — Companion link check

  Per `references/companion-sources.md`, scan for:

  - Carousel at `45.20-Brand-Kit/carousel-templates/<topic-match>/`
  - Talk at `44-Speaking/44.20-Talk-Materials/` or `44-Speaking/44.10-CFPs/15.31-Talks-Materials/`
  - Podcast episode via `qmd search "<topic>" --collection vault` filtered to DKT / AWS-RU dirs

  If a companion exists, plan to include it in a "Related" section at the END of the post (D-09).

  ## Step 5 — Draft generation (EN)

  Apply `references/voice-guide.md` (voice D-31) + `references/frontmatter-schema.md` (frontmatter — crucially: omit `author:` field, see schema note) + D-06 word counts.

  - Karpenter: ~1500-2500 words, stats lede ("Salesforce runs Karpenter on 1,000+ clusters. 70% cluster-cost cut.") + 3 traps (speed / race / churn from carousel slides c03/c04/c05) + 4-step rollout (slide c06) + heuristic close
  - MCP: ~800-1200 words, one-sentence definitions + client↔server explanation + 3-4 concrete examples (AWS Knowledge MCP vs AWS Documentation MCP)
  - Manifests: ~700-1000 words, punchy lede + 3-4 reasons with nuance + one concession + practical heuristic

  Write to `src/content/blog/en/{slug}.md` with valid frontmatter (validates against the tightened schema). Do NOT set `author:` — the Latin default is correct for EN.

  ## Step 6 — Translate (RU)

  Apply `references/translation-rules.md` — tech terms English, prose natural Russian, code blocks identical. CRUCIALLY: do NOT set `author: "Виктор Ведмич"` in RU frontmatter — the RU slug page's locale-aware `authorDisplay` render (Wave 1 Task 3) translates the schema-default Latin "Viktor Vedmich" to Cyrillic at render time automatically.

  Write to `src/content/blog/ru/{slug}.md`. Validation: `diff <(grep -n '^```' en.md) <(grep -n '^```' ru.md)` — code fence positions should be identical (same number of code blocks at same relative positions).

  ## Step 7 — Visuals audit + routing

  Per `references/visuals-routing.md`:

  - **Priority 0 — Reuse first**: check carousel PNGs at `45.20-Brand-Kit/carousel-templates/<slug>/out/`. If a slide fits, copy to `public/blog-assets/{slug}/<descriptive-kebab-name>.png` (rename from raw `c0X-...png`).
  - **Priority 1 — Delegate**: only if Priority 0 yielded nothing. Route per the decision table (mermaid-pro / excalidraw / art / viktor-vedmich-design).

  Every inline image reference in markdown MUST use root-absolute path `/blog-assets/{slug}/file.png`.

  ## Step 8 — Related section

  Only if a companion exists (from Step 4). Append at end-of-post per D-09. See `references/companion-sources.md` § "Placement rule" for the exact format.

  ## Step 9 — Verify + push

  Run `scripts/deploy-post.sh <slug> "<title>"`:

  1. Both locale files exist check
  2. Relative-image-path grep gate
  3. `npm run build` (Zod validates frontmatter; remark computes reading_time; routes compile)
  4. `git add` EN + RU + `public/blog-assets/{slug}/` (if present)
  5. `git commit -m "Post: <title>"` — NO Co-Authored-By trailer (D-43)
  6. `git push origin main`
  7. `gh run list --branch main --limit 3` to confirm GH Actions started

  Report live URLs: `https://vedmich.dev/en/blog/{slug}`, `https://vedmich.dev/ru/blog/{slug}`.

  Deploy latency: ~60-90 seconds typically.

  **Manual post-deploy verification (user performs, not the skill):** after GH Actions finishes, the user visits the live URL in a browser OR runs `playwright-cli` separately to screenshot the post. This matches CLAUDE.md "Deployment monitoring" convention — deploy-post.sh itself does NOT call playwright, keeping the script dependency-free.
  ```

  **3b-2. `workflows/update-post.md`** (5-step edit flow — simpler):

  ```markdown
  # Workflow — update post

  5 sequential steps. Use for edits, typo fixes, additional sections, or visual additions to an EXISTING post.

  ## Step 1 — Identify post

  Ask (or parse from user input): "which post to update". Accept slug (`2026-03-20-karpenter-right-sizing`) or title (partial match works — grep `src/content/blog/en/*.md` for the frontmatter title).

  ## Step 2 — Load EN + RU

  Read both locale files:
  - `src/content/blog/en/{slug}.md`
  - `src/content/blog/ru/{slug}.md`

  Display a short diff-intent summary to the user: what's changing.

  ## Step 3 — Apply edits

  | Edit kind | Action |
  |-----------|--------|
  | Typo / small fix | Edit in both locales, one commit |
  | New section | Draft in EN, then translate to RU via `references/translation-rules.md` |
  | New visual | Run Step 7 from `new-post.md` (reuse-first, then delegate) |
  | Frontmatter tweak (e.g. new tag) | Edit both locales; tags must match between EN and RU |
  | Retract / mark draft | Set `draft: true` in both files. Do NOT delete — preserve URL history; set draft and push |

  Never update one locale without the other — bilingual constraint.

  ## Step 4 — Verify build

  `npm run build` must exit 0.

  If the edit touched images, also verify `scripts/deploy-post.sh`'s relative-path grep gate:

  ```bash
  grep -rnE '\]\(blog-assets/' src/content/blog/
  ```

  Must return nothing.

  ## Step 5 — Push

  ```bash
  git add src/content/blog/en/{slug}.md src/content/blog/ru/{slug}.md
  git commit -m "Post: Update <title> — <reason>"
  git push origin main
  ```

  NO Co-Authored-By trailer (content commit, per CLAUDE.md / D-43).

  **Manual post-deploy verification (user performs):** after GH Actions finishes (~60-90s), the user visits the live URL or runs `playwright-cli` separately to verify the update rendered correctly. This workflow does NOT invoke playwright — matches CLAUDE.md "Deployment monitoring" convention.

  Examples of the `<reason>` suffix:
  - `Post: Update Karpenter post — fix trap-2 race-condition explanation`
  - `Post: Update MCP post — add AWS Documentation MCP example`
  - `Post: Update manifests post — add concession for large platforms`
  ```

  **3b-3. Verify both files**:
  ```bash
  for f in workflows/new-post.md workflows/update-post.md; do
    test -f ".claude/skills/vv-blog-from-vault/$f" || { echo "MISSING: $f"; exit 1; }
    lines=$(wc -l < ".claude/skills/vv-blog-from-vault/$f")
    test "$lines" -ge 30 || { echo "TOO SHORT: $f ($lines lines)"; exit 1; }
  done
  test "$(grep -cE '^## Step [1-9]' .claude/skills/vv-blog-from-vault/workflows/new-post.md)" -eq 9
  test "$(grep -cE '^## Step [1-5]' .claude/skills/vv-blog-from-vault/workflows/update-post.md)" -eq 5
  ! grep -qi "playwright" .claude/skills/vv-blog-from-vault/workflows/new-post.md || grep -q "playwright-cli separately\|Manual post-deploy" .claude/skills/vv-blog-from-vault/workflows/new-post.md
  ```

  Both files must exist, contain at least 30 lines, and have the correct step count. If `playwright` appears in a workflow file, it MUST be in the "Manual post-deploy verification" context (describing what the USER does manually, not what the skill calls).

  Commit message: `feat(09-02): skill workflows — new-post (9-step) + update-post (5-step)`. Co-Authored-By trailer required.
  </action>

  <verify>
    <automated>test -f .claude/skills/vv-blog-from-vault/workflows/new-post.md &amp;&amp; test -f .claude/skills/vv-blog-from-vault/workflows/update-post.md &amp;&amp; test "$(grep -cE '^## Step [1-9]' .claude/skills/vv-blog-from-vault/workflows/new-post.md)" -eq 9 &amp;&amp; test "$(grep -cE '^## Step [1-5]' .claude/skills/vv-blog-from-vault/workflows/update-post.md)" -eq 5</automated>
  </verify>

  <acceptance_criteria>
    - `test -f .claude/skills/vv-blog-from-vault/workflows/new-post.md` — file exists
    - `test -f .claude/skills/vv-blog-from-vault/workflows/update-post.md` — file exists
    - `wc -l .claude/skills/vv-blog-from-vault/workflows/new-post.md | awk '{print $1}'` ≥ 30 (substantive)
    - `wc -l .claude/skills/vv-blog-from-vault/workflows/update-post.md | awk '{print $1}'` ≥ 30 (substantive)
    - `grep -cE "^## Step [1-9]" .claude/skills/vv-blog-from-vault/workflows/new-post.md` returns `9` — all 9 steps present
    - `grep -q "mcp__qmd__query\|mcp__qmd" .claude/skills/vv-blog-from-vault/workflows/new-post.md` — MCP tool cited
    - `grep -q "deploy-post.sh" .claude/skills/vv-blog-from-vault/workflows/new-post.md` — Step 9 references the deploy script
    - `grep -q "Post:" .claude/skills/vv-blog-from-vault/workflows/new-post.md` — CLAUDE.md commit template cited
    - `grep -q "mermaid-pro\|excalidraw\|art\|viktor-vedmich-design" .claude/skills/vv-blog-from-vault/workflows/new-post.md` — visuals delegation mentioned
    - `grep -q "author:\|author field" .claude/skills/vv-blog-from-vault/workflows/new-post.md` — Wave 1 RU author-no-override guidance referenced in Step 5/6
    - `grep -cE "^## Step [1-5]" .claude/skills/vv-blog-from-vault/workflows/update-post.md` returns `5` — all 5 steps present
    - `grep -q "Post: Update" .claude/skills/vv-blog-from-vault/workflows/update-post.md` — update commit message shape
    - `! grep "Co-Authored-By" .claude/skills/vv-blog-from-vault/workflows/update-post.md` — content commit has no trailer
    - If "playwright" appears in either workflow: `grep -q "Manual post-deploy\|playwright-cli separately" .claude/skills/vv-blog-from-vault/workflows/new-post.md` — playwright is described as a MANUAL user step, not a skill-invoked step (WARNING-3 fix)
    - Both workflow files have at least one fenced code block (```bash or ```markdown) to serve as concrete guidance
  </acceptance_criteria>

  <done>
    Both workflow files (new-post 9-step + update-post 5-step) exist with substantive content. Workflows do NOT invoke playwright — where playwright is mentioned, it is framed as a manual post-deploy user step matching CLAUDE.md "Deployment monitoring" pattern. Step 5/6 of new-post.md reference the Wave 1 RU author-no-override guidance. Wave 3 can invoke the skill end-to-end.
  </done>
</task>

</tasks>

<verification>
After all four tasks complete (1, 2, 3a, 3b), run the full Wave 2 acceptance pass:

```bash
# 1. Directory structure
test -d .claude/skills/vv-blog-from-vault/scripts
test -d .claude/skills/vv-blog-from-vault/references
test -d .claude/skills/vv-blog-from-vault/workflows

# 2. SKILL.md triggers (EN + RU)
grep -q "новый пост" .claude/skills/vv-blog-from-vault/SKILL.md
grep -q "new post" .claude/skills/vv-blog-from-vault/SKILL.md

# 3. SKILL.md does NOT mention playwright (WARNING-3 fix)
! grep -qi "playwright" .claude/skills/vv-blog-from-vault/SKILL.md

# 4. Confidential filter enforcement
python3 .claude/skills/vv-blog-from-vault/scripts/vault-search.py --check "10-AWS/11-Active-Clients/x.md" | grep -q DENIED
python3 .claude/skills/vv-blog-from-vault/scripts/vault-search.py --check "40-Content/44-Speaking/x.md" | grep -q ALLOWED

# 5. Commit template enforcement
grep -q "Post: \${TITLE}" .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh
! grep -q "Co-Authored-By" .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh

# 6. deploy-post.sh does NOT call playwright (WARNING-3 fix)
! grep -qi "playwright" .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh

# 7. Image path grep gate
grep -q "grep -rnE" .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh

# 8. All 11 skill files present and non-trivial
for f in SKILL.md \
         scripts/vault-search.py scripts/session-recall.py scripts/deploy-post.sh \
         references/voice-guide.md references/translation-rules.md references/frontmatter-schema.md references/visuals-routing.md references/companion-sources.md \
         workflows/new-post.md workflows/update-post.md; do
  test -f ".claude/skills/vv-blog-from-vault/$f" || { echo "MISSING: $f"; exit 1; }
done

# 9. Delegation targets named
grep -q "mermaid-pro\|excalidraw\|art\|recall" .claude/skills/vv-blog-from-vault/SKILL.md
grep -q "mermaid-pro" .claude/skills/vv-blog-from-vault/references/visuals-routing.md

# 10. Workflow step counts
test "$(grep -cE '^## Step [1-9]' .claude/skills/vv-blog-from-vault/workflows/new-post.md)" = "9"
test "$(grep -cE '^## Step [1-5]' .claude/skills/vv-blog-from-vault/workflows/update-post.md)" = "5"

# 11. Wave 1 schema mirrored
grep -q "reading_time" .claude/skills/vv-blog-from-vault/references/frontmatter-schema.md
grep -q "author" .claude/skills/vv-blog-from-vault/references/frontmatter-schema.md

# 12. BLOCKER-1 Wave 2 tie-in — RU author-no-override guidance
grep -q "Виктор Ведмич" .claude/skills/vv-blog-from-vault/references/frontmatter-schema.md
grep -q "Виктор Ведмич" .claude/skills/vv-blog-from-vault/references/translation-rules.md

# 13. Python + bash syntax valid
python3 -c "import ast; ast.parse(open('.claude/skills/vv-blog-from-vault/scripts/vault-search.py').read())"
python3 -c "import ast; ast.parse(open('.claude/skills/vv-blog-from-vault/scripts/session-recall.py').read())"
bash -n .claude/skills/vv-blog-from-vault/scripts/deploy-post.sh
```

Manual verification (human):
- In a fresh Claude Code session inside this repo, ask "новый пост про Karpenter" — the `vv-blog-from-vault` skill should self-activate via description-trigger match. (Optional dry-run; Wave 3 exercises this in earnest.)
- Spot-check `references/voice-guide.md` for register consistency (Russian speaker reads it and agrees the rules match their voice).
- Spot-check `references/frontmatter-schema.md` — the RU author-field note clearly explains not to set `author:` in RU frontmatter.
</verification>

<success_criteria>
- `.claude/skills/vv-blog-from-vault/` exists with SKILL.md + scripts/ (3) + references/ (5) + workflows/ (2) = 11 files
- SKILL.md declares all EN + RU trigger phrases per D-35
- SKILL.md delegates to mermaid-pro / excalidraw / art / viktor-vedmich-design / recall / episodic-memory MCP / sync-claude-sessions (D-36)
- SKILL.md does NOT mention playwright (WARNING-3 fix — manual post-deploy verification per CLAUDE.md)
- vault-search.py DENIES any of the 3 confidential prefixes and ALLOWS safe paths
- deploy-post.sh uses `Post: <title>` template with NO Co-Authored-By trailer (D-43)
- deploy-post.sh does NOT call playwright (WARNING-3 fix — manual screenshot verification)
- deploy-post.sh has the relative-image-path grep gate (Pitfall 5)
- All 5 references/ markdown files are ≥ 30 lines with topic-appropriate content
- `references/frontmatter-schema.md` documents the Wave 1 Task 3 RU `authorDisplay` locale-aware render — Wave 3 authors know NOT to add `author: "Виктор Ведмич"` to RU frontmatter (BLOCKER-1 Wave 2 tie-in)
- `references/translation-rules.md` mirrors the same RU author-no-override guidance
- workflows/new-post.md has 9 steps matching the RESEARCH.md architecture diagram
- workflows/update-post.md has 5 steps for the edit flow
- Neither workflow invokes playwright programmatically — only describes it as a manual user-performed step (WARNING-3 fix)
- Python scripts syntax-valid; bash script syntax-valid
- `npm run build` still passes (Wave 1 did not regress — Wave 2 only touches `.claude/skills/` which is outside Astro's build graph)
- 4 atomic commits across Wave 2 (one per task: Task 1 SKILL, Task 2 scripts, Task 3a references, Task 3b workflows) for cleaner history (WARNING-2 fix)
</success_criteria>

<output>
After completion, create `.planning/phases/09-blog-3-posts-card-format/09-02-SUMMARY.md` covering:
- All 11 skill files created (paths + one-line purpose)
- Confidential exclusion test output (DENIED + ALLOWED)
- Commit log of all Wave 2 commits (expect 4 — one per task: 1, 2, 3a, 3b — split per WARNING-2)
- Notes for Wave 3 (content):
  - Invoke the skill with "new post about Karpenter" or "новый пост про Karpenter"
  - Follow `workflows/new-post.md` 9 steps
  - Per-post source map (vault paths per D-04) is in `references/companion-sources.md` and `workflows/new-post.md` Step 2
  - Karpenter post: copy carousel PNGs per D-39 (paths in `references/visuals-routing.md` Priority 0 table)
  - Manifests post: primary grounding is `recall` topic mode per Step 3 — flag for user review if < 3 grounded experiences surface
  - **All 3 RU posts: do NOT add `author: "Виктор Ведмич"` to frontmatter** — the Wave 1 Task 3 RU slug page's `authorDisplay` locale-aware render handles Cyrillic translation automatically (same pattern as existing hello-world RU post)
  - All 3 posts commit via `deploy-post.sh {slug} "{title}"` — 3 commits total, one per post, each containing EN + RU + any `public/blog-assets/{slug}/` files
  - Screenshot verification of live posts is manual (browser or `playwright-cli` separately) — deploy-post.sh does NOT call playwright
</output>
</content>
</invoke>