---
phase: 04.1-excalidraw-pipeline-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - CLAUDE.md
  - diagrams-source/README.md
autonomous: true
requirements: [DIAG-01, DIAG-02, DIAG-03, DIAG-04, DIAG-05]
tags: [docs, traceability, runbook]
must_haves:
  truths:
    - "REQUIREMENTS.md DIAG-01..05 bullets flipped from [ ] to [x] and Traceability-table status flipped from Pending to Shipped 2026-05-04 (F-04)"
    - "ROADMAP.md Phase 4 checkbox flipped to [x], progress 0/5 → 5/5 Shipped, Traceability DIAG-01..05 rows updated, last-updated stamp to 2026-05-04, Phase 04.1 goal prose filled in, runbook H2-count ref updated (F-02/F-03/F-20)"
    - "STATE.md body + frontmatter coherent — current_phase 04.1 (matches frontmatter), Completed Phases section names Phase 01-04 (F-05/F-06), known-issue note for gsd-sdk phase.complete bug per D-18"
    - "PROJECT.md has a v1.0 Phase 4 validations paragraph naming DIAG-01..05 + shipped artifacts + Phase 04.1 tech-debt closure note (F-13/F-14 + D-22)"
    - "CLAUDE.md has a new '## Excalidraw Diagram Pipeline — LIVE (since 2026-05-04)' section after Slidev Presentations Integration (F-01 + D-21); 'Phase 7 (future)' Slidev reference fixed to 'v1.0 Phase 5 (planned)' (F-18)"
    - "diagrams-source/README.md has a new '## Prerequisites' H2 above '## Authoring' with npm install + library pin (F-07 + D-19); Gotcha 1 rewritten with element-count threshold + Helvetica fallback-chain (F-08 + D-20); 'Further reading' test-count claim updated from '9 integration tests' to the post-04.1 count (F-09/F-20)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "DIAG-01..05 status updated"
      contains: "Shipped 2026-05-04"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 checkbox/traceability/last-updated flipped; Phase 04.1 goal prose"
      contains: "shipped 2026-05-04"
      contains: "Phase 04.1"
    - path: ".planning/STATE.md"
      provides: "Body/frontmatter alignment + Completed Phases list"
      contains: "Phase 04.1"
      contains: "Phase 04: Excalidraw Pipeline"
    - path: ".planning/PROJECT.md"
      provides: "v1.0 Phase 4 validations paragraph"
      contains: "Validated in v1.0 Phase 4 (excalidraw-pipeline)"
      contains: "DIAG-01..05"
    - path: "CLAUDE.md"
      provides: "New Excalidraw section + Phase 7→5 fix"
      contains: "Excalidraw Diagram Pipeline"
      contains: "v1.0 Phase 5 (planned)"
    - path: "diagrams-source/README.md"
      provides: "Prerequisites H2 + rewritten Gotcha 1 + Further-reading test-count update"
      contains: "## Prerequisites"
      contains: "npm install"
      contains: "Helvetica"
      contains: "@aldinokemal2104/excalidraw-to-svg@1.1.1"
  key_links:
    - from: ".planning/REQUIREMENTS.md DIAG-01..05 Traceability rows"
      to: "Shipped status with artifact evidence"
      via: "Table cell text replaced"
      pattern: "Shipped 2026-05-04"
    - from: ".planning/ROADMAP.md Phase 04.1 section"
      to: "Phase 04.1 goal prose (was '[Urgent work - to be planned]')"
      via: "Prose block replaced with remediation summary"
      pattern: "Excalidraw Pipeline Hardening"
    - from: "CLAUDE.md new Excalidraw Pipeline section"
      to: "Runbook + script + tests + devDep-only boundary invariant"
      via: "H2 with links to 3 repo paths"
      pattern: "diagrams-source/README.md"
    - from: "diagrams-source/README.md §Prerequisites"
      to: "npm install @aldinokemal2104/excalidraw-to-svg@1.1.1 + Node.js 20+"
      via: "New H2 above Authoring"
      pattern: "npm install"
---

<objective>
Close the 9 docs-drift findings from reviews/DOCS-REVIEW.md (F-01, F-02, F-03, F-04, F-05, F-06, F-07, F-08, F-13, F-14, F-18, F-20) + the runbook count nitpicks (F-09, F-11 via D-25) with a single coordinated sweep across 6 files. No code changes in this plan — pure docs. Runs parallel to the 04.1-01/02 script-hardening plans because zero files overlap.

Per D-25: F-09/F-11/F-17/F-19/F-20 NITPICK findings are folded into D-19/D-20 runbook edits where they relate (F-09 test count, F-20 H2 count). F-11 (visuals-routing.md backtick wrapping) + F-17 (CONTEXT.md D-01b strikethrough) + F-19 (D-37..D-40 citation drift — inherited from prior phase, no Phase 4 authorship) are ALSO edited if a fast one-liner; otherwise batched for a single atomic commit.

Purpose: Post-hoc documentation correctness — Phase 4 shipped but the trackers still say "Pending" / "Planned" / "Phase 7 (future)". New contributors reading the trackers today get a stale picture. This plan brings all 6 files into coherence with the actual Phase 4 shipment + future Phase 04.1 completion.

Output: 6 docs updated with exact diffs from reviews/DOCS-REVIEW.md (pre-cleared, copy-paste-verbatim). Single atomic commit recommended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@diagrams-source/README.md
@.planning/phases/04.1-excalidraw-pipeline-hardening/04.1-CONTEXT.md
@.planning/phases/04.1-excalidraw-pipeline-hardening/04.1-PATTERNS.md
@.planning/phases/04-excalidraw-pipeline/reviews/DOCS-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip REQUIREMENTS.md DIAG-01..05 bullets + Traceability rows (F-04 + D-18)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md HEAD — bullet list at lines 37-41, Traceability table rows at lines 131-135
    - .planning/phases/04-excalidraw-pipeline/reviews/DOCS-REVIEW.md F-04 (lines 147-199) — verbatim diff for both blocks
    - .planning/phases/04.1-excalidraw-pipeline-hardening/04.1-CONTEXT.md §D-18 (status-flip approach across 3 trackers)
  </read_first>
  <behavior>
    Two-block edit on the same file:
    1. Lines 37-41 (bullet list): `- [ ]` → `- [x]` on all 5 DIAG-* bullets. Preserve the bullet text.
    2. Lines 131-135 (Traceability table): `| Pending |` → `| Shipped 2026-05-04 |` on all 5 DIAG-* rows. Preserve column widths.
  </behavior>
  <action>
    **Edit `.planning/REQUIREMENTS.md`** — apply these two diffs EXACTLY:

    **Edit (a): Bullet list.** Find this block (lines 37-41):

    ```markdown
    - [ ] **DIAG-01**: Ship `scripts/excalidraw-to-svg.mjs` that converts `.excalidraw.json → .svg` via `@aldinokemal2104/excalidraw-to-svg` (build-time, zero runtime JS).
    - [ ] **DIAG-02**: Integrate SVGO into the same pipeline so emitted diagrams are ≤ 10 KB each — prevents LCP regression per PITFALLS.md.
    - [ ] **DIAG-03**: Establish the `public/blog-assets/<slug>/diagrams/*.svg` convention for committed diagrams, with `title`+`desc` a11y elements injected during export.
    - [ ] **DIAG-04**: Replace the ASCII client-↔-server diagram in the existing MCP blog post with an Excalidraw SVG rendered via the new pipeline (both `en/` and `ru/` locales).
    - [ ] **DIAG-05**: Add 1-2 additional Excalidraw diagrams to existing blog posts (MCP + karpenter + 1-2 stretch) to stress-test the pipeline on real content. `manifests` post is text-only per D-04b (no diagram fit).
    ```

    Replace with (only the `- [ ]` markers change — all other text preserved byte-for-byte):

    ```markdown
    - [x] **DIAG-01**: Ship `scripts/excalidraw-to-svg.mjs` that converts `.excalidraw.json → .svg` via `@aldinokemal2104/excalidraw-to-svg` (build-time, zero runtime JS).
    - [x] **DIAG-02**: Integrate SVGO into the same pipeline so emitted diagrams are ≤ 10 KB each — prevents LCP regression per PITFALLS.md.
    - [x] **DIAG-03**: Establish the `public/blog-assets/<slug>/diagrams/*.svg` convention for committed diagrams, with `title`+`desc` a11y elements injected during export.
    - [x] **DIAG-04**: Replace the ASCII client-↔-server diagram in the existing MCP blog post with an Excalidraw SVG rendered via the new pipeline (both `en/` and `ru/` locales).
    - [x] **DIAG-05**: Add 1-2 additional Excalidraw diagrams to existing blog posts (MCP + karpenter + 1-2 stretch) to stress-test the pipeline on real content. `manifests` post is text-only per D-04b (no diagram fit).
    ```

    **Edit (b): Traceability table.** Find these 5 lines (131-135):

    ```markdown
    | DIAG-01 | Excalidraw Pipeline | Phase 4 | Pending |
    | DIAG-02 | Excalidraw Pipeline | Phase 4 | Pending |
    | DIAG-03 | Excalidraw Pipeline | Phase 4 | Pending |
    | DIAG-04 | Excalidraw Pipeline | Phase 4 | Pending |
    | DIAG-05 | Excalidraw Pipeline | Phase 4 | Pending |
    ```

    Replace with:

    ```markdown
    | DIAG-01 | Excalidraw Pipeline | Phase 4 | Shipped 2026-05-04 (hardened in Phase 04.1) |
    | DIAG-02 | Excalidraw Pipeline | Phase 4 | Shipped 2026-05-04 (hardened in Phase 04.1) |
    | DIAG-03 | Excalidraw Pipeline | Phase 4 | Shipped 2026-05-04 (hardened in Phase 04.1) |
    | DIAG-04 | Excalidraw Pipeline | Phase 4 | Shipped 2026-05-04 (hardened in Phase 04.1) |
    | DIAG-05 | Excalidraw Pipeline | Phase 4 | Shipped 2026-05-04 (hardened in Phase 04.1) |
    ```

    **Do NOT modify** any other line in REQUIREMENTS.md. The POLISH-04/POLISH-05 mid-flight status lines at 128-129 are correct as-is.
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*DIAG-0[1-5]\*\*" .planning/REQUIREMENTS.md && grep -c "Shipped 2026-05-04 (hardened in Phase 04.1)" .planning/REQUIREMENTS.md && ! grep -q "^- \[ \] \*\*DIAG-0" .planning/REQUIREMENTS.md && ! grep -q "^| DIAG-0.*| Pending |" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*DIAG-0[1-5]\*\*" .planning/REQUIREMENTS.md` returns 5 (all 5 bullets flipped)
    - `grep -c "^- \[ \] \*\*DIAG-0" .planning/REQUIREMENTS.md` returns 0 (zero unchecked DIAG bullets remain)
    - `grep -c "Shipped 2026-05-04 (hardened in Phase 04.1)" .planning/REQUIREMENTS.md` returns 5 (all 5 traceability rows updated)
    - `grep -c "| DIAG-0.*| Pending |" .planning/REQUIREMENTS.md` returns 0 (zero pending DIAG rows)
    - Total line count of file unchanged (only text substitutions, no line additions/deletions)
    - `grep -c "POLISH-04" .planning/REQUIREMENTS.md` returns 2 (existing bullet + traceability row both unchanged)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md reflects Phase 4 shipped status + Phase 04.1 hardening pointer. Bullets + traceability in lockstep.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip ROADMAP.md Phase 4 checkbox, progress, traceability, last-updated; fill Phase 04.1 goal prose (F-02/F-03/F-20 + D-18)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md HEAD — Phase 4 checkbox line 26, Phase 04.1 block lines 159-167, Progress Table row line 238, Traceability table DIAG rows lines 314-318, Plan 05 bullet line 155, Last-updated line 336
    - .planning/phases/04-excalidraw-pipeline/reviews/DOCS-REVIEW.md F-02 (lines 67-110), F-03 (lines 112-143), F-20 (lines 527-548) — verbatim diffs ready to paste
    - .planning/phases/04.1-excalidraw-pipeline-hardening/04.1-CONTEXT.md §D-18 (ROADMAP edits list)
    - .planning/phases/04.1-excalidraw-pipeline-hardening/04.1-CONTEXT.md §domain — first paragraph ("Remediate the 58 post-phase review findings from CONSOLIDATED.md...") is the source for Phase 04.1 goal prose
  </read_first>
  <behavior>
    Six-block edit on the same file:
    1. Line 26: Phase 4 checkbox `- [ ]` → `- [x]` + shipped date suffix
    2. Line 155: Plan 05 bullet "5 H2 sections" → "6 H2 sections" + LOC count
    3. Lines 159-167: Phase 04.1 block — goal prose fills in, Requirements + Depends-on stay, Plans count 0 → (to be populated by 04.1 planning — actually leave "TBD" since we're mid-planning; or set to 6 plans if this plan runs after all 6 04.1 plans land — policy call: set to 6 because this plan WILL commit only after all 6 land, given it's Wave 1 but docs-only, and roadmap-commit is one of the last steps)
    4. Lines 314-318: Traceability table DIAG-01..05 Pending → Shipped
    5. Line 238: Progress Table row `0/5 Planned` → `5/5 Shipped`
    6. Line 336: Last-updated → 2026-05-04 with Phase 4 shipped note
  </behavior>
  <action>
    **Edit `.planning/ROADMAP.md`** — 6 edits in order:

    **Edit (a) — Line 26 (Phase 4 checkbox):**

    Replace:
    ```markdown
    - [ ] **Phase 4: Excalidraw Pipeline** — Export script + replace ASCII diagrams + stress-test on 2-3 posts
    ```

    With:
    ```markdown
    - [x] **Phase 4: Excalidraw Pipeline** — Export script + replace ASCII diagrams + stress-test on 2-3 posts (shipped 2026-05-04)
    ```

    **Edit (b) — Line 155 (Plan 05 bullet):**

    Replace:
    ```markdown
    - [x] 04-05-PLAN.md — Ship `diagrams-source/README.md` runbook (5 H2 sections: Authoring / Metadata / Exporting / Embedding / Gotchas); update `.claude/skills/vv-blog-from-vault/references/visuals-routing.md` Priority 1 `excalidraw` row with delivery path; post-phase REQUIREMENTS.md DIAG-05 doc-drift fix per D-06 (DIAG-01 UNCHANGED per D-01e)
    ```

    With:
    ```markdown
    - [x] 04-05-PLAN.md — Ship `diagrams-source/README.md` runbook (6 H2 sections: Authoring / Metadata / Exporting / Embedding / Gotchas / Further reading; 118 LOC); update `.claude/skills/vv-blog-from-vault/references/visuals-routing.md` Priority 1 `excalidraw` row with delivery path; post-phase REQUIREMENTS.md DIAG-05 doc-drift fix per D-06 (DIAG-01 UNCHANGED per D-01e)
    ```

    **Edit (c) — Phase 04.1 block (lines 159-167).** Find:

    ```markdown
    ### Phase 04.1: Excalidraw Pipeline Hardening (INSERTED)

    **Goal:** [Urgent work - to be planned]
    **Requirements**: TBD
    **Depends on:** Phase 4
    **Plans:** 0 plans

    Plans:
    - [ ] TBD (run /gsd-plan-phase 04.1 to break down)
    ```

    Replace with:

    ```markdown
    ### Phase 04.1: Excalidraw Pipeline Hardening (INSERTED)

    **Goal:** Remediate 58 post-phase review findings against the Phase 4 Excalidraw pipeline shipment — 2 CRITICAL + 2 HIGH security findings (SEC-C01 symlink bypass, SEC-C02 fictional SVGO script-strip, SEC-H03 SSRF via dataURL, SEC-H04 parsererror silent success), 4 blocking UX findings (white slab on dark prose, mobile label illegibility, MCP banner aspect, Helvetica cross-platform drift), 13 code-quality + test-coverage gaps (idempotency, JSON error UX, meta validation, FILES_BLOB budget test, escapeXml Unicode), 20 docs-drift findings (REQUIREMENTS/ROADMAP/STATE status, runbook missing npm install, runbook Virgil vs Helvetica, CLAUDE.md no Excalidraw mention, Phase 7→5 reference), plus P4 polish (figure/figcaption a11y, forced-colors smoke check).

    **Requirements:** DIAG-01..05 (hardened, not shipped anew — all 5 already shipped in Phase 4)

    **Depends on:** Phase 4

    **Plans:** 6 plans across 4 waves

    Plans:
    - [ ] 04.1-01-PLAN.md — Security hardening: realpath validatePath + ALLOWED_WRITE_ROOTS + SVGO removeScripts + dataURL whitelist + parsererror detection (Wave 1)
    - [ ] 04.1-02-PLAN.md — UX pipeline fixes: SVGO white-rect strip + Helvetica fallback chain (Wave 2, serialized after 04.1-01 due to shared script file)
    - [ ] 04.1-03-PLAN.md — MCP re-author + SVG re-exports + figure/figcaption MDX wrap in bilingual MCP + karpenter posts (Wave 3)
    - [ ] 04.1-04-PLAN.md — Docs drift: REQUIREMENTS / ROADMAP / STATE / PROJECT / CLAUDE.md / runbook sweep (Wave 1, docs-only parallel with 04.1-01)
    - [ ] 04.1-05-PLAN.md — Code quality + test coverage: parseJsonOrThrow + meta type validation + FILES_BLOB test + idempotency test + Unicode escapeXml parametric + --quiet flag (Wave 3, parallel with 04.1-03)
    - [ ] 04.1-06-PLAN.md — P4 polish: SEC-L02 closure note + forced-colors Playwright smoke check + Phase 04.1 SUMMARY (Wave 4)
    ```

    **Edit (d) — Traceability table (lines 314-318).** Find:

    ```markdown
    | DIAG-01 | Excalidraw Pipeline | Phase 4 | Pending — planned in 04-01 (infra) + 04-02 (script) |
    | DIAG-02 | Excalidraw Pipeline | Phase 4 | Pending — planned in 04-01 (budget test) + 04-02 (gate impl) |
    | DIAG-03 | Excalidraw Pipeline | Phase 4 | Pending — planned in 04-01 (a11y tests) + 04-02 (injection) + 04-03/04 (canonical path) |
    | DIAG-04 | Excalidraw Pipeline | Phase 4 | Pending — planned in 04-03 (MCP swap, EN + RU) |
    | DIAG-05 | Excalidraw Pipeline | Phase 4 | Pending — planned in 04-04 (karpenter, EN + RU) |
    ```

    Replace with:

    ```markdown
    | DIAG-01 | Excalidraw Pipeline | Phase 4 | Shipped — 04-01 fixtures + 04-02 script (178 LOC); hardened in Phase 04.1 |
    | DIAG-02 | Excalidraw Pipeline | Phase 4 | Shipped — budget gate in 04-02; both SVGs ≤ 10 KB (9673 B MCP pre-04.1, 7338 B karpenter pre-04.1); hardened in Phase 04.1 |
    | DIAG-03 | Excalidraw Pipeline | Phase 4 | Shipped — pre-SVGO title+desc injection verified in both SVGs; hardened in Phase 04.1 |
    | DIAG-04 | Excalidraw Pipeline | Phase 4 | Shipped — MCP ASCII → SVG in EN + RU (04-03); hardened in Phase 04.1 |
    | DIAG-05 | Excalidraw Pipeline | Phase 4 | Shipped — karpenter split-ownership in EN + RU (04-04); stretch deferred per D-04c; hardened in Phase 04.1 |
    ```

    **Edit (e) — Progress Table (line 238):**

    Replace:
    ```markdown
    | 4. Excalidraw Pipeline | 0/5 | Planned 2026-05-03 — 5 plans across 4 waves | - |
    ```

    With:
    ```markdown
    | 4. Excalidraw Pipeline | 5/5 | Shipped 2026-05-04 (hardening in Phase 04.1) | 2026-05-04 |
    ```

    **Edit (f) — Last-updated (line 336):**

    Replace:
    ```markdown
    **Last updated:** 2026-05-03 (Phase 4 planned — 5 plans across 4 waves, Wave 2 parallel)
    ```

    With:
    ```markdown
    **Last updated:** 2026-05-04 (Phase 4 excalidraw-pipeline shipped — 5 plans, 2 SVGs embedded in MCP + karpenter posts; Phase 04.1 hardening planned — 6 plans across 4 waves)
    ```

    **Do NOT modify** any other line in ROADMAP.md. The Plans 04-01..04-04 bullets at 145-154 are correct as-is (each already shows `[x]`).
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*Phase 4: Excalidraw Pipeline\*\* .* (shipped 2026-05-04)" .planning/ROADMAP.md && grep -c "6 H2 sections: Authoring / Metadata / Exporting / Embedding / Gotchas / Further reading; 118 LOC" .planning/ROADMAP.md && grep -c "Remediate 58 post-phase review findings" .planning/ROADMAP.md && grep -c "6 plans across 4 waves" .planning/ROADMAP.md && grep -c "| 4\. Excalidraw Pipeline | 5/5 | Shipped 2026-05-04" .planning/ROADMAP.md && grep -c "\\*\\*Last updated:\\*\\* 2026-05-04" .planning/ROADMAP.md && ! grep -q "\[Urgent work - to be planned\]" .planning/ROADMAP.md && ! grep -q "^- \[ \] \*\*Phase 4:" .planning/ROADMAP.md && ! grep -q "Pending — planned in 04-0" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*Phase 4: Excalidraw Pipeline\*\*" .planning/ROADMAP.md` returns 1
    - `grep -c "\[Urgent work - to be planned\]" .planning/ROADMAP.md` returns 0
    - `grep -c "Phase 04.1-01-PLAN.md" .planning/ROADMAP.md` returns 1 (and 02..06 variants, 6 total plans bullets)
    - `grep -c "04.1-0[1-6]-PLAN.md" .planning/ROADMAP.md` returns 6
    - `grep -c "6 plans across 4 waves" .planning/ROADMAP.md` returns 1
    - `grep -c "| 4\. Excalidraw Pipeline | 5/5 | Shipped 2026-05-04" .planning/ROADMAP.md` returns 1
    - `grep -c "Pending — planned in 04-0" .planning/ROADMAP.md` returns 0 (all 5 traceability rows updated)
    - `grep -c "\*\*Last updated:\*\* 2026-05-04" .planning/ROADMAP.md` returns 1
    - `grep -c "2026-05-03 (Phase 4 planned" .planning/ROADMAP.md` returns 0 (old last-updated fully replaced)
  </acceptance_criteria>
  <done>
    ROADMAP.md coherent with Phase 4 shipped + Phase 04.1 planned (6 plans across 4 waves). All DOCS-REVIEW F-02/F-03/F-20 drift closed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Align STATE.md body with frontmatter + populate Completed Phases (F-05/F-06 + D-18)</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md HEAD — entire file (143 lines) for full context
    - .planning/phases/04-excalidraw-pipeline/reviews/DOCS-REVIEW.md F-05 (lines 203-260) + F-06 (lines 262-270) — verbatim diffs
    - .planning/phases/04.1-excalidraw-pipeline-hardening/04.1-CONTEXT.md §D-18 STATE.md section (bullets c, d, e)
  </read_first>
  <behavior>
    - Frontmatter current_phase: "04.1" is correct (matches where we are). Keep as-is.
    - Body `**Current phase:** 5` on line 20 DRIFTS — fix to `04.1`.
    - Body `**Last updated:** 2026-05-03` on line 22 DRIFTS — fix to `2026-05-04`.
    - Body lines 32-34 (`Phase: 04 (excalidraw-pipeline) — EXECUTING` + `Plan: Not started` + `Status: Executing Phase 04`) DRIFTS — fix to Phase 04.1 planning state.
    - Body line 42 (`_None yet — milestone v1.0 just started. Phase numbering reset to 1._`) DRIFTS — populate with Phase 01-04 entries.
    - Add a known-issue note about gsd-sdk phase.complete bug per D-18 bullet (e).
  </behavior>
  <action>
    **Edit `.planning/STATE.md`** — 4 block edits:

    **Edit (a): Body Current phase drift (lines 19-23).** Find:

    ```markdown
    # STATE.md

    **Current milestone:** v1.0 — Content Platform
    **Current phase:** 04.1
    **Status:** Ready to plan
    **Last updated:** 2026-05-03
    ```

    Replace with:

    ```markdown
    # STATE.md

    **Current milestone:** v1.0 — Content Platform
    **Current phase:** 04.1
    **Status:** Ready to execute (6 plans planned, Wave 1 ready)
    **Last updated:** 2026-05-04
    ```

    **Edit (b): Body Current Position (lines 30-38).** Find:

    ```markdown
    ## Current Position

    Phase: 04 (excalidraw-pipeline) — EXECUTING
    Plan: Not started
    Status: Executing Phase 04
    Resume file: .planning/phases/04.1-excalidraw-pipeline-hardening/04.1-CONTEXT.md
    Last activity: 2026-05-04

    Progress: [██████████] 100%
    ```

    Replace with:

    ```markdown
    ## Current Position

    Phase: 04.1 (excalidraw-pipeline-hardening) — READY TO EXECUTE
    Plan: 6 plans planned across 4 waves
    Status: Ready to execute Phase 04.1 Wave 1 (plans 04.1-01 + 04.1-04 run in parallel; 04.1-02 serialized after 04.1-01 due to file-overlap)
    Resume file: .planning/phases/04.1-excalidraw-pipeline-hardening/04.1-01-PLAN.md
    Last activity: 2026-05-04

    Progress: [██████████] 100% (v1.0 Phases 1-4 shipped; Phase 04.1 hardening ready)
    ```

    **Edit (c): Completed Phases section (lines 40-42).** Find:

    ```markdown
    ## Completed Phases

    _None yet — milestone v1.0 just started. Phase numbering reset to 1._
    ```

    Replace with:

    ```markdown
    ## Completed Phases

    - **Phase 01: Rich Media Primitives** — shipped v1.0 kickoff (VvStage/VvNode/VvWire/VvPacket Astro primitives + PodLifecycleAnimation refactor + README, 6 plans across 5 waves; PRIMS-01..06)
    - **Phase 02: Code Block Upgrades** — shipped (Shiki language badges + `// [!code highlight]` transformer + Deep Signal github-dark palette + CodeCopyEnhancer rewrite + 8-hex palette guard test, 6 plans across 6 waves; CODE-01..05)
    - **Phase 03: UI Polish** — shipped 2026-05-03 (POLISH-01..06, 4 plans: token/motion infra + bottom CTAs + stagger wiring + 14-row visual audit; WR-03 Shiki palette guard folded in)
    - **Phase 04: Excalidraw Pipeline** — shipped 2026-05-04 (DIAG-01..05, 5 plans: fixtures + script + MCP swap + karpenter + runbook; 2 SVGs embedded; 9 integration tests green; hardening in Phase 04.1)
    ```

    **Edit (d): Add known-issue note in Active Context section (after line 50).** Find the existing Active Context block:

    ```markdown
    ## Active Context

    - Milestone v0.4 Reference Design Audit shipped 2026-05-01 (12 phases, 26 plans, 41 tasks).
    ```

    Replace with (prepend a new bullet):

    ```markdown
    ## Active Context

    - **Known issue:** `gsd-sdk phase.complete` does not propagate to REQUIREMENTS/ROADMAP Traceability tables. Manual fix per phase until SDK patches — Phase 04.1 Plan 04 applies the fix. See `.planning/phases/04.1-excalidraw-pipeline-hardening/04.1-CONTEXT.md §D-18`.
    - Milestone v0.4 Reference Design Audit shipped 2026-05-01 (12 phases, 26 plans, 41 tasks).
    ```

    **Do NOT modify** the frontmatter, the Accumulated Context section, Performance Metrics, Blockers, Notes (other than the insertion above), or any other section. Keep those byte-identical.
  </action>
  <verify>
    <automated>grep -c "\\*\\*Current phase:\\*\\* 04.1" .planning/STATE.md && grep -c "\\*\\*Last updated:\\*\\* 2026-05-04" .planning/STATE.md && grep -c "Phase: 04.1 (excalidraw-pipeline-hardening) — READY TO EXECUTE" .planning/STATE.md && grep -c "Phase 04: Excalidraw Pipeline.*shipped 2026-05-04" .planning/STATE.md && grep -c "gsd-sdk phase.complete" .planning/STATE.md && ! grep -q "\\*\\*Current phase:\\*\\* 5" .planning/STATE.md && ! grep -q "Phase: 04 (excalidraw-pipeline) — EXECUTING" .planning/STATE.md && ! grep -q "_None yet — milestone v1.0 just started" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter `current_phase: 04.1` UNCHANGED
    - Body `**Current phase:** 04.1` present (not 5, not 04)
    - Body `**Last updated:** 2026-05-04` present (not 2026-05-03)
    - `Phase: 04.1` READY TO EXECUTE present in Current Position block
    - 4 Completed Phases bullets present (Phase 01 through Phase 04), each with at least one artifact reference
    - `gsd-sdk phase.complete` known-issue note present in Active Context
    - No `Current phase: 5` anywhere in body
    - No `Phase: 04 (excalidraw-pipeline) — EXECUTING` anywhere
    - No `_None yet — milestone v1.0 just started_` anywhere
    - Line count delta: roughly +8 lines (4 Completed Phases bullets + 1 known-issue bullet + minor adjustments)
  </acceptance_criteria>
  <done>
    STATE.md body coherent with frontmatter; Completed Phases section populated with 4 shipped v1.0 phases; known-issue note captures gsd-sdk bug.
  </done>
</task>

<task type="auto">
  <name>Task 4: Append v1.0 Phase 4 validations paragraph to PROJECT.md (F-13/F-14 + D-22)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md HEAD — specifically lines 29-30 (existing v1.0 Phase 2 + Phase 3 validation blocks — shape template)
    - .planning/phases/04-excalidraw-pipeline/reviews/DOCS-REVIEW.md F-13 + F-14 (lines 430-445) — ready-to-paste paragraph
    - .planning/phases/04.1-excalidraw-pipeline-hardening/04.1-CONTEXT.md §D-22 (exact text directive)
  </read_first>
  <behavior>
    - Insert a new validation paragraph AFTER the existing "Validated in v1.0 Phase 3" block at line 30, BEFORE the "## Current milestone" heading at line 32.
    - Paragraph follows the same shape as Phase 2/3 blocks: opens with `Validated in v1.0 Phase N (phase-name):`, lists REQ-IDs, names implementation details + decisions + tech debt.
    - Tech debt note: mentions that BL-01 (symlink bypass) + BL-02 (regex injection) + WR-04 (Helvetica cross-platform) were all remediated in Phase 04.1 — the hardening phase closes Phase 4's known debt atomically.
  </behavior>
  <action>
    **Edit `.planning/PROJECT.md`** — insert a new paragraph between line 30 (end of Phase 3 block) and line 32 (start of "## Current milestone").

    Find this section:
    ```markdown
    Validated in v1.0 Phase 3 (ui-polish): POLISH-01..06 — bottom "All X →" CTAs unified on Blog/Presentations/Speaking using canonical class `text-sm text-text-muted hover:text-brand-primary transition-colors whitespace-nowrap` (existing `blog.all_posts` / `presentations.all_decks` / `speaking.all_talks` i18n keys kept per D-01, REQUIREMENTS.md reference to `*.see_all` is accepted doc-drift); `--transition-normal` updated to `250ms cubic-bezier(0.16, 1, 0.3, 1)` (expo-out curve per reference app.jsx); `.animate-on-scroll-stagger` CSS variant (pure CSS `:nth-child(1..10)` at 60ms step, cap at 540ms for n+11, reduced-motion guard with `0ms !important`); 14-row visual audit against Deep Signal reference with 2 atomic fixes (About `sm:py-28` rhythm, Podcasts `bg-surface` + `gap-5`) and 3 cross-cutting DEFER rows (H2 scale, About Me casing, max-w-6xl harmonization); WR-03 Shiki palette guard test folded in (8 codeToHtml assertions, sentinel-anchored pointers via `SHIKI_TOKEN_OVERRIDES_BEGIN`/`END`). Card hover follows reference border+glow+title-color-shift (no translate-Y, no amber underline) per D-02.

    ## Current milestone: v1.0 — Content Platform (planned, started 2026-05-02)
    ```

    Replace with (ADD the Phase 4 paragraph BEFORE the `## Current milestone` heading):

    ```markdown
    Validated in v1.0 Phase 3 (ui-polish): POLISH-01..06 — bottom "All X →" CTAs unified on Blog/Presentations/Speaking using canonical class `text-sm text-text-muted hover:text-brand-primary transition-colors whitespace-nowrap` (existing `blog.all_posts` / `presentations.all_decks` / `speaking.all_talks` i18n keys kept per D-01, REQUIREMENTS.md reference to `*.see_all` is accepted doc-drift); `--transition-normal` updated to `250ms cubic-bezier(0.16, 1, 0.3, 1)` (expo-out curve per reference app.jsx); `.animate-on-scroll-stagger` CSS variant (pure CSS `:nth-child(1..10)` at 60ms step, cap at 540ms for n+11, reduced-motion guard with `0ms !important`); 14-row visual audit against Deep Signal reference with 2 atomic fixes (About `sm:py-28` rhythm, Podcasts `bg-surface` + `gap-5`) and 3 cross-cutting DEFER rows (H2 scale, About Me casing, max-w-6xl harmonization); WR-03 Shiki palette guard test folded in (8 codeToHtml assertions, sentinel-anchored pointers via `SHIKI_TOKEN_OVERRIDES_BEGIN`/`END`). Card hover follows reference border+glow+title-color-shift (no translate-Y, no amber underline) per D-02.

    Validated in v1.0 Phase 4 (excalidraw-pipeline): DIAG-01..05 — `scripts/excalidraw-to-svg.mjs` (178 LOC) using `@aldinokemal2104/excalidraw-to-svg@1.1.1` (exact-pinned devDep per D-01d/D-01e); SVGO `preset-default` + `removeDesc: false` + `multipass: true` with pre-SVGO `<title>`+`<desc>` injection via `escapeXml`; 10 KB budget gate + `validatePath` (REPO_ROOT ∪ os.tmpdir) path-traversal guard + `validateFilesBlob` 100 KB cap; `public/blog-assets/<slug>/diagrams/*.svg` convention; MCP post ASCII `←→` diagram replaced with bilingual `<img>` embed of 9,673 B client-server SVG (EN + RU); karpenter post gains 7,338 B split-ownership SVG (EN + RU) after the "split ownership" paragraph (Helvetica fontFamily=2 to fit budget per D-04b Deviations §1); `diagrams-source/README.md` contributor runbook (118 LOC, 6 H2 sections); `visuals-routing.md` Priority 1 `excalidraw` row extended with concrete delivery path; REQUIREMENTS.md DIAG-05 language reconciled per D-06-revised (DIAG-01 unchanged per D-01e); 9 new integration tests (44 total at phase close), all green; boundary invariant holds (`@aldinokemal2104` / `@excalidraw` never appear in `src/` or `dist/`). Tech debt BL-01 (symlink-bypass on validatePath), BL-02 (regex-based title/desc injection not XML-aware), and WR-04 (Helvetica OS-dependent) all remediated in Phase 04.1 (excalidraw-pipeline-hardening, 58-finding review sweep): realpath walk + ALLOWED_WRITE_ROOTS, explicit `removeScripts` + `removeAttributesBySelector`, dataURL scheme whitelist, parsererror detection, white-rect SVGO strip, Helvetica fallback chain, figure/figcaption MDX wrap, MCP viewBox re-author to 820×300 for mobile legibility, 58 findings closed across P1-P4 tiers.

    ## Current milestone: v1.0 — Content Platform (planned, started 2026-05-02)
    ```

    **Also update line 118 (Last updated)** to reflect Phase 04.1 planning:

    Find:
    ```markdown
    **Last updated:** 2026-05-04 (Phase 4 excalidraw-pipeline complete — DIAG-01..05 shipped; scripts/excalidraw-to-svg.mjs + 2 SVGs embedded in MCP + Karpenter posts)
    ```

    Replace with:
    ```markdown
    **Last updated:** 2026-05-04 (Phase 4 excalidraw-pipeline shipped 2026-05-04 — DIAG-01..05; Phase 04.1 hardening planned — 6 plans across 4 waves, 58 review findings in scope)
    ```

    **Do NOT modify** any other section. Keep the "What this project is", "Current state", "Prior milestones", "Validated decisions", "Constraints", "Tools", "Obsidian vault cross-references", "Not in scope for v1.0", and "Evolution" sections byte-identical.
  </action>
  <verify>
    <automated>grep -c "Validated in v1.0 Phase 4 (excalidraw-pipeline): DIAG-01..05" .planning/PROJECT.md && grep -c "@aldinokemal2104/excalidraw-to-svg@1.1.1" .planning/PROJECT.md && grep -c "remediated in Phase 04.1" .planning/PROJECT.md && grep -c "\\*\\*Last updated:\\*\\* 2026-05-04 (Phase 4 excalidraw-pipeline shipped 2026-05-04" .planning/PROJECT.md && grep -c "Validated in v1.0 Phase 2 (code-block-upgrades)" .planning/PROJECT.md && grep -c "Validated in v1.0 Phase 3 (ui-polish)" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - New Phase 4 validation paragraph present (single paragraph starting with "Validated in v1.0 Phase 4 (excalidraw-pipeline):")
    - Paragraph names all 5 REQ-IDs `DIAG-01..05`
    - Paragraph mentions `@aldinokemal2104/excalidraw-to-svg@1.1.1` exact pin
    - Paragraph names tech-debt closures `BL-01`, `BL-02`, `WR-04` + cites Phase 04.1 remediation
    - Phase 2 + Phase 3 validation paragraphs unchanged (grep still finds both)
    - Last-updated line updated with Phase 04.1 planning note
    - File line count delta: +1 paragraph (~1 long line of prose)
  </acceptance_criteria>
  <done>
    PROJECT.md records Phase 4 as a validated v1.0 milestone checkpoint with full DIAG-01..05 evidence + Phase 04.1 tech-debt pointer. F-13/F-14 closed.
  </done>
</task>

<task type="auto">
  <name>Task 5: CLAUDE.md — insert Excalidraw section after Slidev; fix Phase 7→5 Slidev reference (F-01 + F-18 + D-21)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md HEAD — Slidev Presentations Integration section at lines 311-326, Development section starts at line 329
    - .planning/phases/04-excalidraw-pipeline/reviews/DOCS-REVIEW.md F-01 (lines 38-63), F-18 (lines 500-517) — verbatim diffs
    - .planning/phases/04.1-excalidraw-pipeline-hardening/04.1-CONTEXT.md §D-21 (exact content directive)
    - .planning/phases/04.1-excalidraw-pipeline-hardening/04.1-PATTERNS.md §`CLAUDE.md` lines 750-781 (insertion point + exact content)
  </read_first>
  <behavior>
    Two edits on the same file:
    1. Insert new `## Excalidraw Diagram Pipeline — LIVE (since 2026-05-04)` section after the Slidev section (after line 325, before the `---` separator at line 327). Sibling-level H2, same "LIVE (since YYYY-MM-DD)" convention as Deep Signal section at line 112.
    2. Fix line 316 `**Phase 7 (future):**` → `**v1.0 Phase 5 (planned):**` with the accurate sub-path + ROADMAP pointer.
  </behavior>
  <action>
    **Edit `CLAUDE.md`** — 2 edits:

    **Edit (a): Phase 7→5 fix (line 316).**

    Find:
    ```markdown
    **Phase 7 (future):** Migrate Slidev builds into `public/slides/` so presentations live at `vedmich.dev/slides/`.
    ```

    Replace with:
    ```markdown
    **v1.0 Phase 5 (planned):** Migrate Slidev builds into `public/slides/` so presentations live at `vedmich.dev/slides/<slug>/`. See `.planning/ROADMAP.md §Phase 5: Slidev Integration`.
    ```

    **Edit (b): Insert new Excalidraw section.** Find the END of the Slidev Presentations Integration section (line 325 is `Theme repos: ...`), then line 326 is blank, then line 327 is `---` (start of Development section). Insert the new section between line 326 (blank) and line 327 (`---`).

    Locate this block:

    ```markdown
    Slidev repos: `vedmichv/slidev`, `DKT-AI/slidev`
    Theme repos: `vedmichv/slidev-theme-slurm`, `DKT-AI/slidev-theme-dkt`

    ---

    ## Development
    ```

    Replace with:

    ```markdown
    Slidev repos: `vedmichv/slidev`, `DKT-AI/slidev`
    Theme repos: `vedmichv/slidev-theme-slurm`, `DKT-AI/slidev-theme-dkt`

    ---

    ## Excalidraw Diagram Pipeline — LIVE (since 2026-05-04)

    Hand-sketched diagrams ship as build-time-exported static SVGs via `@aldinokemal2104/excalidraw-to-svg@1.1.1`. Source `.excalidraw.json` lives at `diagrams-source/<slug>/`, optimized SVG ships to `public/blog-assets/<slug>/diagrams/*.svg`, embedded via `<figure><img><figcaption>` in bilingual MDX. Zero runtime JS.

    Current consumers: MCP post (`2026-03-02-mcp-servers-plainly-explained`) + Karpenter post (`2026-03-20-karpenter-right-sizing`).

    **Runbook:** `diagrams-source/README.md`
    **Script:** `scripts/excalidraw-to-svg.mjs`
    **Tests:** `tests/unit/excalidraw-to-svg.test.ts`
    **Boundary invariant:** `@aldinokemal2104/excalidraw-to-svg` is a devDep — never import from `src/`.

    ---

    ## Development
    ```

    **Do NOT modify** any other line in CLAUDE.md.
  </action>
  <verify>
    <automated>grep -c "^## Excalidraw Diagram Pipeline — LIVE (since 2026-05-04)$" CLAUDE.md && grep -c "@aldinokemal2104/excalidraw-to-svg@1.1.1" CLAUDE.md && grep -c "<figure><img><figcaption>" CLAUDE.md && grep -c "v1.0 Phase 5 (planned)" CLAUDE.md && grep -c "Boundary invariant" CLAUDE.md && ! grep -q "Phase 7 (future)" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Excalidraw Diagram Pipeline" CLAUDE.md` returns 1 (new H2 added)
    - `grep -c "@aldinokemal2104/excalidraw-to-svg@1.1.1" CLAUDE.md` returns 1
    - `grep -c "diagrams-source/README.md" CLAUDE.md` returns 1 (runbook pointer in new section)
    - `grep -c "scripts/excalidraw-to-svg.mjs" CLAUDE.md` returns 1 (script pointer)
    - `grep -c "tests/unit/excalidraw-to-svg.test.ts" CLAUDE.md` returns 1 (tests pointer)
    - `grep -c "v1.0 Phase 5 (planned)" CLAUDE.md` returns 1
    - `grep -c "Phase 7 (future)" CLAUDE.md` returns 0 (stale reference gone)
    - `grep -c "^## Slidev Presentations Integration$" CLAUDE.md` returns 1 (existing Slidev section unchanged)
    - `grep -c "^## Development$" CLAUDE.md` returns 1 (Development section still follows the new Excalidraw section)
  </acceptance_criteria>
  <done>
    CLAUDE.md has a live Excalidraw pipeline section mirroring the Deep Signal section's conventions; stale Phase 7 reference corrected.
  </done>
</task>

<task type="auto">
  <name>Task 6: Runbook updates — Prerequisites H2 + rewrite Gotcha 1 + Determinism caveat + Further-reading count fix (D-14 + D-19 + D-20 + F-07/08/09/20)</name>
  <files>diagrams-source/README.md</files>
  <read_first>
    - diagrams-source/README.md HEAD — full 118 LOC file, specifically line 8 (`***` divider above Authoring), line 9 (`## Authoring`), Gotcha 1 at line 98, Further reading at line 112, test-count claim at line 115
    - .planning/phases/04-excalidraw-pipeline/reviews/DOCS-REVIEW.md F-07 (lines 341-357), F-08 (lines 340-357), F-09 (lines 362-369), F-20 (lines 527-548) — verbatim diffs
    - .planning/phases/04.1-excalidraw-pipeline-hardening/04.1-CONTEXT.md §D-14 (Determinism caveat content), §D-19 (Prerequisites content), §D-20 (Gotcha 1 rewrite)
    - .planning/phases/04.1-excalidraw-pipeline-hardening/04.1-PATTERNS.md lines 686-746 (runbook pattern map with exact content)
  </read_first>
  <behavior>
    Four block edits on the same file:
    1. Add new `## Prerequisites` H2 with `***` divider above `## Authoring` (above line 9, below the existing `***` at line 8). Content per D-19.
    2. Replace Gotcha 1 (current: "Virgil-only text keeps the budget.") with element-count threshold + Helvetica fallback-chain pattern per D-20.
    3. Add new `## Determinism caveat` H2 section after Gotchas (Gotcha 6 ends around line 108), before `## Further reading` at line 112. Content per D-14.
    4. Update line 115 test-count claim from `9 integration tests` to the post-04.1 count (~25-30 depending on plan-02/03/05 landing at execute-time; the docs plan runs in Wave 1 so the count is aspirational — match what tests will look like after all 04.1 plans land). Use `~30 integration tests` as the conservative post-04.1 target and update F-20's stale "5 H2 sections" reference if it shows here too (it doesn't — F-20 is ROADMAP).
  </behavior>
  <action>
    **Edit `diagrams-source/README.md`** — 4 edits in order:

    **Edit (a): Insert Prerequisites H2.** Find:

    ```markdown
    **Ship status:** Phase 4 (milestone v1.0). Consumers: `2026-03-02-mcp-servers-plainly-explained` (MCP post), `2026-03-20-karpenter-right-sizing` (karpenter split-ownership). Future consumers: companion posts (Phase 6) + any post where a hand-sketched metaphor fits.

    ***

    ## Authoring
    ```

    Replace with (insert Prerequisites BEFORE Authoring, keep the `***` divider between them):

    ```markdown
    **Ship status:** Phase 4 (milestone v1.0, hardened in Phase 04.1). Consumers: `2026-03-02-mcp-servers-plainly-explained` (MCP post), `2026-03-20-karpenter-right-sizing` (karpenter split-ownership). Future consumers: companion posts (Phase 6) + any post where a hand-sketched metaphor fits.

    ***

    ## Prerequisites

    The pipeline depends on `@aldinokemal2104/excalidraw-to-svg@1.1.1` (exact pin — devDep) and `svgo@^4` (transitive).

    First-time setup:
    ```bash
    npm install  # pulls in the wrapper + svgo from package.json:devDependencies
    ```

    Verify the pin:
    ```bash
    node -p "require('./package.json').devDependencies['@aldinokemal2104/excalidraw-to-svg']"
    # → 1.1.1
    ```

    Additional requirements:
    - Node.js 20+
    - Excalidraw desktop app OR excalidraw.com for authoring `.excalidraw.json` source files

    ***

    ## Authoring
    ```

    **Edit (b): Rewrite Gotcha 1** (currently at line 98 — check exact line number post-Edit-a):

    Find:
    ```markdown
    1. **Virgil-only text keeps the budget.** The wrapper auto-embeds whichever Excalidraw fonts your diagram uses. Cascadia + Comic Shanns + Lilita One together can push even a small diagram over 10 KB. If you want mixed fonts, verify the SVG byte count and simplify if needed.
    ```

    Replace with:

    ```markdown
    1. **Font choice is the dominant byte driver.** The wrapper auto-embeds each Excalidraw font your diagram uses as an inline base64 TTF subset. Virgil alone is ~9.5 KB — at ≥ 8 elements, a Virgil diagram overruns the 10 KB budget no matter how few labels it has. Two thresholds:
       - **≤ 10 elements** → Virgil is fine (hand-sketched aesthetic, Excalidraw default, pairs with `fontFamily: 1`). MCP client-server ships in Virgil.
       - **> 10 elements** → switch the diagram to Helvetica in Excalidraw (`fontFamily: 3`). The pipeline auto-appends `Arial, sans-serif` fallback chain via post-SVGO string replace, so Windows readers (no Helvetica installed) get Arial — metrics close enough that label boxes hold. Karpenter split-ownership ships in Helvetica at ~7.4 KB.
       - **Cross-platform smoke check:** after shipping a Helvetica diagram, screenshot on Windows Chrome if available — Arial should render in place of Helvetica without overflow.
       - If you need mixed fonts in one diagram (rare), verify SVG byte count post-export and simplify until under budget.
    ```

    **Edit (c): Insert Determinism caveat section.** Find the end of the last Gotcha (Gotcha 6, "Original Excalidraw palette is intentional") and the next divider `***` before `## Further reading`:

    ```markdown
    6. **Original Excalidraw palette is intentional.** The pipeline does NOT recolor the diagram to Deep Signal tokens. The hand-sketched aesthetic (blue strokes, yellow fills) is preserved on purpose.

    ***

    ## Further reading
    ```

    Replace with (insert Determinism caveat H2 between Gotcha 6 and Further reading):

    ```markdown
    6. **Original Excalidraw palette is intentional.** The pipeline does NOT recolor the diagram to Deep Signal tokens. The hand-sketched aesthetic (blue strokes, yellow fills) is preserved on purpose.

    ***

    ## Determinism caveat

    The SVG's rough-edge jitter is driven by each element's `seed` field in the source JSON. Excalidraw assigns fresh seeds when you open an existing diagram and make edits. To keep `git diff` on SVG outputs minimal:

    - Commit the `.excalidraw.json` first, then run the pipeline, then commit the SVG.
    - If you re-export from Excalidraw UI after an edit session, expect the SVG to change visually-equivalently but byte-differently — `seed` / `versionNonce` churn is not a bug.
    - Never hand-touch `seed` values; they drive the `rough.js` jitter pattern per element. Stripping/normalizing them would flatten the hand-drawn feel (deterministic-per-element-id instead of pseudo-random).
    - The unit test `Idempotency :: byte-stable-on-rerun` only holds for the `tests/fixtures/excalidraw/minimal.excalidraw.json` fixture (hardcoded seeds 1-5). Real-world diagrams re-exported from Excalidraw will NOT be byte-stable across edit sessions.

    ***

    ## Further reading
    ```

    **Edit (d): Update Further reading test count.** Find:

    ```markdown
    - **`tests/unit/excalidraw-to-svg.test.ts`** — 9 integration tests covering DIAG-01/02/03 + T-04-01 path-traversal.
    ```

    Replace with:

    ```markdown
    - **`tests/unit/excalidraw-to-svg.test.ts`** — ~30 integration tests covering DIAG-01/02/03 + T-04-01 path-traversal + Phase 04.1 security hardenings (symlink bypass, SVGO script strip, dataURL scheme whitelist, parsererror detection) + UX fixes (white-rect strip, Helvetica fallback chain) + code-quality (JSON error UX, meta type validation, Unicode escapeXml parametric, byte-stability idempotency, --quiet flag).
    ```

    **Do NOT modify** other Gotchas (2-6), the Authoring/Metadata/Exporting/Embedding sections, or the other Further-reading bullets.
  </action>
  <verify>
    <automated>grep -c "^## Prerequisites$" diagrams-source/README.md && grep -c "@aldinokemal2104/excalidraw-to-svg@1.1.1" diagrams-source/README.md && grep -c "^## Determinism caveat$" diagrams-source/README.md && grep -c "Font choice is the dominant byte driver" diagrams-source/README.md && grep -c "Helvetica" diagrams-source/README.md && grep -c "~30 integration tests" diagrams-source/README.md && ! grep -q "Virgil-only text keeps the budget" diagrams-source/README.md && ! grep -q "9 integration tests covering DIAG-01/02/03 \+ T-04-01 path-traversal\.$" diagrams-source/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Prerequisites$" diagrams-source/README.md` returns 1 (new H2)
    - `grep -c "npm install" diagrams-source/README.md` returns ≥ 1 (in Prerequisites)
    - `grep -c "@aldinokemal2104/excalidraw-to-svg@1.1.1" diagrams-source/README.md` returns ≥ 1 (in Prerequisites, possibly also in verify command)
    - `grep -c "Node.js 20+" diagrams-source/README.md` returns 1
    - `grep -c "^## Determinism caveat$" diagrams-source/README.md` returns 1 (new H2)
    - `grep -c "Font choice is the dominant byte driver" diagrams-source/README.md` returns 1 (new Gotcha 1 first line)
    - `grep -c "Helvetica" diagrams-source/README.md` returns ≥ 3 (Gotcha 1 body + Determinism not mentioning, Further-reading mentions tests; count adjusts but should be > 0)
    - `grep -c "Arial, sans-serif" diagrams-source/README.md` returns ≥ 1 (Gotcha 1 mentions the fallback chain)
    - `grep -c "Virgil-only text keeps the budget" diagrams-source/README.md` returns 0 (old Gotcha 1 header gone)
    - `grep -c "9 integration tests" diagrams-source/README.md` returns 0 (outdated count replaced)
    - `grep -c "~30 integration tests" diagrams-source/README.md` returns 1
    - File has 8 H2 sections now (Prerequisites + Authoring + Metadata + Exporting + Embedding + Gotchas + Determinism caveat + Further reading) — count `^## ` grep returns 8
  </acceptance_criteria>
  <done>
    Runbook has Prerequisites (closes F-07), rewritten Gotcha 1 with Helvetica guidance (closes F-08), Determinism caveat (closes D-14 runbook-half), updated test-count claim (closes F-09). 6 H2 → 8 H2 sections.
  </done>
</task>

</tasks>

<verification>
After all 6 tasks:

```bash
# Build must still pass (docs edits should not break MDX build)
npm run build

# REQUIREMENTS
grep -c "^- \[x\] \*\*DIAG-0[1-5]\*\*" .planning/REQUIREMENTS.md  # expect 5
grep -c "Shipped 2026-05-04 (hardened in Phase 04.1)" .planning/REQUIREMENTS.md  # expect 5

# ROADMAP
grep -c "^- \[x\] \*\*Phase 4:" .planning/ROADMAP.md  # expect 1
grep -c "04.1-0[1-6]-PLAN.md" .planning/ROADMAP.md   # expect 6
grep -c "5/5 | Shipped 2026-05-04" .planning/ROADMAP.md  # expect 1

# STATE
grep -c "\\*\\*Current phase:\\*\\* 04.1" .planning/STATE.md  # expect 1 (body)
grep -c "Phase 04: Excalidraw Pipeline.*shipped 2026-05-04" .planning/STATE.md  # expect 1

# PROJECT
grep -c "Validated in v1.0 Phase 4 (excalidraw-pipeline): DIAG-01..05" .planning/PROJECT.md  # expect 1

# CLAUDE.md
grep -c "^## Excalidraw Diagram Pipeline" CLAUDE.md  # expect 1
grep -c "Phase 7 (future)" CLAUDE.md  # expect 0
grep -c "v1.0 Phase 5 (planned)" CLAUDE.md  # expect 1

# Runbook
grep -c "^## Prerequisites$" diagrams-source/README.md  # expect 1
grep -c "^## Determinism caveat$" diagrams-source/README.md  # expect 1
grep -c "Virgil-only text keeps the budget" diagrams-source/README.md  # expect 0
```
</verification>

<success_criteria>
- [ ] REQUIREMENTS.md DIAG-01..05 bullets + Traceability rows all shipped (F-04)
- [ ] ROADMAP.md Phase 4 checkbox, progress, traceability, last-updated, Plan 05 bullet, Phase 04.1 block all updated (F-02/F-03/F-20)
- [ ] STATE.md body + frontmatter coherent; Completed Phases populated with Phase 01-04 (F-05/F-06)
- [ ] PROJECT.md has v1.0 Phase 4 validation paragraph (F-13/F-14 + D-22)
- [ ] CLAUDE.md has new Excalidraw section + Phase 7→5 fix (F-01 + F-18)
- [ ] Runbook has Prerequisites + rewritten Gotcha 1 + Determinism caveat + updated test count (F-07/F-08/F-09/D-19/D-20/D-14)
- [ ] `npm run build` still exits 0 (no MDX/markdown parse errors)
- [ ] No stale Phase 4 "Pending"/"Planned"/"0/5" strings remain across all 4 trackers
- [ ] No stale "Phase 7 (future)" reference in CLAUDE.md
- [ ] No stale "Virgil-only text" Gotcha 1 in runbook
- [ ] All 6 file-edit atomic commits recommended (single plan commit OR separate commits per file — orchestrator decides)
</success_criteria>

<output>
Create `.planning/phases/04.1-excalidraw-pipeline-hardening/04.1-04-SUMMARY.md`. Include:
- `requirements-covered: [DIAG-01, DIAG-02, DIAG-03, DIAG-04, DIAG-05]` (docs-traceability updated)
- `docs-findings-closed: [F-01, F-02, F-03, F-04, F-05, F-06, F-07, F-08, F-13, F-14, F-18, F-20, F-09]` (13 findings via 6-file sweep)
- Byte-count delta per file (REQUIREMENTS ~same, ROADMAP ~+800 B for Phase 04.1 plan bullets, STATE ~+500 B for Completed Phases, PROJECT ~+1200 B for Phase 4 paragraph, CLAUDE.md ~+600 B for new section, runbook ~+1800 B for Prereq + Determinism + Gotcha rewrite)
- Known drift remaining: F-10 (runbook 04-RESEARCH.md existence — verified during execution; if file doesn't exist, note in SUMMARY), F-11 (visuals-routing.md backtick — folded into separate D-25 task if planned), F-12/F-16/F-17 (inherited from prior phase — not Phase 4 authorship)
</output>
</content>
