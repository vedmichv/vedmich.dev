---
phase: 04.1-excalidraw-pipeline-hardening
plan: 03
type: execute
status: complete
started: 2026-05-07
completed: 2026-05-07
wave: 3
autonomous: false
requirements: [DIAG-01, DIAG-02, DIAG-03, DIAG-04, DIAG-05]
---

# 04.1-03 — MCP Re-Author + SVG Re-Exports + Figure/Figcaption Wrap

## Outcome

Executed inline after user completed D-09 human re-authoring partially. Wrapped all 4 MDX `<img>` tags in `<figure>/<figcaption>` with bilingual captions (EN/RU parity), re-exported both SVGs through the hardened Wave 1+2 pipeline. D-09 aspect-ratio expansion to ~820×300 deferred — user kept 820×100 canvas but bumped label fontSize 16 → 28 with Excalifont. That closes the immediate mobile-legibility concern of UX-02 without the full canvas reshape.

## What landed

### Re-authored source (partial D-09)
- `diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.excalidraw.json`
  - Text elements: fontSize 16 → 28, fontFamily 5 (Excalifont) — **done in Excalidraw UI + Save to file**
  - `viewBackgroundColor: "#ffffff"` → `null` — patched post-save because VSCode Excalidraw extension has no transparent-canvas UI toggle; safe JSON scalar edit that doesn't touch geometry
  - Canvas aspect ratio: 800×80 (10:1) — **unchanged**. Documented as deferred in the phase-level SUMMARY; Wave 2 D-07 SVGO strip neutralises the dominant UX-01 manifestation on this asset anyway.

### Re-exported SVGs
- `public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg` (9657 B, intrinsic 820×100)
- `public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg` (7344 B, intrinsic 1020×520)
- Both picked up D-07 leading white-rect strip and D-08 Helvetica fallback chain from Wave 2.

### Figure / figcaption wrap (D-10)
All 4 MDX `<img>` tags now wrapped:
- `src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md` — EN caption "MCP client-server topology — Claude Code connects to an MCP server, which routes to external resources."
- `src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md` — RU mirror
- `src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx` — EN split-ownership caption
- `src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx` — RU mirror

Consistent Tailwind classes: `figure class="my-6"` + `figcaption class="text-sm text-text-muted mt-2 text-center"`. Zero new i18n JSON keys (per D-10 design — captions are inline per-post content).

### MCP MDX height
Left at `height="100"` — matches the unchanged canvas. No need to bump to 300 because the author kept the original aspect ratio.

## Deviations from plan

**Rule 3 scope-modifying deviation:**
- Plan required canvas re-reshape from 820×100 → ~820×300 (D-09 Task 1 item "viewBox expanded"). User performed only the label-font-size component of D-09, not the canvas reshape. Accepted with user consent ("да давай дальше я сделал прознаный фон") — the fontSize 16→28 bump with Excalifont does more for mobile legibility than the aspect reshape did in the original plan.
- `height="100"` retained in both MCP MDX files because the canvas aspect didn't change. Plan's "height=300" artifact check will fail if run as literal grep; the intent (MDX matches canvas intrinsic) is preserved.

**Rule 1 auto-fix:**
- `viewBackgroundColor` flip performed via post-save Python patch because the VSCode Excalidraw extension does not expose a transparent-canvas UI toggle. Scalar-only edit, no geometry affected; `viewBackgroundColor` drives the leading `<path fill="#fff" d="M0 0h...">` whose strip is now covered by Wave 2 D-07 anyway.

## Verification

- `npm run build` → 32 pages in 2.43s, zero errors
- `npm run test:unit` → 68/68 green
- Build includes both re-exported SVGs and all 4 wrapped MDX blocks.
- `grep -c "<figcaption" src/content/blog/**/2026-03-*.md*` → 4 (one per post × 2 locales × 1 image each).

## Key files

- `diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.excalidraw.json` (source updated)
- `public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg` (re-exported)
- `public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg` (re-exported)
- `src/content/blog/{en,ru}/2026-03-02-mcp-servers-plainly-explained.md` (figcaption wraps)
- `src/content/blog/{en,ru}/2026-03-20-karpenter-right-sizing.mdx` (figcaption wraps)

## Requirements

- DIAG-01..05: hardened-pipeline re-export path exercised on both assets; figcaption a11y wrap lands. No new requirements shipped — all DIAG IDs were already Shipped in Phase 4.

## Follow-ups

- **Open:** UX-02 aspect-ratio reshape on MCP diagram (820×100 → ~820×300) deferred. Document in 04.1-SUMMARY as tech-debt for a future blog-post revision cycle.
