---
phase: 04.1-excalidraw-pipeline-hardening
plan: 02
subsystem: ux
tags: [ux, excalidraw, svgo, mobile, fonts, windows-fallback]

# Dependency graph
requires:
  - phase: 04.1-excalidraw-pipeline-hardening
    plan: 01
    provides: "Hardened scripts/excalidraw-to-svg.mjs baseline (security: realpath walk, removeScripts + removeAttributesBySelector event-handler strip, dataURL scheme whitelist, parsererror detection); tests/unit/excalidraw-to-svg.test.ts at 17 tests; TESTS_TMP + tmpDest helpers; tests/tmp/ gitignored scratch dir"
provides:
  - "scripts/excalidraw-to-svg.mjs — second removeAttributesBySelector with narrow selector `path[fill=\"#fff\"][d^=\"M0 0h\"]` that strips fill from the leading Excalidraw viewBackgroundColor canvas rect (D-07 / UX-01)"
  - "scripts/excalidraw-to-svg.mjs — post-SVGO string replace `/font-family=\"Helvetica(, Segoe UI Emoji)?\"/g` → `font-family=\"Helvetica, Arial, sans-serif\"` applied BEFORE 10 KB SIZE_BUDGET check and BEFORE fs.writeFile (D-08 / UX-04)"
  - "tests/fixtures/excalidraw/white-rect-plus-shape.excalidraw.json + meta sidecar — narrow-selector test fixture (viewBackgroundColor #fff + interior white rect at x=120,y=50)"
  - "tests/unit/excalidraw-to-svg.test.ts — 3 new UX regression tests (strips-leading-white-rect / preserves-interior-white-shapes / helvetica-fallback-chain), total now 20 test blocks (55 across project unit suite)"
affects: [04.1-03-svg-reexports, 04.1-04-ux-errors, 04.1-05-edge-cases-unicode, 04.1-06-docs-closeout]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "SVGO v4 narrow attribute-strip — removeAttributesBySelector with CSS4-style compound selector `path[fill=\"#fff\"][d^=\"M0 0h\"]` matches only the leading Excalidraw canvas rect without over-matching interior white-filled shapes whose d-string starts with non-M0-0h coordinates."
    - "Post-SVGO string replace between optimize() and SIZE_BUDGET gate — captures both the standalone `Helvetica` variant (D-08 intent) AND the Excalidraw-default `Helvetica, Segoe UI Emoji` variant via the same regex capture group `(, Segoe UI Emoji)?`. Keeps the pre-SVGO → SVGO → post-SVGO phasing explicit."
    - "Fill-strip vs element-delete for SVGO pruning — when SVGO v4 offers removeAttributesBySelector but no removeElementsBySelector primitive, attribute-strip + multipass is the low-risk equivalent: the fill-less <path> is either pruned on subsequent pass or survives invisible on the dark prose (same visual result)."
    - "Deterministic fixture pair — excalidraw.json with hardcoded seeds (401/402) + versionNonces (4001/4002) + meta sidecar with title + descEn gives byte-reproducible SVG output across runs."

key-files:
  created:
    - "tests/fixtures/excalidraw/white-rect-plus-shape.excalidraw.json — 2-element fixture (teal-stroke rect at 10,10 + interior white rect at 120,50) with viewBackgroundColor:#ffffff"
    - "tests/fixtures/excalidraw/white-rect-plus-shape.meta.json — title + descEn documenting fixture intent"
    - ".planning/phases/04.1-excalidraw-pipeline-hardening/04.1-02-SUMMARY.md"
  modified:
    - "scripts/excalidraw-to-svg.mjs — +30 −3 LOC (D-07 SVGO selector addition + D-07 documentary comment block + D-08 post-SVGO replace + withFallback rename threaded through SIZE_BUDGET check and fs.writeFile)"
    - "tests/unit/excalidraw-to-svg.test.ts — +116 LOC (WHITE_RECT_PLUS_SRC constant + 3 new UX regression tests; no modifications to existing 17 tests)"

key-decisions:
  - "Phase 04.1 Plan 02 (2026-05-07): D-07 landed — SVGO_CONFIG.plugins gains a SECOND removeAttributesBySelector with narrow selector `path[fill=\"#fff\"][d^=\"M0 0h\"]` strips the `fill` attribute from the leading Excalidraw viewBackgroundColor canvas rect. Narrow selector proven safe on the white-rect-plus-shape fixture — interior rect with fill=#fff at x=120,y=50 survives (its d-string is `M127.5 50c...`, does NOT start with `M0 0h`). Remediates UX-01 bright-slab regression on Deep Signal dark prose. Retroactively applies to 04.1-03 re-exports of the 2 shipped SVGs."
  - "Phase 04.1 Plan 02 (2026-05-07): D-08 landed — post-SVGO string replace `/font-family=\"Helvetica(, Segoe UI Emoji)?\"/g` → `font-family=\"Helvetica, Arial, sans-serif\"` applied between optimize() and SIZE_BUDGET check. Regex captures BOTH variants in a single pass: standalone `Helvetica` (D-08 intent) AND Excalidraw-default `Helvetica, Segoe UI Emoji` (karpenter reality). Virgil-embedded SVGs (fontFamily=1) unaffected. Windows readers now get Arial metrics instead of platform-default drift that overflowed Excalidraw's pre-computed label boxes. Remediates UX-04."
  - "Phase 04.1 Plan 02 (2026-05-07): Fill-strip vs element-delete tradeoff — SVGO v4 removeAttributesBySelector is the built-in primitive for selector-based attribute modification; SVGO v4 has NO built-in removeElementsBySelector. A custom visitor plugin would be higher risk. Attribute-strip in multipass mode either (a) triggers preset-default's useless-defs pruning on the next pass, or (b) leaves a fill-less path that renders invisible on the Deep Signal dark prose — same visual result as deletion. Empirically on the minimal fixture, the path SURVIVES without fill (verified in the smoke run: grep 'M0 0h' = 1 match but grep 'fill=\"#fff\"' = 0 matches in the M0 0h context). Confirmed safe."
  - "Phase 04.1 Plan 02 (2026-05-07): withFallback is the single source of truth for both SIZE_BUDGET byte-length check and fs.writeFile — not `optimized`. The rename threaded through 3 call sites (declaration + Buffer.byteLength + fs.writeFile). grep -c 'withFallback' scripts/excalidraw-to-svg.mjs = 3 exactly."
  - "Phase 04.1 Plan 02 (2026-05-07): Minimal fixture byte delta — pipeline output shrank 5087 B → 5075 B (−12 B) from the stripped `fill=\"#fff\"` attribute on the leading canvas path. The 10 KB SIZE_BUDGET has ample headroom. Karpenter re-export in 04.1-03 is projected at ~7,338 + ~144 B (6 text elements × +24 B for the added fallback chain characters) = ~7,500 B, still well under budget."

requirements-covered: [DIAG-01, DIAG-02]
ux-findings-mitigated: [UX-01, UX-04]

# Metrics
duration: 7m
completed: 2026-05-07
---

# Phase 04.1 Plan 02: White-rect strip + Helvetica fallback chain summary

**Shipped the two pipeline-layer UX fixes (D-07 + D-08) that retroactively apply to all Excalidraw SVGs — leading `<path fill="#fff" d="M0 0h..."/>` full-canvas rect stripped (no more bright slab on dark prose) and every `<text font-family="Helvetica"|"Helvetica, Segoe UI Emoji">` rewritten to the Windows-safe fallback chain `Helvetica, Arial, sans-serif`. 3 UX regression tests pin both fixes.**

## Performance

- **Duration:** 7 min (415 s)
- **Started:** 2026-05-07T10:36:08Z
- **Completed:** 2026-05-07T10:43:03Z
- **Tasks:** 3
- **Files modified:** 5 (3 created + 2 modified)

## Accomplishments

- **UX-01 mitigated (D-07)** — `SVGO_CONFIG.plugins` gains a second `removeAttributesBySelector` with narrow selector `path[fill="#fff"][d^="M0 0h"]`. Strips the leading Excalidraw `viewBackgroundColor: "#ffffff"` canvas rect (the "bright slab" on Deep Signal dark prose). Smoke-tested: minimal fixture output shrinks 5087 → 5075 B; white-rect-plus-shape fixture output contains `<path d="M0 0h190v90H0z"/>` (no fill, invisible on dark prose) AND `<path fill="#fff" d="M127.5 50c..."/>` (interior rect at x=120,y=50 keeps its white fill — narrow selector did not over-match).
- **UX-04 mitigated (D-08)** — Post-SVGO `String.prototype.replace` with `/font-family="Helvetica(, Segoe UI Emoji)?"/g` → `font-family="Helvetica, Arial, sans-serif"` applied between `optimize()` and the 10 KB `SIZE_BUDGET` byte-length gate. Captures both the standalone `Helvetica` variant AND the Excalidraw-default `Helvetica, Segoe UI Emoji` variant in a single regex. Verified on synthetic Helvetica-text fixture: output's only `font-family=` occurrence is the full fallback chain; no leftover standalone `Helvetica` or `Helvetica, Segoe UI Emoji` survives.
- **`withFallback` threading** — renamed local threaded through 3 call sites (declaration, `Buffer.byteLength(withFallback, 'utf8')`, and `await fs.writeFile(destPath, withFallback, 'utf8')`). Ensures the post-SVGO byte-count reflects the fallback-injected size and what ships to disk matches what got size-checked.
- **3 new UX regression tests** — `strips-leading-white-rect`, `preserves-interior-white-shapes`, `helvetica-fallback-chain`. All three use try/finally cleanup. The Helvetica test synthesizes its source JSON + meta sidecar in `tests/tmp/` (stays inside `ALLOWED_WRITE_ROOTS` per Plan 01's tightening) and cleans all three files on exit. Test block count 17 → 20 (project-wide unit test count 52 → 55).
- **Build green** — `npm run build` still completes in 2.39 s with 32 pages, zero errors. Unit suite 55/55 pass.
- **Zero regressions** — all 17 tests from Plan 01 (DIAG-01/02/03 coverage + 8 Phase 04.1 security regressions) continue to pass exactly as before. No changes to existing test bodies, helper functions, or fixture files.

## Task Commits

Each task was committed atomically:

1. **Task 1: White-rect-plus-shape fixture + meta sidecar** — `7b3d961` (test)
2. **Task 2: scripts/excalidraw-to-svg.mjs — D-07 SVGO selector + D-08 Helvetica replace** — `2f6c152` (feat)
3. **Task 3: 3 UX regression tests (UX-01 strip + preserve + UX-04 Helvetica fallback)** — `10f1ffb` (test)

_TDD gate sequence for the plan as a whole: Task 1 test-data (RED-enabling fixture) → Task 2 implementation (GREEN — pipeline fixes) → Task 3 test coverage (GREEN — regression pins). The new UX tests would FAIL on pre-Task-2 HEAD (no selector, no replace) and PASS only after the implementation lands. Task 1 is labeled `test(...)` because the fixture pair is test data — consumed only by Task 3's tests and by the end-to-end smoke checks._

## Files Created/Modified

**Created (3):**
- `tests/fixtures/excalidraw/white-rect-plus-shape.excalidraw.json` — 2-element fixture: (a) teal-stroke rect at x=10,y=10 / 80×40 for legitimate visible content; (b) interior rectangle with `backgroundColor: "#ffffff"` at x=120,y=50 / 60×30. `viewBackgroundColor: "#ffffff"` so the wrapper emits the leading `M0 0h...` white rect that D-07 must strip. Deterministic seeds (401, 402) + versionNonces (4001, 4002) matching the minimal fixture convention.
- `tests/fixtures/excalidraw/white-rect-plus-shape.meta.json` — title `"White-rect narrow-selector test"` + descEn documenting narrow-selector intent.
- `.planning/phases/04.1-excalidraw-pipeline-hardening/04.1-02-SUMMARY.md` (this file)

**Modified (2):**
- `scripts/excalidraw-to-svg.mjs` — 2 edits: (a) `SVGO_CONFIG.plugins` array gains a fourth entry (second `removeAttributesBySelector` block with narrow white-rect selector) AFTER the event-handler block from Plan 01; (b) post-SVGO Helvetica fallback replace added between `optimize()` and the 10 KB `SIZE_BUDGET` gate, with `withFallback` threading through the `Buffer.byteLength` + `fs.writeFile` call sites. Net: +30 / −3 LOC (230 → 258 LOC).
- `tests/unit/excalidraw-to-svg.test.ts` — 2 additions: (a) `WHITE_RECT_PLUS_SRC` constant added to the fixture-path block at the top of the file (1 line); (b) 3 new `test(...)` blocks appended at the end of the file. Net: +116 LOC (379 → 495 LOC). No modifications to existing constants, helpers, or tests.

## Decisions Made

All 4 plan decisions landed as specified. See `key-decisions` frontmatter for per-decision rationale.

Additional execution-time notes (not scope changes):

- **Minimal fixture byte delta verified** — pipeline output shrank 5087 → 5075 B (−12 B) from the stripped `fill="#fff"` attribute on the leading canvas path. Confirmed live pre-commit. Puts the karpenter re-export projection at ~7,338 B (pre-fix) + ~144 B (6 text × +24 B) − 12 B (stripped fill) ≈ 7,470 B for the hardened output, with 10,240 − 7,470 ≈ 2,770 B of headroom.
- **End-to-end white-rect smoke on the new fixture verified** — pre-commit run through the hardened pipeline produced a 1876 B SVG where `grep 'fill="#fff"'` matches exactly ONCE (the interior rect at x=120,y=50, as intended) and `grep -E 'fill="#fff"[^>]*M0 0h|M0 0h[^>]*fill="#fff"'` matches ZERO times (the leading canvas rect lost its fill as intended).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 — Bookkeeping] Plan acceptance grep counts — documentary comments inflate hit counts**
- **Found during:** Task 2 verification (`grep -c 'path\[fill="#fff"\]\[d\^="M0 0h"\]' scripts/excalidraw-to-svg.mjs` returned 2; plan said expect 1. Similarly `grep -c "removeAttributesBySelector"` returned 3; plan said expect 2.)
- **Issue:** The plan's acceptance grep counts were written assuming zero documentary comments would reference the selector / pattern by name. The actual implementation added a D-07 block comment above the SVGO_CONFIG definition that quotes the selector verbatim in the comment body for readability — `// Narrow selector path[fill="#fff"][d^="M0 0h"] matches only the full-canvas...`. Plan 01 had already added a comment that mentions `removeAttributesBySelector` in the existing block comment. So each grep counts both the documentary comment mention AND the actual code reference.
- **Resolution:** NOT a code bug. The selector IS present exactly where it needs to be (SVGO_CONFIG.plugins array, narrow, operating on `fill` attribute) and the semantic behavior is correct (smoke-tested, regression-tested). The plan's grep-count expectations were approximations; the literal counts are 2 and 3 instead of 1 and 2 because of documentary comments. NOT treated as a bug to fix — modifying the comments to "de-quote" the selector would degrade readability. Deviation tracked here for audit trail; the semantic acceptance criterion ("narrow selector for white rect is present") holds.
- **Files affected:** none (no code change needed)
- **Verification:** All other Task 2 criteria pass exactly as specified: `grep -c "Helvetica, Arial, sans-serif"` = 1, `grep -c "withFallback"` = 3, `grep -c "D-07"` = 2, `grep -c "D-08"` = 2, `npm run test:unit` = 55/55 green, end-to-end pipeline on `minimal.excalidraw.json` + `white-rect-plus-shape.excalidraw.json` both exit 0 with the expected byte-level output shape.

---

**Total deviations:** 1 auto-fixed (1 Rule 1 bookkeeping — plan acceptance grep-count approximation, not a code bug).
**Impact on plan:** none. Same 3 atomic commits, same test coverage delta, same byte-level behavior.

## Issues Encountered

- **`node_modules/` missing in fresh worktree** — resolved by running `npm install --prefer-offline --no-audit --no-fund` at the start of execution. All dependencies (svgo@4.0.0, `@aldinokemal2104/excalidraw-to-svg@1.1.1`) installed without issue.

## User Setup Required

None — no external service configuration required. All changes are self-contained within `scripts/`, `tests/fixtures/excalidraw/`, and `tests/unit/`.

## Next Phase Readiness

- **Plan 04.1-03 (SVG re-exports):** can proceed — re-running the hardened pipeline on the 2 shipped `.excalidraw.json` sources (MCP post client-server + karpenter split-ownership) now retroactively applies the D-07 + D-08 UX fixes:
  - MCP post: leading white-rect strip lands (was `<path fill="#fff" d="M0 0h820v100H0z"/>`; now the path survives without fill, invisible on dark prose). MCP uses Virgil (fontFamily=1), so D-08 has no effect on MCP.
  - Karpenter post: both D-07 strip (was `<path fill="#fff" d="M0 0h1020v520H0z"/>`) and D-08 Helvetica → fallback chain land. Projected output ≤ 7,500 B (well under 10 KB budget per plan's projection).
- **Plan 04.1-04 (UX errors):** can proceed — no dependency on this plan's changes.
- **Plan 04.1-05 (edge cases + Unicode):** can proceed — the new UX tests use `tests/tmp/` so any future synthetic-source tests can follow the same pattern.
- **Plan 04.1-06 (docs closeout):** can proceed — UX-01 and UX-04 findings can be checked off in `reviews/CONSOLIDATED.md`. `diagrams-source/README.md` contributor runbook should mention the Helvetica fallback chain as a shipped-SVG property (next plan or closeout).

No blockers.

## Self-Check

Pre-commit verification of claimed artifacts and commits:

Files created (expected 3):
- `tests/fixtures/excalidraw/white-rect-plus-shape.excalidraw.json` — FOUND
- `tests/fixtures/excalidraw/white-rect-plus-shape.meta.json` — FOUND
- `.planning/phases/04.1-excalidraw-pipeline-hardening/04.1-02-SUMMARY.md` — WRITING NOW

Commits claimed:
- `7b3d961` — to be verified after self-check runs
- `2f6c152` — to be verified after self-check runs
- `10f1ffb` — to be verified after self-check runs

Modified file markers:
- `scripts/excalidraw-to-svg.mjs` contains `path[fill="#fff"][d^="M0 0h"]` — to be verified
- `scripts/excalidraw-to-svg.mjs` contains `Helvetica, Arial, sans-serif` — to be verified
- `scripts/excalidraw-to-svg.mjs` contains `withFallback` (≥3 occurrences) — to be verified
- `tests/unit/excalidraw-to-svg.test.ts` contains `strips-leading-white-rect` — to be verified
- `tests/unit/excalidraw-to-svg.test.ts` contains `preserves-interior-white-shapes` — to be verified
- `tests/unit/excalidraw-to-svg.test.ts` contains `helvetica-fallback-chain` — to be verified
- `tests/unit/excalidraw-to-svg.test.ts` contains `WHITE_RECT_PLUS_SRC` — to be verified

(Self-check status result appended below after Write completes.)

## Self-Check: PASSED

All claimed files exist, all 3 commits are in `git log`, all claimed grep markers are present in the modified source files, and `npm run test:unit` reports 55/55 pass with all 3 new UX tests by name. `npm run build` completes green (32 pages, 2.39 s). End-to-end smoke on the white-rect-plus-shape fixture produces an SVG with ZERO `fill="#fff"` + `M0 0h` combinations (D-07 works) and ONE `fill="#fff"` match in the interior-rect position (narrow selector does not over-match).

---

*Phase: 04.1-excalidraw-pipeline-hardening*
*Completed: 2026-05-07*
