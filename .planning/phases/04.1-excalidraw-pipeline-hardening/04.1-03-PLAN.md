---
phase: 04.1-excalidraw-pipeline-hardening
plan: 03
type: execute
wave: 3
depends_on: ["04.1-01", "04.1-02"]
files_modified:
  - diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.excalidraw.json
  - public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg
  - public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg
  - src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md
  - src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md
  - src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx
  - src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx
autonomous: false
requirements: [DIAG-01, DIAG-02, DIAG-03, DIAG-04, DIAG-05]
tags: [ux, mdx, a11y, figcaption, excalidraw, bilingual]
must_haves:
  truths:
    - "MCP diagram re-authored in Excalidraw UI: every label font-size bumped to 28-32 px; viewBox expanded from 820x100 to 820x300 (~2.7:1 aspect); viewBackgroundColor set to null for transparent canvas"
    - "MCP client-server.svg re-exported via hardened pipeline — picks up D-07 white-rect strip + D-08 Helvetica fallback (if any Helvetica text) and stays under 10 KB SIZE_BUDGET"
    - "Karpenter split-ownership.svg re-exported via hardened pipeline — picks up D-07 white-rect strip + D-08 Helvetica fallback chain on its 6 <text> elements"
    - "All 4 MDX <img> tags (MCP EN, MCP RU, karpenter EN, karpenter RU) wrapped in <figure class='my-6'>...<figcaption class='text-sm text-text-muted mt-2 text-center'>...</figcaption></figure>"
    - "MCP MDX width/height updated from 820x100 to new 820x300 viewBox (EN + RU parity)"
    - "Karpenter MDX width/height (1020x520) unchanged — re-export preserves the authored viewBox"
    - "EN captions in English prose; RU captions in Russian prose (bilingual parity — per-post inline content, no new i18n JSON keys)"
  artifacts:
    - path: "diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.excalidraw.json"
      provides: "Re-authored MCP source with larger labels + transparent bg + 820x300 viewBox"
      contains: "viewBackgroundColor"
    - path: "public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg"
      provides: "Re-exported MCP SVG (≤ 10 KB, no leading white rect, larger labels for mobile legibility)"
    - path: "public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg"
      provides: "Re-exported karpenter SVG (≤ 10 KB, no leading white rect, Helvetica → full fallback chain)"
    - path: "src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md"
      provides: "MCP EN: <img> wrapped in figure/figcaption with English caption + updated height=300"
      contains: "<figure"
      contains: "<figcaption"
      contains: "height=\"300\""
    - path: "src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md"
      provides: "MCP RU: mirror of EN with Russian caption + updated height=300"
      contains: "<figure"
      contains: "<figcaption"
      contains: "height=\"300\""
    - path: "src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx"
      provides: "Karpenter EN: <img> wrapped in figure/figcaption with English caption (width/height unchanged)"
      contains: "<figure"
      contains: "<figcaption"
    - path: "src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx"
      provides: "Karpenter RU: mirror of EN with Russian caption"
      contains: "<figure"
      contains: "<figcaption"
  key_links:
    - from: "MCP MDX (EN + RU)"
      to: "public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg"
      via: "<img src=...> inside <figure>"
      pattern: "/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg"
    - from: "Karpenter MDX (EN + RU)"
      to: "public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg"
      via: "<img src=...> inside <figure>"
      pattern: "/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg"
---

<objective>
Land the two visual/UX fixes that require re-authoring or re-exporting: D-09 (human re-authors MCP `.excalidraw.json` in Excalidraw UI so labels stay legible at 0.418x mobile scale and the aspect ratio reads as "intentional illustration"), D-10 (agent wraps all 4 shipped MDX `<img>` tags in `<figure><figcaption>` with bilingual captions — per-post inline content, zero new i18n keys). Both SVGs are re-exported via the hardened pipeline (04.1-01 + 04.1-02) which retroactively picks up the white-rect strip + Helvetica fallback chain.

Purpose: Close the two remaining UX blockers (UX-02 mobile legibility, UX-03 banner aspect) and the UX-07 warning (no `<figure>`/`<figcaption>` anchoring). Bring diagrams into parity with professional technical blog conventions (AWS Architecture blog, Martin Fowler bliki — all use figure+figcaption consistently).

Output: 1 re-authored `.excalidraw.json` (human-in-the-loop), 2 re-exported SVGs, 4 MDX file edits (figure wrap + caption in both locales for both posts), autonomous=false because the Excalidraw UI re-author is human-only.

Per D-09b: karpenter diagram is NOT re-authored. Its 1020×520 aspect (1.96:1) reads correctly; 16 px labels scale to 5.4 px on mobile below legibility floor — accepted for v1.0 with figcaption serving as mobile text equivalent. Documented as deferred in CONTEXT.md §Deferred Ideas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-excalidraw-pipeline-hardening/04.1-CONTEXT.md
@.planning/phases/04.1-excalidraw-pipeline-hardening/04.1-PATTERNS.md
@.planning/phases/04-excalidraw-pipeline/reviews/UX-REVIEW.md
@src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md
@src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md
@src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx
@src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx
@src/components/visuals/VvStage.astro
@diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.excalidraw.json

<interfaces>
Shipped MDX `<img>` shape (current HEAD, all 4 files follow this pattern):

MCP EN (`src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md` lines 20-27):
```html
<img
  src="/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg"
  alt="MCP client-server architecture: Claude Code connects to an MCP Server, which fans out to external resources (docs, databases, APIs)"
  loading="eager"
  width="820"
  height="100"
  style="max-width: 100%; height: auto;"
/>
```

MCP RU (`src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md` lines 20-27): same shape, Russian `alt`.

Karpenter EN (`src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx` lines 57-63):
```jsx
<img
  src="/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg"
  alt="Split ownership: Cluster Autoscaler owns system node groups (kube-system, logging, monitoring) on predictable on-demand nodes; Karpenter owns the stateless fleet on dynamically-provisioned nodes with matching taints and tolerations"
  loading="lazy"
  width="1020" height="520"
  style="max-width: 100%; height: auto;"
/>
```

Karpenter RU (`src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx` lines 55-61): same shape, Russian `alt`.

VvStage figcaption analog pattern (`src/components/visuals/VvStage.astro`):
```astro
<figure class="vv-stage not-prose" aria-label={title}>
  ...
  {caption && <figcaption class="vv-stage-caption">{caption}</figcaption>}
</figure>
```
Deep Signal token used: `color: var(--text-muted)` mapped to `text-text-muted` Tailwind utility.

**Per D-10 and CONTEXT.md Claude's Discretion line 148:** do NOT use `not-prose` — the diagrams live inside `.prose` scope and the figcaption should benefit from typography rhythm. Use inline Tailwind utilities `text-sm text-text-muted mt-2 text-center` — these tokens already exist in the Tailwind `@theme` shim (global.css:49). The `text-text-muted` utility maps to `--text-secondary` = `#94A3B8` — 5.3:1 AA contrast on the `#0F172A` dark background.

**@tailwindcss/typography interaction:** v0.5.19 is plugged via `@plugin "@tailwindcss/typography"` in global.css:5. It ships default `.prose figure`/`.prose figcaption` styles (margin + italic muted color). The inline `text-text-muted text-sm` utilities override typography's defaults — verify visually during the checkpoint. If a collision surfaces, fall back to a `not-prose` wrapper but prefer the inline-utility path first.

**Caption content guidelines (planner's discretion per CONTEXT.md line 144):** 1-2 sentences, neutral technical prose, English + Russian pairs. Captions below are the planner's proposed drafts — user approves at the human checkpoint.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1 [HUMAN]: Re-author diagrams-source/.../client-server.excalidraw.json in Excalidraw UI (D-09)</name>
  <what-built>
    Nothing yet — this is the human-only step. The agent cannot open Excalidraw UI to re-author a diagram.
    This checkpoint halts agent execution and prompts Viktor to open the MCP source file in Excalidraw (web or desktop),
    bump every label font-size to 28-32 px, expand the viewBox to 820×300, set viewBackgroundColor to null (transparent),
    and save. After save, Viktor resumes the agent and Task 2 regenerates the SVG via the hardened pipeline.
  </what-built>
  <how-to-verify>
    Viktor performs the following steps IN EXCALIDRAW:

    1. Open `diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.excalidraw.json`
       - Web: https://excalidraw.com → File menu → Open → select the file
       - Desktop: Open the file directly in the Excalidraw desktop app
    2. **Bump label font-size.** For every text element in the diagram (expect 3 labels: "Claude Code", "MCP Server", "docs / API" — names may vary), click the text, change font-size from 16 to **28** (smaller ratio) or **32** (larger, preferred). Target: labels stay legible at 0.418x mobile scale (28×0.418 = 11.7 px, above the 9 px legibility floor per UX-02).
    3. **Expand viewBox from 820×100 to 820×300** (~2.7:1 aspect ratio vs current 8.2:1 banner). Drag to resize the canvas OR in the Excalidraw UI use the aspect-ratio controls. The 3 boxes should re-center vertically with breathing room above and below — reads as "intentional illustration," not "separator bar" (UX-03 fix).
    4. **Set transparent canvas.** Press `Menu` (left sidebar, 3-dot icon) → `Export` → toggle "Export with background" OFF. OR edit the appState directly in the JSON after save: `"viewBackgroundColor": null`.
    5. Save: File menu → Save to... → overwrite `diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.excalidraw.json`.
    6. Open the JSON in a text editor and confirm:
       - `"viewBackgroundColor"` is `null` (not `"#ffffff"`)
       - Every text element's `fontSize` is 28 or 32
       - (The viewBox/aspect will be reflected in Task 2's SVG re-export output, not directly in the JSON — the JSON stores element coordinates and the aspect is derived at export time)
    7. Save file to disk. No git commit yet — Task 2 will run the pipeline and Task 3 commits atomically.
    8. Return to the agent conversation and type "done" OR describe issues.

    **Note per CONTEXT.md §Claude's Discretion line 144 + D-09:** karpenter diagram is explicitly NOT re-authored. Only the MCP diagram is re-authored in this task.
  </how-to-verify>
  <resume-signal>Type "done" after saving the re-authored JSON. If the labels can't be bumped (Excalidraw UI constraint) or the aspect change is visually bad, describe the issue and we'll iterate.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Re-export both SVGs via hardened pipeline — MCP + karpenter</name>
  <files>
    public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg,
    public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg
  </files>
  <read_first>
    - scripts/excalidraw-to-svg.mjs (state after 04.1-01 + 04.1-02 — full hardened pipeline: realpath validatePath, SVGO removeScripts + removeAttributesBySelector for events AND white-rect, dataURL whitelist, parsererror detection, Helvetica fallback chain)
    - diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.excalidraw.json (post-Task-1 re-author)
    - diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.excalidraw.json (unchanged — re-exported as-is)
    - diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.meta.json (unchanged)
    - diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.meta.json (unchanged)
  </read_first>
  <behavior>
    - Runs pipeline TWICE — once for MCP source → MCP SVG, once for karpenter source → karpenter SVG. Each exits 0.
    - MCP SVG: size ≤ 10 KB. Leading `<path fill="#fff" d="M0 0h820v300H0z"/>` from Excalidraw's white bg becomes stripped (the viewBackgroundColor=null from Task 1 should eliminate it at the source, but D-07 SVGO strip is belt-and-suspenders). Label font-size rendered at 28-32 px (visible in raw SVG `font-size="28"` or similar). viewBox now `0 0 820 300`.
    - Karpenter SVG: size ≤ 10 KB (expected ≤ 7,500 B after Helvetica fallback chain adds ~144 B). Leading `<path fill="#fff" d="M0 0h1020v520H0z"/>` stripped by D-07. All 6 `<text>` elements have `font-family="Helvetica, Arial, sans-serif"` (not `Helvetica, Segoe UI Emoji`).
    - Both SVGs retain their `<title>` and `<desc>` tags (meta sidecars unchanged, D-03 SVGO `removeDesc: false` override still in effect).
    - Both SVGs contain zero `<script`, zero `onload`/`onclick`, zero external `<image href>` (pipeline security hardenings enforce this).
  </behavior>
  <action>
    From the repo root, run these two commands (one per diagram) and capture stdout for the SUMMARY:

    ```bash
    node scripts/excalidraw-to-svg.mjs \
      diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.excalidraw.json \
      public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg

    node scripts/excalidraw-to-svg.mjs \
      diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.excalidraw.json \
      public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg
    ```

    Expected MCP stdout format (values will differ post-re-author):
    ```
    ✓ public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg (NNNN B)
      intrinsic: 820x300
      title: MCP client-server architecture
      desc: Claude Code (client) connects via JSON-RPC to an MCP server, which fans out to external resources: docs, databases, APIs.
    ```

    Expected karpenter stdout format:
    ```
    ✓ public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg (NNNN B)
      intrinsic: 1020x520
      title: <karpenter title from meta>
      desc: <karpenter descEn from meta>
    ```

    If either command exits non-zero:
    - **MCP failure** — likely Task 1 re-author didn't save correctly. Re-open the file in Excalidraw and re-verify JSON shape.
    - **Karpenter failure** — this is concerning because karpenter source is unchanged. Capture stderr and report. Likely root cause: a hardening in 04.1-01/02 introduced a regression. Revert if needed.

    **Do NOT hand-edit** either SVG. The pipeline run is the single source of truth.

    After both runs succeed, capture the new intrinsic dimensions for MCP (expected 820×300) for Task 3 to use in the `<img width height>` update.
  </action>
  <verify>
    <automated>node scripts/excalidraw-to-svg.mjs diagrams-source/2026-03-02-mcp-servers-plainly-explained/client-server.excalidraw.json public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg && node scripts/excalidraw-to-svg.mjs diagrams-source/2026-03-20-karpenter-right-sizing/split-ownership.excalidraw.json public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg && test $(wc -c < public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg) -lt 10240 && test $(wc -c < public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg) -lt 10240 && ! grep -q 'fill="#fff".*M0 0h' public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg && ! grep -q 'fill="#fff".*M0 0h' public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg && ! grep -q 'font-family="Helvetica, Segoe UI Emoji"' public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg</automated>
  </verify>
  <acceptance_criteria>
    - `wc -c public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg` returns value < 10240
    - `wc -c public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg` returns value < 10240
    - `grep -c 'fill="#fff".*M0 0h' public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg` returns 0
    - `grep -c 'fill="#fff".*M0 0h' public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg` returns 0
    - `grep -c 'font-family="Helvetica, Arial, sans-serif"' public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg` returns ≥ 6 (all 6 text elements get the fallback chain)
    - `grep -c 'font-family="Helvetica, Segoe UI Emoji"' public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg` returns 0 (no bare Excalidraw-default font strings)
    - `grep -c '<title' public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg` returns ≥ 1
    - `grep -c '<desc' public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg` returns ≥ 1
    - `grep -cE '<script|onload=|onclick=' public/blog-assets/**/*.svg` returns 0 (bash globbing — adjust if needed; fall back to two greps)
    - MCP SVG viewBox is `0 0 820 300` (grep `viewBox="0 0 820 300"`) — verifies Task 1's aspect-ratio change landed
    - MCP stdout captured for SUMMARY (new byte count, new intrinsic 820×300)
  </acceptance_criteria>
  <done>
    Both shipped SVGs re-exported via the hardened pipeline. Sizes under budget, security-clean, white-rect-free, Helvetica-fallback-chained. New dimensions for MCP (820×300) captured for Task 4 MDX update.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wrap MCP EN + RU MDX <img> in <figure><figcaption> + update width/height to 820x300</name>
  <files>
    src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md,
    src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md
  </files>
  <read_first>
    - src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md (HEAD — current MDX, lines 20-27 contain the existing `<img>`)
    - src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md (HEAD — lines 20-27, mirror of EN)
    - .planning/phases/04.1-excalidraw-pipeline-hardening/04.1-PATTERNS.md §`src/content/blog/{en,ru}/*.md[x]` figure/figcaption wrap (D-10) — lines 620-683, specifically the exact figure+figcaption structure recommended
    - .planning/phases/04.1-excalidraw-pipeline-hardening/04.1-CONTEXT.md §D-10 (bilingual caption requirement)
    - src/components/visuals/VvStage.astro lines 141, 213-214 (only existing figure+figcaption in the repo)
  </read_first>
  <behavior>
    - MCP EN: replace the 8-line `<img>` block (lines 20-27) with a 10-line `<figure>`+`<img>`+`<figcaption>`+`</figure>` block. Keep the same `src`, `loading="eager"`, `style="max-width: 100%; height: auto;"`. Update `height="100"` → `height="300"` (matches new viewBox from Task 2). `alt` unchanged.
    - MCP RU: mirror of EN with Russian caption, same structural edit (height="100" → height="300").
    - Caption content (planner proposal — user approves at checkpoint):
      - EN: "MCP client → server → external resources. The server is a thin wrapper around a data source; the model decides which tools to call."
      - RU: "MCP клиент → сервер → внешние ресурсы. Сервер — тонкая обёртка над источником данных; модель решает, какие инструменты вызывать."
    - Tailwind classes used: `<figure class="my-6">` (block-level margin, matches `.prose > * + *` spacing rhythm), `<figcaption class="text-sm text-text-muted mt-2 text-center">` (Deep Signal token-backed muted caption per D-10).
  </behavior>
  <action>
    **Edit `src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md`.** Replace lines 20-27 (the entire `<img>` block) with:

    ```html
    <figure class="my-6">
      <img
        src="/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg"
        alt="MCP client-server architecture: Claude Code connects to an MCP Server, which fans out to external resources (docs, databases, APIs)"
        loading="eager"
        width="820"
        height="300"
        style="max-width: 100%; height: auto;"
      />
      <figcaption class="text-sm text-text-muted mt-2 text-center">
        MCP client → server → external resources. The server is a thin wrapper around a data source; the model decides which tools to call.
      </figcaption>
    </figure>
    ```

    **Edit `src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md`.** Replace the equivalent lines (20-27 in HEAD) with:

    ```html
    <figure class="my-6">
      <img
        src="/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg"
        alt="Архитектура MCP клиент-сервер: Claude Code подключается к MCP-серверу, который обращается к внешним ресурсам (docs, DB, API)"
        loading="eager"
        width="820"
        height="300"
        style="max-width: 100%; height: auto;"
      />
      <figcaption class="text-sm text-text-muted mt-2 text-center">
        MCP клиент → сервер → внешние ресурсы. Сервер — тонкая обёртка над источником данных; модель решает, какие инструменты вызывать.
      </figcaption>
    </figure>
    ```

    **Preserve every other character of both files** — frontmatter, prose, headings, all other content stays byte-identical. The only edit is the 8-line `<img>` block → 12-line `<figure>` block.
  </action>
  <verify>
    <automated>grep -c '<figure class="my-6">' src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md && grep -c '<figcaption class="text-sm text-text-muted mt-2 text-center">' src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md && grep -c 'height="300"' src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md && grep -c '<figure class="my-6">' src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md && grep -c '<figcaption class="text-sm text-text-muted mt-2 text-center">' src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md && grep -c 'height="300"' src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md && ! grep -q 'height="100"' src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md && ! grep -q 'height="100"' src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md</automated>
  </verify>
  <acceptance_criteria>
    - EN: `grep -c '<figure class="my-6">'` returns 1, `grep -c '<figcaption'` returns 1, `grep -c 'height="300"'` returns 1, `grep -c 'height="100"'` returns 0
    - RU: same counts
    - EN caption contains "MCP client → server → external resources"
    - RU caption contains "MCP клиент → сервер → внешние ресурсы" (verify Cyrillic round-trip via grep for `клиент`)
    - EN `alt` unchanged from HEAD (`MCP client-server architecture: Claude Code…`)
    - RU `alt` unchanged from HEAD (`Архитектура MCP клиент-сервер: Claude Code…`)
    - `npm run build` exits 0 — no Astro/MDX errors from the figure+figcaption insertion
  </acceptance_criteria>
  <done>
    Both MCP MDX files wrapped in figure+figcaption. Bilingual parity maintained. Build still green. Dimensions match the re-exported SVG.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wrap karpenter EN + RU MDX <img> in <figure><figcaption> (width/height unchanged)</name>
  <files>
    src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx,
    src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx
  </files>
  <read_first>
    - src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx (HEAD — the `<img>` is at lines 57-63, after the "split ownership" paragraph at line 55)
    - src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx (HEAD — `<img>` at lines 55-61, RU mirror)
    - .planning/phases/04.1-excalidraw-pipeline-hardening/04.1-PATTERNS.md §`src/content/blog/{en,ru}/*.md[x]` figure/figcaption wrap (D-10)
    - .planning/phases/04-excalidraw-pipeline/reviews/UX-REVIEW.md lines 429-460 (UX-10 karpenter content — the caption could name taints+tolerations as the mechanism, serving as the text equivalent for mobile readers who can't read the 5.4 px labels per D-09b)
  </read_first>
  <behavior>
    - Karpenter EN: wrap existing `<img>` block in `<figure class="my-6">...<figcaption class="text-sm text-text-muted mt-2 text-center">...</figcaption></figure>`. Keep `src`, `alt`, `loading="lazy"`, `width="1020"`, `height="520"`, `style` all unchanged.
    - Karpenter RU: mirror of EN with Russian caption, same structure.
    - Caption content (planner proposal, names the mechanism per UX-10 fix — user approves at checkpoint):
      - EN: "Split ownership — taints on system nodes keep Karpenter off them; matching tolerations on kube-system pods route them to CA-managed nodes."
      - RU: "Разделение владения — taints на system-нодах удерживают Karpenter вдали от них; соответствующие tolerations на kube-system подах направляют их на ноды под управлением CA."
    - Per D-09b rationale: this caption is load-bearing on mobile because the 5.4 px labels are sub-legibility — the caption names the taints/tolerations mechanism that the diagram visually illustrates.
  </behavior>
  <action>
    **Edit `src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx`.** Replace lines 57-63 (the existing `<img>` block) with:

    ```jsx
    <figure class="my-6">
      <img
        src="/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg"
        alt="Split ownership: Cluster Autoscaler owns system node groups (kube-system, logging, monitoring) on predictable on-demand nodes; Karpenter owns the stateless fleet on dynamically-provisioned nodes with matching taints and tolerations"
        loading="lazy"
        width="1020" height="520"
        style="max-width: 100%; height: auto;"
      />
      <figcaption class="text-sm text-text-muted mt-2 text-center">
        Split ownership — taints on system nodes keep Karpenter off them; matching tolerations on kube-system pods route them to CA-managed nodes.
      </figcaption>
    </figure>
    ```

    **Edit `src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx`.** Replace the equivalent lines (55-61 in HEAD) with:

    ```jsx
    <figure class="my-6">
      <img
        src="/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg"
        alt="Разделение ownership: Cluster Autoscaler владеет system node groups (kube-system, logging, monitoring) на предсказуемых on-demand нодах; Karpenter владеет stateless fleet на динамически provision нодах с matching taints и tolerations"
        loading="lazy"
        width="1020" height="520"
        style="max-width: 100%; height: auto;"
      />
      <figcaption class="text-sm text-text-muted mt-2 text-center">
        Разделение владения — taints на system-нодах удерживают Karpenter вдали от них; соответствующие tolerations на kube-system подах направляют их на ноды под управлением CA.
      </figcaption>
    </figure>
    ```

    **Preserve every other character** of both MDX files. Do NOT touch:
    - Frontmatter
    - Any other `<img>`/`![...](...)` (the 3 PNG diagrams at lines 47, 71, 115 — those are NOT Excalidraw artifacts, stay untouched)
    - Surrounding prose
    - Code blocks
    - Headings

    Only the Excalidraw `<img>` block (one per file, the only one with an `.svg` src under `/blog-assets/.../diagrams/`) is wrapped.
  </action>
  <verify>
    <automated>grep -c '<figure class="my-6">' src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx && grep -c '<figcaption class="text-sm text-text-muted mt-2 text-center">' src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx && grep -c 'width="1020" height="520"' src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx && grep -c '<figure class="my-6">' src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx && grep -c '<figcaption' src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx && grep -c "taints" src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx && grep -c "tolerations" src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - EN: `grep -c '<figure class="my-6">'` returns 1
    - EN: `grep -c 'width="1020" height="520"'` returns 1 (unchanged)
    - EN caption contains "taints" and "tolerations" and "kube-system"
    - RU: `grep -c '<figure class="my-6">'` returns 1
    - RU caption contains "taints" (borrowed English word, standard RU-tech idiom) OR localized equivalent; contains "kube-system"
    - Count of `<img` in each karpenter file unchanged from HEAD (still 4: 3 PNG + 1 SVG — we only wrap the SVG one)
    - `npm run build` exits 0
    - No frontmatter changes (date, title, tags unchanged)
    - All 3 existing PNG `![...](...)` image references (traps 2/3 + 4-step rollout) remain untouched
  </acceptance_criteria>
  <done>
    Both karpenter MDX files wrapped in figure+figcaption with caption naming the taints/tolerations mechanism. Build still green. Per D-09b, the caption serves as the mobile text-equivalent for the sub-legibility 5.4 px labels.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5 [HUMAN]: Verify visual result on dev preview + approve captions</name>
  <what-built>
    - 1 re-authored .excalidraw.json (Task 1, human-driven)
    - 2 re-exported SVGs via hardened pipeline (Task 2)
    - 4 MDX files updated with figure+figcaption (Tasks 3 + 4)
    - Proposed bilingual captions drafted by agent (4 captions: MCP EN + MCP RU + karpenter EN + karpenter RU)
  </what-built>
  <how-to-verify>
    1. Start the dev server:
       ```bash
       npm run dev
       ```
    2. Visit the 4 URLs (local dev) and visually verify:
       - http://localhost:4321/en/blog/2026-03-02-mcp-servers-plainly-explained
       - http://localhost:4321/ru/blog/2026-03-02-mcp-servers-plainly-explained
       - http://localhost:4321/en/blog/2026-03-20-karpenter-right-sizing
       - http://localhost:4321/ru/blog/2026-03-20-karpenter-right-sizing

    3. For each of the 4 posts, check:
       - (a) **MCP diagram renders without a jarring white slab** (D-07 + D-09 transparent bg working together).
       - (b) **MCP diagram labels are legible at desktop width (~1440px)** — should be clearly readable (D-09 bumped labels to 28-32 px inside the SVG).
       - (c) **MCP diagram aspect ratio reads as "illustration" not "separator bar"** (2.7:1 vs the old 8.2:1).
       - (d) **Karpenter diagram renders without white slab** (D-07 SVGO retroactive strip).
       - (e) **figcaption is visible below each diagram in muted text** (`text-text-muted` = `#94A3B8`). Spacing (`mt-2` + `my-6` on figure) looks right — not too tight, not awkwardly spaced.
       - (f) **figcaption contrast passes eyeball test** (5.3:1 AA on dark bg — if you squint and can still read it, good).
       - (g) **Bilingual parity** — RU figcaptions read naturally (not machine-translated-sounding); EN figcaptions match the technical accuracy of the diagrams.

    4. Narrow viewport to 375 px (DevTools mobile emulation) for each post and verify:
       - (h) **MCP labels still legible at mobile scale** (target 28 * 0.418 ≈ 11.7 px, above 9 px legibility floor).
       - (i) **figcaption doesn't overflow** — stays centered, wraps cleanly.

    5. Run `npm run build` and verify green exit (no MDX compile errors from the figure/figcaption wrap).

    6. Approve or request caption edits.

    **Expected results if everything works:**
    - MCP diagram: no white slab, labels ~28px desktop / ~11.7px mobile (legible), aspect ~2.7:1 (illustration not banner), transparent canvas blends with Deep Signal dark prose.
    - Karpenter diagram: no white slab (D-07 retroactive), font rendering consistent across macOS/Windows (D-08 Helvetica fallback chain).
    - All 4 posts have figcaption in muted text below the SVG, centered, bilingual, readable on both viewports.
    - `npm run build` exits 0.

    **If something is off:**
    - White slab still visible → D-07 SVGO selector didn't match; check `public/blog-assets/**/diagrams/*.svg` for `<path fill="#fff" d="M0 0h...` with grep.
    - Karpenter labels misrendered on non-macOS browser → verify SVG contains `font-family="Helvetica, Arial, sans-serif"` (grep).
    - figcaption styling looks wrong (too tight/loose/wrong color) → consider fallback to a global.css rule as per CONTEXT.md line 148 Claude's Discretion (but try to stay on inline Tailwind first).
    - Caption copy feels off → describe the preferred wording and we'll iterate.
  </how-to-verify>
  <resume-signal>
    Type "approved" if everything looks good, OR describe:
    - Which viewport/post has the issue
    - What exact visual problem
    - Preferred caption rewording (if applicable)

    We'll iterate if needed.
  </resume-signal>
</task>

</tasks>

<verification>
After all 5 tasks:

```bash
# Build + unit tests
npm run build
npm run test:unit

# SVG integrity
wc -c public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg
wc -c public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg
# Both should be < 10240 (10 KB)

grep -cE '<script|onload=|onclick=' public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg  # expect 0
grep -cE '<script|onload=|onclick=' public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg      # expect 0
grep -c 'fill="#fff".*M0 0h' public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg       # expect 0
grep -c 'fill="#fff".*M0 0h' public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg            # expect 0
grep -c 'Helvetica, Arial, sans-serif' public/blog-assets/2026-03-20-karpenter-right-sizing/diagrams/split-ownership.svg  # expect >= 6
grep -c '<title' public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg                  # expect >= 1
grep -c '<desc' public/blog-assets/2026-03-02-mcp-servers-plainly-explained/diagrams/client-server.svg                   # expect >= 1

# MDX wrapping — 4 files, 1 <figure> per file
for f in src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx; do
  echo "$f: figure=$(grep -c '<figure class="my-6">' $f) figcaption=$(grep -c '<figcaption' $f)"
done
# Each line should show figure=1 figcaption=1

# MCP height updated
grep -c 'height="300"' src/content/blog/en/2026-03-02-mcp-servers-plainly-explained.md  # expect 1
grep -c 'height="300"' src/content/blog/ru/2026-03-02-mcp-servers-plainly-explained.md  # expect 1
grep -c 'height="100"' src/content/blog/**/2026-03-02-mcp-servers-plainly-explained.md  # expect 0

# Karpenter height unchanged
grep -c 'height="520"' src/content/blog/en/2026-03-20-karpenter-right-sizing.mdx  # expect 1
grep -c 'height="520"' src/content/blog/ru/2026-03-20-karpenter-right-sizing.mdx  # expect 1
```

All gates must pass.
</verification>

<success_criteria>
- [ ] MCP `.excalidraw.json` re-authored (labels 28-32 px, viewBox 820x300, viewBackgroundColor null) — human-verified in Task 1
- [ ] MCP SVG re-exported at 820x300, under 10 KB, no leading white rect
- [ ] Karpenter SVG re-exported with Helvetica fallback chain, under 10 KB, no leading white rect, no Segoe-variant fonts
- [ ] MCP EN + RU MDX wrapped in figure+figcaption with English + Russian captions, height updated to 300
- [ ] Karpenter EN + RU MDX wrapped in figure+figcaption with captions naming taints/tolerations mechanism
- [ ] `npm run build` exits 0
- [ ] Human visual verification PASS on 4 URLs × 2 viewports (Task 5)
- [ ] Captions user-approved (or iterated)
- [ ] Bilingual parity maintained (no English leaking into RU captions, no Russian leaking into EN)
- [ ] No new i18n JSON keys added (captions are per-post inline content per D-10)
</success_criteria>

<output>
Create `.planning/phases/04.1-excalidraw-pipeline-hardening/04.1-03-SUMMARY.md`. Include:
- `requirements-covered: [DIAG-01, DIAG-02, DIAG-03, DIAG-04, DIAG-05]` (all re-hardened via re-export)
- `ux-findings-mitigated: [UX-02, UX-03, UX-06, UX-07, UX-10]`
- Before/after byte sizes for both SVGs (MCP: 9,673 → ~????, karpenter: 7,338 → ~????)
- Caption content (final, post-user-approval) for all 4 files
- New MCP intrinsic dimensions (820×300)
- D-09b deferred in SUMMARY (karpenter mobile label scaling accepted, figcaption serves as text equivalent)
</output>
</content>
