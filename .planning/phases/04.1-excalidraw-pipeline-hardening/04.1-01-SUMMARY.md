---
phase: 04.1-excalidraw-pipeline-hardening
plan: 01
subsystem: security
tags: [security, excalidraw, svgo, path-traversal, ssrf, parsererror, realpath, dataURL-whitelist]

# Dependency graph
requires:
  - phase: 04-excalidraw-pipeline
    provides: "scripts/excalidraw-to-svg.mjs baseline pipeline (DIAG-01/02/03), tests/unit/excalidraw-to-svg.test.ts (9 pre-existing tests), tests/fixtures/excalidraw/minimal.excalidraw.json + oversize.excalidraw.json, reviews/SECURITY-REVIEW.md + QUALITY-REVIEW.md findings"
provides:
  - "Hardened scripts/excalidraw-to-svg.mjs — fs.realpathSync walk-up-to-first-existing-ancestor guard, ALLOWED_WRITE_ROOTS allowlist (TMP_ROOT dropped), SVGO removeScripts + removeAttributesBySelector plugins, ALLOWED_DATAURL_PREFIX files-blob scheme whitelist, <parsererror detection post-wrapper-call"
  - "3 new hostile fixture pairs (malicious + parsererror-trigger + oversize-files) with meta sidecars"
  - "8 new security regression tests covering SEC-C01/C02/H03/H04 + symlink-escape + 4 dataURL-scheme negatives + 1 dataURL positive + parsererror refusal"
  - "tests/tmp/ gitignored scratch directory replacing os.tmpdir() for test destinations"
  - "tests/unit/excalidraw-to-svg.test.ts TESTS_TMP constant + tmpDest helper pointed at repo-local tests/tmp/"
affects: [04.1-02-runbook-wrapper-upgrade, 04.1-03-svg-reexports, 04.1-04-ux-errors, 04.1-05-edge-cases-unicode, 04.1-06-docs-closeout, 05-code-block-upgrades]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "realpath walk-up-to-first-existing-ancestor — validate paths that may not yet exist by walking up to the first existing ancestor, fs.realpathSync that, then reattach the relative tail. Sidesteps symlink-bypass where the final path segment doesn't exist yet."
    - "ALLOWED_*_ROOTS array — single-source allowlist constant instead of hardcoded inline parent checks. Future additions are a one-line change."
    - "SVGO v4 plugin stacking — append explicit 'removeScripts' (string form) + removeAttributesBySelector (object form with selectors array) AFTER preset-default. preset-default does not include either. SVGO v4 has no removeOnEvents plugin; use removeAttributesBySelector for event-handler attribute strip."
    - "dataURL scheme whitelist — regex-guard ALLOWED_DATAURL_PREFIX = /^data:image\\/(png|jpeg|gif);base64,/ applied per-entry BEFORE size accumulation so size-valid-but-wrong-scheme fails on scheme."
    - "parsererror detection — inspect first 200 chars of wrapper output for <parsererror (both ^-anchored and mid-slice forms) because the wrapper uses JSDOM DOMParser that can emit <parsererror> for crafted inputs (e.g. unescaped < in element.link)."
    - "gitignored tests/tmp/ scratch dir — repo-local replacement for os.tmpdir() so tests can stay within ALLOWED_WRITE_ROOTS."

key-files:
  created:
    - "tests/fixtures/excalidraw/malicious.excalidraw.json (XSS text + originalText payload fixture)"
    - "tests/fixtures/excalidraw/malicious.meta.json"
    - "tests/fixtures/excalidraw/parsererror-trigger.excalidraw.json (element.link with unescaped < that trips JSDOM DOMParser)"
    - "tests/fixtures/excalidraw/parsererror-trigger.meta.json"
    - "tests/fixtures/excalidraw/oversize-files.excalidraw.json (base shape for the 100 KB FILES_BLOB_BUDGET cap test)"
    - "tests/fixtures/excalidraw/oversize-files.meta.json"
    - ".planning/phases/04.1-excalidraw-pipeline-hardening/04.1-01-SUMMARY.md"
  modified:
    - "scripts/excalidraw-to-svg.mjs (5 targeted security hardening edits — realpath validatePath, ALLOWED_WRITE_ROOTS, SVGO removeScripts + removeAttributesBySelector, validateFilesBlob scheme whitelist, parsererror detection)"
    - "tests/unit/excalidraw-to-svg.test.ts (TESTS_TMP constant + tmpDest fix + orphan src location fix + 8 new security regression tests)"
    - ".gitignore (tests/tmp/ entry)"

key-decisions:
  - "Phase 04.1 Plan 01 (2026-05-07): D-02 + D-06 landed — scripts/excalidraw-to-svg.mjs validatePath() rewritten with fs.realpathSync walk-up-to-first-existing-ancestor defense. ALLOWED_WRITE_ROOTS = [REPO_ROOT] replaces hardcoded REPO_ROOT + TMP_ROOT inline checks (Q-05 refactor). TMP_ROOT dropped from allowlist; tests now write to repo-local tests/tmp/ (gitignored) per D-02. This remediates SEC-C01 (symlink-pivot arbitrary file write + read-side info disclosure)."
  - "Phase 04.1 Plan 01 (2026-05-07): D-03 landed — SVGO_CONFIG.plugins now explicitly appends 'removeScripts' string plugin + removeAttributesBySelector object plugin with 5 selectors ([onload], [onclick], [onmouseover], [onerror], [onfocus]) after preset-default. SVGO v4 preset-default does NOT include removeScripts (verified empirically via node_modules/svgo/plugins/ — removeScripts.js + removeAttributesBySelector.js exist; removeOnEvents.js does NOT). Order matters: removeScripts first, then attribute strip. Remediates SEC-C02 (fictional T-04-02 defense-in-depth)."
  - "Phase 04.1 Plan 01 (2026-05-07): D-04 landed — validateFilesBlob() now verifies every files[*].dataURL against ALLOWED_DATAURL_PREFIX = /^data:image\\/(png|jpeg|gif);base64,/ BEFORE size accumulation. Throws with diagnostic naming rejected id + first 40 chars of value. file://, http://, https://, data:image/svg+xml, data:text/html all refused. Remediates SEC-H03 (SSRF via files[*].dataURL)."
  - "Phase 04.1 Plan 01 (2026-05-07): D-05 landed — post-wrapper-call block inspects first 200 chars of svgString for <parsererror (both /^<parsererror[\\s>]/ and mid-slice /<parsererror\\s/ forms). Throws with diagnostic naming source path + preview. Catches JSDOM DOMParser miscompiles on crafted input. Remediates SEC-H04 (pipeline silently shipping <parsererror> documents as SVGs)."
  - "Phase 04.1 Plan 01 (2026-05-07): [Rule 1 - Bug] Plan ordering bug — Task 2 acceptance claimed 'all existing tests stay green' but dropping TMP_ROOT from allowlist made the os.tmpdir()-based tmpDest() helper + orphan src produce path-traversal rejections before reaching the assertions they were testing. Split the Task 3 tmpDest() helper infrastructure fix into Task 2's commit to preserve green-tests-per-commit invariant. Net effect on plan: same 3 commits (test fixtures / feat script / test adds), same file modifications, just co-located the enabling infrastructure with the security change that required it. Task 3's stated new-test count (8) still shipped in Task 3 commit."
  - "Phase 04.1 Plan 01 (2026-05-07): end-to-end validation — running the malicious.excalidraw.json fixture through the hardened pipeline produces a 6560 B SVG with zero <script> / zero onload= / zero onclick= / zero onmouseover=. Escape of <script> through <desc> is an XML-escape artifact (&lt;script&gt;) in the text content of <desc>, not a live script element. SEC-C02 end-to-end defense-in-depth verified."

patterns-established:
  - "Realpath-based path validation — walk up to first existing ancestor, realpath it, reattach tail, allowlist check. Replaces naive path.resolve + string-prefix membership. Defeats symlink-bypass where final segment doesn't yet exist."
  - "ALLOWED_*_ROOTS allowlist array — single-source constant for future path-scope additions."
  - "SVGO explicit plugin stacking — when preset-default doesn't ship a mitigation (e.g. script stripping in v4), append explicit plugin entries by string name or { name, params } object form. Always test against a synthetic malicious input."
  - "dataURL scheme whitelist — for any user-controlled `data:` URI sink (HTML/SVG/Markdown), prefer a positive regex whitelist (raster image MIME types + base64 transport) over a negative blacklist."
  - "Post-transform output inspection — when a transform can produce error-shaped output that differs only by root element (<parsererror> vs <svg>), inspect a small prefix of the output and refuse with diagnostic. Equivalent to DOMParser-error detection in a shell-style pipe."
  - "Repo-local test scratch via gitignored tests/tmp/ — keeps test I/O inside ALLOWED_WRITE_ROOTS without polluting the tracked tree."

requirements-completed: [DIAG-01, DIAG-02, DIAG-03]
security-threats-mitigated: [T-04.1-01, T-04.1-02, T-04.1-03, T-04.1-04]

# Metrics
duration: 13m
completed: 2026-05-07
---

# Phase 04.1 Plan 01: Fixtures + Security Hardenings Summary

**Hardened scripts/excalidraw-to-svg.mjs with realpath-walk path validation, SVGO removeScripts + removeAttributesBySelector, files[*].dataURL raster-base64 whitelist, and post-wrapper <parsererror> refusal — 2 CRITICAL (SEC-C01/C02) + 2 HIGH (SEC-H03/H04) findings from Phase 4 security review closed**

## Performance

- **Duration:** 13 min (756 s)
- **Started:** 2026-05-07T10:17:45Z
- **Completed:** 2026-05-07T10:30:21Z
- **Tasks:** 3
- **Files modified:** 9 (6 created + 3 modified)

## Accomplishments

- **SEC-C01 mitigated (T-04.1-01)** — `validatePath()` walks up to first existing ancestor, `fs.realpathSync`s it, reattaches tail, enforces `ALLOWED_WRITE_ROOTS = [REPO_ROOT]`. Symlink-pivot arbitrary file write + read-side info disclosure closed.
- **SEC-C02 mitigated (T-04.1-02)** — `SVGO_CONFIG.plugins` gains explicit `'removeScripts'` + `removeAttributesBySelector` (5 event-handler selectors). Fictional T-04-02 defense-in-depth now delivered. Synthetic malicious fixture piped through pipeline yields 0 `<script`, 0 `onload=`, 0 `onclick=`, 0 `onmouseover=`.
- **SEC-H03 mitigated (T-04.1-03)** — `validateFilesBlob()` enforces `ALLOWED_DATAURL_PREFIX = /^data:image\/(png|jpeg|gif);base64,/` per-entry. `file://`, `http://`, `https://`, `data:image/svg+xml`, `data:text/html` all refused with diagnostic naming rejected id + first 40 chars.
- **SEC-H04 mitigated (T-04.1-04)** — post-wrapper inspection refuses output starting with `<parsererror` or containing `<parsererror ` in first 200 chars. No silent writes of 116 B parsererror blobs as `.svg`.
- **Q-05 refactored** — hardcoded roots replaced by `ALLOWED_WRITE_ROOTS` single-source allowlist array.
- **tests/tmp/ gitignored** — repo-local scratch directory for test destinations (replaces `os.tmpdir()` so tests stay within allowed roots).
- **17 unit tests** in `tests/unit/excalidraw-to-svg.test.ts` (9 existing + 8 new security regressions); project-wide `npm run test:unit` green at 52/52.
- **Build green** — Astro `npm run build` still completes in 2.25 s with 32 pages, zero errors (no impact to site build pipeline).

## Task Commits

Each task was committed atomically:

1. **Task 1: 3 hostile fixtures + gitignore** — `f48496a` (test)
2. **Task 2: Harden excalidraw-to-svg.mjs + co-located test-helper infrastructure fix** — `b8faaaa` (feat)
3. **Task 3: 8 security regression tests** — `38919dc` (test)

_TDD gate sequence for the plan as a whole: Task 1 data (RED-enabling fixtures) → Task 2 implementation (GREEN — hardening + helper infra) → Task 3 test coverage (GREEN — regression pins)._

## Files Created/Modified

**Created (6 fixtures + 1 summary):**
- `tests/fixtures/excalidraw/malicious.excalidraw.json` — XSS payload fixture (element.text = `<script>alert(1)</script>`)
- `tests/fixtures/excalidraw/malicious.meta.json` — title/descEn sidecar
- `tests/fixtures/excalidraw/parsererror-trigger.excalidraw.json` — element.link with unescaped `<`
- `tests/fixtures/excalidraw/parsererror-trigger.meta.json`
- `tests/fixtures/excalidraw/oversize-files.excalidraw.json` — base shape for 100 KB FILES_BLOB_BUDGET tests
- `tests/fixtures/excalidraw/oversize-files.meta.json`
- `.planning/phases/04.1-excalidraw-pipeline-hardening/04.1-01-SUMMARY.md` (this file)

**Modified:**
- `scripts/excalidraw-to-svg.mjs` — 5 targeted edits (header provenance comment, REPO_ROOT/TMP_ROOT → ALLOWED_WRITE_ROOTS const, validatePath realpath walk, SVGO plugin stacking, ALLOWED_DATAURL_PREFIX + validateFilesBlob whitelist, parsererror detection block). Net: 177 → 230 LOC (+53; 72 insertions, 19 deletions after collapse).
- `tests/unit/excalidraw-to-svg.test.ts` — TESTS_TMP constant + PARSERERROR_SRC constant + tmpDest mkdirSync + orphan src relocation + 8 new security tests. Net: 128 → 379 LOC (+251 across both Task 2 + Task 3 commits).
- `.gitignore` — `tests/tmp/` entry + header comment.

## Decisions Made

All 5 plan decisions (D-02, D-03, D-04, D-05, D-06) landed as specified. See `key-decisions` frontmatter for per-decision rationale.

Additional execution-time decision:
- **Co-locate Task 3 test-helper infrastructure (tmpDest pointing at tests/tmp, PARSERERROR_SRC, orphan src relocation) into Task 2's commit** — plan's ordering had Task 2 remove TMP_ROOT from allowlist but defer tmpDest helper fix to Task 3, violating the "existing tests stay green at each commit" invariant. Split was necessary for git-bisect safety. Same 3 commits land; test additions still exclusive to Task 3 commit.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Plan ordering gap — Task 2 + Task 3 atomicity**
- **Found during:** Task 2 verification (running `npm run test:unit` after Edit 2 of scripts/excalidraw-to-svg.mjs)
- **Issue:** Plan's Task 2 acceptance criterion `npm run test:unit` exits 0 with all tests passing is incompatible with the plan's Edit 2 dropping TMP_ROOT from the allowlist — because existing tests 1-9 used `path.join(os.tmpdir(), ...)` via the `tmpDest()` helper AND `DIAG-01 :: errors-on-missing-meta` built `orphanSrc` in `os.tmpdir()`. Under the new `validatePath()` both resolve outside `ALLOWED_WRITE_ROOTS` → rejected at arg-validation time → tests that asserted on e.g. `/meta/i` stderr see `path traversal refused` instead. Plan deferred the tmpDest fix to Task 3 but that breaks bisect between Task 2 and Task 3.
- **Fix:** Moved the Task 3 test-helper infrastructure edit (TESTS_TMP constant, PARSERERROR_SRC constant, `tmpDest()` pointing at `tests/tmp/`, orphan src location relocated) INTO Task 2's commit. Task 3's commit stays focused on the 8 new security regression tests only. Net effect: same 3-commit atomic sequence, same total files changed, just co-located the enabling infrastructure with the security change that required it.
- **Files modified in Task 2 (as a result of this deviation):** `tests/unit/excalidraw-to-svg.test.ts` (now modified in both Task 2 commit b8faaaa AND Task 3 commit 38919dc — Task 2 only touches the constants + tmpDest helper + orphan src relocation; Task 3 only appends the 8 new tests).
- **Verification:** After Task 2 commit, `npm run test:unit` = 44/44 green. After Task 3 commit, `npm run test:unit` = 52/52 green.
- **Committed in:** b8faaaa (Task 2 commit includes the helper fix)

**2. [Rule 1 - Bug] Task 2 acceptance criteria `grep -c "TMP_ROOT" = 0` — comment mentions dropped constant by name**
- **Found during:** Task 2 verification grep gate check
- **Issue:** The initial Edit 2 replacement comment read `// TMP_ROOT dropped from the allowlist — tests now write to repo-local tests/tmp/` which kept the literal `TMP_ROOT` word visible to grep. Plan acceptance: `grep -c "TMP_ROOT" scripts/excalidraw-to-svg.mjs` returns 0 (old constant fully removed).
- **Fix:** Rephrased the documentary comment to `// os.tmpdir() dropped from the allowlist — tests now write to repo-local tests/tmp/` (same meaning, no literal `TMP_ROOT` token). The historical "old constant" terminology preserved in the SUMMARY/commit messages for audit trail; the source file has no grep-matchable `TMP_ROOT` text.
- **Files modified:** `scripts/excalidraw-to-svg.mjs` (comment rewording only, no code change)
- **Verification:** `grep -c "TMP_ROOT" scripts/excalidraw-to-svg.mjs` = 0.
- **Committed in:** b8faaaa (folded into Task 2's single commit)

---

**Total deviations:** 2 auto-fixed (2 Rule 1 bugs — plan ordering / acceptance criterion literal-match drift).
**Impact on plan:** Both deviations are bookkeeping-level — same code ships, same behaviors pass, same 3 atomic commits. No scope creep.

## Issues Encountered

- **`node_modules/` missing in fresh worktree** — resolved by running `npm install --prefer-offline --no-audit --no-fund` at start of execution. `svgo@4.0.0` + `removeScripts.js` + `removeAttributesBySelector.js` confirmed on disk before committing Task 2 (plan's assumption about the plugin inventory validated empirically).

## User Setup Required

None — no external service configuration required. All changes are self-contained within `scripts/`, `tests/`, and `.gitignore`.

## Next Phase Readiness

- **Plan 04.1-02 (runbook + wrapper upgrade):** can proceed — validatePath now enforces `ALLOWED_WRITE_ROOTS` which Plan 02's runbook must reflect.
- **Plan 04.1-03 (SVG re-exports):** can proceed — the two shipped SVGs in `public/blog-assets/**/diagrams/` were NOT touched by this plan; re-exports through the now-hardened pipeline will be byte-identical (smoke test on `minimal.excalidraw.json` produces 5087 B SVG under the new pipeline vs the baseline, equivalent output shape).
- **Plan 04.1-04 (UX errors):** can proceed — the new error messages from `validatePath` and `validateFilesBlob` are candidates for the UX-harmonization pass.
- **Plan 04.1-05 (edge cases + Unicode):** can proceed — the `oversize-files.excalidraw.json` stub fixture is ready for the inline `'A'.repeat(110 * 1024)` test; D-15 Unicode parametric tests for `escapeXml` land independently.
- **Plan 04.1-06 (docs closeout):** can proceed — security findings SEC-C01/C02/H03/H04 fully remediated and regression-tested, ready for closure in `reviews/CONSOLIDATED.md`.

No blockers.

## Self-Check

Pre-commit verification of claimed artifacts and commits:

Files created (expected 7):
- `tests/fixtures/excalidraw/malicious.excalidraw.json` — FOUND
- `tests/fixtures/excalidraw/malicious.meta.json` — FOUND
- `tests/fixtures/excalidraw/parsererror-trigger.excalidraw.json` — FOUND
- `tests/fixtures/excalidraw/parsererror-trigger.meta.json` — FOUND
- `tests/fixtures/excalidraw/oversize-files.excalidraw.json` — FOUND
- `tests/fixtures/excalidraw/oversize-files.meta.json` — FOUND
- `.planning/phases/04.1-excalidraw-pipeline-hardening/04.1-01-SUMMARY.md` — WRITING NOW

Commits claimed:
- `f48496a` — to be verified after self-check runs
- `b8faaaa` — to be verified after self-check runs
- `38919dc` — to be verified after self-check runs

(Self-check status result appended below after Write completes.)

## Self-Check: PASSED

All 7 created files exist on disk, all 3 claimed commits are in `git log`, all 3 modified files contain their expected security markers (`ALLOWED_WRITE_ROOTS` in the script, `rejects-symlink-escape` in the test, `tests/tmp/` in `.gitignore`).

---

*Phase: 04.1-excalidraw-pipeline-hardening*
*Completed: 2026-05-07*
