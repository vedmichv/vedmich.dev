---
phase: 05-slidev-integration
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - docs/slides-onboarding.md
  - /Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md
  - /Users/viktor/Documents/ViktorVedmich/80-Resources/85-Scripts/85.20-Claude-Code/skills/vv-slidev/references/publish-to-vedmich-dev.md
autonomous: true
requirements: [SLIDES-06, CONTENT-04]
tags: [slidev, docs, runbook, skill, three-way-sync]
user_setup: []
threat_model:
  - id: T-05-03
    severity: MEDIUM
    category: Information Disclosure (documentation drift)
    component: docs/slides-onboarding.md (SSoT per D-10) + skill reference publish-to-vedmich-dev.md (short pointer)
    disposition: mitigate
    mitigation: "Skill reference is deliberately short (30-50 LOC) and opens with a sentence declaring docs/slides-onboarding.md as the single source of truth. The skill reference never duplicates gotcha lists or long-form steps — only TL;DR + quick commands. Drift is bounded because the only content replicated across both files is a command-block TL;DR; all context lives in docs/. Verify via `wc -l` gate (<= 55 LOC on the skill side, allowing small slack)."
must_haves:
  truths:
    - "docs/slides-onboarding.md exists in the vedmich.dev repo and is >= 150 LOC"
    - "Runbook has >= 8 H2 sections: Intro/When-to-use/Prerequisites/6 numbered Steps/Gotchas/Further reading"
    - "Runbook's --base command uses `--base /slides/<slug>/` (NOT `/<slug>/`) with trailing slash required"
    - "Runbook names the D-13 anti-pattern: never copy 404.html, CNAME, or index.html from slidev/ root"
    - "Live skill reference at ~/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md exists, is 30-55 LOC, opens with SSoT declaration, has ## TL;DR + ## Quick commands sections"
    - "Vault mirror at 85.20-Claude-Code/skills/vv-slidev/references/publish-to-vedmich-dev.md exists and is byte-identical to live via diff"
    - "Vault git has commit with message 'vault backup: skill vv-slidev — add publish-to-vedmich-dev.md reference'"
  artifacts:
    - path: "docs/slides-onboarding.md"
      provides: "End-to-end runbook for publishing a Slidev deck to vedmich.dev/slides/<slug>/"
      min_lines: 150
      contains:
        - "slidev build"
        - "--base /slides/"
        - "git submodule update --remote --merge slidev"
        - "cp -r slidev/"
        - "Do NOT copy"
        - "404.html"
    - path: "/Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md"
      provides: "Short skill reference pointing to docs/slides-onboarding.md as SSoT"
      min_lines: 30
      contains:
        - "docs/slides-onboarding.md"
        - "TL;DR"
        - "--base /slides/"
    - path: "/Users/viktor/Documents/ViktorVedmich/80-Resources/85-Scripts/85.20-Claude-Code/skills/vv-slidev/references/publish-to-vedmich-dev.md"
      provides: "Vault mirror of the live skill reference (three-way sync target)"
      contains:
        - "docs/slides-onboarding.md"
        - "TL;DR"
  key_links:
    - from: "skill reference publish-to-vedmich-dev.md opening sentence"
      to: "docs/slides-onboarding.md in vedmich.dev repo"
      via: "first-paragraph SSoT declaration"
      pattern: "docs/slides-onboarding.md"
    - from: "docs/slides-onboarding.md Step 4 (whitelist activation)"
      to: ".github/workflows/deploy.yml `Copy Slidev decks` step (created by Plan 01 Task 2)"
      via: "explicit file-path reference in the runbook"
      pattern: "\\.github/workflows/deploy\\.yml"
    - from: "CLAUDE.md §Slidev Integration (created by Plan 01 Task 3) Runbook pointer"
      to: "docs/slides-onboarding.md (this plan creates)"
      via: "forward reference already in CLAUDE.md"
      pattern: "docs/slides-onboarding.md"
---

<objective>
Author `docs/slides-onboarding.md` — the canonical end-to-end runbook for publishing a Slidev deck to `vedmich.dev/slides/<slug>/`. Create the companion `~/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md` as a short (30-50 LOC) pointer into the docs/ runbook per D-10, then mirror it to the vault backup per CLAUDE.md three-way-sync rule.

Purpose: Deliver SLIDES-06 (docs/slides-onboarding.md runbook) and CONTENT-04 (vv-slidev skill reference + vault mirror). The runbook documents the Plan 01 pipeline — how to author a deck in theme repos, push dist to `vedmichv/slidev:gh-pages`, bump the submodule here, activate the whitelist, un-draft MDX, and verify. The skill reference is deliberately short to prevent drift.

Output: One new runbook file (`docs/slides-onboarding.md` >=150 LOC, 8+ H2 sections) + one new skill reference (30-50 LOC pointer) + one vault mirror (byte-identical) + one vault commit.

Dependency: This plan runs after Plan 01 lands because the runbook explicitly documents the `.github/workflows/deploy.yml` extension that Plan 01 Task 2 creates — the runbook Step 4 tells authors how to uncomment the whitelist loop that exists thanks to Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-slidev-integration/05-CONTEXT.md
@.planning/phases/05-slidev-integration/05-RESEARCH.md
@.planning/phases/05-slidev-integration/05-PATTERNS.md
@.planning/phases/05-slidev-integration/05-VALIDATION.md
@CLAUDE.md
@diagrams-source/README.md
@/Users/viktor/.claude/skills/vv-slidev/references/deployment.md
@.github/workflows/deploy.yml

<interfaces>
Runbook structure analog from diagrams-source/README.md (155 LOC, 8 H2 sections):
- `# <Title>` + 1-paragraph intro + triple-asterisk `***` dividers between H2 sections
- H2 sections: Prerequisites / Authoring / Metadata / Exporting / Embedding / Gotchas / Determinism caveat / Further reading
- Conventions: fenced code blocks for every bash command, bold-prefixed gotcha headers, tail "Further reading" cross-linking

Existing skill reference analog from ~/.claude/skills/vv-slidev/references/deployment.md (96 LOC) covers the gh-pages side. The new publish-to-vedmich-dev.md MUST be SHORT per D-10 (30-50 LOC). Defer long-form content to docs/slides-onboarding.md as SSoT.

CLAUDE.md three-way-sync rule (from ~/.claude/CLAUDE.md):
1. Edit live at ~/.claude/skills/<name>/ FIRST (what Claude reads)
2. Mirror to vault at ~/Documents/ViktorVedmich/80-Resources/85-Scripts/85.20-Claude-Code/skills/<name>/
3. Commit vault with message: "vault backup: skill <name> — <what changed>"
cp -R gotcha: trailing slash on source copies CONTENTS into dest, dropping the dir level. Use explicit file paths OR omit trailing slash.

Plan 01 Task 2 creates the CI step that the runbook Step 4 references: `.github/workflows/deploy.yml` contains a `Copy Slidev decks to dist/slides` step with a commented `# for slug in ...; do cp -r slidev/$slug dist/slides/; done` whitelist loop.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/slides-onboarding.md end-to-end runbook (>=150 LOC, 8+ H2 sections)</name>
  <files>docs/slides-onboarding.md</files>
  <read_first>
    - diagrams-source/README.md HEAD — full 155 LOC (structural template — intro + *** rules + fenced code + bold-prefixed gotchas + Further reading)
    - /Users/viktor/.claude/skills/vv-slidev/references/deployment.md HEAD — 96 LOC (covers the gh-pages side; complements the new vedmich.dev-side runbook)
    - .planning/phases/05-slidev-integration/05-RESEARCH.md §"Runbook outline: docs/slides-onboarding.md" lines 672-717 (target outline + 150-200 LOC target)
    - .planning/phases/05-slidev-integration/05-PATTERNS.md §"8. docs/slides-onboarding.md (new, runbook)" lines 424-537 (analog structure + delta for Slidev domain)
    - .planning/phases/05-slidev-integration/05-CONTEXT.md §decisions D-06 (submodule pump), D-08 (source/dist split), D-09 (docs location), D-13 (no 404.html copy)
    - .planning/phases/05-slidev-integration/05-RESEARCH.md §"Common Pitfalls" lines 360-414 (9 pitfalls — Pitfalls 2, 3, 4, 7, 8 all deserve mention in Gotchas)
    - .github/workflows/deploy.yml after Plan 01 Task 2 — the `Copy Slidev decks to dist/slides` step
  </read_first>
  <action>
    Create `docs/slides-onboarding.md`. If the `docs/` directory does not exist, first run `mkdir -p docs/`.

    Write the file with this exact structural outline. The content below is the full file content — write it verbatim.

    Opening section:
    - H1: `# Slidev Deck Onboarding — vedmich.dev/slides/<slug>/`
    - Intro paragraph: "End-to-end runbook for publishing a Slidev deck as a first-party route under `vedmich.dev/slides/<slug>/`. The main site (`vedmich.dev`) and the deck artifact repo (`vedmichv/slidev:gh-pages`) live in two separate repos; this runbook walks through the submodule + CI pipeline that glues them together."
    - Status line: "**Ship status:** Phase 5 (milestone v1.0) — infrastructure ready, whitelist intentionally empty until first deck migrates. Current `/slides/*` consumers: none."
    - `***` divider

    H2 `## When to use this flow`:
    - Describe the scenario: author has a Slidev deck in one of the theme repos (vedmichv/slidev-theme-slurm, vedmichv/slidev-theme-vv, DKT-AI/slidev-theme-dkt) and wants it at `vedmich.dev/slides/<slug>/` instead of (or alongside) `s.vedmich.dev/<slug>/`.
    - Reasons: SEO/brand cohesion (deck lives on main domain), internal link hygiene (PresentationCard emits `/slides/<slug>/` automatically), unified hosting (one deploy, one CDN edge).
    - Alternative pointer: "If you just want to publish to `s.vedmich.dev` and leave it there, follow the existing `vv-slidev` skill's `references/deployment.md` — that covers the gh-pages side only."
    - `***` divider

    H2 `## Prerequisites`:
    - Confirm three local repo checkouts: `~/Documents/GitHub/vedmichv/slidev-theme-*`, `~/Documents/GitHub/vedmichv/slidev/`, `~/Documents/GitHub/vedmich/vedmich.dev/`.
    - Fenced code: `cd ~/Documents/GitHub/vedmich/vedmich.dev/ && git submodule status slidev` — expected output `<SHA> slidev (heads/gh-pages)`; if empty or prefixed with `-`, run `git submodule update --init --recursive`.
    - Fenced code: verify Plan 01 Task 2 landed:
      ```bash
      grep -q 'Copy Slidev decks to dist/slides' .github/workflows/deploy.yml && echo "OK" || echo "MISSING — Plan 01 Task 2 not landed"
      grep -A2 'actions/checkout@v4' .github/workflows/deploy.yml | grep -q 'submodules: recursive' && echo "OK" || echo "MISSING — Plan 01 Task 2 not landed"
      ```
    - `***` divider

    H2 `## Step 1: Build the deck with --base /slides/<slug>/`:
    - Fenced bash:
      ```bash
      cd ~/Documents/GitHub/vedmichv/slidev-theme-vv
      pnpm slidev build presentations/<slug>/slides.md --base /slides/<slug>/
      # Output lands in ./dist/
      ```
    - Explain `--base` is load-bearing (trailing slash required per Slidev hosting docs); without it assets 404 because SPA router expects a different base path than hosting URL.
    - **Critical note:** Legacy deploy uses `--base /<slug>/` (deck at root of s.vedmich.dev/<slug>/). For vedmich.dev/slides/<slug>/ serving, base is `--base /slides/<slug>/` — extra `slides/` segment.
    - `***` divider

    H2 `## Step 2: Publish dist/ to vedmichv/slidev:gh-pages`:
    - Fenced bash:
      ```bash
      cd ~/Documents/GitHub/vedmichv/slidev/
      git checkout gh-pages    # default branch
      git pull origin gh-pages
      rm -rf <slug>
      cp -r ~/Documents/GitHub/vedmichv/slidev-theme-vv/dist <slug>
      git add <slug>
      git commit -m "deploy: <slug> (N slides) --base /slides/<slug>/"
      git push origin gh-pages
      ```
    - Note: Mirrors existing gh-pages deploy flow in `vv-slidev/references/deployment.md`; only difference is `--base` arg (hence per-deck base-path commit message convention).
    - **Do NOT** touch CNAME (belongs to legacy s.vedmich.dev surface — keep live until user closes it manually per SLIDES-05 deferral) or root index.html / 404.html (those still serve legacy /<slug>/ navigation).
    - `***` divider

    H2 `## Step 3: Pump the submodule in vedmich.dev`:
    - Fenced bash:
      ```bash
      cd ~/Documents/GitHub/vedmich/vedmich.dev/
      git submodule update --remote --merge slidev
      git add slidev
      git commit -m "chore(slides): bump <slug>"
      ```
    - Explain: `--remote` pulls latest on tracked branch (gh-pages per .gitmodules); `--merge` records new SHA; `git add slidev` stages updated gitlink.
    - Verify with `git submodule status slidev` — SHA matches commit just pushed.
    - `***` divider

    H2 `## Step 4: Activate the whitelist in .github/workflows/deploy.yml`:
    - Show current HEAD shape of the Copy Slidev decks step (whitelist commented out).
    - Three sub-steps:
      1. Uncomment the `for slug ...; do ...; done` block (remove `#` prefix from three lines).
      2. Add slug to list. Single deck example `for slug in karpenter-prod; do ...`, multi-deck example `for slug in karpenter-prod mcp-platform; do ...`.
      3. Stage + commit: `git add .github/workflows/deploy.yml && git commit -m "ci(slides): activate /slides/<slug>/"`.
    - Invariant: Whitelist-only (never `cp -r slidev/* dist/slides/`) — prevents stray root files leaking. Cross-ref Gotcha 2 below.
    - `***` divider

    H2 `## Step 5: (Optional) Un-draft the MDX entry`:
    - Describe `src/content/presentations/{en,ru}/<slug>.md` frontmatter flip from `draft: true` to `draft: false`.
    - Both locales need the flip (bilingual parity per CLAUDE.md).
    - Optional: remove `slides:` field to fall back to computed /slides/<slug>/ path via PresentationCard `data.slides ?? /slides/<slug>/` precedence.
    - Fenced bash:
      ```bash
      git add src/content/presentations/en/<slug>.md src/content/presentations/ru/<slug>.md
      git commit -m "content: un-draft <slug> presentation"
      ```
    - `***` divider

    H2 `## Step 6: Verify`:
    - Local build:
      ```bash
      npm run build
      mkdir -p dist/slides && cp -r slidev/<slug> dist/slides/
      ls dist/slides/<slug>/
      open dist/slides/<slug>/index.html
      ```
    - Push + deploy: `git push origin <feature-branch>`, open PR, merge after CI green.
    - Post-deploy curl:
      ```bash
      curl -I https://vedmich.dev/slides/<slug>/   # 200 OK expected
      curl -s https://vedmich.dev/slides/<slug>/ | grep -q '<script' && echo "OK: SPA shell served"
      ```
    - Browser Vue Router check: visit /slides/<slug>/1, /2, /presenter/ — all load via SPA internal router.
    - `***` divider

    H2 `## Gotchas` — numbered list of 7 items:
    1. `**--base flag requires a trailing slash.**` Without trailing slash, asset URLs break. Always `--base /slides/<slug>/` in Step 1. Verified by Slidev hosting docs (Context7 /websites/sli_dev).
    2. `**Never copy 404.html, CNAME, or index.html from slidev/ root.**` gh-pages root has 404.html hardcoded for 2-segment /slug/slide (breaks on 3-segment /slides/slug/slide), CNAME s.vedmich.dev (wrong domain), index.html with ./<slug>/ links (wrong structure). Whitelist-only prevents this. (D-13)
    3. `**Never add @slidev/cli, vue, or slidev-theme-* to vedmich.dev/package.json.**` Decks are pre-built artifacts, not sources. Adding deps regresses CI from ~90s to 5+ min. (Pitfall 3)
    4. `**After git checkout <branch>, always run git submodule update --init --recursive.**` Submodules track commits, not branches. Symptom: `git status` shows `modified: slidev (new commits)` after branch switch. (Pitfall 7)
    5. `**.gitmodules MUST have branch = gh-pages.**` If submodule added without `-b gh-pages`, line is missing and `git submodule update --remote` silently follows remote HEAD. Verify: `git config -f .gitmodules --get submodule.slidev.branch` must print `gh-pages`. (Pitfall 4)
    6. `**Three-way sync gotcha — cp -R trailing slash.**` CLAUDE.md-specified form: `cp -R ~/.claude/skills/vv-slidev ~/Documents/.../skills/` (NO trailing slash on source). Trailing slash dumps contents into skills/, breaking mirror. For single-file updates use explicit form: `cp <live>/references/<file>.md <vault>/references/<file>.md`.
    7. `**Ghost decks retain slides: frontmatter URLs per D-03.**` The 6 draft MDX files carry `slides: "https://s.vedmich.dev/<slug>/"`. Phase 5's D-14 verification explicitly scopes `grep -rn 's\.vedmich\.dev' src/` to `src/i18n/ src/components/ src/data/` (EXCLUDING src/content/presentations/). To eliminate reference on un-draft, remove the `slides:` field — PresentationCard falls back to /slides/<slug>/ via `data.slides ??` precedence.
    - `***` divider

    H2 `## Further reading`:
    - Bullet list with cross-refs to:
      - `.github/workflows/deploy.yml` (CI pipeline, Copy Slidev decks step)
      - `.gitmodules` (submodule registration pointing at vedmichv/slidev:gh-pages)
      - `~/.claude/skills/vv-slidev/references/deployment.md` (gh-pages side SSoT for Step 2)
      - `~/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md` (short pointer back to this runbook)
      - `~/.claude/skills/vv-slidev/references/architecture-grid.md` (deck authoring in slidev-theme-vv)
      - `.planning/phases/05-slidev-integration/05-RESEARCH.md` (full Phase 5 research — decisions, pitfalls, exact diffs)
      - `.planning/phases/05-slidev-integration/05-CONTEXT.md` (17 locked decisions D-01..D-17)
      - Slidev hosting docs: `https://sli.dev/guide/hosting.html`
      - GitHub Actions `actions/checkout@v4` submodule flag docs

    Expected final line count: ~170-210 lines (well above >=150 target). Content fills 9 H2 sections (Intro not counted; When-to-use + Prerequisites + 6 Steps + Gotchas + Further reading = 9, well above >=8).

    Do NOT add hex color literals. Do NOT modify any other file in this task.
  </action>
  <verify>
    <automated>test -f docs/slides-onboarding.md && test "$(wc -l < docs/slides-onboarding.md)" -ge 150 && test "$(grep -c '^## ' docs/slides-onboarding.md)" -ge 8 && grep -q 'slidev build' docs/slides-onboarding.md && grep -q -- '--base /slides/' docs/slides-onboarding.md && grep -q 'git submodule update --remote --merge slidev' docs/slides-onboarding.md && grep -q 'cp -r slidev/' docs/slides-onboarding.md && grep -q '404.html' docs/slides-onboarding.md && grep -q 'CNAME' docs/slides-onboarding.md && grep -q '@slidev/cli' docs/slides-onboarding.md && grep -q 'branch = gh-pages' docs/slides-onboarding.md && grep -qE '^\*\*\*$' docs/slides-onboarding.md && test "$(grep -c '#[0-9a-fA-F]\{3,6\}' docs/slides-onboarding.md)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/slides-onboarding.md` succeeds
    - `wc -l < docs/slides-onboarding.md` returns >= 150 (LOC target met)
    - `grep -c '^## ' docs/slides-onboarding.md` returns >= 8 (H2 section count target met)
    - `grep -q 'slidev build'` succeeds (Step 1 command documented)
    - `grep -q -- '--base /slides/'` succeeds (trailing-slash base path documented)
    - `grep -q 'git submodule update --remote --merge slidev'` succeeds (Step 3 command documented)
    - `grep -q 'cp -r slidev/'` succeeds (Step 4 whitelist pattern documented)
    - `grep -q '404.html'` succeeds (Gotcha 2 — D-13 anti-pattern named)
    - `grep -q 'CNAME'` succeeds (Gotcha 2 — CNAME anti-pattern named)
    - `grep -q '@slidev/cli'` succeeds (Gotcha 3 — Pitfall 3 anti-pattern named)
    - `grep -q 'branch = gh-pages'` succeeds (Gotcha 5 — Pitfall 4 invariant named)
    - `grep -qE '^\*\*\*$'` succeeds (triple-asterisk dividers present)
    - `grep -c '#[0-9a-fA-F]\{3,6\}' docs/slides-onboarding.md` returns 0 (no hex literals)
    - No other files modified — `git status --porcelain | grep -v '^.. docs/slides-onboarding.md'` returns empty for this task boundary
  </acceptance_criteria>
  <done>
    docs/slides-onboarding.md authored with >=8 H2 sections, >=150 LOC, 6 numbered Steps + 7 Gotchas + Further-reading tail. All relevant research Pitfalls (1-5, 7-8) surface in the Gotchas section. CLAUDE.md §Slidev Integration (added by Plan 01 Task 3) now points at a real file.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author live skill reference at ~/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md (30-50 LOC, SSoT pointer)</name>
  <files>/Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md</files>
  <read_first>
    - /Users/viktor/.claude/skills/vv-slidev/references/deployment.md HEAD — full 96 LOC sibling reference (analog shape for a skill reference file)
    - /Users/viktor/.claude/skills/vv-slidev/SKILL.md HEAD — skim only to confirm whether SKILL.md carries a references inventory (if yes, note it as future sync target; if no, the new file is auto-discovered via glob — no SKILL.md edit needed in this task)
    - .planning/phases/05-slidev-integration/05-RESEARCH.md §"vv-slidev/references/publish-to-vedmich-dev.md outline" lines 719-752 (target shape + 30-50 LOC target)
    - .planning/phases/05-slidev-integration/05-PATTERNS.md §"9. ~/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md" lines 540-628 (exact ~32-line target + invariants)
    - .planning/phases/05-slidev-integration/05-CONTEXT.md §decisions D-10 (short pointer NOT full mirror; 3-way sync mandatory)
    - docs/slides-onboarding.md (created in Task 1) — confirm the SSoT reference exists
  </read_first>
  <action>
    Create the new file at absolute path `/Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md` (this is the LIVE skill directory, outside the vedmich.dev repo).

    Use the Write tool. Target length: 30-55 LOC (D-10 says 30-50, allow small slack for heredoc template mechanics).

    Write this exact content:

    ```markdown
    # Publishing a deck to vedmich.dev/slides/<slug>/

    Short pointer. Full runbook lives at `docs/slides-onboarding.md` in the `vedmichv/vedmich.dev` repo — that is the single source of truth. This file captures the TL;DR and quick commands; read the docs/ version for context, rationale, and gotchas.

    Complementary skill reference: `references/deployment.md` covers the *gh-pages side* (theme repo -> `vedmichv/slidev:gh-pages`). This file covers the *vedmich.dev side* (slidev repo -> main-site CI pipeline).

    ## TL;DR

    1. Build deck with `--base /slides/<slug>/` (trailing slash required) in the theme repo.
    2. Push built `dist/` to `vedmichv/slidev:gh-pages` (rm old `<slug>/` folder first, then `cp -r ... <slug>`).
    3. Bump submodule in `vedmich.dev`: `git submodule update --remote --merge slidev`.
    4. Uncomment slug in `.github/workflows/deploy.yml` whitelist loop.
    5. Push to `vedmich.dev` main — site auto-deploys in ~90s + submodule overhead.

    ## Quick commands

    ```bash
    # Step 1 — in the theme repo:
    cd ~/Documents/GitHub/vedmichv/slidev-theme-vv
    pnpm slidev build presentations/<slug>/slides.md --base /slides/<slug>/

    # Step 2 — in the artifact repo (vedmichv/slidev, gh-pages branch):
    cd ~/Documents/GitHub/vedmichv/slidev/
    rm -rf <slug> && cp -r ~/Documents/GitHub/vedmichv/slidev-theme-vv/dist <slug>
    git add <slug> && git commit -m "deploy: <slug> --base /slides/<slug>/" && git push

    # Step 3 — in vedmich.dev:
    cd ~/Documents/GitHub/vedmich/vedmich.dev/
    git submodule update --remote --merge slidev
    git add slidev && git commit -m "chore(slides): bump <slug>"

    # Step 4 — edit .github/workflows/deploy.yml; uncomment the `cp -r slidev/<slug> dist/slides/` whitelist entry.

    git add .github/workflows/deploy.yml
    git commit -m "ci(slides): activate /slides/<slug>/"
    git push
    ```

    ## Full workflow

    See `docs/slides-onboarding.md` in the `vedmichv/vedmich.dev` repo for the full 9-H2-section runbook (Prerequisites / 6 numbered Steps / Gotchas / Further reading). Don't duplicate context here — the docs/ file is SSoT.

    ## Related skill references

    - `references/deployment.md` — the gh-pages side (theme repo -> `vedmichv/slidev:gh-pages`).
    - `references/architecture-grid.md` — deck authoring inside slidev-theme-vv.
    ```

    Expected LOC: 42-48.

    Do NOT duplicate content from docs/slides-onboarding.md (gotchas, long explanations). If you catch yourself writing a full explanation, stop and refer the reader to docs/ instead. Drift prevention is the reason D-10 caps this file at 50 LOC.

    Do NOT modify any other skill file in this task (SKILL.md, other references).
  </action>
  <verify>
    <automated>test -f /Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md && test "$(wc -l < /Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md)" -ge 30 && test "$(wc -l < /Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md)" -le 55 && grep -q 'docs/slides-onboarding.md' /Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md && grep -q '^## TL;DR$' /Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md && grep -q '^## Quick commands$' /Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md && grep -q -- '--base /slides/' /Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md && grep -q 'single source of truth' /Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md && grep -q 'references/deployment.md' /Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md && test "$(grep -c '^## ' /Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md)" -ge 3</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md` succeeds
    - `wc -l` returns between 30 and 55 (D-10 target: 30-50 LOC with small slack)
    - `grep -q 'docs/slides-onboarding.md'` matches at least once (SSoT pointer present)
    - `grep -q 'single source of truth'` matches (opening sentence declares SSoT — drift-prevention invariant)
    - `grep -q '^## TL;DR$'` matches (TL;DR section present)
    - `grep -q '^## Quick commands$'` matches (Quick commands section present)
    - `grep -q -- '--base /slides/'` matches (trailing-slash base path in commands)
    - `grep -q 'references/deployment.md'` matches (cross-ref to sibling skill reference)
    - `grep -c '^## '` returns >= 3 (TL;DR + Quick commands + Full workflow + Related skill references = 4; acceptance gate allows slack at >= 3)
  </acceptance_criteria>
  <done>
    Live skill reference authored per D-10 — 30-50 LOC, opens with SSoT declaration, TL;DR + Quick commands + pointer back to docs/, no duplication. Ready for Task 3 mirror + commit.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Mirror skill reference to vault + commit vault (three-way sync per CLAUDE.md)</name>
  <files>/Users/viktor/Documents/ViktorVedmich/80-Resources/85-Scripts/85.20-Claude-Code/skills/vv-slidev/references/publish-to-vedmich-dev.md</files>
  <read_first>
    - /Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md (created in Task 2) — source to mirror
    - ~/.claude/CLAUDE.md §"Skill updates — three-way sync" (user's global instructions) — mandatory protocol: edit live FIRST, mirror SECOND, commit vault THIRD with specific message format
    - .planning/phases/05-slidev-integration/05-CONTEXT.md §decisions D-10 (three-way sync target paths)
    - .planning/phases/05-slidev-integration/05-PATTERNS.md §"10. Vault mirror" lines 631-650 (cp -R gotcha + vault commit message shape)
    - .planning/phases/05-slidev-integration/05-RESEARCH.md §"Common Pitfalls §Pitfall 5" lines 386-390 (cp -R trailing-slash gotcha; explicit-file-path form avoids this)
  </read_first>
  <action>
    Two operations: file mirror + vault commit.

    **Operation A — Mirror the file from live skill to vault.**

    Use the explicit-file-path form (preferred for single-file updates per CLAUDE.md — no trailing-slash gotcha):

    ```bash
    cp /Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md \
       /Users/viktor/Documents/ViktorVedmich/80-Resources/85-Scripts/85.20-Claude-Code/skills/vv-slidev/references/publish-to-vedmich-dev.md
    ```

    Verify byte-identical:

    ```bash
    diff /Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md \
         /Users/viktor/Documents/ViktorVedmich/80-Resources/85-Scripts/85.20-Claude-Code/skills/vv-slidev/references/publish-to-vedmich-dev.md
    # Expected: empty output (diff returns nothing on identical files)
    ```

    If diff shows content, mirror is broken — re-run cp. Common cause: `cp -R` with trailing slash on source (Pitfall 5). The explicit-file form above has no trailing-slash hazard.

    **Operation B — Commit the vault.**

    Vault repo: `~/Documents/ViktorVedmich/`. Stage new file, commit with CLAUDE.md-prescribed message:

    ```bash
    cd ~/Documents/ViktorVedmich/
    git add 80-Resources/85-Scripts/85.20-Claude-Code/skills/vv-slidev/references/publish-to-vedmich-dev.md
    git commit -m "vault backup: skill vv-slidev — add publish-to-vedmich-dev.md reference"
    ```

    Verify commit landed:

    ```bash
    cd ~/Documents/ViktorVedmich/
    git log -1 --format='%s' -- 80-Resources/85-Scripts/85.20-Claude-Code/skills/vv-slidev/references/publish-to-vedmich-dev.md
    # Expected output (exact): vault backup: skill vv-slidev — add publish-to-vedmich-dev.md reference
    ```

    **Do NOT push** the vault. Per CLAUDE.md, the vault is a personal git repo on `main` with its own rhythm; SessionEnd hook also syncs it. Just `git add + git commit` locally.

    **Do NOT modify the live skill in this task** — live is authoritative, vault is backup.

    **Do NOT use `cp -R` with a trailing slash on source path.** Per CLAUDE.md:
    - CORRECT: `cp -R ~/.claude/skills/vv-slidev ~/Documents/.../skills/`  (no trailing slash on source)
    - WRONG: `cp -R ~/.claude/skills/vv-slidev/ ~/Documents/.../skills/`  (trailing slash dumps contents into skills/, drops the vv-slidev/ dir level, breaks mirror)

    For this single-file update use the explicit-file form from Operation A — it has no trailing-slash hazard at all.
  </action>
  <verify>
    <automated>test -f /Users/viktor/Documents/ViktorVedmich/80-Resources/85-Scripts/85.20-Claude-Code/skills/vv-slidev/references/publish-to-vedmich-dev.md && diff -q /Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md /Users/viktor/Documents/ViktorVedmich/80-Resources/85-Scripts/85.20-Claude-Code/skills/vv-slidev/references/publish-to-vedmich-dev.md && cd /Users/viktor/Documents/ViktorVedmich && git log -1 --format='%s' -- 80-Resources/85-Scripts/85.20-Claude-Code/skills/vv-slidev/references/publish-to-vedmich-dev.md | grep -q 'vault backup: skill vv-slidev — add publish-to-vedmich-dev.md reference'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /Users/viktor/Documents/ViktorVedmich/80-Resources/85-Scripts/85.20-Claude-Code/skills/vv-slidev/references/publish-to-vedmich-dev.md` succeeds (vault mirror file exists)
    - `diff -q <live> <vault>` returns empty (byte-identical — no drift)
    - `wc -l <vault>` returns the same value as `wc -l <live>` (redundant with byte-identical, but explicit LOC check useful for debugging)
    - `cd ~/Documents/ViktorVedmich && git log -1 --format='%s' -- <vault-path>` prints exactly `vault backup: skill vv-slidev — add publish-to-vedmich-dev.md reference` (commit message format per CLAUDE.md)
    - `cd ~/Documents/ViktorVedmich && git status --porcelain -- 80-Resources/85-Scripts/85.20-Claude-Code/skills/vv-slidev/references/publish-to-vedmich-dev.md` returns empty (file is committed, not staged or untracked)
    - `cd ~/Documents/ViktorVedmich && git log --oneline -1` shows the new vault commit as HEAD
    - Live skill file unchanged between Task 2 done-state and Task 3 done-state (Operation A is a one-way copy; no edits to live in this task)
  </acceptance_criteria>
  <done>
    Three-way sync complete: live at ~/.claude/skills/, vault at 85.20-Claude-Code/skills/, vault commit with exact CLAUDE.md-prescribed message. `diff` between live + vault is empty. Vault commit visible in `git log`. CONTENT-04 shipped.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks:

```bash
# Runbook
test -f docs/slides-onboarding.md
test "$(wc -l < docs/slides-onboarding.md)" -ge 150
test "$(grep -c '^## ' docs/slides-onboarding.md)" -ge 8
grep -q -- '--base /slides/' docs/slides-onboarding.md
grep -q '404.html' docs/slides-onboarding.md

# Live skill reference
test -f /Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md
test "$(wc -l < /Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md)" -ge 30
test "$(wc -l < /Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md)" -le 55
grep -q 'docs/slides-onboarding.md' /Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md

# Vault mirror
test -f /Users/viktor/Documents/ViktorVedmich/80-Resources/85-Scripts/85.20-Claude-Code/skills/vv-slidev/references/publish-to-vedmich-dev.md
diff -q /Users/viktor/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md \
        /Users/viktor/Documents/ViktorVedmich/80-Resources/85-Scripts/85.20-Claude-Code/skills/vv-slidev/references/publish-to-vedmich-dev.md

# Vault commit
cd /Users/viktor/Documents/ViktorVedmich && git log -1 --format='%s' -- 80-Resources/85-Scripts/85.20-Claude-Code/skills/vv-slidev/references/publish-to-vedmich-dev.md | grep -q 'vault backup: skill vv-slidev'

# CLAUDE.md pointer from Plan 01 Task 3 now points at a real file
cd - && grep -q 'docs/slides-onboarding.md' CLAUDE.md
grep -q 'publish-to-vedmich-dev.md' CLAUDE.md
```
</verification>

<success_criteria>
- [ ] SLIDES-06: docs/slides-onboarding.md exists, >=150 LOC, >=8 H2 sections, 6 numbered Steps + Gotchas + Further reading
- [ ] SLIDES-06: Runbook names all relevant Pitfalls (--base trailing slash, D-13 no-root-copy, Pitfall 3 no-Slidev-deps, Pitfall 4 branch=gh-pages, Pitfall 5 cp -R gotcha, Pitfall 7 branch-switch submodule sync, D-03 slides: retention)
- [ ] CONTENT-04: Live skill file at ~/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md exists, 30-55 LOC, opens with SSoT declaration
- [ ] CONTENT-04: Vault mirror byte-identical to live (diff empty)
- [ ] CONTENT-04: Vault commit with exact CLAUDE.md-prescribed message present
- [ ] Plan 01 Task 3's CLAUDE.md pointers (`**Runbook:** docs/slides-onboarding.md` + `**Skill:** ~/.claude/skills/vv-slidev/references/publish-to-vedmich-dev.md`) now point at real files
</success_criteria>

<output>
Create `.planning/phases/05-slidev-integration/05-03-SUMMARY.md`. Include:
- `requirements-covered: [SLIDES-06, CONTENT-04]`
- docs/slides-onboarding.md final LOC + H2 count
- Live skill file + vault mirror LOC + diff status (expected empty)
- Vault commit SHA (via `git log -1 --format='%h' -- <vault-path>` from ~/Documents/ViktorVedmich/)
- Any deviations (e.g., if SKILL.md needed updating because it carries an explicit references inventory)
- Next: Plan 04 (Wave 3) closes traceability in REQUIREMENTS / ROADMAP / STATE
</output>
</content>
