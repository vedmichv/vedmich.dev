---
phase: 12-footer-match
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Footer.astro
autonomous: true
requirements: [REQ-012]
tags: [component, footer, i18n, tokens, v0.4-closer]

must_haves:
  truths:
    - "REQ-012: Footer matches reference `app.jsx:640-648`  -  simple two-column flex, `(c) {year} Viktor Vedmich` left, `Built with Astro` right, 32px vertical padding, top border"
    - "D-01: `src/components/Footer.astro` contains NO `socialLinks` import and NO `<svg>` tags (5 social-icon SVG blocks deleted)"
    - "D-02: Wrapper uses `border-t border-border` (solid, no `/50` alpha); no `bg-surface/30` class; inner container uses `max-w-[1120px]` (not `max-w-6xl`) with mobile `px-4 sm:px-6` guard"
    - "D-03: Dynamic year via `new Date().getFullYear()` preserved; `i.footer.copyright.replace('{year}', String(year))` + `i.footer.built_with` both rendered"
    - "D-04: Font size is `text-[13px]` (not `text-sm`); bilingual i18n preserved (both `footer.copyright` and `footer.built_with` consumed from i18n JSON)"
    - "D-05: Final shape matches CONTEXT.md target verbatim  -  14 lines (down from 43), single `<footer>` with inner flex `<div>` and two `<span>` siblings"
    - "Both `/en/` and `/ru/` dist HTML render symmetric footer markup (text content differs, structure identical)"
    - "No hardcoded hex in `Footer.astro`; no deprecated cyan (#06B6D4 / #22D3EE)"
    - "Computed font-size of footer `<div>` at 1440px = 13px"
    - "`npm run build` passes with 7 pages; v0.4 milestone closes with Phase 12"
  artifacts:
    - path: "src/components/Footer.astro"
      provides: "Reference-matched minimal two-column footer (REQ-012)"
      min_lines: 12
      max_lines: 18
      contains: 'max-w-[1120px]'
  key_links:
    - from: "src/components/Footer.astro"
      to: "src/i18n/{en,ru}.json"
      via: "t helper reads i.footer.copyright + i.footer.built_with"
      pattern: "i\\.footer\\.(copyright|built_with)"
    - from: "src/components/Footer.astro"
      to: "src/styles/design-tokens.css"
      via: "Tailwind @theme bridge  -  text-text-muted + border-border utilities"
      pattern: "text-text-muted|border-border"
    - from: "src/layouts/BaseLayout.astro:72"
      to: "src/components/Footer.astro"
      via: "Props contract render - BaseLayout consumer at line 72"
      pattern: "<Footer"
---

<objective>
Rewrite `src/components/Footer.astro` to match the Deep Signal reference UI kit
footer (`app.jsx:640-648`) — a minimal two-column flex layout. Delete 5 inline
social-icon SVGs and the now-unused `socialLinks` import (contacts already live
in the Contact section via Phase 10 letter-badges). Swap container width to
`max-w-[1120px]` to align with every other v0.4 section (Hero, Presentations,
Book, Speaking, Blog). Drop `bg-surface/30` background and border `/50` alpha
to solid `border-border`. Bump font size from `text-sm` (14px) to `text-[13px]`
for pixel-match with reference. Preserve bilingual i18n (`i.footer.copyright` +
`i.footer.built_with`) and dynamic `new Date().getFullYear()`.

Purpose: Close D-01…D-05 of Phase 12 and satisfy REQ-012. This is the final
phase of the v0.4-reference-audit milestone — once this ships and builds green,
the live site matches the reference UI kit across every homepage section.

Output: 1 file rewrite (43 → ~14 lines; ≥29 lines deleted). Zero i18n edits,
zero token edits, zero new dependencies, zero changes to `src/data/social.ts`
(the `socialLinks` export stays — Contact section still consumes it).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-footer-match/12-CONTEXT.md
@.planning/phases/12-footer-match/12-PATTERNS.md
@src/components/Footer.astro
@src/components/Hero.astro
@src/layouts/BaseLayout.astro
@src/i18n/en.json
@src/i18n/ru.json

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase; no codebase exploration required. -->

Consumer contract (BaseLayout.astro line 72 invokes the component):
  `<Footer locale={...} />` where the attribute value is the current locale ("en" or "ru").

Props contract (unchanged from current Footer.astro; MUST be preserved):
  - Single typed prop named "locale" with type Locale imported from ../i18n/utils.
  - `interface Props` shape declared in the Astro frontmatter script block.
  - Destructured inline via Astro.props, then aliased through t(...) helper.

i18n keys (already bilingual; both keys live at lines 96-99 of en.json and ru.json):
  - footer.built_with (EN = "Built with Astro", RU = Cyrillic equivalent already present)
  - footer.copyright (EN and RU templates both contain the literal placeholder {year})

Design tokens consumed (design-tokens.css + @theme bridge in global.css):
  - --text-muted at hex 78909C maps to Tailwind utility text-text-muted (matches reference VV.muted).
  - --border at hex 334155 maps to Tailwind utility border-border (matches reference VV.border).

Reference target (from viktor-vedmich-design skill ui_kits/vedmich-dev/app.jsx lines 640-648):
  - `<footer>` element with inline CSS padding 32px/24px and top border 1px solid VV.border.
  - Inner `<div>` with maxWidth 1120, margin auto, flex space-between, body font 13px, color muted.
  - Two `<span>` siblings: left is copyright with year, right is "Built with Astro".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite Footer.astro to reference target (43 → ~14 lines) and verify build</name>
  <files>src/components/Footer.astro</files>
  <read_first>
    - /Users/viktor/Documents/GitHub/vedmich/vedmich.dev/src/components/Footer.astro (current 43-line implementation with 5 SVG blocks + socialLinks import — this is the deletion target)
    - /Users/viktor/Documents/GitHub/vedmich/vedmich.dev/.planning/phases/12-footer-match/12-CONTEXT.md (source of truth — D-01…D-05, target code in D-05, 10 acceptance criteria)
    - /Users/viktor/Documents/GitHub/vedmich/vedmich.dev/.planning/phases/12-footer-match/12-PATTERNS.md (analog references: Hero.astro frontmatter + Presentations.astro container + reference UI kit mapping table)
    - /Users/viktor/Documents/GitHub/vedmich/vedmich.dev/src/components/Hero.astro (lines 1-10 — invariant frontmatter pattern; `import { t, type Locale } from '../i18n/utils';` + `interface Props { locale: Locale; }` + `const { locale } = Astro.props;` + `const i = t(locale);`)
    - /Users/viktor/Documents/GitHub/vedmich/vedmich.dev/src/layouts/BaseLayout.astro (line 72 — consumer contract `<Footer locale={locale} />`; must remain valid)
  </read_first>
  <action>
**Step 1 — Replace `src/components/Footer.astro` wholesale.**

Use the Write tool (not Bash heredoc) to overwrite the file with EXACTLY this 14-line content (lifted verbatim from CONTEXT.md D-05 "Final Footer.astro target shape"):

Astro component script block (between `---` fences in the saved file):

    import { t, type Locale } from '../i18n/utils';

    interface Props { locale: Locale; }

    const { locale } = Astro.props;
    const i = t(locale);
    const year = new Date().getFullYear();

Astro component markup block (after the script `---` fence in the saved file):

    <footer class="border-t border-border">
      <div class="max-w-[1120px] mx-auto px-4 sm:px-6 py-8 flex items-center justify-between text-[13px] text-text-muted">
        <span>{i.footer.copyright.replace('{year}', String(year))}</span>
        <span>{i.footer.built_with}</span>
      </div>
    </footer>

When saving the file, join the two blocks with the standard Astro frontmatter fence (a line containing exactly three hyphens before the script, a blank line, and another three-hyphen line after the script). The target shape is ALSO reproduced verbatim in `.planning/phases/12-footer-match/12-CONTEXT.md` under D-05 — use that as the authoritative reference if any ambiguity arises.

**Five changes from the current file, each traced to a CONTEXT.md decision:**

1. **D-01 — Remove `socialLinks` import + 5 SVG blocks.** Delete line 3 (`import { socialLinks } from '../data/social';`) entirely. Delete the whole `<div class="flex items-center gap-4">…socialLinks.map…</div>` block (current lines 17-36) including all 5 inline `<path>` SVGs for linkedin/github/youtube/twitter/telegram. Do NOT delete `src/data/social.ts` or its `socialLinks` export — Contact section (Phase 10) still consumes it.

2. **D-02 — Container + bg + border alignment.**
   - `<footer class="border-t border-border/50 bg-surface/30">` → `<footer class="border-t border-border">` (drop `/50` alpha, drop `bg-surface/30`)
   - `<div class="max-w-6xl mx-auto px-4 sm:px-6 py-8">` → inner div becomes `<div class="max-w-[1120px] mx-auto px-4 sm:px-6 py-8 flex items-center justify-between text-[13px] text-text-muted">` (switch `max-w-6xl` (1152px) → `max-w-[1120px]`; `px-4 sm:px-6` mobile guard PRESERVED per D-02 rationale; merge flex + font-size + color classes onto this single div)
   - Delete the nested wrapping `<div class="flex flex-col sm:flex-row items-center justify-between gap-4">` — no longer needed after collapsing to two sibling `<span>`s

3. **D-03 — Dynamic year + i18n-template preserved.** Keep `const year = new Date().getFullYear();` in frontmatter. Keep `{i.footer.copyright.replace('{year}', String(year))}` as the left span's text. Do NOT hardcode "2026" even though the reference artifact does — reference was a 2026-snapshot, dynamic year auto-rolls forward.

4. **D-04 — `text-[13px]` + bilingual i18n.** Replace `text-sm` (= 14px) with `text-[13px]` (Tailwind arbitrary value). Keep `text-text-muted` (= `--text-muted` = `#78909C`, matches reference `VV.muted`). Do NOT revert "Built with Astro" to English-only — keep the bilingual `{i.footer.built_with}` consumer so RU renders "Создано на Astro".

5. **D-05 — Two separate `<span>` siblings, not a single span with `&middot;`.** Current line 38 renders both texts in one `<div>` separated by `&middot;`. New shape uses `flex items-center justify-between` on the parent and TWO sibling `<span>`s so the texts visually split to opposite edges (reference behavior: left/right alignment via `justifyContent: 'space-between'`).

**Invariants that MUST stay identical to current code:**
- Frontmatter `interface Props { locale: Locale; }` — `BaseLayout.astro:72` calls `<Footer locale={locale} />`
- `import { t, type Locale } from '../i18n/utils';` — first import line
- `const { locale } = Astro.props;` + `const i = t(locale);` — destructure + alias
- `const year = new Date().getFullYear();` — dynamic year constant
- `px-4 sm:px-6` mobile padding guard (CONTEXT D-02 accepts divergence from reference's literal `24px`)
- `py-8` vertical padding (= 32px, matches reference)
- Zero `<style>` block needed — all tokens flow through Tailwind utilities

**Hex hygiene (CLAUDE.md):** No hex literals in the new file. Especially no `#06B6D4` / `#22D3EE` (deprecated cyan) — both were already absent, stay absent.

**Step 2 — Build verification.** After writing the file, run from repo root:

```
cd /Users/viktor/Documents/GitHub/vedmich/vedmich.dev && npm run build 2>&1 | tee /tmp/ph12-build.log
```

Must exit 0 and build exactly 7 pages. If Astro complains about an unknown import, the `socialLinks` reference was not fully removed — re-read the new file and delete any leftover `socialLinks.map(...)` traces.

**Step 3 — Dist markup symmetry check.** After `npm run build` completes, verify EN and RU dist HTML render symmetric footer markup:

```
grep -c 'max-w-\[1120px\]' dist/en/index.html
grep -c 'max-w-\[1120px\]' dist/ru/index.html
grep -c 'Built with Astro' dist/en/index.html
grep -c 'Создано на Astro' dist/ru/index.html
```

Both dist pages must contain the new container width; EN must contain "Built with Astro" exactly once; RU must contain "Создано на Astro" exactly once. Neither must contain the other's string (no leakage).

Do NOT edit `src/i18n/en.json`, `src/i18n/ru.json`, `src/data/social.ts`, or any other file. This is a single-file rewrite.
  </action>
  <verify>
    <automated>cd /Users/viktor/Documents/GitHub/vedmich/vedmich.dev && test -f src/components/Footer.astro && [ "$(wc -l < src/components/Footer.astro)" -le 18 ] && [ "$(wc -l < src/components/Footer.astro)" -ge 12 ] && ! grep -q 'socialLinks' src/components/Footer.astro && ! grep -q '<svg' src/components/Footer.astro && ! grep -q '<path' src/components/Footer.astro && ! grep -q 'bg-surface/30' src/components/Footer.astro && ! grep -q 'border-border/50' src/components/Footer.astro && ! grep -q 'max-w-6xl' src/components/Footer.astro && ! grep -q 'text-sm' src/components/Footer.astro && [ "$(grep -c 'max-w-\[1120px\]' src/components/Footer.astro)" = "1" ] && [ "$(grep -c 'text-\[13px\]' src/components/Footer.astro)" = "1" ] && [ "$(grep -c 'border-t border-border' src/components/Footer.astro)" = "1" ] && [ "$(grep -c 'new Date().getFullYear()' src/components/Footer.astro)" = "1" ] && [ "$(grep -c 'i.footer.copyright' src/components/Footer.astro)" = "1" ] && [ "$(grep -c 'i.footer.built_with' src/components/Footer.astro)" = "1" ] && [ "$(grep -c 'px-4 sm:px-6' src/components/Footer.astro)" = "1" ] && [ "$(grep -c 'flex items-center justify-between' src/components/Footer.astro)" = "1" ] && [ "$(grep -c '<span>' src/components/Footer.astro)" = "2" ] && ! grep -qE '#06B6D4|#22D3EE' src/components/Footer.astro && ! grep -qE '#[0-9A-Fa-f]{3,6}' src/components/Footer.astro && npm run build 2>&1 | tee /tmp/ph12-build.log && grep -qE '7 page\(s\) built|=== 7 pages' /tmp/ph12-build.log && test -f dist/en/index.html && test -f dist/ru/index.html && [ "$(grep -c 'max-w-\[1120px\]' dist/en/index.html)" -ge "1" ] && [ "$(grep -c 'max-w-\[1120px\]' dist/ru/index.html)" -ge "1" ] && grep -q 'Built with Astro' dist/en/index.html && grep -q 'Создано на Astro' dist/ru/index.html && ! grep -q 'Создано на Astro' dist/en/index.html && ! grep -q 'Built with Astro' dist/ru/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/Footer.astro` exits 0
    - `wc -l < src/components/Footer.astro` returns a number between 12 and 18 inclusive (target 14; LOC delta 43 → ~14 lines)
    - `grep -c 'socialLinks' src/components/Footer.astro` returns `0` (D-01 — import removed, no residual references)
    - `grep -c '<svg' src/components/Footer.astro` returns `0` (D-01 — all 5 SVG blocks deleted)
    - `grep -c '<path' src/components/Footer.astro` returns `0` (D-01 — all 5 inline `<path>` literals deleted)
    - `grep -c 'bg-surface/30' src/components/Footer.astro` returns `0` (D-02 — background dropped)
    - `grep -c 'border-border/50' src/components/Footer.astro` returns `0` (D-02 — alpha dropped)
    - `grep -c 'max-w-6xl' src/components/Footer.astro` returns `0` (D-02 — old container width replaced)
    - `grep -c 'text-sm' src/components/Footer.astro` returns `0` (D-04 — old font size replaced)
    - `grep -c 'max-w-\[1120px\]' src/components/Footer.astro` returns `1` (D-02 — new canonical v0.4 width)
    - `grep -c 'text-\[13px\]' src/components/Footer.astro` returns `1` (D-04 — reference pixel-match)
    - `grep -c 'border-t border-border' src/components/Footer.astro` returns `1` (D-02 — solid border, no alpha)
    - `grep -c 'new Date().getFullYear()' src/components/Footer.astro` returns `1` (D-03 — dynamic year preserved)
    - `grep -c 'i.footer.copyright' src/components/Footer.astro` returns `1` (D-03/D-04 — i18n consumer preserved)
    - `grep -c 'i.footer.built_with' src/components/Footer.astro` returns `1` (D-04 — bilingual i18n preserved)
    - `grep -c 'px-4 sm:px-6' src/components/Footer.astro` returns `1` (D-02 — mobile guard preserved)
    - `grep -c 'flex items-center justify-between' src/components/Footer.astro` returns `1` (D-05 — single flex container, two spans)
    - `grep -c '<span>' src/components/Footer.astro` returns `2` (D-05 — exactly two sibling spans, opposite edges)
    - `grep -c '<footer' src/components/Footer.astro` returns `1` (single footer element)
    - `grep -cE '#06B6D4|#22D3EE' src/components/Footer.astro` returns `0` (CLAUDE.md — no deprecated cyan)
    - `grep -cE '#[0-9A-Fa-f]{3,6}' src/components/Footer.astro` returns `0` (CLAUDE.md — no hex literals at all, tokens only)
    - `grep -c 'interface Props' src/components/Footer.astro` returns `1` (Props contract preserved; BaseLayout.astro:72 consumer still valid)
    - `grep -c "from '../i18n/utils'" src/components/Footer.astro` returns `1` (only import, nothing from `../data/social`)
    - `npm run build` exits 0
    - Build log matches either `7 page\(s\) built` or `=== 7 pages` (Astro success marker)
    - `test -f dist/en/index.html` AND `test -f dist/ru/index.html` after build
    - `grep -c 'max-w-\[1120px\]' dist/en/index.html` returns ≥ 1 (Hero + Footer both use this width; ≥1 is sufficient proof the Footer rendered)
    - `grep -c 'max-w-\[1120px\]' dist/ru/index.html` returns ≥ 1 (same, RU locale)
    - `grep -q 'Built with Astro' dist/en/index.html` succeeds (EN footer text present)
    - `grep -q 'Создано на Astro' dist/ru/index.html` succeeds (RU footer text present)
    - `grep -q 'Создано на Astro' dist/en/index.html` FAILS (no RU leakage into EN)
    - `grep -q 'Built with Astro' dist/ru/index.html` FAILS (no EN leakage into RU)
  </acceptance_criteria>
  <done>
    `src/components/Footer.astro` is 12-18 lines, imports only from `../i18n/utils`,
    contains zero `<svg>`/`<path>`/`socialLinks`/`bg-surface/30`/`border-border/50`/
    `max-w-6xl`/`text-sm`/hex literals, uses `max-w-[1120px]` + `text-[13px]` +
    `border-t border-border` + two sibling `<span>`s inside a `flex items-center
    justify-between` container. `npm run build` green with 7 pages. Both EN and
    RU dist pages render the footer with locale-appropriate copy and no leakage.
    REQ-012 satisfied; v0.4 milestone closer complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User browser ← dist HTML | Footer markup + i18n strings are rendered into every page and served to every visitor |
| Source JSON → compiled HTML | `src/i18n/{en,ru}.json` → `t(locale)` helper → inline text; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | `© {year} Viktor Vedmich` in footer | accept | Public brand identity (same string already shipped on every page since project inception). Year derived from `new Date()` at build time, not runtime — no user-data exposure. |
| T-12-02 | Tampering | `new Date().getFullYear()` at SSG build time | accept | Astro is a static-site generator; date is captured at build time and frozen in dist HTML. No runtime clock drift, no user-input injection. Dynamic year updates on next `npm run build` (each push to main triggers a fresh build). |
| T-12-03 | Spoofing | `i18n` key lookup `i.footer.copyright` / `i.footer.built_with` | accept | Keys are hardcoded string literals at build time — no user input routes to `t(locale)`. Locale is constrained to `'en' \| 'ru'` by the `Locale` type and validated by the `BaseLayout` consumer. |
| T-12-04 | Elevation | Deletion of `socialLinks` import | mitigate | The `socialLinks` export in `src/data/social.ts` is NOT deleted — only the Footer import is removed. Contact section (`src/components/Contact.astro:3`, Phase 10) still consumes it. Verified via grep in 12-PATTERNS.md "Removed Dependency — Dead-Code Note". If the data export were deleted, Phase 10 Contact letter-badges would break. Acceptance criterion `grep -c 'socialLinks' src/components/Footer.astro = 0` enforces Footer-scoped removal only. |
| T-12-05 | Denial of Service | Build-time regression (malformed Astro template) | mitigate | `<automated>` gate runs `npm run build` and asserts exit 0 + `7 pages` marker. Any template syntax error fails the gate. Additionally, 26 grep-checkable acceptance criteria structurally validate the rewritten file before the build even runs — catch-fast. |
</threat_model>

<verification>
End-to-end verification from repo root after Task 1 completes:

```
cd /Users/viktor/Documents/GitHub/vedmich/vedmich.dev

# 1. Structural gates (26 grep checks — see <acceptance_criteria>)
#    Line count, no socialLinks/SVGs/bg-surface/30/max-w-6xl/text-sm/hex,
#    has max-w-[1120px]/text-[13px]/border-t border-border/px-4 sm:px-6/
#    flex items-center justify-between/exactly 2 <span>s/1 <footer>/
#    interface Props/import from i18n/utils only.

# 2. Build gate
npm run build   # must exit 0 with 7 pages

# 3. Dist symmetry
grep -c 'max-w-\[1120px\]' dist/en/index.html   # >= 1
grep -c 'max-w-\[1120px\]' dist/ru/index.html   # >= 1
grep -q 'Built with Astro' dist/en/index.html   # EN footer text
grep -q 'Создано на Astro' dist/ru/index.html   # RU footer text
! grep -q 'Создано на Astro' dist/en/index.html # no RU leakage
! grep -q 'Built with Astro' dist/ru/index.html # no EN leakage

# 4. Hex hygiene (CLAUDE.md)
! grep -qE '#[0-9A-Fa-f]{3,6}' src/components/Footer.astro

# 5. Consumer contract preserved
grep -q '<Footer locale={locale} />' src/layouts/BaseLayout.astro
```

All 5 gate groups must pass. Task 1's `<automated>` command composes them into a single one-liner so the executor gets a single PASS/FAIL signal.

**Human visual verify (post-push, not blocking this plan):**
After push to main, run Playwright on live vedmich.dev at 1440px + 375px, EN + RU:
- Footer renders flush with edge of `max-w-[1120px]` container (aligned with Hero, Presentations above).
- Left span shows `© 2026 Viktor Vedmich` (EN) / `© 2026 Виктор Ведмич` (RU).
- Right span shows `Built with Astro` (EN) / `Создано на Astro` (RU).
- 32px vertical padding (`py-8`).
- Solid `#334155` top border; no `#1E293B` surface tint behind.
- Font size visually 13px (slightly smaller than before).
- No 5 social icons below — clean two-column text layout only.
</verification>

<success_criteria>
- [ ] `src/components/Footer.astro` matches CONTEXT.md D-05 target shape verbatim (14 lines, single-file rewrite).
- [ ] D-01 satisfied: no `socialLinks` import, no `<svg>` tags, no `<path>` literals remain.
- [ ] D-02 satisfied: `border-t border-border` (solid), `max-w-[1120px]` (not `max-w-6xl`), no `bg-surface/30`, mobile `px-4 sm:px-6` preserved.
- [ ] D-03 satisfied: `new Date().getFullYear()` + `i.footer.copyright.replace('{year}', ...)` both present.
- [ ] D-04 satisfied: `text-[13px]` (not `text-sm`), `i.footer.built_with` consumer preserved (bilingual).
- [ ] D-05 satisfied: exactly 2 sibling `<span>`s inside `flex items-center justify-between` container.
- [ ] All 10 CONTEXT.md acceptance criteria pass (covered by the 26 grep-checkable `<acceptance_criteria>` in Task 1).
- [ ] `npm run build` exits 0 with 7 pages.
- [ ] EN and RU dist pages render symmetric footer markup with locale-appropriate copy; zero leakage.
- [ ] Zero hardcoded hex literals in `Footer.astro`; zero deprecated cyan anywhere.
- [ ] REQ-012 closed; v0.4-reference-audit milestone closes on next push to main.
</success_criteria>

<output>
After completion, create `.planning/phases/12-footer-match/12-01-SUMMARY.md`.

Record:
- Final line count of `src/components/Footer.astro` (target: 14; acceptable 12-18).
- LOC delta (43 → new count).
- Build duration from `npm run build` (baseline: ~830ms for 7 pages).
- Confirmation that all 26 grep-checkable acceptance criteria passed.
- Confirmation that `src/data/social.ts socialLinks` export is untouched (Contact Phase 10 still consumes it).
- Any deviations from D-01…D-05 (should be zero).
- v0.4 milestone closure note — Phase 12 is the final phase; after push to main + human visual verify, the v0.4-reference-audit milestone ships.
</output>
