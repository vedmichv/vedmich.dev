---
phase: 02-code-block-upgrades
plan: 06
type: execute
wave: 5
depends_on: ["02-05"]
files_modified:
  - tests/visual/shiki-regression.spec.ts-snapshots/
  - .planning/phases/02-code-block-upgrades/baselines/
  - package.json
autonomous: false
requirements:
  - CODE-05
  - CODE-01
  - CODE-02
  - CODE-03
  - CODE-04
tags: [regression, baseline, astro-pin-restore, sign-off]

must_haves:
  truths:
    - "8 new baseline PNGs reflect the Deep Signal + badge + new Copy UX state (post-Phase-2 visual)"
    - "All 8 shiki-regression.spec.ts tests pass green against the new baselines"
    - "Manual visual audit on 4 posts × 2 locales passes (no layout breakage, no color regressions, no contrast issues)"
    - "Astro caret restored from 5.18.0 → ^5.18.0 in package.json (D-09 post-merge step)"
    - "`.planning/phases/02-code-block-upgrades/baselines/` updated to match new visual state (phase-artifacts snapshot)"
  artifacts:
    - path: "tests/visual/shiki-regression.spec.ts-snapshots/"
      provides: "8 post-Phase-2 baseline PNGs used by Playwright diff gate"
      min_files: 8
    - path: ".planning/phases/02-code-block-upgrades/baselines/"
      provides: "Mirror of Playwright baselines for phase-artifacts record"
      min_files: 8
    - path: "package.json"
      provides: "Astro caret restored to ^5.18.0"
      contains: '"astro": "^5.18.0"'
  key_links:
    - from: "shiki-regression.spec.ts"
      to: "new baselines"
      via: "Playwright --update-snapshots + subsequent re-run"
      pattern: "toHaveScreenshot"
---

<objective>
Close out Phase 2: re-capture the 8 screenshot baselines to reflect the complete Deep Signal + badge + new Copy UX visual state, verify they pass on re-run, run a manual audit on all 4 posts × 2 locales, restore the Astro caret in package.json (`5.18.0` → `^5.18.0` per D-09 post-merge restore), and sign off.

Purpose: Locks in the new visual state as the regression reference for Phase 3+ work. Removes the exact-version pin from D-09 now that Phase 2 changes have landed cleanly (Pitfall 5 mitigation no longer needed — no risk of an accidental minor bump mid-phase). A checkpoint task gates human sign-off on visual and Copy UX before caret restore commits.

Output: Updated baselines (both Playwright snapshot dir and phase-artifacts mirror), passing regression gate, reverted caret on Astro, and a human-verified checkpoint confirming the visual delta matches intent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-code-block-upgrades/02-CONTEXT.md
@.planning/phases/02-code-block-upgrades/02-RESEARCH.md
@.planning/phases/02-code-block-upgrades/02-PATTERNS.md
@.planning/phases/02-code-block-upgrades/02-VALIDATION.md
@.planning/phases/02-code-block-upgrades/02-05-SUMMARY.md
@tests/visual/shiki-regression.spec.ts
@package.json

<interfaces>
<!-- References for the executor. -->

Baseline re-capture command (Playwright):
```bash
npx playwright test tests/visual/shiki-regression.spec.ts --update-snapshots
```
- Regenerates 8 PNGs in `tests/visual/shiki-regression.spec.ts-snapshots/`
- Each PNG reflects the current build's visual state (post-Plan-02-05)

Phase-artifacts mirror copy (per D-08b + PATTERNS.md):
```bash
cp tests/visual/shiki-regression.spec.ts-snapshots/*.png \
   .planning/phases/02-code-block-upgrades/baselines/
```
- Keeps the 8 PNGs browseable as phase artifacts (historical record)
- Playwright's diff gate reads from the snapshots dir, not the phase-artifacts dir

Astro pin restore (package.json diff):
```diff
-  "astro": "5.18.0",
+  "astro": "^5.18.0",
```
- One character changes (`5` → `^5`)
- No lockfile regeneration needed unless the resolved version changes (it won't — 5.18.0 already installed)

Expected visual delta from baseline (pre-phase) → new baseline (post-phase):
- `.prose pre.shiki`: bg #24292e → #0D1117 (Deep Signal code bg)
- Token colors remapped (teal keywords, amber strings, softer teal numbers/types)
- `<figure class="code-block">` introduces top padding for the badge + Copy button row
- `<span class="code-lang-badge">` visible top-left in muted uppercase JetBrains Mono
- `<button class="code-copy-btn">` visible top-right with lucide:copy icon (no "COPY" label at rest)
- On hover: button expands to icon + COPY label
- Highlighted lines: border-left teal + soft teal bg tint
- Diff lines: green/red bg + `+` / `−` gutter glyphs
- Toast not in the `.prose` screenshot (fixed top-right of viewport; default hidden opacity:0)

Manual audit scope (D-08b + VALIDATION.md Manual-Only):
- 4 posts × 2 locales = 8 pages
- On `npm run preview`, open each, check:
  - Code blocks render correctly
  - Badge visible + correct language text
  - Copy button visible icon-only, hover expands
  - Click → toast appears + paste works
  - No hex-literal colors anywhere (would show as e.g. bright cyan in DevTools computed styles)
  - Build performance: `npm run build` still < 2s, 32 pages
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-capture 8 post-Phase-2 baselines + confirm regression gate green</name>
  <files>tests/visual/shiki-regression.spec.ts-snapshots/, .planning/phases/02-code-block-upgrades/baselines/</files>
  <read_first>
    - tests/visual/shiki-regression.spec.ts (final spec as written in Plan 02-01 Task 2)
    - tests/visual/shiki-regression.spec.ts-snapshots/ (existing 8 pre-change baselines)
    - .planning/phases/02-code-block-upgrades/baselines/ (phase-artifacts mirror from Plan 02-01 Task 3)
    - .planning/phases/02-code-block-upgrades/02-CONTEXT.md §D-08 (regression gate semantics)
    - .planning/phases/02-code-block-upgrades/02-RESEARCH.md §Q4 "Baseline capture order" + Pitfall 5 (baseline re-capture cadence)
  </read_first>
  <action>
    Step 1 — Verify the post-Phase-2 build is clean before updating baselines:
    ```bash
    npm run build
    # Expected: 32 pages, build passes. If any error: STOP, fix in the appropriate prior plan.
    ```

    Step 2 — Re-capture the 8 baselines against the new visual state:
    ```bash
    npx playwright test tests/visual/shiki-regression.spec.ts --update-snapshots
    # Expected: 8 passed (Playwright --update-snapshots writes new PNGs and always passes)
    ```

    This regenerates all 8 PNGs in `tests/visual/shiki-regression.spec.ts-snapshots/` to match the post-Phase-2 visual state.

    Step 3 — Confirm the gate passes WITHOUT --update-snapshots (freshly-captured baselines match themselves):
    ```bash
    npx playwright test tests/visual/shiki-regression.spec.ts
    # Expected: 8 passed
    ```

    If any test fails with a pixel diff — likely Chromium is rendering with sub-pixel variance between runs. Re-run ONCE more; if still flaky, bump `maxDiffPixels` to `200` in the spec (but investigate first — `150` worked in Phase 1 for pod-lifecycle-parity under identical settle logic). Do NOT blindly widen tolerance — pixel drift between captures on the same machine is a smell.

    Step 4 — Mirror the new baselines into the phase-artifacts directory:
    ```bash
    rm -f .planning/phases/02-code-block-upgrades/baselines/*.png
    cp tests/visual/shiki-regression.spec.ts-snapshots/*.png \
       .planning/phases/02-code-block-upgrades/baselines/
    ls .planning/phases/02-code-block-upgrades/baselines/*.png | wc -l  # Expected: 8
    ```

    Step 5 — Diff the baselines directory against its git HEAD to confirm exactly 8 files changed (no leftovers):
    ```bash
    git status .planning/phases/02-code-block-upgrades/baselines/
    # Expected: all 8 PNGs show as modified (baselines changed content but filenames stayed same)
    ```

    NOTE: The regression gate is now locked to the new visual state. Phase 3+ work modifying `.prose` or Shiki-related CSS must re-capture if it intentionally changes the code-block visual, or keep these baselines clean if it doesn't.
  </action>
  <verify>
    <automated>ls tests/visual/shiki-regression.spec.ts-snapshots/*.png 2>/dev/null | wc -l | awk '$1==8 {exit 0} {exit 1}' && ls .planning/phases/02-code-block-upgrades/baselines/*.png 2>/dev/null | wc -l | awk '$1==8 {exit 0} {exit 1}' && npx playwright test tests/visual/shiki-regression.spec.ts 2>&1 | grep -qE '8 passed'</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/visual/shiki-regression.spec.ts-snapshots/*.png | wc -l` returns 8
    - `ls .planning/phases/02-code-block-upgrades/baselines/*.png | wc -l` returns 8
    - Each of the 8 PNG filenames contains one of {hello-world, 2026-02-10-why-i-write, 2026-03-02-mcp-servers, 2026-03-20-karpenter} AND one of {en, ru}
    - `npx playwright test tests/visual/shiki-regression.spec.ts` (no --update-snapshots flag) reports "8 passed"
    - `git status .planning/phases/02-code-block-upgrades/baselines/` shows the 8 PNGs as modified (since Plan 02-01 captured the old state)
    - Phase-artifacts dir contents match snapshot dir contents byte-for-byte: `diff -qr tests/visual/shiki-regression.spec.ts-snapshots/ .planning/phases/02-code-block-upgrades/baselines/` reports no differences
    - Visual delta on fence-bearing posts: opening any of the 2 karpenter or mcp-servers PNGs shows Deep Signal colors + visible badge + Copy button (spot-check by file read or open in image viewer)
    - Visual state on fenceless posts: hello-world + why-i-write PNGs unchanged from pre-phase OR show only toast DOM invisible + .prose scope untouched (fenceless posts have no Shiki output to change)
  </acceptance_criteria>
  <done>8 baselines re-captured to post-Phase-2 visual state, regression gate green, phase-artifacts mirror in sync.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 2 delivers:
    - Language badge (top-right pill) on every prose code block, JetBrains Mono uppercase small-caps (CODE-01)
    - `// [!code highlight]` / `# [!code highlight]` support via @shikijs/transformers (CODE-02)
    - `// [!code ++]` / `// [!code --]` diff support with green/red gutter glyphs (CODE-03)
    - Deep Signal theme via CSS variable overrides on github-dark (CODE-04) — teal keywords, amber strings
    - CodeCopyEnhancer upgraded: icon-only-at-rest → icon+label-on-hover, singleton toast for copy feedback (CODE-05)
    - Bilingual labels throughout (EN + RU)
  </what-built>
  <how-to-verify>
    Boot the preview server:
    ```bash
    npm run preview
    # Opens http://localhost:4321
    ```

    Visit each URL below and visually confirm (~5 minutes total):

    **1. http://localhost:4321/en/blog/2026-03-20-karpenter-right-sizing**
       - Dark #0D1117 code block backgrounds (not github-dark's lighter #24292e)
       - Keywords `apiVersion`, `kind`, `spec` render teal #14B8A6
       - String values render amber #F59E0B
       - Each yaml code block shows top-left badge "YAML" in muted JetBrains Mono uppercase
       - Top-right shows a small lucide:copy icon (muted) — no text label at rest
       - Hover a code block → icon button expands showing "COPY" text alongside the icon
       - Click Copy → top-right toast slides in, green bg "Copied to clipboard", fades after ~2s
       - Open a text editor, paste, confirm yaml content matches

    **2. http://localhost:4321/ru/blog/2026-03-20-karpenter-right-sizing**
       - Same visual as EN but RU locale
       - Hover label shows "КОПИРОВАТЬ"
       - Click copy → toast shows "Скопировано в буфер"

    **3. http://localhost:4321/en/blog/__fixture-syntax-highlighting** (draft fixture)
       - 4 code blocks render: bash, yaml, typescript, dockerfile
       - Each shows its language badge top-left
       - Bash block: one line highlighted with border-left teal + soft teal bg
       - YAML block: one `# [!code --]` line red bg + `−` gutter glyph; one `# [!code ++]` line green bg + `+` gutter glyph
       - TypeScript block: one `// [!code highlight]` line + one add + one remove
       - Dockerfile block: one highlight + one remove + two adds

    **4. http://localhost:4321/en/blog/hello-world**
       - No code blocks (expected)
       - No badge, no toast visible (toast hidden via opacity:0 but DOM-present)

    **5. http://localhost:4321/en/** (homepage)
       - No .code-block figures anywhere
       - Hero terminal font-mono still renders correctly (no .prose scope, untouched)
       - Certification badges still render correctly (also not .prose)
       - Open DevTools → search CSS for `#06B6D4` / `#22D3EE` — must return zero matches

    **6. Accessibility spot-check (any blog page with code):**
       - Tab through the page with keyboard — Copy button receives focus with `focus-visible` outline
       - Enter on Copy → toast announces "Copied to clipboard" via VoiceOver / screen reader
       - In DevTools → Settings → Rendering → toggle "prefers-reduced-motion: reduce" → click Copy → toast appears/disappears instantly (no transition)

    **7. Visual regression baselines (already captured in Task 1):**
       - Open `.planning/phases/02-code-block-upgrades/baselines/` in Finder or an image viewer
       - All 8 PNGs visually represent the current state (Deep Signal + badge + Copy)

    **8. Build performance:**
       - `npm run build` — 32 pages in < 2s
       - No errors, no warnings related to shiki / rehype / transformers

    If any of the above fails, REJECT and describe which step + which post. Do NOT restore the Astro caret (Task 3) until sign-off lands.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Task 3 (restore Astro caret), or describe specific failures for fix.</resume-signal>
  <acceptance_criteria>
    - User replies "approved" or equivalent confirmation
    - User confirms at minimum: (a) badge visible on karpenter + fixture posts, (b) Copy+toast works on at least one post in both EN and RU, (c) no color regressions on homepage/nav, (d) reduced-motion disables transitions
    - If user reports issues, each reported issue maps to a specific fix (either revise a prior plan's task or add a follow-up task before Task 3)
  </acceptance_criteria>
  <done>Human confirms Phase 2 visual + interaction delivery is correct and complete.</done>
</task>

<task type="auto">
  <name>Task 3: Restore Astro caret in package.json (D-09 post-merge step)</name>
  <files>package.json</files>
  <read_first>
    - package.json (current state — astro pinned to 5.18.0 from Plan 02-01 Task 1)
    - .planning/phases/02-code-block-upgrades/02-CONTEXT.md §D-09 (pin-then-restore contract)
    - .planning/phases/02-code-block-upgrades/02-RESEARCH.md §Q8 (recommended 5.18.0 value; restore step explicitly called out)
  </read_first>
  <action>
    Edit `package.json` to restore the `^` caret on the Astro entry. Single-character diff:

    ```diff
    -    "astro": "5.18.0",
    +    "astro": "^5.18.0",
    ```

    Do NOT modify any other entry. Do NOT run `npm install` (the installed version 5.18.0 remains compatible with `^5.18.0`).

    Verify:
    ```bash
    grep '"astro":' package.json
    # Expected: "astro": "^5.18.0",

    npm run build
    # Expected: 32 pages, no errors — no version churn because the caret matches 5.18.0

    npm run test:unit
    # Expected: green

    npx playwright test tests/visual/shiki-regression.spec.ts
    # Expected: 8 passed (caret restore doesn't change any runtime behavior)
    ```
  </action>
  <verify>
    <automated>grep -q '"astro": "\^5\.18\.0"' package.json && ! grep -qE '"astro": "5\.18\.0"[,$]' package.json && npm run build 2>&1 | grep -qE '(31|32) page' && npm run test:unit 2>&1 | grep -qE '(# pass|pass [0-9]+)' && npx playwright test tests/visual/shiki-regression.spec.ts 2>&1 | grep -qE '8 passed'</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"astro":' package.json` returns `"astro": "^5.18.0",` (caret restored)
    - No line in package.json reads `"astro": "5.18.0"` without the caret
    - `npm ls astro` reports astro@5.18.0 still resolved (caret range `^5.18.0` satisfied by installed version)
    - `package-lock.json` unchanged (no lockfile churn from caret addition since resolved version is identical)
    - `npm run build` exits 0, 32 pages
    - `npm run test:unit` exits 0 (all unit tests still green)
    - `npx playwright test tests/visual/shiki-regression.spec.ts` reports "8 passed"
    - Full diff summary for Phase 2 across all plans: no file has the raw hex `#06B6D4` or `#22D3EE` anywhere (grep -r in source + dist)
  </acceptance_criteria>
  <done>Astro caret restored to ^5.18.0; build green; unit + visual tests green; Phase 2 sign-off complete.</done>
</task>

</tasks>

<verification>
After all tasks complete (Phase 2 final gate):

1. `npm run build` — 32 pages, no errors
2. `npm run test:unit` — green (scaffold + 6 rehype-code-badge tests + 3 existing vv-*.test.ts suites)
3. `npx playwright test tests/visual/shiki-regression.spec.ts` — 8 passed
4. `grep -r '#06B6D4\|#22D3EE' src/ dist/ 2>&1 | grep -v 'node_modules'` — empty (no deprecated cyan anywhere)
5. `grep '"astro":' package.json` — `"astro": "^5.18.0",` (caret restored per D-09)
6. `ls .planning/phases/02-code-block-upgrades/baselines/*.png | wc -l` — 8 (post-Phase-2 snapshot)
7. Manual smoke test on `npm run preview` confirms CODE-01 through CODE-05 all delivered (checkpoint task 2)
8. `.planning/phases/02-code-block-upgrades/` contains: 02-01 through 02-06 PLAN.md + SUMMARY.md files + baselines/ directory
</verification>

<success_criteria>
- 8 baselines re-captured reflecting final Deep Signal + badge + Copy UX visual state
- Playwright regression gate green against the fresh baselines
- Phase-artifacts baselines directory in sync with Playwright snapshot dir
- Human sign-off on visual + interaction (CODE-01 through CODE-05)
- Astro caret restored to ^5.18.0 per D-09 (no lockfile churn)
- All of `npm run build` / `npm run test:unit` / `npx playwright test tests/visual/shiki-regression.spec.ts` exit 0
- Zero deprecated cyan (#06B6D4 / #22D3EE) anywhere in source or dist — grep-hard-verified
- Phase 2 exit criteria from ROADMAP.md §Phase 2 all satisfied:
  - Every prose code block renders a language badge (CODE-01)
  - `// [!code highlight]` validated on bash, yaml, typescript via fixture (CODE-02)
  - `// [!code ++]` / `// [!code --]` diff styling (CODE-03)
  - Deep Signal CSS variable overrides on github-dark (CODE-04)
  - CodeCopyEnhancer works on all 4 existing posts with no regressions (CODE-05)
</success_criteria>

<output>
After completion, create `.planning/phases/02-code-block-upgrades/02-06-SUMMARY.md` documenting:
- Final baseline re-capture (8 PNGs, both Playwright snapshot dir + phase-artifacts mirror)
- Regression gate green
- Human sign-off signal received on checkpoint task
- Astro caret restored per D-09
- Summary of Phase 2 delivery (link to each CODE-NN requirement + final state)
- Zero deprecated cyan confirmed via grep
- Phase 2 status: DONE. Ready to move to Phase 3 (UI Polish).
</output>
